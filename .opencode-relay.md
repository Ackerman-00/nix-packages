# nix-packages autonomous-run relay state

run_id: 32375902538
status: complete

## 0. Pickup

- Previous relay (run 32365316766) status: complete. Repo was rewritten to a single owner commit `8ed3071` (initial commit by Qᴜɪᴛᴇᴄʀᴀꜰᴛ). Working tree clean at pickup.
- This run (32375902538): fresh start, RELAY=false. Inventory discovered from flake.nix: x86_64-linux = helium, protonplus, zen-browser, opencode-desktop, rootapp (+ default=rootapp); aarch64-linux = helium, protonplus, zen-browser, opencode-desktop (+ default=helium). 5 packages.

## 1. Work done this run

### 1a. helium bumped 0.15.5.1 -> 0.15.6.1 (PUSHED `1a1f994`)
- Upstream released 0.15.6.1 on 2026-08-19T21:00:35Z (stable, not prerelease; `gh api repos/imputnet/helium-linux/releases/latest` => 0.15.6.1; `git ls-remote --tags` confirms tag exists).
- New hashes prefetched with --refresh: x86_64 `sha256-TVzi5ZWyvFsh6ovoBhU5DW0k3wlQ5/gUDD4+ykAQPTY=`, arm64 `sha256-Y1nU48e8UsUtQ67zRHVh5ul0vRMD4jfAe87L2p9oIf4=`.
- Tore the tar.xz: same structure (helium binary, chromium deps, wrapper, desktop, metainfo, icons). installCheckPhase `$out/bin/helium --version` => "Helium 0.15.6.1 (Chromium 151.0.7922.169)" (matches pinned). Container smoke: LDD_ZERO_NOT_FOUND, --version exit 0.
- Committed `1a1f994 fix(autonomous): helium: bump 0.15.5.1 -> 0.15.6.1` and pushed.

### 1b. Issue #3 CLOSED (update.yml dual-arch fix already present on main)
- The current `.github/workflows/update.yml` (owner commit 8ed3071) already contains the exact dual-arch Opencode Desktop block the issue requested (per-arch `grep -A1 ... | head -1`, per-arch prefetch, per-arch range-seds anchored on `<arch>.deb";`).
- Verified by direct simulation on a copy of pkgs/opencode-desktop.nix:
  1. Current state (v1.18.19): OLD_VER==NEW_VER, OLD_X64==NEW_X64, OLD_ARM==NEW_ARM => updater would NOT commit (no churn).
  2. Hypothetical v1.18.20 with distinct fake hashes: seds write amd64 hash to the amd64 slot and arm64 hash to the arm64 slot (verified byte-for-byte).
- Closed with evidence comment. No owner action needed.

### 1c. Version sweep (COMPLETE - 100% of inventory)
- helium 0.15.6.1 == upstream latest (UPDATED, 1a). x86_64 TVzi5ZWy... and arm64 Y1nU48e8... byte-match re-prefetch.
- protonplus 0.6.4 == upstream latest v0.6.4. x86_64 J0ddq9ho... and arm64 o4sUkr3J... byte-match.
- zen-browser 1.21.15b == upstream latest. x86_64 Lq6mZLhA... and arm64 c6YJuVTn... byte-match.
- opencode-desktop v1.18.19 == upstream latest. amd64 WpSBGv97... and arm64 8J7rRv4U... byte-match.
- rootapp: fixed URL re-prefetched, hash sha256-LFTZZC9q... byte-matches pinned (no change).
- All 9 GitHub/fixed asset URLs return HTTP 200.
- Real versions from inside artifacts: helium --version "Helium 0.15.6.1 (Chromium 151.0.7922.169)", zen application.ini Version=1.21.15b, protonplus X-AppImage-Version=0.6.4, opencode app.asar package.json = 1.18.19, rootapp Root.desktop X-AppImage-Version=0.9.127. All match pinned.
- flake.lock nixpkgs rev d407951; all packages build with it - no update (no-churn rule).

### 1d. Updater simulation (update.yml logic replayed by hand, ALL blocks)
- RootApp: prefetch hash == pinned => no change (updater would NOT commit).
- Helium: pre-bump simulation produces EXACTLY commit 1a1f994's diff (version + both hashes); post-bump simulation = no change.
- Opencode: dual-arch block works (no churn on current state, correct slot-writing on updates) - see 1b.
- ProtonPlus: version + both hashes match => no change.
- Zen: version + both hashes match => no change.
- GitHub Actions health: last "Packages Auto-Update" run (178, 2026-08-20 00:58 UTC) SUCCESS. One failed run (32375450926, event=push, workflow-file-on-push) is the owner's initial-commit artifact, log expired - not the updater, not a repo-content bug.

## 2. Build/install ledger (COMPLETE)

| package | build (x86_64) | aarch64 eval | binary smoke | status |
|---|---|---|---|---|
| helium 0.15.6.1 | PASS | PASS (eval) | --version -> "Helium 0.15.6.1 (Chromium 151.0.7922.169)" exit 0 | pass |
| protonplus 0.6.4 | PASS | PASS (eval) | version -> "ProtonPlus 0.6.4" exit 0; hooks /tmp/pKE + /tmp/3J=1Z | pass |
| zen-browser 1.21.15b | PASS | PASS (eval) | --version -> "Mozilla Zen 1.21.15b" exit 0 | pass |
| opencode-desktop v1.18.19 | PASS | PASS (eval) | starts, stops at "Missing X server or $DISPLAY" (GUI gap) | pass |
| rootapp 0.9.127 | PASS | n/a (x86_64 only) | bwrap wrapper -> "Starting application" exit 0 | pass |

- `nix build .#helium .#opencode-desktop .#protonplus .#rootapp .#zen-browser --no-link --refresh` => EXIT 0 (twice: baseline at 0.15.5.1 + final at 0.15.6.1).
- `nix flake check --all-systems` => all checks passed (both systems eval; aarch64 BUILD impossible on x86_64 runner - named gap).
- Store paths: helium aavmgij8h...-0.15.6.1, opencode jska7nxg...-v1.18.19, protonplus bjmcfaj5...-0.6.4, rootapp qia4835h...-latest, zen gp084iqxw...-1.21.15b.

## 3. Dependency audit (COMPLETE)

| package | NEEDED | in pkgs/*.nix | missing/added | status |
|---|---|---|---|---|
| helium 0.15.6.1 | 32 direct NEEDED on main ELF; all resolve via nix loader + wrapper LD_LIBRARY_PATH (0 not-found) | runtimeDeps covers | none | deps-verified |
| protonplus 0.6.4 | static-pie bun runtime, NEEDED=0 | n/a | none | deps-verified |
| zen-browser 1.21.15b | libxul ~56 NEEDED (firefox family) resolve (0 not-found in container) | buildInputs+runtimeDeps+appendRunpaths | none | deps-verified |
| opencode-desktop v1.18.19 | wrapper -> system electron; app.asar.unpacked native modules (parcel-watcher glibc+musl, node-pty, msgpackr-extract) present | electron_42 + stdenv | none | deps-verified |
| rootapp 0.9.127 | FHS wrapType2 + bwrap | wrapType2 | none | deps-verified |

- Interpreters: helium and zen main ELFs point at nix loader (glibc-2.42-67). opencode wrapper is makeBinaryWrapper -> system electron. protonplus static-pie (no interpreter). rootapp FHS-wrapped.
- Confidence: high (all 5 container-tested in clean containers on final store paths).

## 4. Container runtime ledger (COMPLETE)

| package | nix build | container ldd | container smoke | status |
|---|---|---|---|---|
| helium 0.15.6.1 | PASS | ZERO_NOT_FOUND | --version -> Helium 0.15.6.1 exit 0 | verified |
| protonplus 0.6.4 | PASS | ZERO_NOT_FOUND (NEEDED=0) | version -> ProtonPlus 0.6.4 exit 0; hooks created /tmp/pKE + /tmp/3J=1Z | verified |
| zen-browser 1.21.15b | PASS | ZERO_NOT_FOUND | --version -> Mozilla Zen 1.21.15b exit 0 | verified |
| opencode-desktop v1.18.19 | PASS | ZERO_NOT_FOUND | starts, "Missing X server or $DISPLAY" (GUI gap) | verified |
| rootapp 0.9.127 | PASS | ZERO_NOT_FOUND | bwrap wrapper -> "Starting application" exit 0 (--privileged) | verified |

## 5. Fresh-checker pass (COMPLETE)

- Fresh clone `/tmp/freshcheck` of main (1a1f994): `nix build .#helium .#opencode-desktop .#protonplus .#rootapp .#zen-browser --no-link --refresh` EXIT 0; `nix flake check --all-systems` passed; aarch64 eval all 4 names OK (helium-0.15.6.1, opencode-desktop-v1.18.19, protonplus-0.6.4, zen-browser-1.21.15b).
- Store paths deterministic/identical to main-repo build.
- Fresh containers (new `docker run --rm` per test) on fresh store paths: helium/zen/protonplus smoke exit 0, rootapp "Starting application", opencode reaches display error. All pass.

## 6. Missing-package audit (COMPLETE)

- Inventory sync: flake.nix (x86_64 5 pkgs, aarch64 4 pkgs), README.md (5 entries), update.yml (5 blocks), pkgs/*.nix (5 files) ALL agree. No discrepancies.
- Upstream artifact types unchanged: helium/zen tar.xz, protonplus AppImage (sharun), opencode deb, rootapp AppImage (fixed URL). No arch added/dropped. opencode still ships amd64+arm64 deb (verified in v1.18.19 assets).
- New-app scan: no new official companion apps with prebuilt Linux artifacts found for helium/opencode/protonplus/zen/rootapp (web-verified: helium = browser only; opencode desktop = already packaged; protonplus = standalone; zen = browser only; rootapp = standalone).

## 7. Known gaps / notes

- aarch64 BUILD not possible on x86_64 runner (named gap) - aarch64 verified by eval + hash prefetch only.
- GUI apps (opencode-desktop) stop at "Missing X server or $DISPLAY" headless - expected, not a bug.
- REPO_FULL_ACCESS_TOKEN not set: workflow-file changes cannot be pushed (TOKENS rule). Not needed this run - update.yml already correct.
- Run 32375450926 (event=push, workflow-file-on-push) failure is an owner-commit artifact, log expired; updater + scheduled runs green.

## 8. Final state

- main HEAD: `1a1f994` (helium 0.15.6.1 bump), pushed. Working tree clean.
- Commits this run: `1a1f994` (helium bump).
- Issues: #3 closed (fix already present, verified). No open issues, no open PRs.
- Relay status: complete. run_id: 32375902538.