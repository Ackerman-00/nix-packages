# nix-packages autonomous-run relay state

run_id: 32314148677
status: complete

## 0. Pickup

- Previous relay (run 32283148412, commit 93ef1bf) status: complete. Nothing unfinished to continue.
- This run (32314148677): `git fetch origin && git pull --rebase origin main` => up to date at 93ef1bf.
- Runner: Nix 2.35.2, Docker 28.0.4, gh 2.97.0. Git identity verified: `opencode-agent[bot] <41898282+opencode-agent[bot]@users.noreply.github.com>`.
- Inventory discovered from flake.nix: x86_64-linux = helium, protonplus, zen-browser, rootapp, opencode-desktop (+ default); aarch64-linux = helium, protonplus, zen-browser, opencode-desktop (+ default). 5 packages.

## 1. Work done this run

### 1a. rootapp hash update (PUSHED `0581f23`)
- RootApp "latest" URL re-rolled on 2026-08-19T20:18Z. Old pinned hash `sha256-BzaSb96...` was stale; new `sha256-LFTZZC9qhHdRgQXzNa6j74hNn8oxjIR/J+bTM0DB3Js=` verified via `nix store prefetch-file --refresh` (3x, stable) and by sha256sum of the downloaded 392,239,608-byte AppImage. Real version inside artifact: **0.9.127** (Root.desktop X-AppImage-Version).
- `nix build .#rootapp --no-link` PASS. Container runtime test (ubuntu:24.04, --privileged, fresh): ldd 0 not-found, bwrap wrapper smoke "Starting application" exit 0.
- Committed `0581f23 fix(autonomous): rootapp: update hash for upstream re-roll (0.9.127)` and pushed.

### 1b. Version sweep (COMPLETE - all up-to-date, no churn)
- helium 0.15.5.1 == upstream latest. x86_64 80oe4aar..., arm64 2TI9B3Gj... byte-match re-prefetch.
- protonplus 0.6.4 == upstream latest v0.6.4. x86_64 J0ddq9ho..., arm64 o4sUkr3J... byte-match.
- zen-browser 1.21.15b == upstream latest. x86_64 Lq6mZLhA..., arm64 c6YJuVTn... byte-match.
- opencode-desktop v1.18.18 == upstream latest. amd64 e31ka5x..., arm64 7qP2pb+... byte-match.
- rootapp: fixed URL, hash updated (1a).
- All 9 GitHub asset URLs + rootapp URL return HTTP 200.
- Real versions verified from inside every artifact (application.ini / X-AppImage-Version / deb control / app.asar package.json): helium 0.15.5.1, protonplus 0.6.4, zen 1.21.15b, opencode 1.18.18, rootapp 0.9.127.

### 1c. update.yml clobber MONITORED + FIXED (PUSHED `ba2215a`)
- Predicted (simulated issue #3 bug): daily update.yml run would write the amd64 hash into the opencode-desktop arm64 slot.
- Run 32319293398 ("Packages Auto-Update") fired Aug 20 00:58 UTC, commit `0b4e981` "chore: auto-update packages" DID clobber the arm64 hash (`7qP2pb+...` -> `e31ka5x...`), only file touched.
- Verified broken: real arm64 hash (`sha256-7qP2pb+BfTPx0wYVialVP6M3Cb8SWqz+T+Z38x8f6FM=`) != clobbered value (amd64 hash), so aarch64 users would hit a failed fetch.
- Fixed: restored correct arm64 hash, `nix build .#opencode-desktop` PASS, `nix flake check --all-systems` PASS, aarch64 eval PASS. Committed `ba2215a fix(autonomous): opencode-desktop: restore correct arm64 hash after updater clobber (issue #3)` and pushed.
- Commented on issue #3 (comment 5349912322) confirming the bug fired again and that owner workflow fix is still REQUIRED (bot cannot push to .github/workflows - TOKENS rule). Each daily run will re-clobber; bot restores it.

## 2. Build/install ledger (COMPLETE)

| package | build (x86_64) | aarch64 eval | binary smoke | status |
|---|---|---|---|---|
| helium 0.15.5.1 | PASS | PASS (eval) | --version -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)" exit 0 | pass |
| protonplus 0.6.4 | PASS | PASS (eval) | version -> "ProtonPlus 0.6.4" exit 0; hooks /tmp/pKE + /tmp/3J=1Z | pass |
| zen-browser 1.21.15b | PASS | PASS (eval) | --version -> "Mozilla Zen 1.21.15b" exit 0 | pass |
| opencode-desktop v1.18.18 | PASS | PASS (eval) | starts, stops at display (GUI gap) | pass |
| rootapp 0.9.127 | PASS | n/a (x86_64 only) | bwrap wrapper -> "Starting application" exit 0 | pass |

- `nix build .#helium .#protonplus .#zen-browser .#opencode-desktop .#rootapp --no-link` => EXIT 0.
- `nix flake check --all-systems` => all checks passed.
- doInstallCheck confirmed enabled (derivation env doInstallCheck=1, installCheckPhase runs `$out/bin/helium --version`).

## 3. Dependency audit (COMPLETE)

| package | NEEDED | in pkgs/*.nix | missing/added | status |
|---|---|---|---|---|
| helium 0.15.5.1 | 32 direct NEEDED on main ELF; 72 libs resolve via nix loader+wrapper LDP (0 not-found) | runtimeDeps covers | none | deps-verified |
| protonplus 0.6.4 | static-pie bun, NEEDED=0 | n/a | none | deps-verified |
| zen-browser 1.21.15b | libxul 83 resolve (0 not-found) | buildInputs+runtimeDeps | none | deps-verified |
| opencode-desktop v1.18.18 | deb Version=1.18.18; sidecars glibc resolve; electron-42.5.1 from flake-locked nixpkgs | electron_42 + stdenv | none | deps-verified |
| rootapp 0.9.127 | FHS wrapType2 + bwrap | wrapType2 | none | deps-verified |

- All interpreters point to nix loader `/nix/store/8kvxvr3pmsypxiypq4g8zy13glnfr7nx-glibc-2.42-67/lib/ld-linux-x86-64.so.2`.
- protonplus completeness tear: packaged tree == artifact tree verbatim (bin, etc, lib incl. lib/pkcs11/p11-kit-trust.so, share incl. glib schemas + 25 locales, shared/bin protonplus + protonplus-runtime symlink). Desktop entry + 256px icon + gio-launch-desktop static-pie all present.

## 4. Container runtime ledger (COMPLETE)

| package | nix build | container ldd | container smoke | status |
|---|---|---|---|---|
| helium 0.15.5.1 | PASS | ZERO_NOT_FOUND (nix loader + wrapper LDP) | --version -> Helium 0.15.5.1 exit 0 | verified |
| protonplus 0.6.4 | PASS | ZERO_NOT_FOUND (NEEDED=0) | version -> ProtonPlus 0.6.4 exit 0; hooks: /tmp/pKE + /tmp/3J=1Z symlinks created | verified |
| zen-browser 1.21.15b | PASS | ZERO_NOT_FOUND (libxul 83/83) | --version -> Mozilla Zen 1.21.15b exit 0 | verified |
| opencode-desktop v1.18.18 | PASS | ZERO_NOT_FOUND | starts, missing X server exits (GUI gap) | verified |
| rootapp 0.9.127 | PASS | ZERO_NOT_FOUND | bwrap wrapper -> Starting application exit 0 (--privileged) | verified |

## 5. Fresh-checker pass (COMPLETE)

- Fresh clone `/tmp/freshcheck` of final main (ba2215a): `nix build .#all --no-link --refresh` EXIT 0; `nix flake check --all-systems` passed; aarch64 eval all 4 names OK.
- Store paths identical/deterministic: helium `/nix/store/4lph1w68qgfcs1j9yyvyqrnpby345fdi-helium-0.15.5.1`, protonplus `/nix/store/bjmcfaj5zv7z0d0z6l4xs794qxl54r21-protonplus-0.6.4`, zen `/nix/store/gp084iqxw14xhh003yvmvyby4immr79p-zen-browser-1.21.15b`, opencode `/nix/store/bd1brhshikpbxfalgj8wngrialm5n59b-opencode-desktop-v1.18.18`, rootapp `/nix/store/qia4835h81cgiyxpigv52ygfnhms8hxf-rootapp-latest`.
- Final container smoke on fresh main state: all 5 packages pass (rootapp profile init + "Starting application"; opencode initializes then GUI gap).
- flake.lock fresh (nixpkgs rev d407951, Aug 2026); no update needed (no-churn rule).

## 6. Issue #3 (open)

- Bug: update.yml dual-arch sed clobbers opencode-desktop arm64 hash on every daily run. FIRED AGAIN today (0b4e981), fixed by ba2215a.
- Comment added: comment 5349912322 (verification + owner action request).
- **Owner action REQUIRED**: apply the dual-arch diff from issue #3 body to `.github/workflows/update.yml`. Bot cannot (TOKENS rule). Bot will keep restoring the arm64 hash daily until fixed.

## 7. Known gaps / notes

- aarch64 BUILD not possible on x86_64 runner (named gap) - aarch64 verified by eval + hash prefetch only.
- Container ldd shows a glibc-version-mix artifact when ubuntu's loader is used; the nix-loader check is authoritative (0 not-found).
- GUI apps (opencode-desktop/electron) stop at "Missing X server or $DISPLAY" headless - expected, not a bug.
- No new companion apps found; inventory in sync across README.md, flake.nix, update.yml, pkgs/*.nix.

## 8. Final state

- main HEAD: `ba2215a` (opencode arm64 hash restore), pushed. Working tree clean.
- Commits this run: `0581f23` (rootapp hash), `f55a7b3` (relay state update), `ba2215a` (opencode arm64 restore).
- Relay status: complete. run_id: 32314148677.
