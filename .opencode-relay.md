# nix-packages autonomous-run relay state

run_id: 32283148412
status: complete

## 0. Pickup

- Previous relay (commit d445907, run 32248930204) had `run_id: (pending - set by workflow env)` instead of a real run ID, which made the workflow post-check mark that run as FAILED and auto-open issue #4 ("autonomous run failed: 2026-08-19"). Nothing unfinished to continue from that relay's substance.
- This run (32283148412): `git fetch origin && git pull --rebase origin main` => up to date.
- Runner: Nix 2.35.2, Docker 28.0.4, gh 2.97.0. Git identity verified: `opencode-agent[bot] <41898282+opencode-agent[bot]@users.noreply.github.com>`.
- Inventory discovered from flake.nix: x86_64-linux = helium, protonplus, zen-browser, rootapp, opencode-desktop (+ default); aarch64-linux = helium, protonplus, zen-browser, opencode-desktop (+ default). 5 packages.

## 1. Actions health / issues / PRs

- update.yml ("Packages Auto-Update"): last 5 runs all SUCCESS (most recent 32203223778, 2026-08-19 00:58 UTC). That run committed `6899836 chore: auto-update packages` which clobbered the opencode-desktop arm64 hash (amd64 hash written to arm64 slot). Restored in ddcf613; 28d3bfc switched URLs to `${version}` templating. CURRENT main has both hashes correct.
- WARNING: the next daily update.yml run (2026-08-20 00:00 UTC) will re-clobber the opencode-desktop arm64 hash (single-hash grep bug, issue #3). Owner MUST apply the diff from issue #3 to .github/workflows/update.yml.
- Open issues: #3 (update.yml dual-arch opencode block; owner action required, added verification comment), #4 (autonomous run failed — this run resolves the root cause: proper relay run_id; closing).
- Open PRs: none.

## 2. Changes this run

- **zen-browser bumped 1.21.14b -> 1.21.15b** (upstream released 1.21.15b on 2026-08-19T17:13Z). New hashes verified via `nix store prefetch-file --refresh` (x86_64 sha256-Lq6mZLhABnygrOYjvU9FSPrpj0apji3b39y5JTtnS78=, aarch64 sha256-c6YJuVTn3O+NxJse/JwTtojs+iS6Gz85rWU+kyS4ic8=). Build + flake check + clean-container runtime test all PASS. Committed as `e3abf22` and pushed.
- No other package changes: helium 0.15.5.1, protonplus 0.6.4, opencode-desktop v1.18.18, rootapp (fixed URL) all verified up-to-date with byte-matching hashes.

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package          | packaged version | upstream latest (verified)                                  | status          |
|---|---|---|---|
| helium           | 0.15.5.1         | 0.15.5.1 (gh releases/latest)                              | up-to-date      |
| protonplus       | 0.6.4            | v0.6.4 -> 0.6.4 (gh releases/latest)                       | up-to-date      |
| zen-browser      | 1.21.15b         | 1.21.15b (gh releases/latest)                              | UPDATED         |
| opencode-desktop | v1.18.18         | v1.18.18 (gh releases/latest)                              | up-to-date      |
| rootapp          | latest (fixed URL)| hash unchanged after --refresh                             | up-to-date      |

- All 9 x86_64+aarch64 asset URLs + rootapp fixed URL return HTTP 200/302.
- All pinned SRI hashes re-prefetched with --refresh and byte-match pinned values (confirmed again in the final updater simulation).
- All 5 packages `meta.sourceProvenance = binaryNativeCode` verified.

## 4. Build/install verification ledger

| package          | build (x86_64) | aarch64 eval | binary smoke                                             | status |
|---|---|---|---|---|
| helium 0.15.5.1  | PASS           | PASS (eval)  | `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0 | pass |
| protonplus 0.6.4 | PASS           | PASS (eval)  | `version` -> "ProtonPlus 0.6.4", exit 0                   | pass |
| zen-browser 1.21.15b | PASS      | PASS (eval)  | `--version` -> "Mozilla Zen 1.21.15b", exit 0             | pass |
| opencode-desktop v1.18.18 | PASS | PASS (eval)  | starts, stops at sandbox/display (GUI gap)               | pass |
| rootapp latest   | PASS           | n/a (x86_64 only) | bwrap wrapper -> "Starting application", exit 0     | pass |

- Full build: `nix build .#helium .#protonplus .#zen-browser .#opencode-desktop .#rootapp --no-link` => EXIT 0 (in the live working tree AND in a fresh `git clone` of main).
- `nix flake check --all-systems` => all checks passed (live tree and fresh clone).
- aarch64 eval: all 4 aarch64-supported packages evaluate; aarch64 BUILD impossible on x86_64 runner (named gap).

## 5. Dependency audit (100% of inventory)

| package | upstream deps found | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.5.1 | 32 NEEDED on `helium` ELF (glib, nspr/nss, gtk3, cairo, pango, atk, dbus, cups, expat, xcb, xkbcommon, asound, gbm, X11 libs, udev, ...); interpreter patched to nix glibc 2.42 | runtimeDeps covers all 32 | none | none | deps-verified |
| protonplus 0.6.4 | static-pie bun binary, NEEDED=0 (bundles everything); hooks 01-check-ca-certs + 01-path-mapping-hardcoded present | n/a | none | none | deps-verified |
| zen-browser 1.21.15b | libxul 47 NEEDED; external gtk3/alsa/dbus/atk/cairo/pango/X libs/ffmpeg; 27+ bundled mozilla libs | buildInputs + runtimeDependencies | none | none | deps-verified |
| opencode-desktop v1.18.18 | deb Version=1.18.18; glibc .node sidecars (pty.node links libutil/libstdc++/libc); musl sidecars ignored | electron_42 + stdenv.cc.cc.lib | none | none | deps-verified |
| rootapp 0.9.126 | .NET Root binary; interpreter via bwrap FHS; NEEDED = libc/libdl/libstdc++/librt only | wrapType2 FHS+bwrap | none | none | deps-verified |

- Re-torn every artifact this run (tar.xz, sharun AppImage self-extract, deb via dpkg-deb, AppImage via --appimage-extract). Real versions read from inside artifacts match pinned versions (X-AppImage-Version / application.ini / deb control). Confidence: high.

## 6. Container runtime test ledger

| package          | nix build | container ldd          | container smoke                                          | status   |
|---|---|---|---|---|
| helium 0.15.5.1  | PASS      | ZERO_NOT_FOUND (nix interp with wrapper LD_LIBRARY_PATH) | `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0 | verified |
| protonplus 0.6.4 | PASS      | ZERO_NOT_FOUND (NEEDED=0) | `version` -> "ProtonPlus 0.6.4", exit 0 (dconf warning is benign) | verified |
| zen-browser 1.21.15b | PASS | ZERO_NOT_FOUND       | `--version` -> "Mozilla Zen 1.21.15b", exit 0           | verified |
| opencode-desktop v1.18.18 | PASS | ZERO_NOT_FOUND       | starts, missing X server exits (GUI gap)               | verified |
| rootapp 0.9.126  | PASS      | ZERO_NOT_FOUND        | bwrap wrapper -> "Starting application", exit 0         | verified |

- All tests in FRESH containers (ubuntu:24.04, `--rm`, nix store mounted read-only), re-run on the fresh clone of main for the fresh-checker pass.
- rootapp requires `--privileged` (bwrap userns).

## 7. Missing-package / new-app audit

- Inventory sync: README (5) == flake.nix (x86_64 5, aarch64 4) == update.yml (5) == pkgs/*.nix (5). In sync.
- Upstream artifact types unchanged for all 5 packages (helium tar.xz, protonplus sharun AppImage, zen tar.xz, opencode deb, rootapp AppImage).
- New-app scan: no new official companion Linux desktop apps shipping prebuilt binaries found.

## 8. Issues / PRs handled

- Issue #3: verification comment added (this run's evidence: hashes correct, all packages green, bug still active). Open, owner must apply the diff.
- Issue #4: commented with root cause (relay run_id was a placeholder in the previous run) and resolution (this run writes real run_id). Closing as resolved.

## 9. Final state

- HEAD = e3abf22 (zen-browser bump) + this relay completion commit. Working tree clean.
- flake.lock not touched (no concrete reason to update nixpkgs; all builds green).

## Named gaps (not verified, and why)

- aarch64 BUILD: cannot build aarch64 binaries on this x86_64 runner. Verified via `nix flake check --all-systems` (eval green) + prefetched arm64 hashes match. Named gap.
- GUI launch in container: no display/X server. Verified via headless flags + main-process start. Named gap.
- opencode-desktop musl .node sidecars legitimately link musl libc; ignored via autoPatchelfIgnoreMissingDeps. Correct by design.
- rootapp bwrap needs userns; verified with `docker run --privileged`. Named gap.
- update.yml opencode block still has single-hash grep bug (issue #3). Cannot push workflow changes (TOKENS rule). Next daily run (2026-08-20 00:00 UTC) will re-clobber the arm64 hash - on next pickup, verify and restore opencode-desktop arm64 hash if needed.