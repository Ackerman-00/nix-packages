# nix-packages autonomous-run relay state

run_id: 32198000513
status: complete

## 0. Pickup

- Previous relay (run 32158523109) was `status: complete`; nothing unfinished to continue.
- `git fetch origin && git pull --rebase origin main` => Already up to date.
- Runner Nix 2.35.2, Docker 28.0.4 (hello-world pass), gh 2.97.0. Git identity verified before every commit: `opencode-agent[bot] <41898282+opencode-agent[bot]@users.noreply.github.com>`.
- Inventory discovered from flake.nix / `nix eval --apply attrNames`, not hardcoded. After this run's change: x86_64-linux = helium, protonplus, zen-browser, rootapp, opencode-desktop (+ default); aarch64-linux = helium, protonplus, zen-browser, opencode-desktop (+ default).

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): last 10 runs all SUCCESS (latest 32086477571, 2026-08-18). No commits made by it recently.
- opencode-schedule: run 32167287654 FAILED on 2026-08-18 due to runner infrastructure (`No space left on device` writing the Actions worker diag log `/home/runner/actions-runner/cached/2.336.0/_diag/Worker_20260818-174523-utc.log`). NOT a repo bug - verified all packages build/run green on a fresh runner this run.
- Open issues at start: #2 (auto-generated "autonomous run failed: 2026-08-18"). Diagnosed as runner ENOSPC infra failure, replied with evidence + closed as not-a-repo-bug.
- Open PRs: none.

## 2. Changes this run (one atomic commit, pushed to main as `5f64d18`)

- **opencode-desktop: added aarch64-linux support.** Upstream (anomalyco/opencode) now ships `opencode-desktop-linux-arm64.deb` in every release (verified in v1.18.18 assets). The package was x86_64-only. Changed:
  - pkgs/opencode-desktop.nix: per-arch `archives` map (amd64 deb hash unchanged `sha256-e31ka5x82X8N8JipTEcITx4Wbw38RStyhwRZUuES1Io=`, arm64 deb hash `sha256-7qP2pb+BfTPx0wYVialVP6M3Cb8SWqz+T+Z38x8f6FM=` verified via `nix store prefetch-file --refresh`, URL 200); `platforms` now `[ "x86_64-linux" "aarch64-linux" ]`; added `libc.musl-aarch64.so*` to autoPatchelfIgnoreMissingDeps.
  - flake.nix: wired opencode-desktop for both systems (rootapp stays x86_64-only).
  - README.md: opencode-desktop now "(x86_64 + aarch64)".
  - arm64 deb torn in /tmp/ocd_arm: identical layout to amd64 (opt/OpenCode/resources/app.asar + unpacked, icons 32/64/128, desktop entries). `electron_42` exists for aarch64 in locked nixpkgs (`pkgs.electron_42.pname` = "electron").
  - x86_64 store path changed from vi5mdchvnv1h26vi1hmrkd27iwibhpfs to bd1brhshikpbxfalgj8wngrialm5n59b (derivation inputs changed via ignore-deps list); content verified identical (app.asar sha256 d02785a88e0e35e5e5b755fa8fe5cb444f5825a67030673abaaeb10af4dc3808).
  - aarch64 build is eval-only on this x86_64 runner (named gap); eval green via flake check.
- No version/hash churn on helium, protonplus, zen-browser, rootapp - all byte-match their pinned hashes and upstream versions (verified with `--refresh` this run).

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package          | packaged version | upstream latest (verified 2026-08-18)                        | status     |
|---|---|---|---|
| helium           | 0.15.5.1         | 0.15.5.1 (gh releases/latest + tags newest)                  | up-to-date |
| protonplus       | 0.6.4            | v0.6.4 -> 0.6.4 (gh releases/latest + tags newest)           | up-to-date |
| zen-browser      | 1.21.14b         | 1.21.14b (gh releases/latest + tags newest)                  | up-to-date |
| opencode-desktop | v1.18.18         | v1.18.18 (gh releases/latest + tags)                         | up-to-date (arch support added) |
| rootapp          | latest (fixed URL) | hash unchanged after `--refresh`; in-artifact X-AppImage-Version = 0.9.126 (Root.desktop, from prior run tear) | up-to-date |

- All 9 x86_64+aarch64 asset URLs + rootapp fixed URL return HTTP 200 (`curl -sIL`).
- All 9 pinned SRI hashes re-prefetched with `--refresh` and byte-match the pinned values (helium x64/arm, protonplus x64/arm, zen x64/arm, opencode amd64, opencode arm64 NEW, rootapp).
- Updater simulation for all 5 blocks = "no change" except opencode which now needs the arm64 hash block (see issue #3).

## 4. Build/install verification ledger

| package          | build (x86_64) | aarch64 eval | binary smoke                                                                   | status |
|---|---|---|---|---|
| helium 0.15.5.1    | PASS           | PASS (eval)  | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0      | pass   |
| protonplus 0.6.4   | PASS           | PASS (eval)  | `version` -> "ProtonPlus 0.6.4", exit 0                                        | pass   |
| zen-browser 1.21.14b | PASS         | PASS (eval)  | `--version` -> "Mozilla Zen 1.21.14b", exit 0 (sandbox EPERM warning normal)   | pass   |
| opencode-desktop v1.18.18 | PASS    | PASS (eval)  | main process starts, stops at "Missing X server or $DISPLAY" (GUI gap)         | pass   |
| rootapp latest     | PASS           | n/a (x86_64 only) | bwrap wrapper (--privileged) -> "Starting application" / "Using profile default", exit 0 | pass |

- `nix build .#helium .#protonplus .#zen-browser .#rootapp .#opencode-desktop --no-link` => EXIT 0 (both before and after the opencode commit, and on the fresh clone).
- Store paths: helium=4lph1w68qgfcs1j9yyvyqrnpby345fdi, protonplus=bjmcfaj5zv7z0d0z6l4xs794qxl54r21, zen=v78acsdymsjr8k667aiiwswmp6s2xm1x, rootapp=l45vk28x07x9zr1x9400dl0ya17x2bhk, opencode=bd1brhshikpbxfalgj8wngrialm5n59b. First four byte-identical to previous run (zero churn).
- `nix flake check --all-systems` => all checks passed (x86_64 + aarch64 eval green, including new aarch64 opencode-desktop derivation).
- All 5 packages `meta.sourceProvenance = binaryNativeCode` (verified via `nix eval`).
- Fresh-checker pass: cloned main to /tmp/freshcheck, `nix build --refresh` all 5 => EXIT 0, store paths byte-identical. Fresh container test on the changed opencode path passed.

## 5. Dependency audit (100% of inventory; no upstream change for helium/protonplus/zen/rootapp => no RE-TEAR required; opencode arm64 re-torn this run)

| package          | upstream deps found (NEEDED sonames) | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.5.1    | 32 NEEDED sonames across helium ELF; interpreter patched to nix glibc 2.42 | all 32 in runtimeDeps; patchelf --set-interpreter | none | none | deps-verified (container: full wrapper LD_LIBRARY_PATH reconstructed (90 components, 5785 chars); nix loader --list on helium/crashpad/chromedriver => not-found=0 each) |
| protonplus 0.6.4   | static-pie bun binary (NEEDED=0); desktop X-AppImage-Version=0.6.4 | n/a (bundles everything) | none | none | deps-verified (container: NEEDED=0, hooks create /tmp/pKE -> lib + /tmp/3J=1Z -> share; CA-certs warning expected in bare container) |
| zen-browser 1.21.14b | libxul 46 NEEDED, 27 bundled; external gtk3/alsa/dbus-glib/libXtst/ffmpeg + runtimeDeps curl/libva/pciutils/libGL | buildInputs + runtimeDependencies + appendRunpaths + autoPatchelfHook + patchelfUnstable --no-clobber-old-sections | none | none | deps-verified (container: zen ELF + libxul loader --list => not-found=0 each; application.ini Version=1.21.14b BuildID=20260811103047) |
| opencode-desktop v1.18.18 | electron real ELF resolves via nix loader; 4 glibc .node sidecars (watcher, pty, napi.glibc, abi115.glibc) resolve; musl sidecars link libc.so (musl, expected/ignored) | electron_42 + autoPatchelfIgnoreMissingDeps libc.musl-* | none | none | deps-verified (container: electron ELF not-found=0; glibc sidecars not-found=0 each; arm64 deb structure identical to amd64) |
| rootapp 0.9.126    | .NET Root binary; interpreter /lib64/ld-linux (correct - bundled glibc via bwrap FHS env) | wrapType2 FHS+bwrap, container-init | none | none | deps-verified (container --privileged: app starts, exit 0) |

## 6. Container runtime test ledger (fresh `docker run --rm` per test, base ubuntu:24.04, /nix/store mounted ro)

| package          | nix build | container ldd                          | container smoke                                                           | status   |
|---|---|---|---|---|
| helium 0.15.5.1    | PASS      | ZERO_NOT_FOUND (nix loader --list on 3 ELFs with full wrapper LD_LIBRARY_PATH, not-found=0 each) | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0 | verified |
| protonplus 0.6.4   | PASS      | ZERO_NOT_FOUND (NEEDED=0)             | `version` -> "ProtonPlus 0.6.4", exit 0; hooks created /tmp/pKE + /tmp/3J=1Z | verified |
| zen-browser 1.21.14b | PASS    | ZERO_NOT_FOUND (zen + libxul via nix loader) | `--version` -> "Mozilla Zen 1.21.14b", exit 0 (EPERM normal)              | verified |
| opencode-desktop v1.18.18 | PASS | ZERO_NOT_FOUND (electron real ELF + 4 glibc sidecars) | main process starts, "Missing X server or $DISPLAY" (GUI gap), reached display-init cleanly | verified |
| rootapp 0.9.126    | PASS      | ZERO_NOT_FOUND (.NET Root via bwrap FHS env) | bwrap wrapper (--privileged) -> "Starting application" / "Using profile default", exit 0 | verified |

- Interpreters verified: helium = nix glibc 2.42 loader (patchelf); zen = nix glibc 2.42 (autoPatchelfHook); opencode electron = nix glibc 2.42; rootapp = /lib64/ld-linux-x86-64.so.2 (correct - bundled glibc via bwrap FHS env).

## 7. Missing-package / new-app audit

- Inventory sync: README package list == flake.nix packages == update.yml blocks == pkgs/*.nix files (now 5 packages across both systems; x86_64 5, aarch64 4). In sync after this run's change.
- Upstream artifact types unchanged: helium tar.xz, protonplus sharun AppImage, zen tar.xz, opencode amd64+arm64 deb, rootapp fixed-URL AppImage. opencode NEW arm64 deb was the only arch/format change found - handled in commit 5f64d18.
- New-app scan (web search on all 5 upstream projects): no new official companion Linux desktop apps shipping prebuilt binaries that this repo should add. opencode's CLI is already in nixpkgs; desktop client already packaged; zen Twilight is a pre-release channel of the same app (not a separate package); rootapp closed-source no companion.

## 8. Issues / PRs handled

- Issue #2 (open): replied with diagnosis + closed - runner ENOSPC infra failure, repo healthy (evidence: all builds green this run).
- Issue #3 (NEW): filed "update.yml: extend Opencode Desktop block for arm64 hash (dual-arch)" with the exact diff. Root cause: `OPEN_OLD_HASH=$(grep -oP 'hash = ...')` now matches 2 hash lines, and the sed range only spans the amd64 URL. CANNOT push under .github/workflows/ (GITHUB_TOKEN lacks workflows scope) - owner must apply manually.
- PRs: none open.

## 9. Final state

- HEAD = 5f64d18 (this run's opencode aarch64 commit). Working tree clean (`git status --porcelain` empty) after the relay commit.
- flake.lock not touched (no nixpkgs bump; builds green with locked rev).

## Named gaps (not verified, and why)

- aarch64 BUILD: cannot build aarch64 binaries on this x86_64 runner. Verified aarch64 via `nix flake check --all-systems` (derivation eval green for all 4 aarch64 packages incl. new opencode-desktop) + prefetched hashes byte-matching. Named gap.
- GUI launch in container: no display/X server in ubuntu:24.04. Verified via headless flags + main-process start up to display init only. Named gap.
- opencode-desktop musl .node sidecars legitimately link musl libc (libc.so); intentionally ignored via autoPatchelfIgnoreMissingDeps - Electron loads the glibc variants on glibc systems. Correct by design.
- rootapp bwrap in non-privileged container needs userns; verified with `docker run --privileged`. On real NixOS desktops user namespaces are enabled by default.
- update.yml cannot be extended for the opencode arm64 hash by the autonomous bot (TOKENS rule) - documented in issue #3 for the owner. The package itself is current and correct regardless.