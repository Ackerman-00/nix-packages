# nix-packages autonomous-run relay state

run_id: (pending - set by workflow env)
status: complete

## 0. Pickup

- Previous relay (run 32220753750) was `status: complete`; nothing unfinished to continue.
- `git fetch origin && git pull --rebase origin main` => Already up to date.
- Runner Nix available, Docker 28.0.4 available, gh available. Git identity verified: `opencode-agent[bot] <41898282+opencode-agent[bot]@users.noreply.github.com>`.
- Inventory discovered from flake.nix: x86_64-linux = helium, protonplus, zen-browser, rootapp, opencode-desktop (+ default); aarch64-linux = helium, protonplus, zen-browser, opencode-desktop (+ default). 5 packages across both systems.

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): last 5 runs all SUCCESS. Most recent run 32203223778 (2026-08-19 00:58 UTC) committed `6899836 chore: auto-update packages` which clobbered the opencode-desktop arm64 hash (documented in issue #3). The package-side fixes from the previous run (ddcf613, 28d3bfc) are intact and the arm64 hash is correct.
- WARNING: the next daily update.yml run (2026-08-20 00:00 UTC) will re-clobber the arm64 hash because the opencode block in update.yml still has the single-hash grep bug. Owner must apply the diff from issue #3.
- Open issues: #3 (update.yml dual-arch opencode block - still open, owner must apply the diff). Added a verification comment this run.
- Open PRs: none.

## 2. Changes this run

- **No code changes pushed.** This was a verification-only run. All packages were already up-to-date and building green.
- Added a comment to issue #3 with this run's verification results.

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package          | packaged version | upstream latest (verified)                                  | status     |
|---|---|---|---|
| helium           | 0.15.5.1         | 0.15.5.1 (gh releases/latest)                               | up-to-date |
| protonplus       | 0.6.4            | v0.6.4 -> 0.6.4 (gh releases/latest)                       | up-to-date |
| zen-browser      | 1.21.14b         | 1.21.14b (gh releases/latest)                               | up-to-date |
| opencode-desktop | v1.18.18         | v1.18.18 (gh releases/latest)                               | up-to-date |
| rootapp          | latest (fixed URL) | hash unchanged after --refresh                              | up-to-date |

- All 9 x86_64+aarch64 asset URLs + rootapp fixed URL return HTTP 200.
- All 9 pinned SRI hashes re-prefetched with --refresh and byte-match pinned values.
- All 5 packages `meta.sourceProvenance = binaryNativeCode` verified.

## 4. Build/install verification ledger

| package          | build (x86_64) | aarch64 eval | binary smoke                                          | status |
|---|---|---|---|---|
| helium 0.15.5.1    | PASS           | PASS (eval)  | `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0 | pass   |
| protonplus 0.6.4   | PASS           | PASS (eval)  | `version` -> "ProtonPlus 0.6.4", exit 0               | pass   |
| zen-browser 1.21.14b | PASS         | PASS (eval)  | `--version` -> "Mozilla Zen 1.21.14b", exit 0         | pass   |
| opencode-desktop v1.18.18 | PASS    | PASS (eval)  | starts, stops at sandbox/display (GUI gap)            | pass   |
| rootapp latest     | PASS           | n/a (x86_64 only) | bwrap wrapper -> "Starting application", exit 0   | pass |

- `nix build .#helium .#protonplus .#zen-browser .#rootapp .#opencode-desktop --no-link` => EXIT 0.
- Store paths: helium=4lph1w68qgfcs1j9yyvyqrnpby345fdi, protonplus=bjmcfaj5zv7z0d0z6l4xs794qxl54r21, zen=v78acsdymsjr8k667aiiwswmp6s2xm1x, rootapp=l45vk28x07x9zr1x9400dl0ya17x2bhk, opencode=bd1brhshikpbxfalgj8wngrialm5n59b.
- `nix flake check --all-systems` => all checks passed.

## 5. Dependency audit (100% of inventory)

No packages changed since last audit (byte-identical artifacts). Previous audit stands.

| package          | upstream deps found | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.5.1    | 32 NEEDED; interpreter nix glibc 2.42 | all 32 in runtimeDeps | none | none | deps-verified |
| protonplus 0.6.4   | static-pie bun binary (NEEDED=0) | n/a (bundles everything) | none | none | deps-verified |
| zen-browser 1.21.14b | libxul 46 NEEDED, 27 bundled; external gtk3/alsa/dbus-glib/libXtst/ffmpeg | buildInputs + runtimeDependencies | none | none | deps-verified |
| opencode-desktop v1.18.18 | electron + 3 glibc .node sidecars; musl sidecars ignored | electron_42 + stdenv.cc.cc.lib | none | none | deps-verified |
| rootapp 0.9.126    | .NET Root binary; interpreter /lib64/ld-linux (bwrap FHS) | wrapType2 FHS+bwrap | none | none | deps-verified |

## 6. Container runtime test ledger

| package          | nix build | container ldd       | container smoke                                          | status   |
|---|---|---|---|---|
| helium 0.15.5.1    | PASS      | ZERO_NOT_FOUND      | `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0 | verified |
| protonplus 0.6.4   | PASS      | ZERO_NOT_FOUND (NEEDED=0) | `version` -> "ProtonPlus 0.6.4", exit 0          | verified |
| zen-browser 1.21.14b | PASS    | ZERO_NOT_FOUND      | `--version` -> "Mozilla Zen 1.21.14b", exit 0    | verified |
| opencode-desktop v1.18.18 | PASS | ZERO_NOT_FOUND      | starts, sandbox/display stop (GUI gap)             | verified |
| rootapp 0.9.126    | PASS      | ZERO_NOT_FOUND      | bwrap wrapper -> "Starting application", exit 0    | verified |

## 7. Missing-package / new-app audit

- Inventory sync: README (5) == flake.nix (x86_64 5, aarch64 4) == update.yml (5) == pkgs/*.nix (5). In sync.
- Upstream artifact types unchanged for all 5 packages.
- New-app scan: no new official companion Linux desktop apps shipping prebuilt binaries found.

## 8. Issues / PRs handled

- Issue #3: added verification comment documenting all 5 packages verified, hashes correct, updater bug still active. Owner must apply the diff.
- PRs: none open.

## 9. Final state

- HEAD = 71c259c (previous run's relay completion). Working tree clean. No new commits this run.
- flake.lock not touched.

## Named gaps (not verified, and why)

- aarch64 BUILD: cannot build aarch64 binaries on this x86_64 runner. Verified via `nix flake check --all-systems` (eval green) + prefetched arm64 hashes match. Named gap.
- GUI launch in container: no display/X server. Verified via headless flags + main-process start. Named gap.
- opencode-desktop musl .node sidecars legitimately link musl libc; ignored via autoPatchelfIgnoreMissingDeps. Correct by design.
- rootapp bwrap needs userns; verified with `docker run --privileged`. Named gap.
- update.yml opencode block still has single-hash grep bug (issue #3). Cannot push workflow changes (TOKENS rule). Next daily run will re-clobber arm64 hash - check and restore on pickup.
