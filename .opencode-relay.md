# nix-packages autonomous-run relay state

status: complete
run_id: 31791923440

## 0. Pickup

- Previous relay (run 31777329288) was `status: complete`; nothing unfinished to continue.
- Started fresh on main @ ca7af52. Nix on this fresh runner was 2.16.1; upgraded to 2.34.8 (locked nixpkgs d407951 requires >= 2.18) via `sudo env "PATH=$PATH" nix upgrade-nix`. Shallow clone unshallowed for NO-CHURN git-log checks.

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): last 12 runs ALL green; last run 31760732709 (2026-08-14 01:28) committed nothing (no upstream change).
- No open issues. No open PRs.

## 2. Fixes this run

- **zen-browser (BROKEN, fixed)**: commit 74016fd added the package with `cp -r "$src"/* $out/lib/zen-bin-${version}/` in installPhase. `$src` is the tar.xz FILE (store path), not the unpacked dir — cwd after unpack is the source root `zen`. Reproduced: `cp: missing destination file operand after '.../zen-bin-1.21.14b/'`. Fixed to `cp -r . $out/lib/zen-bin-${version}/`. Verified: `nix build .#zen-browser` green; binary prints "Mozilla Zen 1.21.14b", exit 0. **Pushed as 4209ef7** ("fix(autonomous): zen-browser: copy unpacked tree instead of tar.xz glob (broken build)").
- Nothing else was stale or broken; no churn commits (NO-CHURN rule). Nix on runner upgraded to 2.34.8 (runner-only).

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| helium | 0.15.4.1 | 0.15.4.1 (gh releases latest, pub 2026-08-12) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (strip v, pub 2026-08-13) | up-to-date |
| zen-browser | 1.21.14b | 1.21.14b (gh releases latest, pub 2026-08-13) | up-to-date |
| opencode-desktop | v1.18.18 | v1.18.18 (gh releases latest, pub 2026-08-13) | up-to-date |
| rootapp | latest (fixed URL) | hash unchanged after `--refresh` re-prefetch (BzaSb96...) | up-to-date |

- All pinned SRI hashes re-prefetched with `--refresh` and match byte-for-byte (x86_64 + aarch64 for helium/protonplus/zen-browser; opencode-deb; rootapp fixed URL).
- All 8 source URLs return HTTP 200 (`curl -sI`).
- README package list matches flake.nix exactly (rootapp, opencode-desktop, helium, protonplus, zen-browser).

## 4. Build/install verification ledger

| package | build (x86_64) | aarch64 eval | binary smoke | status |
|---|---|---|---|---|
| helium | PASS (fresh --refresh) | PASS (eval .name) | `--version` -> "Helium 0.15.4.1 (Chromium 151.0.7922.137)" | pass |
| protonplus | PASS (fresh --refresh) | PASS (eval .name) | `version` -> "ProtonPlus 0.6.4" | pass |
| zen-browser | PASS (fresh --refresh, fixed) | PASS (eval .name) | `--version` -> "Mozilla Zen 1.21.14b" | pass |
| opencode-desktop | PASS (fresh --refresh) | n/a (x86_64 only) | launches under xvfb, stays alive until timeout (GUI needs display = gap) | pass |
| rootapp | PASS (fresh --refresh) | n/a (x86_64 only) | starts ("Starting application", "Using profile default") under `--privileged` | pass |

- `nix flake check --all-systems` green on final main (both systems evaluate; aarch64 BUILD impossible on x86_64 runner - named gap). Also re-run from a FRESH clone at /tmp/fresh-check on committed main @ 4209ef7 - all checks passed.
- sourceProvenance = [binaryNativeCode] on all 5 packages (nix eval verified).

## 5. Dependency audit (100% of inventory)

| package | upstream deps found | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium | union NEEDED of all ELFs = 39 sonames | all covered by runtimeDeps (Qt shims resolve to qtbase) | none | extra-but-harmless (dlopen coverage) | deps-verified (re-read from tarball this run, unchanged since 0.15.4.1) |
| protonplus | static bun + bundled bin/shared/lib/etc | binary bundles everything | none | none | deps-verified |
| zen-browser | NEEDED union of all ELFs (zen-bin, libxul.so, 20+ libs): X11/xcb family, asound, atk, dbus, fontconfig, freetype, gtk3/gdk/pango/cairo/glib, ffmpeg/avcodec (dlopen), libva, libpulse (dlopen, soft audio fallback to ALSA), glibc | gtk3 (pulls X11/fontconfig/freetype/dbus/cairo/pango), alsa-lib, dbus-glib, libXtst, ffmpeg_8, runtimeDependencies [curl libva pciutils libGL], appendRunpaths [libGL pipewire] | none added (all resolved) | none | deps-verified (lddtree on every ELF: ZERO not-found) |
| opencode-desktop | electron_42 (42.5.1 from locked nixpkgs; deb bundled 42.3.3, same major) + .node sidecars (glibc variants only) | electron_42 via callPackage | none | none | deps-verified |
| rootapp | .NET self-contained + bwrap FHS sandbox | wrapType2 FHS | none | none | deps-verified (medium confidence - FHS self-contained, ldd not applicable to bwrap wrapper) |

## 6. Container runtime test ledger (fresh `docker run --rm` per test, base ubuntu:24.04)

| package | nix build | container ldd | container smoke | status |
|---|---|---|---|---|
| helium | PASS | interpreter = nix ld-linux; all NEEDED resolve (binary runs, so all 39 present) | `--version` -> "Helium 0.15.4.1 (Chromium 151.0.7922.137)", exit 0 | verified |
| protonplus | PASS | 0 not found (static bun) | `version` -> "ProtonPlus 0.6.4", exit 0; hook artifacts verified: /tmp/.___host-certs/ca-certificates.crt -> host CA, /tmp/pKE -> lib, /tmp/3J=1Z -> share | verified |
| zen-browser | PASS | lddtree every ELF: ZERO not-found (container `ldd` shows a GLIBC_ABI_DT_X86_64_PLT artifact only because Ubuntu's ld.so 2.39 interprets nix glibc 2.42 libs - binary itself runs) | `--version` -> "Mozilla Zen 1.21.14b", exit 0 (fresh-clone store path identical v78acsdym...) | verified |
| opencode-desktop | PASS | wrapper + electron 42.5.1 binary: ZERO not-found | electron --version -> "v42.5.1", exit 0; app stays alive under xvfb until timeout | verified (GUI launch gap) |
| rootapp | PASS | FHS sandbox resolves (bwrap wrapper) | starts ("Starting application", "Using profile default") under `--privileged` (docker blocks userns -> privileged simulates real machine) | verified (needs userns) |

- Named gaps: aarch64 BUILD impossible on x86_64 runner (eval only, all 3 aarch64 packages evaluate green); GUI apps full window rendering needs a display (xvfb used for smoke only).

## 7. What remains

- Nothing. Repo is current, all 5 packages build and pass clean-container runtime tests, all hashes match upstream, no open issues/PRs.

## 8. Final state

- main @ 4209ef7 (zen-browser build fix). Working tree clean.
- Runner nix upgraded to 2.34.8 (runner-only, not a repo change).
- README.md package list matches flake.nix (rootapp, opencode-desktop, helium, protonplus, zen-browser).