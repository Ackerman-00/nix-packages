# nix-packages autonomous-run relay state

run_id: 32220753750
status: complete

## 0. Pickup

- Previous relay (run 32198000513) was `status: complete`; nothing unfinished to continue.
- `git fetch origin && git pull --rebase origin main` => Already up to date.
- Runner Nix 2.35.2, Docker 28.0.4 (hello-world pass), gh 2.97.0. Git identity verified before every commit: `opencode-agent[bot] <41898282+opencode-agent[bot]@users.noreply.github.com>`.
- Inventory discovered from flake.nix / `nix eval --apply attrNames`, not hardcoded: x86_64-linux = helium, protonplus, zen-browser, rootapp, opencode-desktop (+ default); aarch64-linux = helium, protonplus, zen-browser, opencode-desktop (+ default). 5 packages across both systems.

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): run 32203223778 (2026-08-19 00:58 UTC) reported SUCCESS but committed `6899836 chore: auto-update packages`, which CLOBBERED the opencode-desktop arm64 hash (set it to the amd64 hash `sha256-e31ka...`). This is exactly the bug documented in issue #3: `OPEN_OLD_HASH=$(grep -oP 'hash = "\K[^"]+' "$OPEN_FILE")` now matches TWO hash lines (multi-line), so the `[ "$OPEN_OLD_HASH" != "$OPEN_NEW_HASH" ]` comparison ALWAYS fires and the range-sed overwrites BOTH hashes with the amd64 hash. The aarch64-linux build was broken by that commit (hash mismatch on the arm64 deb). Prior runs (32086477571 and earlier) all SUCCESS with no churn.
- opencode-schedule: this run 32220753750 is current. Prior run 32198000513 SUCCESS.
- Open issues: #3 (update.yml dual-arch opencode block - still open, owner must apply the diff, workflow files cannot be pushed by the bot). This run added a comment to #3 documenting that the bug FIRED and what was fixed package-side.
- Open PRs: none.

## 2. Changes this run (two atomic commits, pushed to main)

- **ddcf613** `fix(autonomous): opencode-desktop: restore correct aarch64 arm64 deb hash (clobbered by updater sed)`:
  - pkgs/opencode-desktop.nix aarch64 hash restored to `sha256-7qP2pb+BfTPx0wYVialVP6M3Cb8SWqz+T+Z38x8f6FM=` (verified via `nix store prefetch-file --refresh` on the arm64 URL; the file had been set to the amd64 hash by the auto-update commit).
  - This was a BROKEN package on aarch64-linux before the fix (hash mismatch). x86_64 unaffected (same hash), verified `nix build .#opencode-desktop` green.
- **28d3bfc** `fix(autonomous): opencode-desktop: use ${version} in URLs so version bumps propagate to both arch URLs`:
  - The aarch64 support commit (5f64d18) had hardcoded `v1.18.18` in both URL strings instead of `${version}`. update.yml only rewrites `version = "..."` (via `sed -i 's|version = ".*"|...'`), so on the next opencode version bump the URLs would stay at v1.18.18 while the hash moved to the new version's artifact - a broken build. Changed URLs to `.../download/${version}/...`, hoisted `version = "v1.18.18"` + `pname` into the `let` binding.
  - Store path unchanged: `bd1brhshikpbxfalgj8wngrialm5n59b` (verified before/after - zero churn).
- No version/hash churn on helium, protonplus, zen-browser, rootapp - all byte-match their pinned hashes and upstream versions (re-prefetched with `--refresh` this run).

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package          | packaged version | upstream latest (verified 2026-08-19)                        | status     |
|---|---|---|---|
| helium           | 0.15.5.1         | 0.15.5.1 (gh releases/latest + releasealert 2026-08-15)      | up-to-date |
| protonplus       | 0.6.4            | v0.6.4 -> 0.6.4 (gh releases/latest + release page)          | up-to-date |
| zen-browser      | 1.21.14b         | 1.21.14b (gh releases/latest + release page)                 | up-to-date |
| opencode-desktop | v1.18.18         | v1.18.18 (gh releases/latest + changelog 2026-08-13)         | up-to-date (arch hash FIXED) |
| rootapp          | latest (fixed URL) | hash unchanged after `--refresh`; in-artifact X-AppImage-Version = 0.9.126 (Root.desktop, re-torn this run) | up-to-date |

- All 9 x86_64+aarch64 asset URLs + rootapp fixed URL return HTTP 200 (`curl -sIL`).
- All 9 pinned SRI hashes re-prefetched with `--refresh` and byte-match the pinned values (helium x64/arm, protonplus x64/arm, zen x64/arm, opencode amd64 + arm64, rootapp).
- Updater simulation for all 5 blocks = "no change" for helium/protonplus/zen/rootapp; opencode block SIMULATES THE BUG: `OPEN_OLD_HASH` is 2-line so it always fires (see section 1 / issue #3).

## 4. Build/install verification ledger

| package          | build (x86_64) | aarch64 eval | binary smoke                                                                   | status |
|---|---|---|---|---|
| helium 0.15.5.1    | PASS           | PASS (eval)  | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0      | pass   |
| protonplus 0.6.4   | PASS           | PASS (eval)  | `version` -> "ProtonPlus 0.6.4", exit 0                                        | pass   |
| zen-browser 1.21.14b | PASS         | PASS (eval)  | `--version` -> "Mozilla Zen 1.21.14b", exit 0 (sandbox EPERM warning normal)   | pass   |
| opencode-desktop v1.18.18 | PASS    | PASS (eval)  | main process starts, stops at "Missing X server or $DISPLAY" (GUI gap)         | pass   |
| rootapp latest     | PASS           | n/a (x86_64 only) | bwrap wrapper (--privileged) -> "Starting application" / "Using profile default", exit 0 | pass |

- `nix build .#helium .#protonplus .#zen-browser .#rootapp .#opencode-desktop --no-link` => EXIT 0 on current main.
- Store paths: helium=4lph1w68qgfcs1j9yyvyqrnpby345fdi, protonplus=bjmcfaj5zv7z0d0z6l4xs794qxl54r21, zen=v78acsdymsjr8k667aiiwswmp6s2xm1x, rootapp=l45vk28x07x9zr1x9400dl0ya17x2bhk, opencode=bd1brhshikpbxfalgj8wngrialm5n59b. All five byte-identical to the previous verified run (zero churn).
- `nix flake check --all-systems` => all checks passed (x86_64 + aarch64 eval green) on current main and on a fresh clone.
- All 5 packages `meta.sourceProvenance = binaryNativeCode` (verified via `nix eval` this run).
- opencode app.asar sha256 = d02785a88e0e35e5e5b755fa8fe5cb444f5825a67030673abaaeb10af4dc3808 (unchanged, matches record); in-asar package.json version = 1.18.18 (torn this run).

## 5. Dependency audit (100% of inventory)

- Only opencode-desktop changed (hash/URL restructure; artifact bytes identical to last audit) => no other RE-TEAR required; all other packages byte-match last run's audited artifacts.

| package          | upstream deps found (NEEDED sonames) | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.5.1    | 32 NEEDED across helium ELF; interpreter patched to nix glibc 2.42 | all 32 in runtimeDeps; patchelf --set-interpreter | none | none | deps-verified (container: wrapper LD_LIBRARY_PATH 43 dirs reconstructed; nix loader --list on helium + crashpad => not-found=0 each) |
| protonplus 0.6.4   | static-pie bun binary (NEEDED=0); desktop X-AppImage-Version=0.6.4 | n/a (bundles everything) | none | none | deps-verified (container: NEEDED=0, hooks create /tmp/pKE -> lib + /tmp/3J=1Z -> share; CA-certs warning expected in bare container) |
| zen-browser 1.21.14b | libxul 46 NEEDED, 27 bundled; external gtk3/alsa/dbus-glib/libXtst/ffmpeg + runtimeDeps curl/libva/pciutils/libGL | buildInputs + runtimeDependencies + appendRunpaths + autoPatchelfHook + patchelfUnstable | none | none | deps-verified (container: zen + libxul loader --list => not-found=0 each; application.ini Version=1.21.14b BuildID=20260811103047) |
| opencode-desktop v1.18.18 | electron real ELF resolves via nix loader; 3 glibc .node sidecars (watcher, napi.glibc, abi115.glibc) resolve; musl sidecars link libc.so (musl, expected/ignored) | electron_42 + stdenv.cc.cc.lib + autoPatchelfIgnoreMissingDeps libc.musl-* | none | none | deps-verified (container: electron not-found=0; glibc sidecars not-found=0 each; arm64 URL eval store path == prefetch store path) |
| rootapp 0.9.126    | .NET Root binary; interpreter /lib64/ld-linux (correct - bundled glibc via bwrap FHS env) | wrapType2 FHS+bwrap, container-init | none | none | deps-verified (container --privileged: app starts, exit 0) |

## 6. Container runtime test ledger (fresh `docker run --rm` per test, base ubuntu:24.04, /nix/store mounted ro)

| package          | nix build | container ldd                          | container smoke                                                           | status   |
|---|---|---|---|---|
| helium 0.15.5.1    | PASS      | ZERO_NOT_FOUND (nix loader on helium + crashpad with wrapper LD_LIBRARY_PATH) | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0 | verified |
| protonplus 0.6.4   | PASS      | ZERO_NOT_FOUND (NEEDED=0)             | `version` -> "ProtonPlus 0.6.4", exit 0; hooks created /tmp/pKE + /tmp/3J=1Z | verified |
| zen-browser 1.21.14b | PASS    | ZERO_NOT_FOUND (zen + libxul via nix loader) | `--version` -> "Mozilla Zen 1.21.14b", exit 0 (EPERM normal)              | verified |
| opencode-desktop v1.18.18 | PASS | ZERO_NOT_FOUND (electron real ELF + 3 glibc sidecars) | main process starts, "Missing X server or $DISPLAY" (GUI gap), reached display-init cleanly | verified |
| rootapp 0.9.126    | PASS      | ZERO_NOT_FOUND (.NET Root via bwrap FHS env) | bwrap wrapper (--privileged) -> "Starting application" / "Using profile default", exit 0 | verified |

- Interpreters verified: helium = nix glibc 2.42 loader (patchelf); zen = nix glibc 2.42 (autoPatchelfHook); opencode electron = nix glibc 2.42; rootapp = /lib64/ld-linux-x86-64.so.2 (correct - bundled glibc via bwrap FHS env).

## 7. Missing-package / new-app audit

- Inventory sync: README package list (5) == flake.nix packages (x86_64 5 + default, aarch64 4 + default) == update.yml blocks (5) == pkgs/*.nix files (5). In sync.
- Upstream artifact types unchanged: helium tar.xz, protonplus sharun AppImage, zen tar.xz, opencode amd64+arm64 deb, rootapp fixed-URL AppImage. opencode still ships both arch debs in v1.18.18 (verified assets).
- New-app scan (web search on all 5 upstream projects 2026-08-19): no new official companion Linux desktop apps shipping prebuilt binaries that this repo should add. opencode CLI already in nixpkgs; desktop already packaged; Helium/ProtonPlus/Zen/RootApp have no new companions. No pending feature requests in open issues.

## 8. Issues / PRs handled

- Issue #3 (open, workflow bug): added a comment this run documenting that the bug FIRED (commit 6899836 clobbered the arm64 hash on 2026-08-19), the package-side fixes (ddcf613, 28d3bfc), and reaffirming the owner must apply the update.yml diff (cannot be pushed by the bot - GITHUB_TOKEN lacks workflows scope).
- PRs: none open.

## 9. Final state

- HEAD = 28d3bfc (this run's second fix). Working tree clean (`git status --porcelain` empty) after the relay commit.
- flake.lock not touched (no nixpkgs bump; builds green with locked rev).

## Named gaps (not verified, and why)

- aarch64 BUILD: cannot build aarch64 binaries on this x86_64 runner. Verified aarch64 via `nix flake check --all-systems` (derivation eval green for all 4 aarch64 packages) + prefetched arm64 hash byte-matching + eval store path == prefetch store path for the arm64 deb. Named gap.
- GUI launch in container: no display/X server in ubuntu:24.04. Verified via headless flags + main-process start up to display init only. Named gap.
- opencode-desktop musl .node sidecars legitimately link musl libc (libc.so); intentionally ignored via autoPatchelfIgnoreMissingDeps - Electron loads the glibc variants on glibc systems. Correct by design.
- rootapp bwrap in non-privileged container needs userns; verified with `docker run --privileged`. On real NixOS desktops user namespaces are enabled by default.
- update.yml cannot be extended/fixed for the opencode dual-arch block by the autonomous bot (TOKENS rule) - documented in issue #3 for the owner. WARNING FOR NEXT RUN: if the auto-update fires again (it will, because OPEN_OLD_HASH is multi-line), it will re-clobber the arm64 hash. Check pkgs/opencode-desktop.nix arm64 hash on pickup and restore to sha256-7qP2pb+BfTPx0wYVialVP6M3Cb8SWqz+T+Z38x8f6FM= if changed (or prefetch the arm64 URL fresh). The package itself is correct and current regardless.