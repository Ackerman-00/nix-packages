# nix-packages autonomous-run relay state

status: complete
run_id: 31898813985

## 0. Pickup

- Previous relay (run 31882403357) was `status: complete`; nothing unfinished to continue. Started fresh on main @ 9ad7011.
- Runner nix was 2.16.1 but the locked nixpkgs (rev d407951) requires >= 2.18 (`nix flake show` aborted on rootapp eval with "expected a derivation"). Upgraded to 2.34.8 via `sudo env "PATH=$PATH" nix upgrade-nix`. Runner-only change, not committed. After upgrade: `nix flake show` evaluates all 5 packages on both systems.
- Docker 28.0.4 verified working (hello-world); ubuntu:24.04 used for all container tests.

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): last 10 runs ALL green; last run 31855199837 (2026-08-15 00:58) succeeded. No failed runs, no churn commits.
- No open issues (`gh api "repos/Ackerman-00/nix-packages/issues?state=open"` -> empty). No open PRs (`gh api .../pulls?state=open` -> empty).

## 2. Fixes this run

- NONE REQUIRED. Full independent fresh-checker verification: every package current, builds green, runtime-verified in clean containers, hashes match upstream byte-for-byte, real versions torn from inside artifacts.

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| helium | 0.15.4.1 | 0.15.4.1 (gh releases latest) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (strip v) | up-to-date |
| zen-browser | 1.21.14b | 1.21.14b (gh releases latest) | up-to-date |
| opencode-desktop | v1.18.18 | v1.18.18 (gh releases latest) | up-to-date |
| rootapp | latest (fixed URL) | hash unchanged after `--refresh` re-prefetch | up-to-date |

- All 8 pinned SRI hashes re-prefetched with `--refresh` and match byte-for-byte (helium x86_64+aarch64, protonplus x86_64+aarch64, zen x86_64+aarch64, opencode deb). Rootapp fixed URL re-prefetched -> same hash (sha256-BzaSb96SOBnSdqxtSjr+ZcbYfHNGia9HJ2/E6/B12RA=).
- Real versions torn from inside artifacts in /tmp/audit: helium tar.xz dir name `helium-0.15.4.1-x86_64_linux`; protonplus `version` -> "ProtonPlus 0.6.4"; zen application.ini Version=1.21.14b + BuildID=20260811103047; opencode deb top-level package.json + app.asar package.json both "1.18.18"; rootapp X-AppImage-Version=0.9.126 (Type-2 AppImage, pinned version "latest").
- update.yml source URLs all in sync with pkgs/*.nix (5 blocks, no drift).
- README package list (rootapp, opencode-desktop, helium, protonplus, zen-browser) matches flake.nix exactly.

## 4. Build/install verification ledger

| package | build (x86_64) | aarch64 eval | binary smoke | status |
|---|---|---|---|---|
| helium | PASS | PASS (eval .name = "helium-0.15.4.1") | installCheckPhase + container: "Helium 0.15.4.1 (Chromium 151.0.7922.137)" | pass |
| protonplus | PASS | PASS (eval .name) | installCheckPhase + container: "ProtonPlus 0.6.4" | pass |
| zen-browser | PASS | PASS (eval .name) | `--version` -> "Mozilla Zen 1.21.14b" | pass |
| opencode-desktop | PASS | n/a (x86_64 only) | electron --no-sandbox --version -> "v42.5.1"; app main process starts headless (X11 init fails without DISPLAY = gap) | pass (GUI launch gap) |
| rootapp | PASS | n/a (x86_64 only) | starts ("Starting application", "Using profile default") under `--privileged` | pass |

- `nix flake check --all-systems` green on main AND on a fresh clone at /tmp/fresh-check (both systems eval; aarch64 BUILD impossible on x86_64 runner = accepted named gap).
- Fresh clone with `--refresh` produced IDENTICAL store paths (helium xmsjwcf..., protonplus bjmcfaj..., zen v78acsd..., opencode vi5mdch..., rootapp l45vk2...) - deterministic.
- sourceProvenance = [binaryNativeCode] on all 5 packages (nix eval verified).

## 5. Dependency audit (100% of inventory)

| package | upstream deps found | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium | 32 NEEDED sonames torn from artifact binary (X11 family, asound, atk/atspi, cairo, cups, dbus, expat, gbm, nspr/nss, pango, udev, xcb, xkbcommon) | all covered by runtimeDeps | none | extra-but-harmless (dlopen coverage) | deps-verified (nix loader --list with wrapper LD_LIBRARY_PATH: ZERO "not found" in container; every non-glibc soname individually confirmed resolved) |
| protonplus | static bun + bundled bin/shared/lib/etc; hooks 01-check-ca-certs + 01-path-mapping-hardcoded | binary bundles everything; wrapper sources 01-* hooks | none | none | deps-verified |
| zen-browser | libxul + libmozsqlite3 + gtk3/alsa/dbus/cairo/pango/X11 sonames | gtk3, alsa-lib, dbus-glib, libXtst, ffmpeg_8, autoPatchelfHook, runtimeDependencies, appendRunpaths | none | none | deps-verified (container ldd on zen + libxul: ZERO not found) |
| opencode-desktop | electron_42 (42.5.1) + 7 .node sidecars (parcel watcher glibc/musl, node-pty, msgpackr) | electron_42 via callPackage; autoPatchelfIgnoreMissingDeps for musl variants | none (musl variants intentionally unloadable on glibc - glibc twins exist and resolve) | none | deps-verified (nix loader on pty.node + glibc watcher: ZERO not found in container; electron ldd ZERO not found) |
| rootapp | .NET self-contained + bwrap FHS sandbox | wrapType2 FHS | none | none | deps-verified (medium confidence - FHS self-contained; bwrap ldd ZERO not found) |

- No upstream change since last audit (all versions/hashes byte-identical), so previous dependency derivations stand; independently re-verified each package's key sonames in fresh containers.

## 6. Container runtime test ledger (fresh `docker run --rm` per test, base ubuntu:24.04)

| package | nix build | container ldd | container smoke | status |
|---|---|---|---|---|
| helium | PASS | ZERO not found (nix loader --list, wrapper LD_LIBRARY_PATH) | `--version` -> "Helium 0.15.4.1 (Chromium 151.0.7922.137)", exit 0 | verified |
| protonplus | PASS | ZERO not found (static bun) | `version` -> "ProtonPlus 0.6.4", exit 0; hooks: /tmp/pKE + /tmp/3J=1Z symlinks created; CA-certs hook verified firing (mounted a host cert -> /tmp/.___host-certs symlink created) | verified |
| zen-browser | PASS | ZERO not found (container ldd on zen + libxul) | `--version` -> "Mozilla Zen 1.21.14b", exit 0; interpreter = nix glibc loader | verified |
| opencode-desktop | PASS | ZERO not found (nix loader on pty.node + glibc watcher; electron ldd) | electron --no-sandbox --version -> "v42.5.1", exit 0; app wrapper launches electron main process headless (fails only at X11 init, no DISPLAY) | verified (GUI launch gap) |
| rootapp | PASS | ZERO not found (bwrap) | starts ("Starting application", "Using profile default") under `--privileged` (bwrap needs userns), exit 0 | verified (needs userns) |

- Named gaps: aarch64 BUILD impossible on x86_64 runner (eval only, all 3 aarch64 packages evaluate green); GUI apps full window rendering needs a display (headless smoke only); rootapp needs user-namespace support (--privileged on the runner simulates a real machine); protonplus CA-cert hook only fires when a host cert store exists (verified it fires with one mounted).

## 7. Adversarial audit

- Re-queried all 4 GitHub release tags at end of run (unchanged: 0.15.4.1 / v0.6.4 / 1.21.14b / v1.18.18) and rootapp fixed-URL hash (unchanged) - no mid-run drift.
- Store paths identical between main working tree and fresh `--refresh` clone - deterministic, not stale cache.
- Interpreter on zen + helium ELFs points at nix glibc loader, not /lib64 (verified).
- update.yml/pkgs URL sync + README/flake inventory sync confirmed; no auto-update churn in history.

## 8. What remains

- Nothing. Repo is current, all 5 packages build and pass clean-container runtime tests, all hashes match upstream, no open issues/PRs, no failing Actions runs.

## 9. Final state

- main @ 9ad7011 + relay update commit (no package changes needed this run - everything already current; runner-only nix upgrade to 2.34.8). Working tree clean.
- README.md package list matches flake.nix (rootapp, opencode-desktop, helium, protonplus, zen-browser).