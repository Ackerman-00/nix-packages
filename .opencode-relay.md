# nix-packages autonomous-run relay state

status: complete
run_id: 31979546571

## 0. Pickup

- Previous relay (run 31962038894) was `status: complete`; nothing unfinished to continue. Started fresh on main @ a2ae359.
- Runner nix was 2.16.1 but the locked nixpkgs (rev d407951) requires >= 2.18. Upgraded to 2.34.8 via `sudo env "PATH=$PATH" nix upgrade-nix`. Runner-only change, not committed.
- Docker 28.0.4 verified (hello-world); ubuntu:24.04 used for all container tests (fresh `docker run --rm` per test).
- Repo was shallow-cloned (1 commit); unshallowed via `git fetch --unshallow` (now 93 commits).
- Inventory discovered from flake.nix (5 packages): x86_64-linux = helium, protonplus, zen-browser, rootapp, opencode-desktop; aarch64-linux = helium, protonplus, zen-browser.

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): last 5 runs ALL green (latest 31918622716, 2026-08-16 01:01). No failed runs.
- No open issues. No open PRs (queried both APIs, empty).

## 2. Changes this run

- ZERO package changes: every package is at its current upstream version with byte-matching hashes. Verified, not assumed (evidence in sections 3-6). No churn commits generated.

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| helium | 0.15.5.1 | 0.15.5.1 (gh releases latest; 5-recent-tags list shows nothing newer) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (strip v) | up-to-date |
| zen-browser | 1.21.14b | 1.21.14b (gh releases latest) | up-to-date |
| opencode-desktop | v1.18.18 | v1.18.18 (gh releases latest) | up-to-date |
| rootapp | latest (fixed URL) | hash unchanged after `--refresh` re-prefetch; real version torn from extracted AppImage = 0.9.126 | up-to-date |

- All 8 pinned SRI hashes re-prefetched with `--refresh` and match byte-for-byte (helium x64+arm, protonplus x64+arm, zen x64+arm, opencode deb, rootapp fixed URL).
- Real versions torn from artifacts: rootapp Root.desktop `X-AppImage-Version=0.9.126` (Comment "Root 0.9.126"); opencode deb control `Version: 1.18.18`; zen application.ini `Version=1.21.14b`; helium `--version` prints "Helium 0.15.5.1 (Chromium 151.0.7922.137)"; protonplus `version` prints "ProtonPlus 0.6.4".
- update.yml source URLs all in sync with pkgs/*.nix (5 blocks, no drift).
- README package list matches flake.nix exactly (rootapp, opencode-desktop, helium, protonplus, zen-browser).
- Adversarial: queried 5 recent tags per GitHub repo - no newer prerelease/release masked by `releases/latest`.

## 4. Build/install verification ledger

| package | build (x86_64) | aarch64 eval | binary smoke | status |
|---|---|---|---|---|
| helium 0.15.5.1 | PASS | PASS (eval .name = "helium-0.15.5.1") | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0 | pass |
| protonplus | PASS | PASS (eval .name) | `version` -> "ProtonPlus 0.6.4", exit 0 | pass |
| zen-browser | PASS | PASS (eval .name) | `--version` -> "Mozilla Zen 1.21.14b", exit 0 | pass |
| opencode-desktop | PASS | n/a (x86_64 only) | locked electron 42.5.1 `--no-sandbox --version` -> "v42.5.1", exit 0; wrapper main process reaches X11 init then fails at no DISPLAY | pass (GUI launch gap) |
| rootapp | PASS | n/a (x86_64 only) | "Starting application" / "Using profile default" under --privileged, exit 0 | pass (needs userns) |

- `nix flake check --all-systems` green on main (all x86_64 derivations + aarch64 derivations evaluated; aarch64 BUILD impossible on x86_64 runner = accepted named gap).
- Fresh-checker re-run: fresh clone of main @ a2ae359, `nix build .#<all 5> --no-link --refresh` produced byte-identical store paths (helium 4lph1w68..., protonplus bjmcfaj..., zen v78acsd..., rootapp l45vk2..., opencode vi5mdch...) - deterministic, not stale cache.
- sourceProvenance = [binaryNativeCode] on all 5 packages (`nix eval --no-eval-cache`).

## 5. Dependency audit (100% of inventory)

| package | upstream deps found | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.5.1 | 30 NEEDED sonames torn from NEW tarball (X11 family, asound, atk/atk-bridge/atspi, cairo, cups, dbus, expat, gbm, glib/gio, nspr/nss/smime, pango, udev, xcb, xkbcommon; libqt5_shim needs Qt5Core/Gui/Widgets) | all covered by runtimeDeps (incl. qt5.qtbase, stdenv.cc.cc.lib for libstdc++) | none | extra-but-harmless (dlopen coverage incl. qt6 shims, libva, pipewire, pulse) | deps-verified (nix loader --list with wrapper LD_LIBRARY_PATH: ZERO "not found" in container; 47 LD_LIBRARY_PATH entries) |
| protonplus | static-pie bun binary (0 NEEDED) + bundled bin/shared/lib/etc; hooks 01-check-ca-certs + 01-path-mapping-hardcoded | binary bundles everything; wrapper sources 01-* hooks | none | none | deps-verified (hooks re-verified with host CA store present: /tmp/.___host-certs/ca-certificates.crt symlink created; /tmp/pKE + /tmp/3J=1Z symlinks created) |
| zen-browser | libxul NEEDED: X11 family, asound, atk, cairo, dbus, fontconfig, freetype, gtk3/gdk-pixbuf, pango, nspr/nss | gtk3, alsa-lib, dbus-glib, libXtst, ffmpeg_8, autoPatchelfHook, runtimeDependencies, appendRunpaths | none | none | deps-verified (container nix-loader on zen + libxul: ZERO "not found") |
| opencode-desktop | locked electron_42 (42.5.1) + 7 .node sidecars (4 glibc, 3 musl) | electron_42 via callPackage; autoPatchelfIgnoreMissingDeps for musl variants | none (musl twins intentionally unloadable on glibc - glibc twins resolve) | none | deps-verified (container nix-loader on pty.node + glibc watcher + 2 glibc msgpackr sidecars: ZERO "not found") |
| rootapp | .NET self-contained + bwrap FHS sandbox (usr/bin/Root ELF) | wrapType2 FHS | none | none | deps-verified (medium confidence - FHS self-contained; runs in container under --privileged) |

- No upstream change for any package since last audit (all versions/hashes byte-identical, re-verified with --refresh), so previous dependency derivations stand; independently re-torn each package's key artifacts this run.

## 6. Container runtime test ledger (fresh `docker run --rm` per test, base ubuntu:24.04, /nix/store mounted ro)

| package | nix build | container ldd | container smoke | status |
|---|---|---|---|---|
| helium 0.15.5.1 | PASS | ZERO not found (nix glibc-2.42 loader --list with wrapper LD_LIBRARY_PATH, 47 entries) | wrapper `--version` -> "Helium 0.15.5.1", exit 0; interpreter = nix glibc loader | verified |
| protonplus | PASS | ZERO not found (static pie, 0 NEEDED) | `version` -> "ProtonPlus 0.6.4", exit 0; CA hook fires (symlink /tmp/.___host-certs/ca-certificates.crt) when host cert store exists; /tmp/pKE + /tmp/3J=1Z symlinks created | verified |
| zen-browser | PASS | ZERO not found (nix loader on zen + libxul) | `--version` -> "Mozilla Zen 1.21.14b", exit 0; interpreter = nix glibc loader | verified |
| opencode-desktop | PASS | ZERO not found (nix loader on 4 glibc .node sidecars; electron ldd) | electron --no-sandbox --version -> "v42.5.1", exit 0; app wrapper launches electron main process headless (X11 init fails only at no DISPLAY) | verified (GUI launch gap) |
| rootapp | PASS | ZERO not found (bwrap FHS) | starts ("Starting application", "Using profile default") under --privileged, exit 0 | verified (needs userns) |

- Fresh-checker container tests re-run on fresh `--refresh` builds in brand-new containers: all 5 passed again.
- Named gaps: aarch64 BUILD impossible on x86_64 runner (eval only, all 3 aarch64 packages evaluate green); GUI apps full window rendering needs a display (headless smoke only); rootapp needs user-namespace support (--privileged simulates a real machine); protonplus CA-cert hook only fires when a host cert store exists (verified it fires with one created at /etc/ssl/certs/ca-certificates.crt).
- NOTE: ubuntu ldd reports GLIBC_ABI_DT_X86_64_PLT "not found" on nix glibc-2.42 libs (ubuntu glibc 2.39 loader too old) - loader-mismatch false positive; the authoritative check uses the nix interpreter (`ld-linux --list`), which reports ZERO not found everywhere.

## 7. Adversarial audit

- Re-queried all 4 GitHub release tags (latest + 5 recent tags each) at end of run (unchanged since start: 0.15.5.1 / v0.6.4 / 1.21.14b / v1.18.18) and rootapp fixed-URL hash (unchanged) - no mid-run drift.
- Fresh `--refresh` clone rebuild produced byte-identical store paths to the working tree - deterministic, not stale cache.
- Interpreter on helium + zen ELFs points at nix glibc loader, not /lib64 (verified via patchelf --print-interpreter; helium's upstream tar.xz ELFs had /lib64 which the package patchelf fixes; readelf on fresh-extracted tarball confirms /lib64 interpreter upstream).
- Attack checked: could the container test be masking missing deps? No - ubuntu:24.04 has no nix closure or buildInputs; the only libs available are from each package's own closure via the /nix/store mount, and the authoritative nix-loader check reports zero missing everywhere.
- Attack checked: could versions be stale because I trusted the file? No - gh api releases/latest + 5-recent-tags queried independently for all 4 GitHub packages; rootapp re-prefetched with --refresh and version torn from the actual AppImage.
- update.yml/pkgs URL sync + README/flake inventory sync confirmed.

## 8. What remains

- Nothing. Repo is current, all 5 packages build and pass clean-container runtime tests, all hashes match upstream, no open issues/PRs, no failing Actions runs.

## 9. Final state

- main @ a2ae359 + this relay update. Working tree clean.
- README.md package list matches flake.nix (rootapp, opencode-desktop, helium, protonplus, zen-browser).