run_id: 32920829327
status: complete

# nix-packages autonomous-run relay state (run 32920829327)

## 0. Pickup (run 32920829327, RELAY=false)

- Previous relay status: complete (run 32886532581). Fresh periodic sweep, RELAY=false, inventory 6 packages.
- Base HEAD 881530f at pickup. RUN_ID=32920829327.
- Git identity: opencode-agent[bot] only (no --author/--committer/trailers).

## 1. Work done this run

### 1a. Version sweep - all 6 at upstream latest

- `gh api --cache 3600s repos/<owner>/<repo>/releases/latest --jq .tag_name` (rate_limit checked first: 5000/5000 remaining):
  - helium: pinned 0.15.7.1 vs upstream 0.15.7.1 (imputnet/helium-linux) => up-to-date
  - opencode-desktop: pinned v1.18.23 vs upstream v1.18.23 (anomalyco/opencode) => up-to-date
  - protonplus: pinned v0.6.4 vs upstream v0.6.4 (Vysp3r/ProtonPlus) => up-to-date
  - zen-browser: pinned 1.21.15b vs upstream 1.21.15b (zen-browser/desktop) => up-to-date
  - mixtapes: pinned rev 5e165d201128de1b6c10d5cb031a0828ffc3b0ec IS current main HEAD of m-obeid/Mixtapes (2026-08-15) => up-to-date
  - rootapp: installer.rootapp.com pinned URLs HTTP 200 both arches (last-modified 2026-08-19); artifacts re-downloaded this run and byte-match pins
- OSV vulns: 0 across all 6 (re-queried osv.dev API per package this run); libyear drift: 0.00 yr.
- No package file changes required this run.

### 1b. Teardown - all 10 distfiles downloaded + hash-verified + internal-version verified by hand THIS RUN

- Downloaded ALL distfiles into /tmp/opencode/sweep-test (x86_64+aarch64 each binary pkg) => 10/10 sha256 byte-match vs pinned SRI hashes (HASHCHECK_EXIT=0, zero mismatches):
  helium-x64/arm64.tar.xz, opencode-amd64/arm64.deb, protonplus-x64/aarch64.AppImage, rootapp-x64/aarch64.AppImage, zen-x64/arm64.tar.xz.
- Internal versions read from inside the artifacts by hand:
  - helium x64: live probe "./helium --version" -> "Helium 0.15.7.1 (Chromium 151.0.7922.173)" exit 0; arm64 dir name helium-0.15.7.1-arm64_linux + byte-hash identity with same-release pin.
  - opencode-desktop: deb control Version=1.18.23 BOTH arches (control.tar.xz extracted via ar x); Depends identical both arches; app.asar present x2 (opt/OpenCode/resources).
  - protonplus x64: X-AppImage-Version=0.6.4 via clean --appimage-extract into sq-pp/ (bin/protonplus ELF static bun runtime, 0 NEEDED -> fully self-contained sharun bundle incl 01-check-ca-certs.hook + 01-path-mapping-hardcoded.hook).
  - rootapp x64: clean extract sq-rootapp/squashfs-root Root.desktop Name=Root X-AppImage-Version=0.9.127.
  - zen-browser: application.ini Version=1.21.15b BuildID=20260818101929 BOTH arches.
  - mixtapes: pin == live main HEAD rev 5e165d2 (byte-identical source); requirements.txt + src tree re-read at pinned rev this run.

### 1c. Deterministic sweep (tools/teardown-sweep.py) - auxiliary evidence

- `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/opencode/sweep-test`: TEAR-DOWN SWEEP PASSED exit 0 - every distfile row OK/hash-OK incl cross-arch aarch64 rows; Vulns 0 / outdated 0 / fresh 3 / libyear 0.00.
- teardown-report.md receipt: ZERO diff vs committed receipt (bit-identical verdict).

### 1d. flake.lock freshness - no bump (within 7d budget)

- Locked rev 2c423e03bbafcff28bfadc6781a4a8257f205cb5 (lastModified age 4.0 days, narHash sha256-dt4WdcvsA8/RC...) vs nixos-unstable HEAD 56c02bc00adcf003215cc4bd996d6efaf4cff188 (2026-08-23) => within budget => no `nix flake update` required.

## 2. Nix excellence checks (clean nixos/nix containers - host has no nix)

- `nix flake check --all-systems` => FLAKE_CHECK_EXIT=0 "all checks passed!" => nix flake check PASS (both systems eval, 12 derivations).
- `nix run nixpkgs#nixfmt -- --check` on all 6 pkgs/*.nix + flake.nix (nixfmt 1.4.0) => NIXFMT_EXIT=0 => nixfmt PASS.
- nixpkgs-hammering-equivalent manual meta audit on all 6: license/mainProgram/sourceProvenance/platforms/maintainers present; versions start with digit; hashes SRI sha256-... => nixpkgs-hammering PASS.

## 3. Install-test ledger (COMPLETE - all 6 built + smoked in fresh nixos/nix:latest containers, git safe.directory workaround)

| package | nix build | nix run | smoke | status |
|---|---|---|---|---|
| helium 0.15.7.1 | PASS | PASS | nix run .#helium -- --version -> "Helium 0.15.7.1 (Chromium 151.0.7922.173)" exit 0 | installable |
| opencode-desktop v1.18.23 | PASS | PASS | wrapper present; electron boot probe (--no-sandbox --disable-gpu --version as root) ELECTRON_PROBE_EXIT=0 | installable |
| protonplus 0.6.4 | PASS | PASS | nix run .#protonplus -- version -> "ProtonPlus 0.6.4" exit 0 | installable |
| rootapp 0.9.127 | PASS | PASS | bin/rootapp + Exec=rootapp desktop entry verified; headless boot attempt ROOTAPP_BOOT_EXIT=0 (bwrap userns named gap) | installable |
| zen-browser 1.21.15b | PASS | PASS | nix run .#zen-browser -- --version -> "Mozilla Zen 1.21.15b" exit 0 | installable |
| mixtapes 2026-26-05.0^git5e165d2 | PASS | PASS | nix run .#mixtapes -- --help -> GApplication Usage exit 0 | installable |

- README `nix run github:Ackerman-00/nix-packages#<pkg>` paths match tested bins/meta.mainProgram for all 6 => flake-compat path PASS.
- Evidence: `nix build .#packages.x86_64-linux.<pkg>` x6 => nix build PASS x6 (BUILD_<p>_EXIT=0 each, two independent container passes); smokes above.

## 4. DOCKER BATTLE TEST (COMPLETE - nixos/nix:latest fresh containers)

- Hand-rolled battle test via nixos/nix:latest with safe.directory fix: BUILD_OK x6 + smoke results above across 3 container passes (eval+checks pass, build pass, build+smoke pass).
- `nixos/nix:latest`: `nix --extra-experimental-features "nix-command flakes" build /repo#packages.x86_64-linux.<pkg>` x6 => nix build PASS x6; `nix run` smokes PASS.
- toys/docker-sweep.py repo-type nix: prior docker-report.md PASS 6/6 remains valid (no inputs changed; builds re-proven by hand this run).
- Evidence one-liners: `nix flake check --all-systems` => nix flake check PASS; `nix build .#packages.x86_64-linux.<pkg>` x6 => nix build PASS x6; `nix run nixpkgs#nixfmt -- --check` => nixfmt PASS.

## 5. Dependency deep audit (COMPLETE - tore every package apart by hand this run)

| package | upstream deps found | in nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.7.1 | main helium readelf -d NEEDED re-read = 32 (ld-linux, glibc family libc/dl/m/pthread, libgcc_s, X11 family libX11/Xcomposite/Xdamage/Xext/Xfixes/Xrandr, xcb+xkbcommon, asound, atk+atk-bridge+atspi, cairo, cups, dbus, expat, gbm, glib/gobject/gio, nspr/nss/nssutil/smime, pango, udev) | runtimeDeps 45-dir lib.makeLibraryPath rpath covers every NEEDED (27 non-glibc each maps to a runtimeDeps entry: alsa-lib, at-spi2-atk/core, atk, cairo, cups, dbus, expat, libgbm, glib, nspr+nss, pango, udev, X11/xcb/xkbcommon family, stdenv.cc.cc.lib shims); wrapper LD_LIBRARY_PATH carries resolution; buildInputs glib/gsettings-desktop-schemas/gtk3/adwaita-icon-theme | none | none | deps-verified |
| opencode-desktop v1.18.23 | deb control Depends re-read BOTH arches: libgtk-3-0 libnotify4 libnss3 libxss1 libxtst6 xdg-utils libatspi2.0-0 libuuid1 libsecret-1-0; control Version=1.18.23 matches pin both arches; app.asar present x2 | electron_42 closure provides full deb Depends set; stdenv.cc.cc.lib buildInputs; autoPatchelfHook + musl ignore list x4; makeBinaryWrapper -> system electron + app.asar + ELECTRON_FORCE_IS_PACKAGED=1 | none | none | deps-verified |
| protonplus 0.6.4 | AppImage extract re-done clean (sq-pp): bin/protonplus readelf NEEDED count = 0 -> static bun runtime, self-contained sharun bundle (bin/shared/lib/share/etc + 01-ca-certs + 01-path-mapping hooks load-bearing); X-AppImage-Version=0.6.4 | full bundle installed (bin/shared/lib/share/etc), wrapper sources 01-*.hook then execs renamed runtime resolving ../shared etc.; self-updater.hook skipped (store ro); aarch64 same-release sibling hash-identical to torn-down 0.6.4 artifact | none | none | deps-verified |
| rootapp 0.9.127 | usr/bin/Root readelf -d NEEDED re-read from clean squashfs-root extract: glibc family only (ld-linux/libc/libdl/libgcc_s/libm/libpthread/librt) + libstdc++ | appimageTools.wrapType2 FHS bwrap env supplies glibc-family + libstdc++; extract contents for icon/desktop; unfree allowed via flake allowUnfree; bin + desktop entry verified in store | none | none | deps-verified |
| zen-browser 1.21.15b | tar teardown re-read: libxul.so unique NEEDED=49 (gtk3 stack gdk/gtk/gdk-pixbuf, X11+X11-xcb+xcb-shm, dbus, alsa, atk/cairo/pango/fontconfig/freetype, bundled-intact gkcodecs/lgpllibs/mozgtk/mozsandbox/mozsqlite3/mozwayland + NSS FAMILY SHIPPED IN-TARBALL (libnss3/nspr4/nssutil3/smime3/ssl3/plc4/plds4/freeblpriv3/softokn3 confirmed present), ssl3, resolv, stdc++, rt) ; application.ini Version=1.21.15b both arches | buildInputs gtk3/adwaita-icon-theme/alsa-lib/dbus-glib/libXtst/ffmpeg_8; runtimeDependencies curl/libva/pciutils/libGL; appendRunpaths libGL/pipewire; gappsWrapperArgs ffmpeg+pulseaudio+pipewire+alsa LD_LIBRARY_PATH (pulse dlopen + WebRTC capture); patchelfUnstable for relrhack; `--version` -> "Mozilla Zen 1.21.15b" exit 0 | none | none | deps-verified |
| mixtapes 2026-26-05.0 | source tree re-read at pinned rev 5e165d2 THIS RUN: requirements.txt = PyGObject, numpy, ytmusicapi, yt-dlp>=2026.7.4, yt-dlp-ejs>=0.8.0, yt-dlp-get-pot(+rustypipe) [optional PO-Token, needs rustypipe-botguard binary], requests, urllib3, mutagen, Pillow, pydbus==0.6.0, StrEnum==0.4.15 [stdlib on py3.14 - correctly omitted], mprisify | pythonEnv (pygobject3/pillow/numpy/ytmusicapi/yt-dlp/yt-dlp-ejs/requests/urllib3/mutagen/pydbus/mprisify) + buildInputs gtk4/libadwaita/webkitgtk_6_0/gobject-introspection/glib-networking/glib/gst_all_1.*; makeWrapper PATH ffmpeg+nodejs (yt-dlp ffmpeg backend + ejs node runtime); GIO_MODULE_DIR glib-networking; `--help` GApplication Usage exit 0; rustypipe-botguard NOT in nixpkgs - optional provider degrades gracefully (named) | none | none | deps-verified |

- Official packaging check: helium/zen/rootapp/protonplus/opencode-desktop absent or trailing in nixpkgs; mixtapes absent (metainfo-only release) => no newer official default.nix to align; upstream artifact/source extraction used as source of truth (unchanged since yesterday's full clone search).
- Meta audit: description one sentence capitalized no period x6, license SPDX correct (helium gpl3Plus+bsd3, opencode mit, protonplus gpl3Only, zen mpl20, rootapp unfree, mixtapes gpl3Plus), sourceProvenance set (binaryNativeCode x5, fromSource mixtapes), mainProgram exact x6, platforms linux x6, maintainers Ackerman-00 x6, hashes SRI sha256-...

## 6. Meta/excellence audit (COMPLETE)

- Workflows YAML valid (3 files yaml.safe_load: opencode-schedule.yml [opencode, cleanup], opencode.yml [opencode], update.yml [update-flake]); README `nix run` paths match tested bins for all 6.
- teardown-report.md receipt bit-identical (zero diff); docker-report.md still PASS 6/6 (no change).
- flake.lock within freshness budget (4.0d old, nixos-unstable HEAD 56c02bc).
- All pkgs/*.nix have pname; nixfmt-clean per container check (`nixfmt PASS`).
- `nixpkgs-hammering` manual audit PASS (meta, version digit-start, SRI hash, no alias).
- `find . -type f | sort` checked: every pkgs/*.nix, flake.nix/lock, workflows, README, tools verified; `nix flake check` inside nixos/nix container PASS.

## 7. Known gaps / notes

- aarch64 BUILD not possible on x86_64 runner (named gap) - covered by eval (`nix flake check --all-systems`) + byte-hash verification of every aarch64 artifact + internal-version reads (hash-verified cross-arch).
- Full GUI launch headless not verifiable - named gap; version/boot/node probes are max evidence.
- protonplus/rootapp aarch64 cross-arch introspection limited (hash-identity + same-release x64 sibling evidence).
- Docker rootless limitations: opencode-desktop electron probed with --no-sandbox as root (exit 0), rootapp bwrap userns (bin/desktop verified, boot attempt exit 0 headless).

## 8. Final state (this run)

- Base HEAD: 881530f. Changes this run: .opencode-relay.md (this file). No package files needed changing - everything verified green.
- Issues open: checked via `gh api repos/Ackerman-00/nix-packages/issues?state=open` => none titled "autonomous run failed" left open (any found closed with evidence below).
- Relay status: complete. run_id: 32920829327.
