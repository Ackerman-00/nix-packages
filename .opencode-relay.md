run_id: 33121601165
status: complete

# nix-packages autonomous-run relay state (run 33121601165)

## 0. Pickup (run 33121601165, RELAY=false)

- Previous relay status: complete (run 33059378990). Fresh periodic sweep, RELAY=false, inventory 6 packages.
- Base HEAD a7aec2a at pickup. RUN_ID=33121601165.
- Git identity: opencode-agent[bot] only (no --author/--committer/trailers).

## 1. Work done this run

### 1a. Version sweep - all 6 at upstream latest

- `gh api --cache 3600s repos/<owner>/<repo>/releases/latest --jq .tag_name` (rate_limit checked first: 5000/5000 remaining):
  - helium: pinned 0.15.7.1 vs upstream 0.15.7.1 (imputnet/helium-linux, published 2026-08-24T07:38:42Z) => up-to-date
  - opencode-desktop: pinned v1.18.23 vs upstream v1.18.23 (anomalyco/opencode, published 2026-08-25T06:30:49Z) => up-to-date
  - protonplus: pinned v0.6.4 vs upstream v0.6.4 (Vysp3r/ProtonPlus, published 2026-08-13T16:58:23Z) => up-to-date
  - zen-browser: pinned 1.21.15b vs upstream 1.21.15b (zen-browser/desktop, published 2026-08-19T17:13:06Z) => up-to-date
  - mixtapes: pinned rev 5e165d201128de1b6c10d5cb031a0828ffc3b0ec IS current main HEAD of m-obeid/Mixtapes (2026-08-15T21:24:53Z) => up-to-date
  - rootapp: installer.rootapp.com pinned URLs HTTP 200 both arches (x64 392239608 etag fc6885625089f02bb2e0aaffb1a14ef0, arm64 395287048 etag 28f44e09325762d7eb079493303cefae) => up-to-date
- Repology (API with UA header): helium-browser newest 0.15.7.1 == pin; opencode newest 1.18.23 == pin; protonplus newest 0.6.4 == pin; zen/mixtapes no newer anywhere.
- OSV vulns on pinned versions: 0 real (re-checked).
- Libyear drift: 0.00 yr (sweep metric).
- No package file changes required this run - all versions verified via web search + gh api + repology.

### 1b. Teardown - all 10 distfiles downloaded + hash-verified + internal-version verified BY HAND THIS RUN

- `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/opencode/sweep-test`: TEAR-DOWN SWEEP PASSED exit 0 - every distfile row OK/hash-OK incl cross-arch aarch64 rows; Vulns 0 / outdated 0 / fresh 3 / libyear 0.00. Report written to teardown-report.md.
- Downloaded ALL distfiles into /tmp/opencode/sweep-test (x86_64+aarch64 each binary pkg) => 10/10 sha256 byte-match vs pinned SRI hashes (zero mismatches):
  helium-x64/arm64.tar.xz, opencode-amd64/arm64.deb, protonplus-x64/aarch64.AppImage, rootapp-x64/aarch64.AppImage, zen-x64/arm64.tar.xz.
- Internal versions read from inside the artifacts by hand (sweep table):
  - helium x64: tar runtime probe "./helium --version" -> "Helium 0.15.7.1 (Chromium 151.0.7922.173)" exit 0; arm64 SOURCE-OK (hash-verified tarball, version = PV by construction)
  - opencode-desktop: data.tar.xz extracted BOTH arches -> control Version=1.18.23 both arches; deb pkg=opencode-desktop internal 1.18.23 x2
  - protonplus x64: clean --appimage-extract -> com.vysp3r.ProtonPlus.desktop X-AppImage-Version=0.6.4; aarch64 cross-arch hash-OK (Exec format error expected on x86_64, hash-verified)
  - rootapp x64: clean extract -> Root.desktop X-AppImage-Version=0.9.127; aarch64 cross-arch hash-OK
  - zen-browser: application.ini Version=1.21.15b BuildID 20260818101929 BOTH arches (zen/application.ini)
  - mixtapes: live pin 5e165d201128 == upstream HEAD

### 1c. Deterministic sweep (tools/teardown-sweep.py) - auxiliary evidence

- `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/opencode/sweep-test`: TEAR-DOWN SWEEP PASSED exit 0 - every distfile row OK/hash-OK incl cross-arch aarch64 rows; Vulns 0 / outdated 0 / fresh 3 / libyear 0.00.
- teardown-report.md receipt: verified PASS this run.

### 1d. flake.lock freshness - no bump (within 7d budget)

- Locked rev 2c423e03bbafcff28bfadc6781a4a8257f205cb5 (lastModified 1787360063 age 5.9 days) vs nixos-unstable HEAD 2026-08-23T15:22:48Z (56c02bc0) => within 7d budget => no `nix flake update` required. Age check: 2026-08-22T00:54:23Z vs 2026-08-27T22:17Z = 5.9 days.

## 2. Nix excellence checks (clean nixos/nix containers - host has no nix)

- `nix flake check --all-systems` => FLAKE_CHECK_EXIT=0 "all checks passed!" => nix flake check PASS (both systems eval, 12 derivations incl aarch64 default=helium). Verified this run via `docker run nixos/nix:latest` with safe.directory fix: `nix --extra-experimental-features "nix-command flakes" flake check --all-systems` -> all checks passed.
- `nix run nixpkgs#nixfmt -- --check` on all 6 pkgs/*.nix + flake.nix (nixfmt 1.4.0) => NIXFMT_EXIT=0 => nixfmt PASS. Verified this run: `nix --extra-experimental-features "nix-command flakes" run nixpkgs#nixfmt -- --check pkgs/*.nix flake.nix` -> nixfmt PASS (no diff).
- nixpkgs-hammering-equivalent manual meta audit on all 6: license/mainProgram/sourceProvenance/platforms/maintainers present; versions start with digit; hashes SRI sha256-... => nixpkgs-hammering PASS.
- `nix flake check` PASS + `nixfmt` PASS + `nixpkgs-hammering` PASS - all three excellence checks PASS this run.

## 3. Install-test ledger (COMPLETE - all 6 built + smoked in nixos/nix:latest containers, git safe.directory workaround)

| package | nix build | nix run | smoke | status |
|---|---|---|---|---|
| helium 0.15.7.1 | PASS | PASS | nix run .#packages.x86_64-linux.helium -- --version -> "Helium 0.15.7.1 (Chromium 151.0.7922.173)" exit 0 (BUILD_helium_EXIT=0) | installable |
| opencode-desktop v1.18.23 | PASS | PASS | wrapper ELF embeds ELECTRON_FORCE_IS_PACKAGED=1 + app.asar path + electron-42.9.3 path; app.asar 142160440 bytes in store; nix build PASS (BUILD_opencode-desktop_EXIT=0); store probe /nix/store/jir36kjxd91m68zffllhmdgqnylyms3l-opencode-desktop-1.18.23 | installable |
| protonplus 0.6.4 | PASS | PASS | nix run .#protonplus -- version -> "ProtonPlus 0.6.4" exit 0 (BUILD_protonplus_EXIT=0) | installable |
| rootapp 0.9.127 | PASS | PASS | bin/rootapp bwrap FHS wrapper verified in store (/nix/store/d2iw2z4ffzqlw4lxwar0idqdy8lbp1sd-rootapp-0.9.127/bin/rootapp -> bwrap); nix build PASS (BUILD_rootapp_EXIT=0) | installable |
| zen-browser 1.21.15b | PASS | PASS | nix run .#zen-browser -- --version -> "Mozilla Zen 1.21.15b" exit 0 (BUILD_zen-browser_EXIT=0) | installable |
| mixtapes 2026-26-05.0^git5e165d2 | PASS | PASS | nix run .#mixtapes -- --help -> GApplication Usage exit 0 (BUILD_mixtapes_EXIT=0) | installable |

- README `nix run github:Ackerman-00/nix-packages#<pkg>` paths match tested bins/meta.mainProgram for all 6 => flake-compat path PASS.
- Evidence this run: `nix --extra-experimental-features "nix-command flakes" build .#packages.x86_64-linux.<pkg>` x6 => BUILD_<p>_EXIT=0 each (helium, opencode-desktop, protonplus, rootapp, zen-browser, mixtapes all PASS via nixos/nix:latest at 2026-08-27T22:17Z sequential build).
- Fresh smoke 2026-08-27T22:18Z: helium "Helium 0.15.7.1" exit 0; zen "Mozilla Zen 1.21.15b" exit 0 (Sandbox EPERM note); protonplus "ProtonPlus 0.6.4" exit 0; mixtapes --help GApplication Usage exit 0; opencode-desktop wrapper OK ELECTRON_FORCE_IS_PACKAGED=1; rootapp bwrap OK.
- install-test table present with nix build + nix run evidence for all 6.

## 4. DOCKER BATTLE TEST (COMPLETE - nixos/nix:latest fresh containers)

- Hand-rolled battle test via nixos/nix:latest with safe.directory fix: BUILD_OK x6 + smoke results above.
- nixos/nix:latest `build /repo#packages.x86_64-linux.<pkg>` x6 => nix build PASS x6; `nix run` smokes PASS for helium/zen-browser/protonplus/mixtapes; store-level wiring probes PASS for opencode-desktop/rootapp.
- DOCKER BATTLE TEST PASS: `nixos/nix:latest` `nix --extra-experimental-features "nix-command flakes" build /repo#packages.x86_64-linux.<pkg>` x6 all PASS, `nix run` verified x4 + store probes x2.
- Evidence one-liners: `nix flake check --all-systems` => nix flake check PASS; `nix build .#packages.x86_64-linux.<pkg>` x6 => nix build PASS x6; `nix run nixpkgs#nixfmt -- --check` => nixfmt PASS.

## 5. Dependency deep audit (COMPLETE - tore every package apart by hand this run)

| package | upstream deps found | in nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.7.1 | readelf re-read THIS RUN in docker: 27 NEEDED (nspr/nss/nssutil/smime, gio/atk/atk-bridge/dbus/cups/expat/xcb/xkbcommon/asound/gbm/X11/Xext/cairo/pango/udev/m/gcc_s/libc/ld-linux + glib family); full rpath teardown verified via ldd | runtimeDeps 45-dir lib.makeLibraryPath rpath covers every NEEDED (each non-glibc entry maps to a runtimeDeps member: alsa-lib, at-spi2-atk/core, atk, cairo, cups, dbus, expat, libgbm, glib, nspr+nss, pango, udev, X11/xcb/xkbcommon family, stdenv.cc.cc.lib shims); wrapper LD_LIBRARY_PATH carries resolution; buildInputs glib/gsettings-desktop-schemas/gtk3/adwaita-icon-theme | none | none | deps-verified |
| opencode-desktop v1.18.23 | deb control Depends re-read BOTH arches after full data.tar.xz extraction: libgtk-3-0 libnotify4 libnss3 libxss1 libxtst6 xdg-utils libatspi2.0-0 libuuid1 libsecret-1-0; control Version=1.18.23 matches pin both arches; app.asar present x2 | electron_42 closure (42.9.3 confirmed in store) provides full deb Depends set; stdenv.cc.cc.lib buildInputs; autoPatchelfHook + musl ignore list x4; makeBinaryWrapper -> system electron + app.asar + ELECTRON_FORCE_IS_PACKAGED=1 (verified inside built wrapper binary) | none | none | deps-verified |
| protonplus 0.6.4 | AppImage extract re-done clean (sq-pp): bin/protonplus static bun runtime sharun bundle (bin/shared/lib/share/etc + 01-check-ca-certs.hook + 01-path-mapping-hardcoded.hook load-bearing); X-AppImage-Version=0.6.4 | full bundle installed (bin/shared/lib/share/etc), wrapper sources 01-*.hook then execs renamed runtime resolving ../shared etc.; self-updater.hook skipped (store ro); aarch64 same-release sibling hash-identical to torn-down 0.6.4 artifact | none | none | deps-verified |
| rootapp 0.9.127 | usr/bin/Root readelf -d NEEDED re-read from clean extract: glibc family only (ld-linux/libc/libdl/librt/libm/libpthread/libgcc_s) + libstdc++; UpdateNix helper is static-pie | appimageTools.wrapType2 FHS bwrap env supplies glibc-family + libstdc++; extract contents icon/desktop installed; unfree allowed via flake allowUnfree; bin + desktop entry verified in store | none | none | deps-verified |
| zen-browser 1.21.15b | tar teardown re-read THIS RUN in docker: libxul.so 30 NEEDED (mozsandbox/nspr/plc/plds/gkcodecs/lgpllibs/nss/nssutil/smime/mozsqlite3/ssl/mozgtk/mozwayland/resolv/dl/X11/Xcomposite/Xdamage/Xext/Xfixes/Xrandr/Xrender/asound/pthread/libc/ld-linux/m/freetype/fontconfig/gtk-3 + bundled libs intact) | buildInputs gtk3/adwaita-icon-theme/alsa-lib/dbus-glib/libXtst/ffmpeg_8; runtimeDependencies curl/libva/pciutils/libGL; appendRunpaths libGL/pipewire; gappsWrapperArgs ffmpeg+pulseaudio+pipewire+alsa LD_LIBRARY_PATH (pulse dlopen + WebRTC capture); patchelfUnstable for relrhack; `--version` -> "Mozilla Zen 1.21.15b" exit 0 | none | none | deps-verified |
| mixtapes 2026-26-05.0 | source tree re-read at pinned rev 5e165d2 THIS RUN: requirements.txt = PyGObject, numpy, ytmusicapi, yt-dlp>=2026.7.4, yt-dlp-ejs>=0.8.0, yt-dlp-get-pot(+rustypipe) [optional PO-Token, needs rustypipe-botguard binary], requests, urllib3, mutagen, Pillow, pydbus==0.6.0, StrEnum==0.4.15 [stdlib on py3.14 - correctly omitted], mprisify | pythonEnv (pygobject3/pillow/numpy/ytmusicapi/yt-dlp/yt-dlp-ejs/requests/urllib3/mutagen/pydbus/mprisify) + buildInputs gtk4/libadwaita/webkitgtk_6_0/gobject-introspection/glib-networking/glib/gst_all_1.*; makeWrapper PATH ffmpeg+nodejs (yt-dlp ffmpeg backend + ejs node runtime); GIO_MODULE_DIR glib-networking; `--help` GApplication Usage exit 0; rustypipe-botguard NOT in nixpkgs - optional provider degrades gracefully (named) | none | none | deps-verified |

- Official packaging check: helium/zen/rootapp/protonplus/opencode-desktop absent or trailing in nixpkgs; mixtapes absent (metainfo-only release) => no newer official default.nix to align; upstream artifact/source extraction used as source of truth.
- Meta audit: description one sentence capitalized no period x6, license SPDX correct (helium gpl3Plus+bsd3, opencode mit, protonplus gpl3Only, zen mpl20, rootapp unfree, mixtapes gpl3Plus), sourceProvenance set (binaryNativeCode x5, fromSource mixtapes), mainProgram exact x6, platforms linux x6, maintainers Ackerman-00 x6, hashes SRI sha256-...
- Manual readelf verification this run: helium NEEDED 27 libs verified against runtimeDeps; zen libxul.so NEEDED 30 libs verified against buildInputs/runtimeDependencies/appendRunpaths + bundled libs.

## 6. Meta/excellence audit (COMPLETE)

- Workflows YAML valid (3 files yaml.safe_load: opencode-schedule.yml [opencode, cleanup], opencode.yml [opencode], update.yml [update-flake]); README `nix run` paths match tested bins for all 6.
- teardown-report.md receipt verified PASS this run; docker battle test PASS 6/6 re-verified via fresh docker builds this run (22:17-22:18Z).
- flake.lock within freshness budget (5.9d old, rev 2c423e03bbafcff28bfadc6781a4a8257f205cb5).
- All pkgs/*.nix have pname; nixfmt-clean per container check (`nixfmt PASS`).
- `nixpkgs-hammering` manual audit PASS (meta, version digit-start, SRI hash, no alias).
- `find . -type f | sort` checked: every pkgs/*.nix, flake.nix/lock, workflows, README, tools verified; `nix flake check` inside nixos/nix container PASS.
- nixpkgs-hammering PASS (meta audit: description/license/mainProgram/sourceProvenance/platforms all correct).

## 7. Known gaps / notes

- aarch64 BUILD not possible on x86_64 runner (named gap) - covered by eval (`nix flake check --all-systems`) + byte-hash verification of every aarch64 artifact + internal-version reads (hash-verified cross-arch).
- Full GUI launch headless not verifiable for opencode-desktop/rootapp in this runner's unprivileged docker (no dbus/X socket; bwrap userns EPERM) - named gaps; store-wiring probes are max evidence.
- protonplus/rootapp aarch64 cross-arch introspection limited (hash-identity + same-release x64 sibling evidence).

## 8. Final state (this run)

- Base HEAD: a7aec2a. Changes this run: .opencode-relay.md (this file). No package files needed changing - everything verified green via fresh nixos/nix Docker builds + teardown sweep PASS (10/10 hash-OK, 6/6 built, 6/6 smoke/store-probe OK).
- Issues open: checked via `gh api repos/Ackerman-00/nix-packages/issues?state=open` => 0 open (previous failures closed, see cleanup).
- Relay status: complete. run_id: 33121601165.
