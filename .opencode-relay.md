run_id: 33623403545
status: complete

# nix-packages autonomous-run relay state (run 33623403545)

## 0. Pickup

- Previous relay status: complete (run 33623403545). Fresh periodic sweep.
- Base HEAD 396674b at pickup. RUN_ID=33623403545.
- Git identity: opencode-agent[bot] only (no --author/--committer/trailers).

## 1. Work done this run

### 1a. Version sweep - all 6 at upstream latest

- `gh api --cache 3600s repos/<owner>/<repo>/releases/latest --jq .tag_name`:
  - helium: pinned 0.16.3.1 == upstream 0.16.3.1 [releases/latest] => up-to-date
  - opencode-desktop: pinned 1.18.26 == upstream v1.18.26 [releases/latest] => up-to-date
  - protonplus: pinned 0.6.5 == upstream v0.6.5 [releases/latest] => up-to-date
  - zen-browser: pinned 1.21.16b == upstream 1.21.16b [releases/latest] => up-to-date
  - mixtapes: pinned rev 5e165d201128 == upstream HEAD => up-to-date
  - rootapp: pinned 0.9.128 == upstream 0.9.128 (etag 77ce18cc, HEAD 200) => up-to-date
- Repology: all fresh or at upstream. OSV vulns: 0. Libyear: 0.00 yr.
- Fix applied: none required; all 6 at upstream latest.

### 1b. Teardown sweep - all 11 distfiles hash-verified

- `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/opencode/sweep-current`: TEAR-DOWN SWEEP PASSED exit 0.
- 11/11 sha256 byte-match vs pinned SRI hashes (zero mismatches).
- Internal versions verified: helium 0.16.3.1, opencode-desktop 1.18.26, protonplus 0.6.5, rootapp 0.9.128, zen-browser 1.21.16b, mixtapes 5e165d2 == HEAD.

### 1c. flake.lock freshness

- nixos-unstable HEAD: 34ab99075ac4f7e40cf037eef32cb1c360bb85e9 (2026-08-31).
- flake.lock rev: 34ab99075ac4f7e40cf037eef32cb1c360bb85e9 (same) => up-to-date, no update needed.

## 2. Nix excellence checks

- `nix flake check --all-systems` => PASS ("all checks passed!" EXIT 0)
- `nix run nixpkgs#nixfmt -- --check pkgs/*.nix flake.nix` => NIXFMT_EXIT=0 PASS
- nixpkgs-hammering manual meta audit: PASS (description/license/mainProgram/sourceProvenance/platforms all correct)
- `nix build .#packages.x86_64-linux.<pkg>` x6 => PASS

## 3. Install-test ledger (fresh Docker builds 2026-09-02)

| package | nix build | nix run | smoke | status |
|---|---|---|---|---|
| helium 0.16.3.1 | PASS | PASS | "Helium 0.16.3.1 (Chromium 152.0.7977.64)" exit 0 | installable |
| opencode-desktop 1.18.26 | PASS | PASS | wrapper ELECTRON_FORCE_IS_PACKAGED=1 grep 2 + app.asar grep 2 | installable |
| protonplus 0.6.5 | PASS | PASS | "ProtonPlus 0.6.5" exit 0 | installable |
| rootapp 0.9.128 | PASS | PASS | bwrap FHS wrapper rootapp_bwrap_OK grep 2 | installable |
| zen-browser 1.21.16b | PASS | PASS | "Mozilla Zen 1.21.16b" exit 0 | installable |
| mixtapes 2026-26-05.0 | PASS | PASS | --help "Usage: main.py" exit 0 | installable |

## 4. DOCKER BATTLE TEST

- `nixos/nix:latest` `nix --extra-experimental-features "nix-command flakes" build /repo#packages.x86_64-linux.<pkg>` x6 => PASS
- Store-wiring smokes: opencode-desktop ELECTRON_FORCE_IS_PACKAGED=1 + app.asar OK, rootapp bwrap OK, helium/zen/protonplus/mixtapes runtime OK.
- DOCKER BATTLE TEST PASS 6/6.

## 5. Dependency deep audit

| package | upstream deps found | in nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.16.3.1 | 32 NEEDED via readelf + ldd: glib/gobject/nspr/nss/gio/atk/dbus/cups/xcb/X11/cairo/pango/udev/alsa/gbm etc | runtimeDeps 44 entries rpath covers all | none | none | deps-verified |
| opencode-desktop 1.18.26 | deb Depends: libgtk-3-0/libnotify4/libnss3/libxss1/libxtst6/xdg-utils/libatspi2.0-0/libuuid1/libsecret-1-0; electron_42 closure provides all | electron_42 + stdenv.cc.cc.lib + autoPatchelfHook | none | none | deps-verified |
| protonplus 0.6.5 | AppImage sharun bundle with bundled libs; readelf NEEDED glibc-only (host provides) | full bundle installed; wrapper sources 01-*.hook | none | none | deps-verified |
| rootapp 0.9.128 | readelf NEEDED: glibc family only + libstdc++ (7 libs + ld) | appimageTools.wrapType2 FHS bwrap supplies all | none | none | deps-verified |
| zen-browser 1.21.16b | libxul.so NEEDED 50+ libs (mozsandbox/nspr/nss/X11/gtk3/cairo/pango/dbus/freetype etc) + bundled | buildInputs + runtimeDependencies + gappsWrapperArgs cover all | none | none | deps-verified |
| mixtapes 2026-26-05.0 | pythonEnv (pygobject3/pillow/numpy/ytmusicapi/yt-dlp etc) + buildInputs (gtk4/libadwaita/webkitgtk_6_0/glib-networking/gst_all_1.*) | all present; makeWrapper PATH ffmpeg+nodejs | none | none | deps-verified |

- Official packaging: nixpkgs protonplus source 0.5.21 < our 0.6.5 AppImage; no default.nix for helium/zen/rootapp/opencode/mixtapes.
- Meta audit: description one sentence capitalized no period x6, license SPDX correct, sourceProvenance binaryNativeCode x5/fromSource x1, mainProgram exact x6, platforms linux x6, maintainers Ackerman-00 x6, hashes SRI sha256-...

## 6. Meta/excellence audit

- Workflows YAML valid (3 files). README nix run paths match tested bins for all 6.
- teardown-report.md PASS. docker battle test PASS 6/6.
- flake.lock rev 34ab990 (2026-08-31) up-to-date.
- All pkgs/*.nix have pname; nixfmt PASS; nixpkgs-hammering PASS.
- `find . -type f | sort` checked; `nix flake check` inside nixos/nix container PASS.

## 7. Known gaps

- aarch64 BUILD not possible on x86_64 runner (covered by flake check --all-systems + hash verification).
- Full GUI launch headless not verifiable (no dbus/X; store-wiring probes are max evidence).

## 8. Final state

- Base HEAD: 396674b. Changes this run: .opencode-relay.md only - no code changes needed.
- Issues open: 0 (no autonomous run failed issues to close).
- Relay status: complete. run_id: 33623403545
