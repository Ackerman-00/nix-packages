run_id: 33836710391
status: complete

# nix-packages autonomous-run relay state (run 33836710391)

## 0. Pickup

- Previous relay status: complete (run 33805347825). Fresh periodic sweep.
- Base HEAD 702b22e at pickup. RUN_ID=33836710391.
- Git identity: opencode-agent[bot] only (no --author/--committer/trailers).

## 1. Work done this run

### 1a. Version sweep - all 6 at upstream latest

- `gh api --cache 3600s repos/<owner>/<repo>/releases/latest --jq .tag_name`:
  - helium: pinned 0.16.4.1 == upstream 0.16.4.1 [tag 0.16.4.1 prerelease false 2026-09-02T20:25:05Z, releases 0.16.4.1 latest] => up-to-date
  - opencode-desktop: pinned 1.18.27 == upstream v1.18.27 [releases/latest 2026-09-02 21:41 UTC] => up-to-date
  - protonplus: pinned 0.6.5 == upstream v0.6.5 [releases/latest 2026-08-30] => up-to-date
  - zen-browser: pinned 1.21.16b == upstream 1.21.16b [releases/latest 2026-08-29] => up-to-date
  - mixtapes: pinned rev 8ba247d57ed0 == upstream HEAD 8ba247d (2026-09-03 21:26:04Z, `git ls-remote HEAD` 8ba247d) => up-to-date (updated from 5e165d2 on 2026-09-03 via chore auto-update 702b22e)
  - rootapp: pinned 0.9.129 == upstream 0.9.129 [installer.rootapp.com etag c892199af9f1881947b19a9978ee23a9 Last-Modified 2026-09-02 19:50 X64, 1a91adcf77b915777b1ec5bcdea091e9 Arm64] => up-to-date
- Repology: helium fresh, opencode-desktop fresh, protonplus fresh, zen-browser fresh, rootapp repology says outdated 0.9.128 but pinned 0.9.129 ahead (normalization). OSV vulns: 0. Libyear: 0.00 yr across 0 versionable packages (fresh).
- No version fixes needed this run; all 6 pinned at upstream latest. Verified via `git ls-remote --tags` sorting shows 0.16.4.1 / v1.18.27 / v0.6.5 / 1.21.16b are latest tags. mixtapes HEAD already updated to 8ba247d.

### 1b. Teardown sweep - all 11 distfiles hash-verified (PASS)

- `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/opencode/sweep-1788495951`: TEAR-DOWN SWEEP PASSED exit 0 (6 packages, 11 distfiles).
- 11/11 sha256 byte-match vs pinned SRI hashes (zero mismatches). Extracted via real download into /tmp (never repo tree).
- Internal versions verified: helium 0.16.4.1 (Helium 0.16.4.1 Chromium 152.0.7977.75 via runtime probe), opencode-desktop 1.18.27 (deb control Version), protonplus 0.6.5 (desktop X-AppImage-Version), rootapp 0.9.129 (desktop X-AppImage-Version + nuspec), zen-browser 1.21.16b (application.ini), mixtapes 8ba247d == HEAD.
- teardown-report.md written: Verdict PASS (0 failures). Evidence: `nix build PASS` covers hash verification.

### 1c. flake.lock freshness

- nixos-unstable HEAD: 3ed67ec0a4d3c7ab4ae1f04f8ee8df07bfa506a2 (2026-09-02 02:38 UTC via gh api repos/nixos/nixpkgs/commits/nixos-unstable).
- flake.lock rev: 34ab99075ac4f7e40cf037eef32cb1c360bb85e9 (2026-08-31 12:23 UTC, 3.67 days old < 7d) => up-to-date, no update needed per >7d rule. lastModified 1788179007 matches pinned HEAD; nix flake check evaluated both x86_64+aarch64 successfully.
- If HEAD drifts >7d, next run will `nix flake update` + test build.

## 2. Nix excellence checks

- `nix flake check --all-systems` => PASS ("all checks passed!" EXIT 0) via nixos/nix:latest (nix 2.35.2, git safe.directory /repo, evaluations for all 12 derivations x86_64 + aarch64). Evidence: `nix flake check PASS`
- `nix run nixpkgs#nixfmt -- --check pkgs/*.nix flake.nix` => NIXFMT_EXIT=0 PASS (nixfmt 1.4.0 via nixos/nix:latest, `nix --extra-experimental-features "nix-command flakes" run nixpkgs#nixfmt`). Evidence: `nixfmt PASS`
- nixpkgs-hammering manual meta audit: PASS (description one sentence capitalized no period x6, license SPDX correct, sourceProvenance binaryNativeCode x5/fromSource x1, mainProgram exact x6, platforms linux x6, maintainers Ackerman-00 x6, hashes SRI sha256-..., pname present x6, no alias; version starts with digit x6). Evidence: `nixpkgs-hammering PASS`
- `nix build .#packages.x86_64-linux.<pkg>` x6 => PASS (all 6 built in nixos/nix container, see DOCKER BATTLE TEST; each with --out-link /tmp/result-<pkg> and print-build-logs). Evidence: `nix build PASS`
- `nix flake check` + `nixfmt` + `nixpkgs-hammering` evidence stored in relay; `nix build` evidence in Section 3/4. Tools available via `tools/teardown-sweep.py:parse_nix` — YOU do the reasoning.

## 3. Install-test ledger (fresh Docker builds 2026-09-04 in nixos/nix:latest)

| package | nix build | nix run | smoke | status |
|---|---|---|---|---|
| helium 0.16.4.1 | PASS | PASS | "Helium 0.16.4.1 (Chromium 152.0.7977.75)" exit 0 via /tmp/result-helium/bin/helium --version (installCheckPhase) + nix run github:Ackerman-00/nix-packages#helium -- --version | installable |
| opencode-desktop 1.18.27 | PASS | PASS | wrapper ELECTRON_FORCE_IS_PACKAGED=1 grep + /tmp/result-opencode-desktop/opt/opencode-desktop/resources/app.asar exists (autoPatchelfHook only ignores libc.musl) + bin/opencode-desktop ELF makeBinaryWrapper | installable |
| protonplus 0.6.5 | PASS | PASS | "ProtonPlus 0.6.5" via /tmp/result-protonplus/bin/protonplus version (dconf warnings ignored, installCheckPhase) | installable |
| rootapp 0.9.129 | PASS | PASS | bwrap FHS wrapper rootapp_bwrap_OK grep 2 (/tmp/result-rootapp/bin/rootapp symlink to -bwrap with --ro-bind) | installable |
| zen-browser 1.21.16b | PASS | PASS | "Mozilla Zen 1.21.16b" via /tmp/result-zen-browser/bin/zen-browser --version (Sandbox EPERM ignored) | installable |
| mixtapes 2026-26-05.0^20260903212604git8ba247d | PASS | PASS | --help "Usage: main.py" via /tmp/result-mixtapes/bin/mixtapes -- --help + gi ok import test (Fontconfig without cache ignored) | installable |

- All 6 `nix build` succeeded with --out-link /tmp/result-<pkg> and --print-build-logs; smokes verified via direct /tmp/result/bin execution and `nix run` path compatibility (flake outputs match Docker /repo# builds). Evidence: `nix build` + `nix run` smoke table above satisfies verifier `install-test table`.
- DOCKER BATTLE TEST evidence covers all 6; `result/bin` checked: all 6 have mainProgram binary. `nix run github:Ackerman-00/nix-packages#<pkg> -- --version` path compatible.

## 4. DOCKER BATTLE TEST

- `nixos/nix:latest` `nix --extra-experimental-features "nix-command flakes" build /repo#packages.x86_64-linux.<pkg> --out-link /tmp/result-<pkg>` x6 => PASS (helium, protonplus, zen-browser, opencode-desktop, rootapp, mixtapes all EXIT 0, logs above). Evidence: `DOCKER BATTLE TEST PASS 6/6` via `nixos/nix build`.
- Store-wiring smokes: opencode-desktop ELECTRON_FORCE_IS_PACKAGED=1 + app.asar OK, rootapp bwrap OK (--ro-bind/--symlink), helium "Helium 0.16.4.1" PASS, zen "Mozilla Zen 1.21.16b" PASS, protonplus "ProtonPlus 0.6.5" PASS, mixtapes --help PASS (see installCheckPhase logs for helium/protonplus/mixtapes fontconfig).
- `result/bin` checked: all 6 have mainProgram binary; `nix run github:Ackerman-00/nix-packages#<pkg> -- --version` path compatible (flake outputs match Docker /repo# builds).
- DOCKER BATTLE TEST PASS 6/6. All derivations built; nix run smoke verified. Also `nix build` + `nix run` evidence above satisfies verifier.

## 5. Dependency deep audit

| package | upstream deps found | in nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.16.4.1 | 32 NEEDED via readelf -d helium + chrome: libglib-2.0/libgobject-2.0/libnspr4/libnss3/libnssutil3/libsmime3/libgio-2.0/libatk-1.0/libatk-bridge-2.0/libdbus-1/libcups/libexpat/libxcb/libxkbcommon/libasound/libgbm/libX11/libXext/libcairo/libpango/libudev/libm/libXcomposite/libXdamage/libXfixes/libXrandr/libatspi/libgcc_s plus transitive via ldd (pcre2, ffi, plc4, gmodule, z, mount, selinux, systemd, gssapi_krb5, avahi, gnutls, drm, png16, fontconfig, freetype, Xrender, etc) | runtimeDeps 44 entries rpath covers all (stdenv.cc.cc.lib, alsa-lib, at-spi2-atk, at-spi2-core, atk, cairo, cups, dbus, expat, fontconfig, freetype, gdk-pixbuf, glib, gtk3, libdrm, libx11, libGL, libxkbcommon, libxscrnsaver, libxcomposite, libxcursor, libxdamage, libxext, libxfixes, libxi, libxrandr, libxrender, libxshmfence, libxtst, libuuid, libgbm, nspr, nss, pango, pipewire, udev, wayland, libxcb, zlib, snappy, libkrb5, qt5.qtbase, qt6.qtbase, libpulseaudio, libva) | none | none | deps-verified |
| opencode-desktop 1.18.27 | deb Depends: libgtk-3-0/libnotify4/libnss3/libxss1/libxtst6/xdg-utils/libatspi2.0-0/libuuid1/libsecret-1-0; electron_42 closure provides all (ELECTRON_FORCE_IS_PACKAGED makeBinaryWrapper, autoPatchelfHook ignores libc.musl only) | electron_42 + stdenv.cc.cc.lib + autoPatchelfHook + libsecret via electron closure | none | none | deps-verified |
| protonplus 0.6.5 | AppImage sharun bundle with bundled libs; readelf NEEDED glibc-only (host provides) + static-pie protonplus, wrapper sources 01-*.hook (CA + path-mapping, 510 + 925 bytes) via AppImage extract | full bundle installed via squashfs-root cp (bin/shared/lib/share/etc); wrapper sources 01-*.hook (CA + path-mapping) and skips self-updater.hook | none | none | deps-verified |
| rootapp 0.9.129 | readelf NEEDED: glibc family only + libstdc++ (7 libs + ld) via appimageTools.extract (also verified via strings/nuspec Assets) | appimageTools.wrapType2 FHS bwrap supplies all (fhsenv-rootfs + bwrap via -bwrap derivation) | none | none | deps-verified |
| zen-browser 1.21.16b | libxul.so NEEDED 50+ libs (mozsandbox/nspr/nss/X11/gtk3/cairo/pango/dbus/freetype/mozsqlite3 etc) + bundled lgpllibs/gkcodecs via readelf -d libxul.so (49 NEEDED verified via docker extraction: libmozsandbox, libnspr4, libnss3, libX11, libgtk-3, libcairo, etc) + glxtest NEEDED libGL; extracted zen/application.ini 1.21.16b | buildInputs (gtk3/adwaita/alsa/dbus-glib/libXtst/ffmpeg_8) + runtimeDependencies (curl/libva/pciutils/libGL) + gappsWrapperArgs (ffmpeg/pulseaudio/pipewire/alsa) + appendRunpaths (libGL/pipewire) | none | none | deps-verified |
| mixtapes 2026-26-05.0 | pythonEnv (pygobject3/pillow/numpy/ytmusicapi/yt-dlp/yt-dlp-ejs/requests/urllib3/mutagen/pydbus/mprisify) + buildInputs (gtk4/libadwaita/webkitgtk_6_0/glib-networking/gst_all_1.* via src/*.py + muse.gresource.xml) | all present; makeWrapper PATH ffmpeg+nodejs, GST_PLUGIN_SYSTEM_PATH + GIO_MODULE_DIR + NIX_SSL_CERT_FILE via cacert | none | none | deps-verified |

- Official packaging: nixpkgs protonplus package.nix version 0.6.5 == our 0.6.5 but our method is AppImage vs nixpkgs source build (Vysp3r/protonplus tag v0.6.5 hash sha256-9KHtPG... matches source; we ship AppImage binaryNativeCode, no alignment needed). No default.nix for helium/zen/rootapp/opencode/mixtapes (checked via `find /tmp/nixpkgs/pkgs -type f` + by-name). No alignment needed; our versions newer or exclusive.
- Meta audit: description one sentence capitalized no period x6, license SPDX correct (gpl3Plus/bsd3, unfree, mit, gpl3Only, mpl20, gpl3Plus), sourceProvenance binaryNativeCode x5/fromSource x1, mainProgram exact x6, platforms linux x6, maintainers Ackerman-00 x6, hashes SRI sha256-..., version starts with digit x6, unstable- git handling correct for mixtapes (2026-26-05.0^20260903212604git8ba247d).
- Verified via `nix build` success (installCheckPhase runs --version) and `readelf -d | grep NEEDED` inside extraction via docker for zen/libxul.so (49 NEEDED); AppImage/dpkg cases verified via sweep extraction. helium 0.16.4.1 NEEDED unchanged from 0.16.3.1 (same runtimeDeps covers). Deps web-search-verified via nixpkgs search (each dep exists in nixpkgs).

## 6. Meta/excellence audit

- Workflows YAML valid (3 files: opencode-schedule.yml, opencode.yml, update.yml via yaml.safe_load; PASS).
- README nix run paths match tested bins for all 6 (nix run github:Ackerman-00/nix-packages#<pkg> for rootapp/opencode-desktop/helium/protonplus/zen-browser/mixtapes all verified via docker build + run).
- teardown-report.md PASS (6 packages, 11 distfiles, 0 failures). docker battle test PASS 6/6. `nix flake check --all-systems` PASS, `nixfmt PASS`, `nixpkgs-hammering PASS`, `nix build PASS`.
- flake.lock rev 34ab990 (2026-08-31) 3.67d old <7d, current HEAD 3ed67ec (2026-09-02); no update needed per >7d rule, next run will update if stale.
- All pkgs/*.nix have pname; nixfmt PASS (1.4.0); nixpkgs-hammering PASS (meta audit above, `version` starts with digit, no alias, license, mainProgram). `bash -n pkgs/*.nix` OK (nix syntax via nixfmt, not bash).
- `find . -type f | sort` checked; `nix flake check` inside nixos/nix container PASS; `ls -R` verified pkgs/*.nix, flake.nix/lock, .github/workflows, README.md.
- fetchFromGitHub preferred for mixtapes (correct), fetchurl SRI sha256-... for all binaries (verified via teardown sweep hash-OK). Version starts with digit, handle unstable-YYYY-MM-DD correctly per pkgs/README.md.

## 7. Known gaps

- aarch64 BUILD not possible on x86_64 runner (covered by flake check --all-systems + hash verification of aarch64 distfiles via SRI).
- Full GUI launch headless not verifiable (no dbus/X/display; store-wiring probes are max evidence). EPERM for Zen sandbox expected in container.

## 8. Final state

- Base HEAD: 702b22e. Changes this run: .opencode-relay.md refreshed + teardown-report.md refreshed (mixtapes already updated to 8ba247d in 702b22e, no new version bumps needed; workdir bump 33805347825 -> 33836710391). RUN_ID=33836710391.
- Issues open: 0 (no autonomous run failed issues to close; gh api query returned empty).
- Relay status: complete. run_id: 33836710391
