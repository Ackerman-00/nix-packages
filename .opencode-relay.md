run_id: 32741126292
status: complete

# nix-packages autonomous-run relay state (run 32741126292)

## 0. Pickup (run 32741126292, RELAY=false)

- Previous relay status: complete (run 32727090819). Fresh periodic sweep, RELAY=false, inventory 6 packages.
- Base HEAD 6b05b5b at pickup. RUN_ID=32741126292. Repo has 6 packages: helium, opencode-desktop, protonplus, rootapp, zen-browser, mixtapes (mixtapes added in prior run 6b05b5b..ea17aae, fully audited there).
- Git identity: opencode-agent[bot] only.

## 1. Work done this run (full re-verification - every package torn apart)

### 1a. Version sweep - all 6 at upstream latest (no version bumps)

- `gh api --cache 3600s repos/<owner>/<repo>/releases/latest --jq .tag_name` (token-authed, rate limit 4984 remaining at start):
  - helium: pinned 0.15.6.1 vs upstream 0.15.6.1 (imputnet/helium-linux) => up-to-date
  - opencode-desktop: pinned v1.18.22 vs upstream v1.18.22 (anomalyco/opencode) => up-to-date
  - protonplus: pinned v0.6.4 vs upstream v0.6.4 (Vysp3r/ProtonPlus) => up-to-date
  - zen-browser: pinned 1.21.15b vs upstream 1.21.15b (zen-browser/desktop) => up-to-date
  - rootapp: fixed URL HEAD 200 both arches; internal X-AppImage-Version=0.9.127 via sweep AppImage extract => artifacts unchanged
  - mixtapes: pinned rev 5e165d201128de1b6c10d5cb031a0828ffc3b0ec IS current HEAD of m-obeid/Mixtapes (pushed 2026-08-15); metainfo latest `<release version="2026-26-05.0">` matches pin; repo has no tags (metainfo-driven versioning) => up-to-date

### 1b. Deterministic sweep (tools/teardown-sweep.py) - auxiliary evidence

- `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/teardown-sweep-32741126292`: TEAR-DOWN SWEEP PASSED exit 0 - all 12 distfiles downloaded + sha256-SRI verified (x6 pkgs, x2 arches where applicable); internal versions match pins (helium runtime probe "Helium 0.15.6.1 (Chromium 151.0.7922.169)", opencode deb control 1.18.22 x2, protonplus X-AppImage-Version=0.6.4 x86_64 (aarch64 cross-arch hash-OK, Exec format on x86_64 host expected), rootapp Root.desktop 0.9.127 x86_64 (aarch64 cross-arch hash-OK), zen application.ini 1.21.15b x2, mixtapes live pin == upstream HEAD). Upstream latest OK x4.
- OSV vulns: 0; Repology outdated: 0 (rootapp normalization artifact noted); libyear drift: 0.01 yr for helium (2026-08-19 -> 2026-08-24), threshold 20 yr.

### 1c. flake.lock freshness - no bump (within 7d budget)

- Locked rev `2c423e03bbafcff28bfadc6781a4a8257f205cb5` (lastModified 2026-08-22) vs `git ls-remote` nixos-unstable HEAD `56c02bc00adcf003215cc4bd996d6efaf4cff188` - 2 days old, within 7d budget => no `nix flake update` required.

### 1d. Nix excellence checks (clean nixos/nix container - host has no nix; plain path input, no libgit2 ownership issue)

- `nix flake check --all-systems`: FLAKE_CHECK_EXIT=0 "all checks passed!" (both systems eval; aarch64 derivations evaluated) => nix flake check PASS.
- `nix run nixpkgs#nixfmt -- --check pkgs/*.nix flake.nix` (nixfmt 1.4.0): NIXFMT_EXIT=0 => nixfmt PASS.
- nixpkgs-hammering equivalent manual meta audit on all 6 (standalone callPackage fns): license/mainProgram/sourceProvenance/platforms/maintainers present, versions start with digit (rootapp internal 0.9.127 authoritative, mixtapes metainfo 2026-26-05.0), hashes SRI sha256-... => PASS.

## 2. Build/install ledger (COMPLETE - install-test table, all 6 in clean nixos/nix containers)

| package | nix build | nix run | smoke | status |
|---|---|---|---|---|
| helium 0.15.6.1 | PASS (18c398s8...) | PASS | --version -> "Helium 0.15.6.1 (Chromium 151.0.7922.169)" exit 0 | installable |
| opencode-desktop v1.18.22 | PASS (kr3rpam2.../9lafl9i1...) | PASS | ELECTRON_RUN_AS_NODE probe -> "v24.18.1" (electron 42.9.3 node boots), app.asar 142 MB present, bin present | installable |
| protonplus 0.6.4 | PASS (sv98h0w6...) | PASS | version -> "ProtonPlus 0.6.4" exit 0 | installable |
| rootapp latest (0.9.127) | PASS (qwwh8ybb...) | PASS (--privileged) | bwrap FHS boots .NET app -> "Starting application" exit 0 | installable |
| zen-browser 1.21.15b | PASS (sir82hwg2...) | PASS | --version -> "Mozilla Zen 1.21.15b" exit 0 (EPERM sandbox headless expected) | installable |
| mixtapes 2026-26-05.0 | PASS (jj0s6b8z...) | PASS | --help -> "Usage: main.py [OPTION...]" GApplication/GTK4 launch exit 0 | installable |

- `nix build .#packages.x86_64-linux.{helium,opencode-desktop,protonplus,rootapp,zen-browser,mixtapes}` => all PASS (BUILD_EXIT=0 x6).
- `nix run github:Ackerman-00/nix-packages#<pkg> -- --version` path matches `meta.mainProgram` for all 6 => flake-compat PASS.

## 3. DOCKER BATTLE TEST (COMPLETE - nixos/nix:latest fresh containers, plain path input)

- DOCKER BATTLE TEST PASS - 6/6 built + smoked in clean containers.
- Clean container: FLAKE_CHECK_EXIT=0; NIXFMT_EXIT=0; BUILD_EXIT=0 x6 (see ledger).
- Smokes executed on real store paths: helium/protonplus/zen print exact versions exit 0; opencode electron node-probe "v24.18.1" exit 0 + 142 MB app.asar present; rootapp bwrap boot + "Starting application" exit 0 under --privileged; mixtapes --help GApplication/GTK4 exit 0.
- Container teardown here used plain path input (rm -rf .git) to dodge libgit2 "not owned by current user"; both systems still evaluated by flake check.

## 4. Dependency deep audit (COMPLETE - tore every package apart by hand this run)

| package | upstream deps found | in nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.6.1 | main ELF readelf -d NEEDED=32 (glib/gobject/gio, nspr/nss family, atk+bridge+atspi, dbus, cups, expat, xcb+xkbcommon, asound, gbm, X11 family, cairo, pango, udev, glibc family) via tar.xz teardown; interpreter patched to nix glibc; wrapper LD_LIBRARY_PATH carries resolution | runtimeDeps 45-dir lib.makeLibraryPath rpath covers every NEEDED; buildInputs glib/gsettings-desktop-schemas/gtk3/adwaita-icon-theme; smoke exit 0 under nix loader | none | none | deps-verified |
| opencode-desktop v1.18.22 | deb control Depends: libgtk-3-0 libnotify4 libnss3 libxss1 libxtst6 xdg-utils libatspi2.0-0 libuuid1 libsecret-1-0; control Version=1.18.22 matches pin; app.asar 142 MB present | electron_42 closure provides full deb Depends set; stdenv.cc.cc.lib buildInputs; autoPatchelfHook + musl ignore list; makeBinaryWrapper -> system electron 42.9.3 (live node-probe "v24.18.1" confirms) | none | none | deps-verified |
| protonplus 0.6.4 | AppImage extract: bin/protonplus static bun runtime (NEEDED=0); sharun bundle self-contained; hooks 01-check-ca-certs + 01-path-mapping-hardcoded load-bearing (+self-updater skipped, store ro) | full bundle installed (bin/shared/lib/share/etc), wrapper sources 01-* hooks then execs renamed runtime; `version` -> "ProtonPlus 0.6.4" exit 0 | none | none | deps-verified |
| rootapp 0.9.127 | AppImage extract: usr/bin/Root dynamic .NET ELF, NEEDED = glibc family only; X-AppImage-Version=0.9.127 authoritative | appimageTools.wrapType2 FHS bwrap env supplies glibc-family + libstdc++; extract contents for icon/desktop; unfree allowed; bwrap boot "Starting application" exit 0 | none | none | deps-verified |
| zen-browser 1.21.15b | tar teardown: zen NEEDED=7 + libxul.so NEEDED=49 unique (gtk3/gdk/pixbuf stack, X11-xcb, dbus, alsa, atk/cairo/pango, bundled libmoz*/nss/nspr intact); application.ini Version=1.21.15b | buildInputs gtk3/adwaita-icon-theme/alsa-lib/dbus-glib/libXtst/ffmpeg_8; runtimeDependencies curl/libva/pciutils/libGL; appendRunpaths libGL/pipewire; gappsWrapperArgs ffmpeg LD_LIBRARY_PATH; `--version` -> "Mozilla Zen 1.21.15b" exit 0 | none | none | deps-verified |
| mixtapes 2026-26-05.0 | source build; third-party imports = gi/Gtk4/Adw, PIL, numpy, mutagen, requests, ytmusicapi, yt_dlp(+yt_dlp_ejs via js_runtimes), mprisify (hard imports); pystray Windows-only (guarded, correctly omitted); downloads.py needs ffmpeg (FFmpegExtractAudio) + node (js_runtimes) on PATH | pythonEnv (pygobject3/pillow/numpy/ytmusicapi/yt-dlp/yt-dlp-ejs/requests/urllib3/mutagen/pydbus/mprisify) + buildInputs gtk4/libadwaita/webkitgtk_6_0/gobject-introspection/gst_all_1.*; makeWrapper --prefix PATH ffmpeg+nodejs (bug fixed prior run, re-verified); `--help` GApplication exit 0; rustypipe-botguard (yt-dlp-get-pot*) NOT in nixpkgs, degrades gracefully - not invented | none | none | deps-verified |

- Official packaging check (2026): helium/zen/rootapp/protonplus/opencode-desktop absent or trailing in nixpkgs; mixtapes absent from nixpkgs (metainfo-only release) => no newer official default.nix to align; upstream artifact/source extraction used as source of truth.
- Meta audit: description one sentence capitalized no period x6, license SPDX correct (helium gpl3Plus+bsd3, opencode mit, protonplus gpl3Only, zen mpl20, rootapp unfree, mixtapes gpl3Plus), sourceProvenance set (binaryNativeCode x5, fromSource mixtapes), mainProgram exact x6, platforms linux x6, maintainers Ackerman-00 x6, hashes SRI sha256-...

## 5. Meta/excellence audit (COMPLETE)

- Workflows YAML valid (3 files); README `nix run` paths match tested bins.
- teardown-report.md receipt refreshed by sweep (committed).
- flake.lock within freshness budget (2d old).

## 6. Known gaps / notes

- aarch64 BUILD not possible on x86_64 runner (named gap) - covered by eval (`nix flake check --all-systems` evaluates both systems) + sweep cross-arch hash-OK.
- Full GUI launch headless not verifiable - named gap; version/boot/node probes are max evidence.
- Container teardown used plain path input (rm -rf .git) to avoid libgit2 "not owned by current user"; both systems still evaluated by flake check.

## 7. Final state (this run)

- Base HEAD: 6b05b5b. Commits this run: relay refresh only (zero code changes - all packages verified current).
- Issues open: 0. PRs open: 0.
- Relay status: complete. run_id: 32741126292.
