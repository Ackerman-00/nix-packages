# nix-packages autonomous-run relay state

status: complete
run_id: 31882403357

## 0. Pickup

- Previous relay (run 31867629117) was `status: complete`; nothing unfinished to continue. Started fresh on main @ f7dadfb.
- Runner nix was 2.16.1 but the locked nixpkgs (rev d407951) requires >= 2.18 (`nix build` aborted). Upgraded to 2.34.8 via `sudo env "PATH=$PATH" nix upgrade-nix`. Runner-only change, not committed.
- Docker 28.0.4 verified working; ubuntu:24.04 pulled and used for all container tests.

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): last 20 runs ALL green; last run 31855199837 (2026-08-15 00:58) succeeded. No failed runs, no churn commits ("chore: auto-update packages" count in history = 0).
- No open issues (`gh api "repos/Ackerman-00/nix-packages/issues?state=open"` -> []). No open PRs (`gh api .../pulls?state=open` -> []).

## 2. Fixes this run

- NONE REQUIRED. Full independent fresh-checker verification: every package current, builds green, runtime-verified in clean containers, hashes match upstream byte-for-byte.

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| helium | 0.15.4.1 | 0.15.4.1 (gh releases latest) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (strip v) | up-to-date |
| zen-browser | 1.21.14b | 1.21.14b (gh releases latest) | up-to-date |
| opencode-desktop | v1.18.18 | v1.18.18 (gh releases latest) | up-to-date |
| rootapp | latest (fixed URL) | hash unchanged after `--refresh` re-prefetch | up-to-date |

- All 8 pinned SRI hashes re-prefetched with `--refresh` and match byte-for-byte (helium x86_64+aarch64, protonplus x86_64+aarch64, zen x86_64+aarch64, opencode deb). Rootapp fixed URL re-prefetched -> same hash.
- Independently downloaded all 5 x86_64 artifacts to /tmp/audit and `sha256sum` -> base64 SRI matches pinned exactly (helium qx92G..., protonplus J0ddq..., zen s6W7e..., opencode e31ka..., rootapp BzaSb...).
- Real versions torn from inside artifacts: helium dir name 0.15.4.1; protonplus `version` -> "ProtonPlus 0.6.4"; zen application.ini Version=1.21.14b + BuildID=20260811103047; opencode app.asar package.json version 1.18.18 (read via pickle header walk); rootapp X-AppImage-Version=0.9.126 (Type-2 AppImage, no pinned version).
- update.yml source URLs all in sync with pkgs/*.nix (5 blocks, no drift).
- README package list (rootapp, opencode-desktop, helium, protonplus, zen-browser) matches flake.nix exactly.

## 4. Build/install verification ledger

| package | build (x86_64) | aarch64 eval | binary smoke | status |
|---|---|---|---|---|
| helium | PASS | PASS (eval .name) | installCheckPhase: "Helium 0.15.4.1 (Chromium 151.0.7922.137)" | pass |
| protonplus | PASS | PASS (eval .name) | installCheckPhase: "ProtonPlus 0.6.4" | pass |
| zen-browser | PASS | PASS (eval .name) | `--version` -> "Mozilla Zen 1.21.14b" | pass |
| opencode-desktop | PASS | n/a (x86_64 only) | wrapper + electron v42.5.1; app main process starts headless | pass (GUI needs display = gap) |
| rootapp | PASS | n/a (x86_64 only) | starts ("Starting application", "Using profile default") under `--privileged` | pass |

- `nix flake check --all-systems` green on main AND on a fresh clone at /tmp/fresh-check; `--refresh` rebuild in fresh clone produced IDENTICAL store paths (helium xmsjwcf..., protonplus bjmcfaj..., zen v78acsd..., rootapp l45vk2..., opencode vi5mdch...).
- sourceProvenance = [binaryNativeCode] on all 5 packages (nix eval verified).

## 5. Dependency audit (100% of inventory)

| package | upstream deps found | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium | real ELF NEEDED = 32 sonames torn from artifact (X11 family, asound, atk/atspi, cairo, cups, dbus, expat, gbm, nspr/nss, pango, udev, xcb, xkbcommon) | all covered by runtimeDeps | none | extra-but-harmless (dlopen coverage) | deps-verified (nix loader --list: 73 libs resolved, ZERO errors in container) |
| protonplus | static bun + bundled bin/shared/lib/etc; hooks 01-check-ca-certs + 01-path-mapping-hardcoded | binary bundles everything; wrapper sources 01-* hooks | none | none | deps-verified |
| zen-browser | libxul + libmozsqlite3 + gtk3/alsa/dbus/cairo/pango/X11 sonames | gtk3, alsa-lib, dbus-glib, libXtst, ffmpeg_8, autoPatchelfHook, runtimeDependencies, appendRunpaths | none | none | deps-verified (container ldd on zen + libxul: ZERO not found) |
| opencode-desktop | electron_42 (42.5.1) + 7 .node sidecars (parcel watcher glibc/musl, node-pty, msgpackr) | electron_42 via callPackage; autoPatchelfIgnoreMissingDeps for musl variants | none (musl variants intentionally unloadable on glibc - glibc twins exist and resolve) | none | deps-verified (nix loader on pty.node: ZERO not found; host-ldd GLIBC_ABI_DT_X86_64_PLT is the known glibc-mix false positive) |
| rootapp | .NET self-contained + bwrap FHS sandbox | wrapType2 FHS | none | none | deps-verified (medium confidence - FHS self-contained, ldd n/a for bwrap) |

## 6. Container runtime test ledger (fresh `docker run --rm` per test, base ubuntu:24.04)

| package | nix build | container ldd | container smoke | status |
|---|---|---|---|---|
| helium | PASS | ZERO not found (nix loader --list, 73 libs resolved with wrapper LD_LIBRARY_PATH) | `--version` -> "Helium 0.15.4.1 (Chromium 151.0.7922.137)", exit 0 | verified |
| protonplus | PASS | ZERO not found (static bun) | `version` -> "ProtonPlus 0.6.4", exit 0; hooks: /tmp/pKE -> lib, /tmp/3J=1Z -> share symlinks created; CA-certs hook fires when a host cert store exists (verified by mounting one) | verified |
| zen-browser | PASS | ZERO not found (container ldd on zen + libxul) | `--version` -> "Mozilla Zen 1.21.14b", exit 0; interpreter = nix glibc loader | verified |
| opencode-desktop | PASS | ZERO not found (nix loader on pty.node + glibc watcher) | electron --no-sandbox --version -> "v42.5.1", exit 0; app wrapper launches electron main process headless | verified (GUI launch gap) |
| rootapp | PASS | ZERO not found (bwrap wrapper) | starts ("Starting application", "Using profile default") under `--privileged` (bwrap needs userns) | verified (needs userns) |

- Named gaps: aarch64 BUILD impossible on x86_64 runner (eval only, all 3 aarch64 packages evaluate green); GUI apps full window rendering needs a display (headless smoke only); rootapp needs user-namespace support (--privileged on the runner simulates a real machine); protonplus CA-cert hook only fires when a host cert store exists (base ubuntu image ships none - verified it fires with one mounted).
- Adversarial audit done: re-queried all 4 GitHub release tags at end of run (unchanged), interpreters patched to nix loader, nix-loader --list ZERO not found, deterministic rebuild in fresh clone (identical store paths), update.yml/pkgs URL sync, README/flake sync, no auto-update churn in history.

## 7. What remains

- Nothing. Repo is current, all 5 packages build and pass clean-container runtime tests, all hashes match upstream, no open issues/PRs, no failing Actions runs.

## 8. Final state

- main @ f7dadfb (no commits needed this run - everything already current; runner-only nix upgrade to 2.34.8). Working tree clean.
- README.md package list matches flake.nix (rootapp, opencode-desktop, helium, protonplus, zen-browser).