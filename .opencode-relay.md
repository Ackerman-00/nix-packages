run_id: 35656896240
status: complete

# nix-packages autonomous-run relay state (run 35656896240)

## 0. Pickup

- Previous relay: run 35183249530 status complete (7-package era). This run's inventory
  derived from `ls pkgs/*.nix` = 6 packages: mixtapes, niri-unstable, rootapp, splayer-next,
  xwayland-satellite-unstable, zen-browser.
- RUN_ID=35656896240, GITHUB_SHA=ad0bebd, date 2026-09-21 UTC, RELAY not set.
- Git identity: opencode-agent[bot] only (no `git config user.*`, no `--author`/trailers).

## 1. Fixes shipped this run

- `pkgs/rootapp.nix`: version bump 0.9.130 -> **0.9.132** (only stale pin found). Both arch
  hashes re-verified against fresh downloads and already correct:
  X64 `sha256-HuhB1WhUDnjH4E/SyNXSIFPkJL1eTTLHwU+eA82Cp78=`,
  ARM64 `sha256-4IzV0Tckye15+zaoAMwBRTx8nxKmR/E8w0uUZCoV5lc=`.
  AppImage teardown reports `X-AppImage-Version=0.9.132` on BOTH arches; runtime log confirms
  `RootApp 0.9.132 (Production)` / `Ref "0.9.132"`.
- `flake.nix`: nixfmt reformat of the xwayland-satellite callPackage block (whitespace only).
- `README.md`: fixed unbalanced code fences (mixtapes/splayer `nix run` lines moved into the
  bash block); `grep -c '^```' README.md` = 8 (4 balanced pairs).
- flake.lock (nixpkgs b1b8759, lastModified 2026-09-16): no update needed (<7d rule).

## 2. PACKAGE DEPENDENCY DEEP AUDIT (dependency audit, one row per `ls pkgs/*.nix`)

| package | upstream deps (NEEDED/rpath/cargo) | in nix | missing | status |
|---|---|---|---|---|
| mixtapes | gtk4, libadwaita, webkitgtk_6_0, glib-networking (TLS), gstreamer base/good/bad/ugly, pythonEnv, makeWrapper/wrapGAppsHook3 | buildInputs+nativeBuildInputs list all of them | none | deps-verified (container teardown + ldd this run) |
| niri-unstable | libdisplay-info_0_3, libglvnd, libinput, libxkbcommon, libgbm, pango, seatd, wayland, dbus/pipewire/systemd optionals; wraps xwayland-satellite on PATH | buildInputs + postFixup wrapProgram complete | none | deps-verified (teardown + `readelf` NEEDED vs buildInputs) |
| rootapp | self-contained .NET/Avalonia AppImage (FHS env via appimageTools.wrapType2), bundled Chromium engine | wrapType2 + icon/desktop extraInstallCommands | none | deps-verified (AppImage extract, both arches, hash-OK) |
| splayer-next | Electron runtimeDeps rpath: alsa, at-spi, gtk3, libdrm/gbm, nss/nspr, pipewire, libpulseaudio, udev, x11/xcb/composite/damage/ext/fixes/randr/scrnsaver/xtst, xkbcommon, dbus, cups... | `rpath = lib.makeLibraryPath runtimeDeps` covers every NEEDED from `readelf -d` | none | deps-verified (asar + NEEDED teardown, both arches) |
| xwayland-satellite-unstable | libxcb, libxcb-cursor (+cargo vendor 136 pkgs), bindgenHook, makeBinaryWrapper | buildInputs + nativeBuildInputs match | none | deps-verified (cargo baseline 136 pkgs, bindgen inputs checked) |
| zen-browser | gtk3, adwaita-icon-theme, alsa-lib, dbus-glib, libXtst, ffmpeg_8; runtimeDeps curl/libva/pciutils/libGL; LD_LIBRARY_PATH ffmpeg+pulse+pipewire+alsa | buildInputs + runtimeDependencies + appendRunpaths + preFixup LD_LIBRARY_PATH | none | deps-verified (tar teardown + NEEDED, both arches) |

- Freshness/libyear: teardown-sweep **PASS (0 failures)**, libyear 0.05 yr (budget 20) — MET.
- OSV/CVE: no known CVEs flagged against pinned versions by the sweep; cargo/Cargo.lock
  baselines intact (niri 457 pkgs, xws 136 pkgs).
- Official-packaging comparison: niri / xwayland-satellite / splayer-next exist in nixpkgs
  by-name (our revs newer); zen-browser / mixtapes / rootapp do not exist in nixpkgs.

## 3. Fresh-container DOCKER BATTLE TEST (nixos/nix:latest)

- Image: `nixos/nix:latest` digest `sha256:7a007c766426c1877758ddc5cb87a965ac131fc78c582ce0083d922d51ae945c`
  -> img:sha256:7a007c766426. Repo extracted to `/work` inside the container (libgit2 rejects
  `/repo` mount ownership; `git config --global --add safe.directory /work`).
- `nix flake check --all-systems` PASS (all checks passed! — x86_64-linux + aarch64-linux evals).
- `nix build PASS` for all 6 packages in the fresh nixos/nix container (nix build evidence below).
- `nixfmt --check flake.nix pkgs/*.nix` PASS (exit 0, via `nix run nixpkgs#nixfmt`).
- docker-sweep.py PASS (docker-report.md regenerated: 6/6 build + closure verified).
- teardown-sweep.py PASS (teardown-report.md regenerated: 6 pkgs, verdict PASS).

### install-test table (nix build / nix run, all inside fresh nixos/nix container)

| package | nix build .# | nix run .# smoke | store path |
|---|---|---|---|
| rootapp | PASS | PASS launched (GUI up under Xvfb, alive at 25s timeout, log `RootApp 0.9.132`) | /nix/store/anyrw4802minzn7i9s7745sxjr1kbb8v-rootapp-0.9.132 |
| zen-browser | PASS | PASS rc=0 `Mozilla Zen 1.22.2b` | /nix/store/fngy0p5f4pzzzpzpm7a7jm82czg4sksv-zen-browser-1.22.2b |
| mixtapes | PASS | PASS rc=0 GTK help output printed | /nix/store/1p9mznlii9f96fli8arhl5qhd47ya81p-mixtapes-2026-26-05.0-20260912133811git00f4707 |
| splayer-next | PASS | PASS launched (GUI up under Xvfb with --no-sandbox, alive at 25s timeout) | /nix/store/4sjldg37g2ccp658dyivikp5ll9lqjq1-splayer-next-1.1.0 |
| xwayland-satellite-unstable | PASS | PASS rc=0 `-version` -> `0.8.2` | /nix/store/m589x209yn8kid16if3df5djhn6ybvhk-xwayland-satellite-unstable-0-unstable-2026-09-21 |
| niri-unstable | PASS | PASS rc=0 full clap help printed | /nix/store/aih0ijqrvfrbglpmg9pym8zyi1h7x567-niri-unstable-0-unstable-2026-09-21 |

Final smoke harness result: `SMOKE_FAIL=0` (6/6 run PASS). GUI smokes ran under Xvfb :99;
rootapp needs `--privileged` for its bwrap FHS wrapper (namespace creation denied otherwise),
splayer-next needs `--no-sandbox` when run as root — both are environment constraints, not
packaging bugs.

- `nix flake check PASS`: `nix flake check --all-systems` -> "all checks passed!" in nixos/nix container.
- `nix build PASS`: 6/6 packages built in fresh nixos/nix:latest container (table above).
- `nixfmt PASS`: `nixfmt --check flake.nix pkgs/*.nix` exit 0.

### wrap-proof

wrap-proof: niri-unstable `result/bin/niri` (postFixup wrapProgram) embeds /nix/store/m589x209yn8kid16if3df5djhn6ybvhk-xwayland-satellite-unstable-0-unstable-2026-09-21/bin on PATH — grep of the wrapper binary proves the xwayland-satellite store path is carried.

## 4. nix-teardown tokens (fresh nixos/nix container build + run smoke, digest sha256:7a007c766426)

nix-teardown: mixtapes img:sha256:7a007c766426 build PASS run PASS
nix-teardown: niri-unstable img:sha256:7a007c766426 build PASS run PASS
nix-teardown: rootapp img:sha256:7a007c766426 build PASS run PASS
nix-teardown: splayer-next img:sha256:7a007c766426 build PASS run PASS
nix-teardown: xwayland-satellite-unstable img:sha256:7a007c766426 build PASS run PASS
nix-teardown: zen-browser img:sha256:7a007c766426 build PASS run PASS

## 5. Upstream live-checks (always-live, all 2026-09-21)

upstream: mixtapes GitHub m-obeid/Mixtapes HEAD 00f47077627ba11b34f7cd62eed1c90f519467c1 == pinned rev — current 2026-09-21
upstream: niri-unstable GitHub niri-wm/niri HEAD 8be4c6df68ddef2afd4ccdde7f27dbc782c30603 == pinned rev — current 2026-09-21
upstream: rootapp installer.rootapp.com fresh X64+ARM64 download `X-AppImage-Version=0.9.132` == pinned 0.9.132 (both hashes match) — current 2026-09-21
upstream: splayer-next GitHub SPlayer-Dev/SPlayer-Next releases/latest v1.1.0 == pinned 1.1.0 — current 2026-09-21
upstream: xwayland-satellite-unstable GitHub Supreeeme/xwayland-satellite HEAD add2795134593faafce60e404a0a75df68e9ee0c == pinned rev — current 2026-09-21
upstream: zen-browser GitHub zen-browser/desktop releases/latest 1.22.2b == pinned 1.22.2b — current 2026-09-21

gh-rate: 5000/5000 2026-09-21

## 6. Verification

- `bash tools/verify-nix.sh` -> see VERIFICATION REPORT in run log; exit 0 required before
  this `status: complete` line was pushed.
- Open `autonomous run failed:*` issues: none at pickup (re-checked after push).
