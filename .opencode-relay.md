run_id: 32819929457
status: complete

# nix-packages autonomous-run relay state (run 32819929457)

## 0. Pickup (run 32819929457, RELAY=false)

- Previous relay status: complete (run 32794792298). Fresh periodic sweep, RELAY=false, inventory 6 packages.
- Base HEAD d61ef21 at pickup. RUN_ID=32819929457. Packages: helium, opencode-desktop, protonplus, rootapp, zen-browser, mixtapes.
- Git identity: opencode-agent[bot] only.

## 1. Work done this run

### 1a. Version sweep - all 6 at upstream latest

- `gh api --cache 3600s repos/<owner>/<repo>/releases/latest --jq .tag_name` (PAT-authed, 5000 core remaining at start):
  - helium: pinned 0.15.7.1 vs upstream 0.15.7.1 (imputnet/helium-linux) => up-to-date
  - opencode-desktop: pinned v1.18.23 vs upstream v1.18.23 (anomalyco/opencode) => up-to-date (update.yml auto-bumped 1.18.22 -> 1.18.23 in d61ef21 but shipped a BROKEN aarch64 pin - see 1b)
  - protonplus: pinned v0.6.4 vs upstream v0.6.4 (Vysp3r/ProtonPlus) => up-to-date
  - zen-browser: pinned 1.21.15b vs upstream 1.21.15b (zen-browser/desktop) => up-to-date
  - mixtapes: pinned rev 5e165d201128de1b6c10d5cb031a0828ffc3b0ec IS current HEAD of m-obeid/Mixtapes (2026-08-15) => up-to-date
  - rootapp: installer.rootapp.com HEAD probe 200 both arches; artifacts byte-match pins
- OSV vulns: 0; Repology outdated: 0; libyear drift: 0.00 yr, threshold 20 yr.

### 1b. BUG FOUND + FIXED: stale opencode-desktop aarch64 fetchurl hash

- Manual teardown downloaded opencode-desktop-linux-arm64.deb (116326200 bytes == release asset size), computed sha256-SRI = `sha256-dzAdtB1QaOAR6V8OAWnTX2U/U39+yoHmQintNpS0+xc=` vs pinned `sha256-zBtN2VjI1zGBfTJiR2XJUSP/gvSqRX+bF8n0Nh+t+2k=` => MISMATCH. Re-download reproduced byte-exact => not a fluke.
- Cross-proof via GitHub's own asset digests (`gh api releases/tags/v1.18.23`): official arm64 .deb digest hex `77301db41d50...` == my download; stale pin hex decodes to `cc1b4dd958c8...` which == the official **v1.18.22** arm64 digest. So nix-update bumped version + x86_64 hash but left aarch64 still pointing at 1.18.22 - same systematic single-arch failure mode as helium 0.15.7.1 last run.
- Fix: pkgs/opencode-desktop.nix aarch64 hash updated to verified `sha256-dzAdtB1QaOAR6V8OAWnTX2U/U39+yoHmQintNpS0+xc=`. amd64 hash re-verified against official digest (`4fb07956d46b...`).

### 1c. UPDATER FIX (defense-in-depth): multi-arch hash safety net for .github/workflows/update.yml

- Root cause is systemic: nix-update only rewrites the src it evaluates; in an `archives.${system}` attrset sibling arches keep STALE hashes after version bumps. This has now shipped broken aarch64 builds twice (helium 0.15.7.1, opencode-desktop 1.18.23).
- Wrote a generic post-pass for update.yml (before commit): python parses EVERY `url`/`hash` pair in every pkgs/*.nix, interpolates `${pname}`/`${version}`, re-prefetches each URL via `nix store prefetch-file --refresh`, and fixes any hash that no longer matches; sets UPDATED=1 so repairs land in the auto-update commit. YAML validated with yaml.safe_load.
- PUSH BLOCKED by permissions: the run's GITHUB_TOKEN is a GitHub App token without `workflows` scope ("refusing to allow a GitHub App to create or update workflow"). Change preserved as committed patch `tools/update-yml-multiarch-safety-net.patch` (f3cddc5) and tracking issue https://github.com/Ackerman-00/nix-packages/issues/8 asks a workflows-permitted maintainer to `git apply` it. Working tree restored clean; the package fix itself is NOT blocked and was pushed.

### 1d. Deterministic sweep (tools/teardown-sweep.py) - auxiliary evidence

- `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/opencode/sweep-32819929457`: TEAR-DOWN SWEEP PASSED exit 0 - all 12 distfile rows OK (x6 pkgs, x2 arches where applicable); internal versions match pins (helium runtime probe "Helium 0.15.7.1 (Chromium 151.0.7922.173)", opencode deb control 1.18.23 x2, protonplus X-AppImage-Version=0.6.4 + cross-arch hash-OK, rootapp Root.desktop 0.9.127 + cross-arch hash-OK, zen application.ini 1.21.15b x2, mixtapes live pin == upstream HEAD). Vulns 0 / outdated 0 / libyear 0.00. teardown-report.md receipt refreshed (committed).

### 1e. flake.lock freshness - no bump (within 7d budget)

- Locked rev `2c423e03bbafcff28bfadc6781a4a8257f205cb5` (lastModified 1787360063, 3.3 days old) vs nixos-unstable HEAD `56c02bc00adcf003215cc4bd996d6efaf4cff188` - within 7d budget => no `nix flake update` required (available bump verified via container dry-run update: resolves cleanly).

### 1f. Nix excellence checks (clean nixos/nix containers - host has no nix; plain path input, no libgit2 ownership issue)

- `nix flake check --all-systems`: FLAKE_CHECK_EXIT=0 "all checks passed!" (both systems eval, incl. fixed aarch64 opencode-desktop derivation) => nix flake check PASS.
- `nix run nixpkgs#nixfmt -- --check pkgs/*.nix flake.nix` (nixfmt 1.4.0): NIXFMT_EXIT=0 => nixfmt PASS.
- nixpkgs-hammering-equivalent manual meta audit on all 6: license/mainProgram/sourceProvenance/platforms/maintainers present; versions start with digit; hashes SRI sha256-... => PASS.

## 2. Build/install ledger (COMPLETE - install-test table, all 6 in clean nixos/nix containers)

| package | nix build | nix run | smoke | status |
|---|---|---|---|---|
| helium 0.15.7.1 | PASS (87kg5p1s...) | PASS | --version -> "Helium 0.15.7.1 (Chromium 151.0.7922.173)" exit 0 | installable |
| opencode-desktop v1.18.23 | PASS (jir36kjx...) | PASS | compiled wrapper carries app.asar+ELECTRON_FORCE_IS_PACKAGED strings; asar 142160440 B present; system electron 42.9.3 node-probe -> "node v24.18.1 electron-boot-ok" exit 0 | installable |
| protonplus 0.6.4 | PASS (sgb0sqi8...) | PASS | version -> "ProtonPlus 0.6.4" exit 0 | installable |
| rootapp latest (0.9.127) | PASS (d2iw2z4f...) | PASS (--privileged) | bwrap FHS boots .NET app -> "Starting application", HttpMetadataStore init, exit 0 | installable |
| zen-browser 1.21.15b | PASS (5y7i0cfm...) | PASS | --version -> "Mozilla Zen 1.21.15b" exit 0 | installable |
| mixtapes 2026-26-05.0^git5e165d2 | PASS (26jpd48g...) | PASS | --help -> GApplication/GTK4 options exit 0 | installable |

- `nix build .#packages.x86_64-linux.{helium,opencode-desktop,protonplus,rootapp,zen-browser,mixtapes}` => BUILD_OK x6 in fresh containers.
- README `nix run github:Ackerman-00/nix-packages#<pkg>` paths match tested bins/meta.mainProgram for all 6 => flake-compat path PASS.

## 3. DOCKER BATTLE TEST (COMPLETE - nixos/nix:latest fresh containers, plain path input)

- DOCKER BATTLE TEST PASS - 6/6 built + smoked in clean containers (battle.sh + smoke.sh logs retained in runner /tmp/opencode/).
- Clean container: FLAKE_CHECK_EXIT=0; NIXFMT_EXIT=0; BUILD_OK x6 (see ledger).
- Evidence one-liners: `nix flake check --all-systems` => nix flake check PASS; `nix build .#packages.x86_64-linux.<pkg>` x6 => nix build PASS x6; `nix run nixpkgs#nixfmt -- --check` => nixfmt PASS.

## 4. Dependency deep audit (COMPLETE - tore every package apart by hand this run)

| package | upstream deps found | in nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.7.1 | main chrome readelf -d NEEDED=32 (ld-linux, X11 family, xcb+xkbcommon, asound, atk+bridge+atspi, cairo, cups, dbus, expat, gbm, glib/gobject/gio, nspr/nss/nssutil/smime, pango, udev, glibc family, libgcc/libm) via tar.xz teardown; interpreter patched to nix glibc; wrapper LD_LIBRARY_PATH carries resolution | runtimeDeps 45-dir lib.makeLibraryPath rpath covers every NEEDED incl qt5.qtbase/qt6.qtbase/stdenv.cc.cc.lib shims; buildInputs glib/gsettings-desktop-schemas/gtk3/adwaita-icon-theme; smoke exit 0 under nix loader | none | none | deps-verified |
| opencode-desktop v1.18.23 | deb control Depends: libgtk-3-0 libnotify4 libnss3 libxss1 libxtst6 xdg-utils libatspi2.0-0 libuuid1 libsecret-1-0; control Version=1.18.23 matches pin BOTH arches; app.asar present x2 (142160440 B amd64 / 142159928 B arm64); asar declares electron 39.8.5 upstream | electron_42 closure provides full deb Depends set; stdenv.cc.cc.lib buildInputs; autoPatchelfHook + musl ignore list; makeBinaryWrapper -> system electron 42.9.3 (live node-probe confirms boot) | none | none | deps-verified |
| protonplus 0.6.4 | AppImage extract (clean dir): bin/protonplus static bun runtime; sharun bundle self-contained; hooks 01-check-ca-certs + 01-path-mapping-hardcoded load-bearing (+self-updater skipped, store ro); X-AppImage-Version=0.6.4 | full bundle installed (bin/shared/lib/share/etc), wrapper sources 01-* hooks then execs renamed runtime; `version` -> "ProtonPlus 0.6.4" exit 0 | none | none | deps-verified |
| rootapp 0.9.127 | AppImage extract: usr/bin/Root dynamic .NET ELF, NEEDED = glibc family only (ld-linux/libc/libdl/libgcc_s/libm/libpthread/librt/libstdc++) re-read this run; X-AppImage-Version=0.9.127 authoritative; artifacts unchanged vs pins (byte-identical hashes) | appimageTools.wrapType2 FHS bwrap env supplies glibc-family + libstdc++; extract contents for icon/desktop; unfree allowed; bwrap boot "Starting application" exit 0 | none | none | deps-verified |
| zen-browser 1.21.15b | tar teardown: zen NEEDED=7 (glibc family + libstdc++) + libxul.so NEEDED=49 unique (gtk3 stack, X11-xcb, dbus, alsa, atk/cairo/pango, bundled libmoz*/nss/nspr intact); application.ini Version=1.21.15b BuildID 20260818101929 both arches | buildInputs gtk3/adwaita-icon-theme/alsa-lib/dbus-glib/libXtst/ffmpeg_8; runtimeDependencies curl/libva/pciutils/libGL; appendRunpaths libGL/pipewire; gappsWrapperArgs ffmpeg LD_LIBRARY_PATH; `--version` -> "Mozilla Zen 1.21.15b" exit 0 | none | none | deps-verified |
| mixtapes 2026-26-05.0 | source build; third-party imports = gi/Gtk4/Adw, PIL, numpy, mutagen, requests, ytmusicapi, yt_dlp(+yt_dlp_ejs via js_runtimes), mprisify (hard imports); pystray Windows-only (guarded, correctly omitted); downloads.py needs ffmpeg (FFmpegExtractAudio) + node (js_runtimes) on PATH | pythonEnv (pygobject3/pillow/numpy/ytmusicapi/yt-dlp/yt-dlp-ejs/requests/urllib3/mutagen/pydbus/mprisify) + buildInputs gtk4/libadwaita/webkitgtk_6_0/gobject-introspection/gst_all_1.*; makeWrapper --prefix PATH ffmpeg+nodejs; `--help` GApplication exit 0; rustypipe-botguard (yt-dlp-get-pot*) NOT in nixpkgs, degrades gracefully - not invented | none | none | deps-verified |

- Official packaging check (2026): helium/zen/rootapp/protonplus/opencode-desktop absent or trailing in nixpkgs; mixtapes absent from nixpkgs (metainfo-only release) => no newer official default.nix to align; upstream artifact/source extraction used as source of truth.
- Meta audit: description one sentence capitalized no period x6, license SPDX correct (helium gpl3Plus+bsd3, opencode mit, protonplus gpl3Only, zen mpl20, rootapp unfree, mixtapes gpl3Plus), sourceProvenance set (binaryNativeCode x5, fromSource mixtapes), mainProgram exact x6, platforms linux x6, maintainers Ackerman-00 x6, hashes SRI sha256-...

## 5. Meta/excellence audit (COMPLETE)

- Workflows YAML valid (3 files: opencode-schedule.yml [opencode, cleanup], opencode.yml, update.yml [update-flake; multi-arch safety net staged as patch + issue #8 - App token lacks workflows permission]); README `nix run` paths match tested bins.
- teardown-report.md receipt refreshed by sweep (committed).
- flake.lock within freshness budget (3.3d old).
- All pkgs/*.nix have pname; nixfmt-clean per container check.

## 6. Known gaps / notes

- aarch64 BUILD not possible on x86_64 runner (named gap) - covered by eval (`nix flake check --all-systems`) + sweep cross-arch hash verification (the exact check fetchurl performs at build time) + GitHub official asset digest cross-proof.
- Full GUI launch headless not verifiable - named gap; version/boot/node probes are max evidence.
- Container probes must avoid awk/sed/strings/find -type f assumptions (nixos/nix minimal image lacks some); use bash/readlink/grep/nix-store instead.

## 7. Final state (this run)

- Base HEAD: d61ef21. Changes this run: pkgs/opencode-desktop.nix (aarch64 hash fix, cdef8d9), tools/update-yml-multiarch-safety-net.patch (staged updater fix, f3cddc5), teardown-report.md (receipt refresh), .opencode-relay.md (this file). Direct update.yml edit blocked by App workflows permission - tracked as issue #8.
- Issues open: #8 (update.yml safety net apply - needs workflows-permitted maintainer). "autonomous run failed" issues: checked and closed post-push. PRs open: 0.
- Relay status: complete. run_id: 32819929457.
