run_id: 32794792298
status: complete

# nix-packages autonomous-run relay state (run 32794792298)

## 0. Pickup (run 32794792298, RELAY=false)

- Previous relay status: complete (run 32741126292). Fresh periodic sweep, RELAY=false, inventory 6 packages.
- Base HEAD 57b499f at pickup. RUN_ID=32794792298. Packages: helium, opencode-desktop, protonplus, rootapp, zen-browser, mixtapes.
- Git identity: opencode-agent[bot] only.

## 1. Work done this run

### 1a. Version sweep - all 6 at upstream latest

- `gh api --cache 3600s repos/<owner>/<repo>/releases/latest --jq .tag_name` (PAT-authed, rate limit 5000 at start):
  - helium: pinned 0.15.7.1 vs upstream 0.15.7.1 (imputnet/helium-linux) => up-to-date (update.yml had auto-bumped 0.15.6.1 -> 0.15.7.1 in 294188a but shipped a BROKEN aarch64 pin - see 1c)
  - opencode-desktop: pinned v1.18.22 vs upstream v1.18.22 (anomalyco/opencode) => up-to-date
  - protonplus: pinned v0.6.4 vs upstream v0.6.4 (Vysp3r/ProtonPlus) => up-to-date
  - zen-browser: pinned 1.21.15b vs upstream 1.21.15b (zen-browser/desktop) => up-to-date
  - rootapp: fixed URL HEAD 200 both arches; sha256 of downloaded artifacts byte-matches pins; internal X-AppImage-Version=0.9.127 => artifacts unchanged
  - mixtapes: pinned rev 5e165d201128de1b6c10d5cb031a0828ffc3b0ec IS current HEAD of m-obeid/Mixtapes (2026-08-15) => up-to-date
- OSV vulns: 0; Repology outdated: 0 (rootapp normalization artifact noted); libyear drift: 0.00 yr, threshold 20 yr.

### 1b. BUG FOUND + FIXED: stale helium aarch64 fetchurl hash

- Manual teardown downloaded helium-0.15.7.1-arm64_linux.tar.xz (119384568 bytes == release asset size, xz CRC64 integrity OK) and computed sha256-SRI = `sha256-CYHUDYEMwe+2//4cDPMS2VqcRfbRJD0USDxDJ7kKu6Q=` vs pinned `sha256-Y1nU48e8UsUtQ67zRHVh5ul0vRMD4jfAe87L2p9oIf4=` => MISMATCH (pin was still 0.15.6.1's arm64 hash; update.yml bumped version + x86_64 hash only). `nix build .#packages.aarch64-linux.helium` would fail.
- Fix: pkgs/helium.nix aarch64 hash updated to the verified value. x86_64 hash re-verified byte-exact (`sha256-MQKJLN69/g1R46Y44ADo1UvKAXxS2vKsq/XlAwBO+58=`).
- Cross-validation of new arm64 hash: sweep re-download + python hashlib SRI match == what fetchurl verifies at build time; `nix flake check --all-systems` evaluates the aarch64 derivation cleanly.

### 1c. SWEEP TOOL FIX (false-negative defense): hard-failure override bug in tools/teardown-sweep.py

- First sweep run against broken tree CAUGHT `[FAIL] helium : sha256 mismatch for helium-0.15.7.1-arm64_linux.tar.xz` but still exited 0 "SWEEP PASSED": commit 3f49693's per-package verdict let any good artifact row override a hard FAIL (designed to silence inner-archive UNVERIFIED noise, e.g. .deb data.tar.*, but it also swallowed genuine per-arch hash failures).
- Fixed verdict semantics: HARD statuses (FAIL/MISMATCH/STALE) on any artifact are never overridden (every pinned fetchurl hash must verify or some system is broken); SOFT UNVERIFIED rows remain overridable by a verifying artifact. Unit-tested 4 cases (hard-after-good both orders, soft overridden by OK, only-soft bad). Full sweep re-run after fix: exit 0, 14 OK rows, 0 FAIL.

### 1d. Deterministic sweep (tools/teardown-sweep.py) - auxiliary evidence

- `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/teardown-sweep-32794792298b`: TEAR-DOWN SWEEP PASSED exit 0 - all 12 distfiles downloaded + sha256-SRI verified (x6 pkgs, x2 arches where applicable); internal versions match pins (helium runtime probe "Helium 0.15.7.1 (Chromium 151.0.7922.173)", opencode deb control 1.18.22 x2, protonplus X-AppImage-Version=0.6.4 x86_64 + cross-arch hash-OK, rootapp Root.desktop 0.9.127 x86_64 + cross-arch hash-OK, zen application.ini 1.21.15b x2, mixtapes live pin == upstream HEAD).

### 1e. flake.lock freshness - no bump (within 7d budget)

- Locked rev `2c423e03bbafcff28bfadc6781a4a8257f205cb5` (lastModified 1787360063, ~3 days old) vs nixos-unstable HEAD `56c02bc00adcf003215cc4bd996d6efaf4cff188` - within 7d budget => no `nix flake update` required.

### 1f. Nix excellence checks (clean nixos/nix containers - host has no nix; plain path input, no libgit2 ownership issue)

- `nix flake check --all-systems`: FLAKE_CHECK_EXIT=0 "all checks passed!" (both systems eval) => nix flake check PASS.
- `nix run nixpkgs#nixfmt -- --check pkgs/*.nix flake.nix` (nixfmt 1.4.0): NIXFMT_EXIT=0 => nixfmt PASS.
- nixpkgs-hammering-equivalent manual meta audit on all 6: license/mainProgram/sourceProvenance/platforms/maintainers present; versions start with digit; hashes SRI sha256-... => PASS.

## 2. Build/install ledger (COMPLETE - install-test table, all 6 in clean nixos/nix containers)

| package | nix build | nix run | smoke | status |
|---|---|---|---|---|
| helium 0.15.7.1 | PASS (87kg5p1s...) | PASS | --version -> "Helium 0.15.7.1 (Chromium 151.0.7922.173)" exit 0 | installable |
| opencode-desktop v1.18.22 | PASS (9lafl9i1.../both arches hashed) | PASS | ELECTRON_RUN_AS_NODE direct electron-42.9.3 probe -> "node 24.18.1 electron-boot-ok" exit 0; wrapper strings confirm exec electron + app.asar; app.asar 142160804 bytes present | installable |
| protonplus 0.6.4 | PASS (sgb0sqi8...) | PASS | version -> "ProtonPlus 0.6.4" exit 0 | installable |
| rootapp latest (0.9.127) | PASS (d2iw2z4f...) | PASS (--privileged) | bwrap FHS boots .NET app -> "Starting application", HttpMetadataStore init, exit 0 | installable |
| zen-browser 1.21.15b | PASS (5y7i0cfm...) | PASS | --version -> "Mozilla Zen 1.21.15b" exit 0 | installable |
| mixtapes 2026-26-05.0^git5e165d2 | PASS (26jpd48g...) | PASS | --help -> GApplication/GTK4 "Usage: main.py [OPTION...]" exit 0 | installable |

- `nix build .#packages.x86_64-linux.{helium,opencode-desktop,protonplus,rootapp,zen-browser,mixtapes}` => BUILD_EXIT=0 x6.
- README `nix run github:Ackerman-00/nix-packages#<pkg>` paths match tested bins/meta.mainProgram for all 6 => flake-compat path PASS.

## 3. DOCKER BATTLE TEST (COMPLETE - nixos/nix:latest fresh containers, plain path input)

- DOCKER BATTLE TEST PASS - 6/6 built + smoked in clean containers (battle.sh + followup.sh logs retained in runner /tmp/opencode/).
- Clean container: FLAKE_CHECK_EXIT=0; NIXFMT_EXIT=0; BUILD_EXIT=0 x6 (see ledger).
- Evidence one-liners: `nix flake check --all-systems` => nix flake check PASS; `nix build .#packages.x86_64-linux.<pkg>` x6 => nix build PASS x6; `nix run nixpkgs#nixfmt -- --check` => nixfmt PASS.
- Smokes on real store paths: helium/protonplus/zen print exact versions exit 0; opencode system-electron node-probe "node 24.18.1 electron-boot-ok" exit 0 + asar present; rootapp bwrap boot "Starting application" exit 0 under --privileged; mixtapes --help GApplication exit 0.
- Container teardown used plain path input (rm -rf .git) to dodge libgit2 "not owned by current user"; both systems evaluated by flake check.

## 4. Dependency deep audit (COMPLETE - tore every package apart by hand this run)

| package | upstream deps found | in nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.7.1 | main ELF readelf -d NEEDED=32 (glib/gobject/gio, nspr/nss family, atk+bridge+atspi, dbus, cups, expat, xcb+xkbcommon, asound, gbm, X11 family, cairo, pango, udev, glibc family) + bundle-wide unique 39 (+Qt5/Qt6 core/gui/widgets shims, libstdc++) via tar.xz teardown; interpreter patched to nix glibc; wrapper LD_LIBRARY_PATH carries resolution | runtimeDeps 45-dir lib.makeLibraryPath rpath covers every NEEDED incl qt5.qtbase/qt6.qtbase/stdenv.cc.cc.lib; buildInputs glib/gsettings-desktop-schemas/gtk3/adwaita-icon-theme; smoke exit 0 under nix loader | none | none | deps-verified |
| opencode-desktop v1.18.22 | deb control Depends: libgtk-3-0 libnotify4 libnss3 libxss1 libxtst6 xdg-utils libatspi2.0-0 libuuid1 libsecret-1-0; control Version=1.18.22 matches pin; app.asar 142 MB present | electron_42 closure provides full deb Depends set; stdenv.cc.cc.lib buildInputs; autoPatchelfHook + musl ignore list; makeBinaryWrapper -> system electron 42.9.3 (live node-probe confirms boot) | none | none | deps-verified |
| protonplus 0.6.4 | AppImage extract (clean dir): bin/protonplus static bun runtime; sharun bundle self-contained; hooks 01-check-ca-certs + 01-path-mapping-hardcoded load-bearing (+self-updater skipped, store ro); X-AppImage-Version=0.6.4 | full bundle installed (bin/shared/lib/share/etc), wrapper sources 01-* hooks then execs renamed runtime; `version` -> "ProtonPlus 0.6.4" exit 0 | none | none | deps-verified |
| rootapp 0.9.127 | AppImage extract: usr/bin/Root dynamic .NET ELF, NEEDED = glibc family only (ld-linux/libc/libdl/libgcc_s/libm/libpthread/librt/libstdc++); X-AppImage-Version=0.9.127 authoritative; artifacts unchanged vs pins (byte-identical hashes) | appimageTools.wrapType2 FHS bwrap env supplies glibc-family + libstdc++; extract contents for icon/desktop; unfree allowed; bwrap boot "Starting application" exit 0 | none | none | deps-verified |
| zen-browser 1.21.15b | tar teardown: zen NEEDED=7 (glibc family + libstdc++) + libxul.so NEEDED=49 unique (gtk3/gdk/pixbuf stack, X11-xcb, dbus, alsa, atk/cairo/pango, bundled libmoz*/nss/nspr intact); application.ini Version=1.21.15b BuildID 20260818101929 | buildInputs gtk3/adwaita-icon-theme/alsa-lib/dbus-glib/libXtst/ffmpeg_8; runtimeDependencies curl/libva/pciutils/libGL; appendRunpaths libGL/pipewire; gappsWrapperArgs ffmpeg LD_LIBRARY_PATH; `--version` -> "Mozilla Zen 1.21.15b" exit 0 | none | none | deps-verified |
| mixtapes 2026-26-05.0 | source build; third-party imports = gi/Gtk4/Adw, PIL, numpy, mutagen, requests, ytmusicapi, yt_dlp(+yt_dlp_ejs via js_runtimes), mprisify (hard imports); pystray Windows-only (guarded, correctly omitted); downloads.py needs ffmpeg (FFmpegExtractAudio) + node (js_runtimes) on PATH | pythonEnv (pygobject3/pillow/numpy/ytmusicapi/yt-dlp/yt-dlp-ejs/requests/urllib3/mutagen/pydbus/mprisify) + buildInputs gtk4/libadwaita/webkitgtk_6_0/gobject-introspection/gst_all_1.*; makeWrapper --prefix PATH ffmpeg+nodejs; `--help` GApplication exit 0; rustypipe-botguard (yt-dlp-get-pot*) NOT in nixpkgs, degrades gracefully - not invented | none | none | deps-verified |

- Official packaging check (2026): helium/zen/rootapp/protonplus/opencode-desktop absent or trailing in nixpkgs; mixtapes absent from nixpkgs (metainfo-only release) => no newer official default.nix to align; upstream artifact/source extraction used as source of truth.
- Meta audit: description one sentence capitalized no period x6, license SPDX correct (helium gpl3Plus+bsd3, opencode mit, protonplus gpl3Only, zen mpl20, rootapp unfree, mixtapes gpl3Plus), sourceProvenance set (binaryNativeCode x5, fromSource mixtapes), mainProgram exact x6, platforms linux x6, maintainers Ackerman-00 x6, hashes SRI sha256-...

## 5. Meta/excellence audit (COMPLETE)

- Workflows YAML valid (3 files: opencode-schedule.yml [opencode, cleanup], opencode.yml, update.yml [update-flake]); README `nix run` paths match tested bins.
- teardown-report.md receipt refreshed by post-fix sweep (committed).
- flake.lock within freshness budget (~3d old).
- All pkgs/*.nix have pname; nixfmt-clean per container check.

## 6. Known gaps / notes

- aarch64 BUILD not possible on x86_64 runner (named gap) - covered by eval (`nix flake check --all-systems`) + sweep cross-arch hash verification (the exact check fetchurl performs at build time).
- Full GUI launch headless not verifiable - named gap; version/boot/node probes are max evidence.
- Container teardown used plain path input (rm -rf .git) to avoid libgit2 "not owned by current user"; both systems still evaluated by flake check.

## 7. Final state (this run)

- Base HEAD: 57b499f. Changes this run: pkgs/helium.nix (aarch64 hash fix), tools/teardown-sweep.py (hard-fail verdict fix), teardown-report.md (receipt refresh), .opencode-relay.md (this file).
- Issues open: checked post-push. PRs open: 0.
- Relay status: complete. run_id: 32794792298.
