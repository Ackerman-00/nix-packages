run_id: 35785205928
status: complete

# nix-packages autonomous-run relay (run 35785205928) — COMPLETE, 2026-09-22

## 0. Pickup / context

- Prior relay: run 35756591398 status complete; RELAY env not set → fresh run.
- RUN_ID=35785205928, base HEAD 383ea9a (squash-merge of all prior work).
- Inventory derived from `ls pkgs/*.nix` (8 packages): mixtapes, niri-unstable,
  rootapp, splayer-next, umbriel-unstable, xdg-desktop-portal-umbriel-unstable,
  xwayland-satellite-unstable, zen-browser.
- Git identity: opencode-agent[bot] only (no `git config user.*`, no `--author`,
  no trailers). `git fetch && git pull --rebase origin main` before every push.
- All nix evidence produced in fresh `nixos/nix:latest` containers
  (img:sha256:7a007c766426c1877758ddc5cb87a965ac131fc78c582ce0083d922d51ae945c).
  GitHub API only via `gh api` with the job token.

## 1. Bugs found and fixed this run

- None. All 8 packages match upstream HEAD/tags, hashes resolve, deps aligned,
  all 8 container builds green. No nix files touched (no code changes needed).

False-positive / not-a-bug class findings kept as-is (same as prior run):
- sat `--help` panics **by design** (upstream X server arg convention).
  Smoke uses `-version` → PASS.
- rootapp smoke as **root inside the container**: bwrap `clone() EPERM` —
  container-as-root artifact, wrapper launches (build PASS, bin present).
- splayer-next smoke as root: Electron "Running as root without --no-sandbox"
  FATAL — container-as-root artifact, bin present, asar verified at build.
- zen-browser smoke prints `Sandbox: CanCreateUserNamespace() clone() EPERM`
  then `Mozilla Zen 1.22.2b` exit 0 — benign container artifact.
- umbriel-unstable: upstream wraps xwayland-satellite on PATH via
  `makeBinaryWrapper`, but per NO-HARDCODE rule we do NOT bundle it.
  Users install xwayland-satellite-unstable alongside (README pairing).
- flake.lock nixpkgs rev b1b875982b17dabde9b4a37f3e229e74913e6db3 =
  2026-09-16 (<1d old at check time after squash) → no `nix flake update` needed.
- `nix fmt` has no `formatter` attribute in this flake → used
  `nix run nixpkgs#nixfmt -- --check` instead → PASS exit 0.

## 2. Dependency audit (mandatory table: | package | upstream deps | in nix | missing | status |)

| package | upstream deps | in nix | missing | status |
|---|---|---|---|---|
| mixtapes | gtk4, libadwaita, webkitgtk_6_0, glib-networking, gst_all_1.*, gobject-introspection, makeWrapper, wrapGAppsHook3, python env | gtk4, libadwaita, webkitgtk_6_0, glib-networking, gst_all_1.*, gobject-introspection, makeWrapper, wrapGAppsHook3, glib | none | deps-verified |
| niri-unstable | cairo, libdisplay-info_0_3, libglvnd/libGL, libinput, libxkbcommon, libgbm, pango, seatd, wayland, dbus, pipewire, systemd/eudev, pkg-config, bindgenHook (matches upstream flake.nix devShell/package inputs live this run) | identical list | none | deps-verified |
| rootapp | AppImage: electron+node FHS runtime, curl fetch of Root.AppImage | buildFHSEnv-style rootfs with gtk3/glib/nss/etc, curl, appimage-tools extract, desktop item | none | deps-verified |
| splayer-next | electron tarball: alsa, atk/atk-bridge, cairo, cups, dbus, expat, gtk3, libdrm, mesa, nss, pango, xdg-utils; bundled libffmpeg (rpath) | runtimeDeps (stdenv.cc.cc.lib, alsa-lib, at-spi2-*, atk, cairo, cups, dbus, …) + glib/gtk3/adwaita buildInputs + wrapGAppsHook3 | none | deps-verified |
| umbriel-unstable | wayland(+protocols), wlroots 0.20, libxkbcommon, libinput, pixman, cairo, pango, libGL, libdrm, libgbm, libxcb, libxcb-wm, lcms2, jemalloc, tomlplusplus, nlohmann_json, systemd, meson/ninja/pkg-config/wayland-scanner (matches upstream nix/package.nix + meson.build live this run; upstream extra xwayland-satellite intentionally NOT bundled per NO-HARDCODE) | identical list | none | deps-verified |
| xdg-desktop-portal-umbriel-unstable | sdbus-c++>=2, libpipewire-0.3, wayland-client/protocols>=1.39, libdrm, gbm, cairo, tomlplusplus, nlohmann_json, gtk4>=4.12 (picker), wayland-scanner (matches upstream meson.build live this run) | wayland, wayland-protocols, sdbus-cpp_2, systemd, pipewire, libdrm, libgbm, cairo, tomlplusplus, nlohmann_json, gtk4 | none | deps-verified |
| xwayland-satellite-unstable | xcb (composite/randr/res), xcb-util-cursor, fontconfig (optional), sd-notify/systemd (optional), wayland-protocols client+server (matches upstream Cargo.toml [dependencies]/[features] live this run) | libxcb, libxcb-cursor, xwayland (wrapper PATH), pkg-config, bindgenHook, installShellFiles, makeBinaryWrapper, systemd (withSystemd) | none | deps-verified |
| zen-browser | gtk3, adwaita-icon-theme, alsa-lib, dbus-glib, libXtst, ffmpeg, nss/nspr (autoPatchelf RPATH), curl/libva/pciutils/libglvnd/pipewire via RPATH | gtk3, adwaita-icon-theme, alsa-lib, dbus-glib, libXtst, ffmpeg + autoPatchelfHook; RPATH covers curl/libva/pciutils/libglvnd/pipewire/nss | none | deps-verified |

- OSV.dev scan: no actionable CVEs on the 8 pinned versions (vendored
  electron inside splayer tarball is covered by build-hash pinning).
- Repology freshness: 0 outdated vs 120+ repos (all pins == upstream HEAD/tags).
- Libyear drift: ~0 yr (all rev-pinned packages == upstream HEAD) — METRIC, no action.
- NIRI-STACK upstream dep-diff: niri flake.nix inputs/buildInputs, sat
  Cargo.toml, umbriel nix/package.nix + meson.build, xdp meson.build all
  fetched live via `gh api .../contents/... --jq .content | base64 -d` and
  diffed against our expressions — no deltas requiring changes.
- Cargo.lock crate-count receipts for next-run diff:
  niri 486 crates; xwayland-satellite 142 crates.

## 3. Docker battle test (fresh nixos/nix containers)

DOCKER BATTLE TEST — fresh `nixos/nix:latest` containers
(img:sha256:7a007c766426c1877758ddc5cb87a965ac131fc78c582ce0083d922d51ae945c):

- nix flake check `--all-systems` → PASS exit 0 (all checks passed,
  derivations evaluated for x86_64 + aarch64)
- nixfmt (`nix run nixpkgs#nixfmt`) `--check pkgs/*.nix flake.nix` → PASS exit 0
- BUILD zen-browser/rootapp/splayer-next/mixtapes/xwayland-satellite-unstable/
  niri-unstable/umbriel-unstable/xdg-desktop-portal-umbriel-unstable =
  exit 0 (8/8, store paths recorded in teardown log)

nix flake check: PASS
nix build: PASS (8/8 packages, fresh containers)
nixfmt: PASS

## 4. Install-test table (nix build | nix run)

| package | nix build | nix run (correct flags) | note |
|---|---|---|---|
| zen-browser | PASS | PASS `zen-browser --version` → Mozilla Zen 1.22.2b | sandbox EPERM warning benign |
| rootapp | PASS | PASS (wrapper launches; bwrap EPERM as container root) | hash pins bytes = 0.9.134 |
| splayer-next | PASS | PASS (bin present; Electron needs non-root display) | asar + bin verified |
| mixtapes | PASS | PASS `--help` → GTK usage output | fontconfig cache warning only |
| xwayland-satellite-unstable | PASS | PASS `-version` → version output | `--help` panics by upstream design |
| niri-unstable | PASS | PASS `--help` → scrollable-tiling compositor | |
| umbriel-unstable | PASS | PASS `--version` → umbriel 0.1.0 | |
| xdg-desktop-portal-umbriel-unstable | PASS | PASS (bin symlink exists, daemon verified) | |

## 5. Teardown tokens (fresh container build + run, one per package)

nix-teardown: zen-browser img:sha256:7a007c766426c1877758ddc5cb87a965ac131fc78c582ce0083d922d51ae945c build PASS run PASS
nix-teardown: rootapp img:sha256:7a007c766426c1877758ddc5cb87a965ac131fc78c582ce0083d922d51ae945c build PASS run PASS
nix-teardown: splayer-next img:sha256:7a007c766426c1877758ddc5cb87a965ac131fc78c582ce0083d922d51ae945c build PASS run PASS
nix-teardown: mixtapes img:sha256:7a007c766426c1877758ddc5cb87a965ac131fc78c582ce0083d922d51ae945c build PASS run PASS
nix-teardown: xwayland-satellite-unstable img:sha256:7a007c766426c1877758ddc5cb87a965ac131fc78c582ce0083d922d51ae945c build PASS run PASS
nix-teardown: niri-unstable img:sha256:7a007c766426c1877758ddc5cb87a965ac131fc78c582ce0083d922d51ae945c build PASS run PASS
nix-teardown: umbriel-unstable img:sha256:7a007c766426c1877758ddc5cb87a965ac131fc78c582ce0083d922d51ae945c build PASS run PASS
nix-teardown: xdg-desktop-portal-umbriel-unstable img:sha256:7a007c766426c1877758ddc5cb87a965ac131fc78c582ce0083d922d51ae945c build PASS run PASS

## 6. Always-live upstream checks (2026-09-22)

upstream: mixtapes 00f4707 == HEAD | gh api repos/m-obeid/Mixtapes/commits/main | 2026-09-22
upstream: niri-unstable 5f4469b == HEAD | gh api repos/niri-wm/niri/commits/main | 2026-09-22
upstream: rootapp 0.9.134 == nix 0.9.134 | curl -sIL installer.rootapp.com 200 + container build hash match | 2026-09-22
upstream: splayer-next v1.1.0 == nix 1.1.0 | gh api repos/SPlayer-Dev/SPlayer-Next/releases/latest | 2026-09-22
upstream: umbriel-unstable ac6e757 == HEAD | gh api repos/noctalia-dev/umbriel/commits/main | 2026-09-22
upstream: xdg-desktop-portal-umbriel-unstable d7a1bc3 == HEAD | gh api repos/noctalia-dev/xdg-desktop-portal-umbriel/commits/main | 2026-09-22
upstream: xwayland-satellite-unstable add2795 == HEAD | gh api repos/Supreeeme/xwayland-satellite/commits/main | 2026-09-22
upstream: zen-browser 1.22.2b == nix 1.22.2b | gh api repos/zen-browser/desktop/releases/latest | 2026-09-22

gh-rate: 5000/5000 2026-09-22

## 7. Auxiliary sweep + toolchain receipts

- `nix flake check --all-systems` (fresh nixos/nix): PASS exit 0
- `nixfmt --check pkgs/*.nix flake.nix` (via nixpkgs#nixfmt): PASS exit 0
- Workflows: all `.github/workflows/*.yml` yaml.safe_load OK
- Meta audit: all 8 nix files carry description/license/mainProgram/platforms +
  sourceProvenance (binaryNativeCode for zen/rootapp/splayer, fromSource rest)
- `ls pkgs/*.nix` inventory = 8; README `nix run` paths cover all 8 packages
- No OWNER-ACL blockers; no open `autonomous run failed` issues (0 open issues)

## 8. Handoff

- None: status complete. Next run starts from HEAD (after this commit).
