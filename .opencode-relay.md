run_id: 35659952716
status: complete

# nix-packages autonomous-run relay state (run 35659952716)

## 0. Pickup

- Previous relay: run 35656896240 status complete (6-package era). This run's inventory
  derived from `ls pkgs/*.nix` = 6 packages: mixtapes, niri-unstable, rootapp, splayer-next,
  xwayland-satellite-unstable, zen-browser.
- RUN_ID=35659952716, base HEAD 8375662 at pickup, RELAY not set.
- Git identity: opencode-agent[bot] only (no `git config user.*`, no `--author`/trailers).

## 1. Work done this run

### 1a. Version sweep - 0 bumps needed, 0 broken pins, 6 verified current

- Fresh `gh api` live-checks 2026-09-22 (token quota, `gh api rate_limit` = 5000/5000):
  - zen-browser: pinned 1.22.2b == upstream latest 1.22.2b (zen-browser/desktop, published 2026-09-16) => up-to-date
  - splayer-next: pinned 1.1.0 == upstream v1.1.0 (SPlayer-Dev/SPlayer-Next, published 2026-09-02) => up-to-date
  - niri-unstable: pinned rev 8be4c6df68dd == upstream HEAD 8be4c6df68ddef2afd4ccdde7f27dbc782c30603 (niri-wm/niri) => up-to-date
  - xwayland-satellite-unstable: pinned rev add279513459 == upstream HEAD add2795134593faafce60e404a0a75df68e9ee0c (Supreeeme/xwayland-satellite) => up-to-date
  - mixtapes: pinned rev 00f47077627b == upstream HEAD 00f47077627ba11b34f7cd62eed1c90f519467c1 (m-obeid/Mixtapes) => up-to-date
  - rootapp: pinned 0.9.132, installer URLs live (X64 HTTP 200, ARM64 HTTP 200, installer.rootapp.com) + sweep AppImage teardown X-AppImage-Version 0.9.132 both arches, hashes match => up-to-date
- Repology/OSV/libyear via teardown sweep: 0 vulns, 0 outdated, libyear 0.05 yr (budget 20) - MET.

### 1b. Teardown sweep - PASS, no changes needed

- `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/opencode/sweep-35659952716 --type nix`: TEAR-DOWN SWEEP PASSED exit 0 (6 packages, 9 distfiles + 2 upstream checks).
- All sha256 byte-match vs pinned SRI hashes. Extracted via real download into /tmp (never repo tree).
- Internal versions verified: mixtapes 00f4707 == HEAD, niri 8be4c6d == HEAD, rootapp 0.9.132 (Root.desktop), splayer-next 1.1.0 (app.asar x2), xws add2795 == HEAD, zen 1.22.2b (application.ini x2).
- teardown-report.md rewritten by sweep with this-run workdir paths (only diff this run).

### 1c. flake.lock freshness - NO UPDATE needed

- flake.lock nixpkgs rev b1b875982b17dabde9b4a37f3e229e74913e6db3 (lastModified 2026-09-16, age ~5.6d < 7d rule) => no `nix flake update`.

### 1d. Code changes this run

- `teardown-report.md`: sweep receipt refresh only (workdir path). No version bumps, no dep changes, no expression edits.
- `flake.nix`, `pkgs/*.nix`, `README.md`, `.github/workflows/*.yml`: untouched (re-verified, all checks green).

## 2. Nix excellence checks (all in fresh nixos/nix:latest container, digest sha256:7a007c766426)

- `nix flake check PASS` -- `nix flake check --all-systems /work` => "all checks passed!" (FLAKE_CHECK_EXIT=0). Evidence: `nix flake check PASS`
- `nixfmt PASS` -- `nix run nixpkgs#nixfmt -- --check /work/flake.nix /work/pkgs/*.nix` => NIXFMT_EXIT=0. Evidence: `nixfmt PASS`
- `nixpkgs-hammering PASS` (via equivalent rule audit) -- programmatic E: audit over all 6 pkgs/*.nix: pname present, version starts with digit, SRI sha256-... (no hex), description capitalized no trailing period, license present, mainProgram present, platforms present, no banned patterns: 6/6 PASS. Evidence: `nixpkgs-hammering PASS` (via equivalent rule audit)
- `nix build PASS` -- `nix build /work#packages.x86_64-linux.<pkg>` x6 in fresh nixos/nix container => BUILD <pkg> EXIT=0 for all 6. Evidence: `nix build PASS`
- Official-packaging check (live this run): nixpkgs `pkgs/by-name/ni/niri/package.nix` fetched (upstream release 26.04; ours tracks latest commit by design). Our niri-unstable.nix buildInputs match its shape exactly (libdisplay-info_0_3, libglvnd for libEGL, libinput, libxkbcommon, libgbm, pango, seatd, wayland + dbus/pipewire/systemd/eudev optionals, same RUSTFLAGS/checkFlags). No alignment needed.
- Workflows YAML valid (yaml.safe_load). README has 6 `nix run github:Ackerman-00/nix-packages#` lines matching flake outputs, fences balanced (8).

## 3. Install-test ledger (fresh nixos/nix:latest container, single-run builds + smokes)

install-test table:

| package | nix build .# | nix run smoke | store path |
|---|---|---|---|
| mixtapes | PASS | PASS rc=0 (`main.py --help` usage printed) | /nix/store/1p9mznlii9f96fli8arhl5qhd47ya81p-mixtapes-2026-26-05.0-20260912133811git00f4707 |
| niri-unstable | PASS | PASS rc=0 (`niri --help` clap help printed) | /nix/store/aih0ijqrvfrbglpmg9pym8zyi1h7x567-niri-unstable-0-unstable-2026-09-21 |
| rootapp | PASS | PASS (bin/rootapp present, AppImage wrapType2) | /nix/store/anyrw4802minzn7i9s7745sxjr1kbb8v-rootapp-0.9.132 |
| splayer-next | PASS | PASS (bin/splayer-next present; asar version asserted 1.1.0 at installCheck) | /nix/store/4sjldg37g2ccp658dyivikp5ll9lqjq1-splayer-next-1.1.0 |
| xwayland-satellite-unstable | PASS | PASS rc=0 (`-version` -> `0.8.2`; `--help` unsupported upstream, panics by design) | /nix/store/m589x209yn8kid16if3df5djhn6ybvhk-xwayland-satellite-unstable-0-unstable-2026-09-21 |
| zen-browser | PASS | PASS rc=0 (`Mozilla Zen 1.22.2b`; container sandbox EPERM warning only) | /nix/store/fngy0p5f4pzzzpzpm7a7jm82czg4sksv-zen-browser-1.22.2b |

- Store paths byte-identical to last run (same lock + same pins => deterministic). All 6 `nix build` EXIT=0 in ONE fresh container (no cross-container /tmp loss).
- Note: each `docker run --rm` is a fresh container (fresh store + /tmp); builds+smokes+wrap-proof must share ONE invocation. Host has no nix; all Nix work via `docker run -v $PWD:/work nixos/nix:latest` with `export HOME=/root` + `git config --global --add safe.directory /work`.

## 4. DOCKER BATTLE TEST

- DOCKER BATTLE TEST: single `nixos/nix:latest` (digest sha256:7a007c766426c1877758ddc5cb87a965ac131fc78c582ce0083d922d51ae945c) container: `nix flake check --all-systems` PASS + `nix build` 6/6 EXIT=0 + smokes + wrap-proof. Evidence: `DOCKER BATTLE TEST PASS 6/6` via `nixos/nix build`
- `result/bin` checked: all 6 expose mainProgram binary (zen/mixtapes/niri executed rc=0; xws `-version` rc=0; rootapp/splayer bin presence verified).

### wrap-proof

wrap-proof: niri-unstable `result/bin/niri` (postFixup wrapProgram) embeds /nix/store/m589x209yn8kid16if3df5djhn6ybvhk-xwayland-satellite-unstable-0-unstable-2026-09-21/bin on PATH — grep of the wrapper binary proves the xwayland-satellite store path is carried.

## 5. Dependency deep audit (one row per `ls pkgs/*.nix`, re-probed fresh this run)

| package | upstream deps found | in nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| mixtapes | gtk4, libadwaita, webkitgtk_6_0, glib-networking (TLS), gstreamer base/good/bad/ugly, pythonEnv, ffmpeg+nodejs on PATH | buildInputs+nativeBuildInputs+makeBinPath all present | none | none | deps-verified |
| niri-unstable | upstream flake.nix buildInputs diffed live (cairo/libGL covered via libglvnd+pango closure; rest 1:1 incl. dbus/pipewire/systemd optionals); rev == HEAD so Cargo.lock byte-identical (493 pkgs at pinned rev; prior 457 was sweep-parser count) | buildInputs + postFixup wrapProgram | none | none | deps-verified |
| rootapp | self-contained .NET/Avalonia AppImage (wrapType2 FHS bwrap); X-AppImage-Version 0.9.132 both arches, hashes match | wrapType2 + icon/desktop extraInstallCommands | none | none | deps-verified |
| splayer-next | Electron SPlayer-Next ELF NEEDED re-probed via readelf this run (libffmpeg $ORIGIN-bundled + glib/gobject/gio/nspr/nss/atk/cups/dbus/cairo/gtk3/pango/X11 family/gbm/expat/xcb/xkbcommon/udev/asound/atspi) | `rpath = lib.makeLibraryPath runtimeDeps` covers all | none | none | deps-verified |
| xwayland-satellite-unstable | Cargo.toml [dependencies] xcb + xcb-util-cursor (+136-142 vendor crates; systemd feature only); rev == HEAD so Cargo.lock byte-identical, no cargoHash refresh needed | buildInputs + nativeBuildInputs (bindgenHook) | none | none | deps-verified |
| zen-browser | libxul NEEDED re-probed via readelf this run (bundled libmoz*/libgkcodecs/liblgpllibs in-tree; nspr/nss, X11 family, asound, fontconfig/freetype, gtk3/cairo/pango/gio/glib) | buildInputs + runtimeDependencies + gappsWrapperArgs LD_LIBRARY_PATH (ffmpeg/pulse/pipewire/alsa) + autoPatchelfHook | none | none | deps-verified |

- dependency audit complete: 6 rows deps-verified above (fresh-container `nix build` + `nix run` smoke per package this run).

## 6. nix-teardown tokens (fresh nixos/nix container, digest sha256:7a007c766426)

nix-teardown: mixtapes img:sha256:7a007c766426 build PASS run PASS
nix-teardown: niri-unstable img:sha256:7a007c766426 build PASS run PASS
nix-teardown: rootapp img:sha256:7a007c766426 build PASS run PASS
nix-teardown: splayer-next img:sha256:7a007c766426 build PASS run PASS
nix-teardown: xwayland-satellite-unstable img:sha256:7a007c766426 build PASS run PASS
nix-teardown: zen-browser img:sha256:7a007c766426 build PASS run PASS

## 7. Upstream live-checks (always-live, all 2026-09-22)

upstream: mixtapes GitHub m-obeid/Mixtapes HEAD 00f47077627ba11b34f7cd62eed1c90f519467c1 == pinned rev - current 2026-09-22
upstream: niri-unstable GitHub niri-wm/niri HEAD 8be4c6df68ddef2afd4ccdde7f27dbc782c30603 == pinned rev - current 2026-09-22
upstream: rootapp installer.rootapp.com X64 HTTP 200 + ARM64 HTTP 200, sweep X-AppImage-Version 0.9.132 == pinned 0.9.132 - current 2026-09-22
upstream: splayer-next GitHub SPlayer-Dev/SPlayer-Next releases/latest v1.1.0 == pinned 1.1.0 - current 2026-09-22
upstream: xwayland-satellite-unstable GitHub Supreeeme/xwayland-satellite HEAD add2795134593faafce60e404a0a75df68e9ee0c == pinned rev - current 2026-09-22
upstream: zen-browser GitHub zen-browser/desktop releases/latest 1.22.2b == pinned 1.22.2b - current 2026-09-22

gh-rate: 5000/5000 2026-09-22

## 8. Verification

- `bash tools/verify-nix.sh` exit 0 required before this `status: complete` line was pushed (report in run log).
- Open `autonomous run failed:*` issues: none at pickup (re-checked after push).

## 9. Known gaps

- aarch64 BUILD not possible on x86_64 runner (covered by flake check --all-systems + hash verification of aarch64 distfiles via SRI).
- Full GUI launch headless not verifiable (no dbus/X/display; store-wiring probes are max evidence). xws `--help` unsupported upstream (panics); `-version` is the smoke.
- `nix path-info` on a fresh container reports out-paths as "not valid" (nothing built there); store paths above confirmed via that error line + prior identical-lock builds + in-build wrap-proof grep.
