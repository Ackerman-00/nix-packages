# nix-packages autonomous-run relay state

status: complete
run_id: 31731650097

## 0. Pickup

- Previous run (31728470036) was cancelled before writing a relay; no unfinished relay handoff to continue.
- Started fresh on main @ ef8e1d6 (single squashed commit; later owner pushed 2e49769 docs).
- Nix upgraded on runner from 2.16.1 to 2.24.9 (pinned nixpkgs d407951 requires >= 2.18).

## 1. What the auto-updater did this run

- update.yml ("Packages Auto-Update") runs are all green (last 10+).
- Last run (31658331405) on 2026-08-13: no version changes for any package; committed nothing.
- All pinned versions verified current against upstream: helium 0.15.4.1, protonplus 0.6.4, opencode-desktop v1.18.18, rootapp hash unchanged. NO churn commits made.

## 2. Fixes this run

- **protonplus** (commit b32e89c): installPhase dropped the AppImage's `share/` data directory (glib schemas incl. com.vysp3r.ProtonPlus.gschema.xml, 25 locales, mime, icons, X11/alsa config). Added `cp -r squashfs-root/share $out/`. Before: `GSettings is not initialized` warning + missing translations; After: share/ present (25 locales), warning gone, `protonplus version` -> "ProtonPlus 0.6.4" in clean container.

## 3. Version accuracy sweep

| package | packaged version | upstream latest | status |
|---|---|---|---|
| helium | 0.15.4.1 | 0.15.4.1 (gh releases latest tag) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (tag, strip v) | up-to-date |
| opencode-desktop | v1.18.18 | v1.18.18 (gh releases latest tag) | up-to-date |
| rootapp | latest (fixed URL) | hash unchanged after re-prefetch; real version from artifact = 0.9.126 | up-to-date |

- All hashes re-prefetched with `--refresh` and match the pinned SRI values byte-for-byte.
- All source URLs return HTTP 200 (curl -sI).

## 4. Build/install verification ledger

| package | build (x86_64) | aarch64 eval | binary smoke | status |
|---|---|---|---|---|
| helium | PASS (fresh --refresh) | PASS (eval .name) | `--version` -> Helium 0.15.4.1 | pass |
| protonplus | PASS (fresh --refresh) | PASS (eval .name) | `version` -> ProtonPlus 0.6.4 | pass |
| rootapp | PASS (fresh --refresh, unfree allowed) | n/a (x86_64 only) | starts (bwrap, needs userns) | pass |
| opencode-desktop | PASS (fresh --refresh) | n/a (x86_64 only) | runs under xvfb (GUI, no display = gap) | pass |

- `nix flake check --all-systems` green on final main (both systems evaluate; aarch64 BUILD impossible on x86_64 runner - named gap).
- sourceProvenance = [binaryNativeCode] on all 4 packages (nix eval verified).

## 5. Dependency audit

| package | upstream deps found | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium | glib,gobject,gio,nspr,nss,atk,atk-bridge,dbus,cups,expat,xcb,xkbcommon,asound,gbm,X11,Xext,cairo,pango,udev,Xcomposite,Xdamage,Xfixes,Xrandr,atspi | all in runtimeDeps | none | none | deps-verified (high confidence - readelf NEEDED all covered; container smoke passes) |
| protonplus | statically-linked bun binary + bundled lib/shared/share | binary bundles everything | share/ data dir was dropped, ADDED | none | deps-fixed |
| opencode-desktop | electron 42.x + node .node sidecars | electron_42 via callPackage (42.5.1 from locked nixpkgs; deb bundled 42.3.3, same major) | none (glibc variants resolve; musl variants ignored via autoPatchelfIgnoreMissingDeps - never loaded on glibc) | none | deps-verified (high confidence) |
| rootapp | .NET + bundled usr/bin tree (libSkiaSharp, DotNetBrowser, etc.) | bwrap FHS sandbox (wrapType2) | none | none | deps-verified (medium confidence - AppImage FHS self-contained) |

## 6. Container runtime test ledger

| package | nix build | container ldd | container smoke | status |
|---|---|---|---|---|
| helium | PASS | interpreter = nix ld-linux; libs resolve under wrapper LD_LIBRARY_PATH (raw ldd "not found" is container glibc-2.39-vs-nix-2.42 artifact) | `--version` -> "Helium 0.15.4.1 (Chromium 151.0.7922.137)", exit 0 | verified |
| protonplus | PASS | statically linked (0 not found) | `version` -> "ProtonPlus 0.6.4", exit 0 | verified |
| opencode-desktop | PASS | electron ldd 0 not found | runs under xvfb (stays alive; GUI needs display = named gap) | verified (GUI launch gap) |
| rootapp | PASS | FHS sandbox resolves | starts ("Starting application"; bwrap needs user namespaces; docker blocks userns -> used --privileged to simulate real machine) | verified (needs userns) |

- Named gaps: aarch64 BUILD impossible on x86_64 runner (eval only); GUI apps full window rendering needs a display.

## 7. Issues / PRs

- No open issues. No open PRs. Nothing to triage this run.

## 8. Final state

- main @ 2e49769 + b32e89c (protonplus fix). Working tree clean.
- README.md package list matches flake.nix (rootapp, opencode-desktop, helium, protonplus).
- Nix upgraded to 2.24.9 on runner only (not a repo change).
