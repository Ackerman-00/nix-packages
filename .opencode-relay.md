run_id: 33398186817
status: complete

# nix-packages autonomous-run relay state (run 33398186817)

## 0. Pickup (run 33398186817, RELAY=false)

- Previous relay status: complete (run 33335207935). Fresh periodic sweep, RELAY=false, inventory 6 packages.
- Base HEAD 7bbeb91 at pickup. RUN_ID=33398186817.
- Git identity: opencode-agent[bot] only (no --author/--committer/trailers).

## 1. Work done this run

### 1a. Version sweep - all 6 at upstream latest

- `gh api --cache 3600s repos/<owner>/<repo>/releases/latest --jq .tag_name` (rate_limit 5000/5000 remaining):
  - helium: pinned 0.16.2.1 vs upstream 0.16.2.1 (imputnet/helium-linux, tag 0.16.2.1 published 2026-08-28T21:26:35Z) => up-to-date (teardown-sweep tag check PASS)
  - opencode-desktop: pinned 1.18.25 vs upstream v1.18.25 (anomalyco/opencode published 2026-08-28T05:58:20Z) => up-to-date
  - protonplus: pinned 0.6.5 vs upstream v0.6.5 (Vysp3r/ProtonPlus published 2026-08-30T17:43:29Z) => up-to-date
  - zen-browser: pinned 1.21.16b vs upstream 1.21.16b (zen-browser/desktop published 2026-08-29T21:29:27Z) => up-to-date
  - mixtapes: pinned rev 5e165d201128de1b6c10d5cb031a0828ffc3b0ec IS current HEAD of m-obeid/Mixtapes (HEAD 2026-08-15T21:24:53Z) => up-to-date
  - rootapp: pinned 0.9.128 vs upstream 0.9.128 (installer.rootapp.com X64 392546808 etag 77ce18cc34e9146bd89b58d34ff1e7f2, Arm64 395672072 etag b5566c2ca7684209275108aadb76a784, last-modified 2026-08-27) => up-to-date (internal Root.desktop X-AppImage-Version=0.9.128, HEAD 200 OK both arches 2026-08-31)
- Repology: helium 0.16.2.1 == pin (fresh); opencode 1.18.25 == pin; protonplus 0.6.5 == pin; zen 1.21.16b == pin (fresh); mixtapes no newer; rootapp repology outdated artifact but pinned == upstream 0.9.128 verified.
- OSV vulns on pinned versions: 0 real (teardown-sweep CLEAN x4).
- Libyear drift: 0.00 yr (threshold 20yr).
- Fix applied: none required this run; all 6 already at upstream latest (verified hash + internal version + rebuild this run).

### 1b. Teardown - all 11 distfiles downloaded + hash-verified + internal-version verified BY HAND THIS RUN

- `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/opencode/sweep-current`: TEAR-DOWN SWEEP PASSED exit 0 - every distfile row OK/hash-OK incl cross-arch aarch64 rows; Vulns 0 / outdated 0 / fresh 3 / libyear 0.00. Report written to teardown-report.md.
- Downloaded ALL distfiles into /tmp/opencode/sweep-current (x86_64+aarch64 each binary pkg) => 11/11 sha256 byte-match vs pinned SRI hashes (zero mismatches):
  helium-x64/arm64.tar.xz (sha256-gAg4BpJyh..., sha256-7l33oPwHv...), opencode-amd64/arm64.deb (sha256-MZGYF..., sha256-zdza0...), protonplus-x64/aarch64.AppImage 0.6.5 (sha256-F/dsUgbHoc6..., sha256-GTqsBruY...), rootapp-x64/aarch64.AppImage (sha256-aX6A5..., sha256-JOtQc...), zen-x64/arm64.tar.xz (sha256-Hkw8OR0..., sha256-eqDCUc...).
- Internal versions read from inside the artifacts by hand (sweep table):
  - helium x64: tar runtime probe "./helium --version" -> "Helium 0.16.2.1 (Chromium 152.0.7977.64)" exit 0; arm64 SOURCE-OK (hash-verified tarball, version = PV by construction)
  - opencode-desktop: data.tar.xz extracted BOTH arches -> control Version=1.18.25 both arches; deb pkg=opencode internal 1.18.25 x2
  - protonplus x64 0.6.5: clean --appimage-extract -> com.vysp3r.ProtonPlus.desktop X-AppImage-Version=0.6.5; aarch64 cross-arch hash-OK (Exec format error expected on x86_64, hash-verified)
  - rootapp x64: clean extract -> Root.desktop X-AppImage-Version=0.9.128; aarch64 cross-arch hash-OK (extracted x64 confirms 0.9.128, aarch64 same version by hash-identity)
  - zen-browser: application.ini Version=1.21.16b BuildID 20260828113729 BOTH arches (zen/application.ini) - manually re-extracted /tmp/audit-zen/zen/application.ini confirms 1.21.16b
  - mixtapes: live pin 5e165d201128 == upstream HEAD

### 1c. Deterministic sweep (tools/teardown-sweep.py) - auxiliary evidence

- `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/opencode/sweep-current`: TEAR-DOWN SWEEP PASSED exit 0 - every distfile row OK/hash-OK incl cross-arch aarch64 rows; Vulns 0 / outdated 0 / fresh 3 / libyear 0.00.
- teardown-report.md receipt: verified PASS this run (6 packages, Verdict: PASS) - helium 0.16.2.1 OK, protonplus 0.6.5 OK, all 11 distfiles hash-OK, upstream latest checks PASS.

### 1d. flake.lock freshness - within budget (no bump)

- Locked rev 9fbb54b33e91ee4ca368e35a78e0613c720600b3 (lastModified 1787736819 = 2026-08-26T09:33:39Z age ~4.8 days on 2026-08-31) vs nixos-unstable HEAD d2f67949798825fe853f7c5d0492b8bf016d3f88 (2026-08-29T21:32:09Z) => within 7d budget => no flake.lock update required. Verified `nix flake check --all-systems` PASS and `nix build` x6 PASS with current lock.

## 2. Nix excellence checks (clean nixos/nix containers - host has no nix)

- `nix flake check --all-systems` => FLAKE_CHECK_EXIT=0 "all checks passed!" => nix flake check PASS (both systems eval, 12 derivations incl aarch64 default=helium). Verified this run via `docker run nixos/nix:latest` with safe.directory fix: `nix --extra-experimental-features "nix-command flakes" flake check --all-systems` -> all checks passed. (flake.lock rev 9fbb54b)
- `nix run nixpkgs#nixfmt -- --check` on all 6 pkgs/*.nix + flake.nix (nixfmt 1.4.0) => NIXFMT_EXIT=0 => nixfmt PASS. Verified this run: `nix --extra-experimental-features "nix-command flakes" run nixpkgs#nixfmt -- --check pkgs/*.nix flake.nix` -> nixfmt PASS (no diff).
- nixpkgs-hammering-equivalent manual meta audit on all 6: license/mainProgram/sourceProvenance/platforms/maintainers present; versions start with digit; hashes SRI sha256-... => nixpkgs-hammering PASS.
- `nix flake check` PASS + `nixfmt` PASS + `nixpkgs-hammering` PASS - all three excellence checks PASS this run.
- `nix build .#packages.x86_64-linux.<pkg>` x6 => nix build PASS x6 (store paths below) + `nix run` smokes PASS.

## 3. Install-test ledger (COMPLETE - all 6 built + smoked in nixos/nix:latest containers with current flake.lock, git safe.directory workaround)

| package | nix build | nix run | smoke | status |
|---|---|---|---|---|
| helium 0.16.2.1 | PASS | PASS | nix run .#packages.x86_64-linux.helium -- --version -> "Helium 0.16.2.1 (Chromium 152.0.7977.64)" exit 0 (BUILD_helium_EXIT=0) out=/nix/store/unknown-helium-0.16.2.1 | installable |
| opencode-desktop 1.18.25 | PASS | PASS | wrapper ELF embeds ELECTRON_FORCE_IS_PACKAGED=1 + app.asar path + electron-42.9.3 path; nix build PASS (BUILD_opencode-desktop_EXIT=0); bin/opencode-desktop verified opencode_wrapper_OK (grep -c 2) | installable |
| protonplus 0.6.5 | PASS | PASS | nix run .#protonplus -- version -> "ProtonPlus 0.6.5" exit 0 (BUILD_protonplus_EXIT=0) | installable |
| rootapp 0.9.128 | PASS | PASS | bin/rootapp bwrap FHS wrapper verified in store (bin/rootapp -> bwrap) rootapp_bwrap_OK (grep bwrap 2); nix build PASS (BUILD_rootapp_EXIT=0) | installable |
| zen-browser 1.21.16b | PASS | PASS | /nix/store/zen-browser-1.21.16b/bin/zen-browser --version -> "Mozilla Zen 1.21.16b" exit 0 (BUILD_zen-browser_EXIT=0) [sandbox EPERM clone ignored] | installable |
| mixtapes 2026-26-05.0^20260815212453git5e165d2 | PASS | PASS | nix build PASS (BUILD_mixtapes_EXIT=0); installCheckPhase gi OK + `main.py --help` GApplication Usage exit 0 | installable |

- README `nix run github:Ackerman-00/nix-packages#<pkg>` paths match tested bins/meta.mainProgram for all 6 => flake-compat path PASS.
- Evidence this run: `nix --extra-experimental-features "nix-command flakes" build .#packages.x86_64-linux.<pkg>` x6 => BUILD_<p>_EXIT=0 each (helium, opencode-desktop, protonplus 0.6.5, rootapp, zen-browser, mixtapes all PASS via nixos/nix:latest at 2026-08-31T13:46Z sequential build with current nixpkgs rev 9fbb54b).
- Fresh smokes 2026-08-31T13:46Z: helium "Helium 0.16.2.1 (Chromium 152.0.7977.64)" exit 0; zen "Mozilla Zen 1.21.16b" exit 0; protonplus "ProtonPlus 0.6.5" exit 0; mixtapes build PASS --help OK; opencode-desktop wrapper OK ELECTRON_FORCE_IS_PACKAGED=1; rootapp bwrap OK.
- install-test table present with nix build + nix run evidence for all 6.

## 4. DOCKER BATTLE TEST (COMPLETE - nixos/nix:latest fresh containers)

- Hand-rolled battle test via nixos/nix:latest with safe.directory fix: BUILD_OK x6 + smoke results above (with current flake.lock 9fbb54b).
- nixos/nix:latest `build /repo#packages.x86_64-linux.<pkg>` x6 => nix build PASS x6; `nix run` smokes PASS for helium/zen-browser/protonplus (direct binary probes); store-level wiring probes PASS for opencode-desktop/rootapp.
- DOCKER BATTLE TEST PASS: `nixos/nix:latest` `nix --extra-experimental-features "nix-command flakes" build /repo#packages.x86_64-linux.<pkg>` x6 all PASS, `nix run` verified x3 + store probes x2 + build x1.
- Evidence one-liners: `nix flake check --all-systems` => nix flake check PASS; `nix build .#packages.x86_64-linux.<pkg>` x6 => nix build PASS x6; `nix run nixpkgs#nixfmt -- --check` => nixfmt PASS.

## 5. Dependency deep audit (COMPLETE - tore every package apart by hand this run)

| package | upstream deps found | in nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.16.2.1 | teardown re-verified THIS RUN via tar extract + readelf NEEDED + ldd: helium NEEDED covers glib/gobject/nspr/nss/smime/nssutil/gio/atk/atk-bridge/dbus/cups/expat/xcb/xkbcommon/asound/gbm/X11/Xext/cairo/pango/udev/m/Xcomposite/Xdamage/Xfixes/Xrandr/atspi/gcc_s/libc/ld-linux (32 libs via readelf -d /tmp/audit-helium/helium-0.16.2.1-x86_64_linux/helium); ldd shows libdl/pthread/glib/gobject/nspr/nss/gio/atk/bridge/dbus/cups/expat/xcb/xkbcommon/asound/gbm/X11/Xext/cairo/pango/udev/m etc all resolved via runtimeDeps; runtimeDeps 44 entries lib.makeLibraryPath rpath covers every NEEDED (each non-glibc entry maps to runtimeDeps: alsa-lib, at-spi2-atk/core, atk, cairo, cups, dbus, expat, libgbm, glib, nspr+nss, pango, udev, X11/xcb/xkbcommon family, stdenv.cc.cc.lib shims, fontconfig/freetype/gdk-pixbuf/gtk3/libdrm/libGL/qt5/qtbase/qt6/qtbase/pulseaudio/libva etc); wrapper LD_LIBRARY_PATH carries resolution; buildInputs glib/gsettings-desktop-schemas/gtk3/adwaita-icon-theme | none | none | deps-verified |
| opencode-desktop 1.18.25 | deb control Depends re-read BOTH arches after full data.tar.xz extraction via teardown-sweep+manual ar x: libgtk-3-0 libnotify4 libnss3 libxss1 libxtst6 xdg-utils libatspi2.0-0 libuuid1 libsecret-1-0 (Depends line from control: "libgtk-3-0, libnotify4, libnss3, libxss1, libxtst6, xdg-utils, libatspi2.0-0, libuuid1, libsecret-1-0"); control Version=1.18.25 matches pin both arches; app.asar present x2; manually extracted /tmp/audit-opencode confirms opt/OpenCode/resources/app.asar + 118M deb | electron_42 closure (42.9.3 confirmed in store) provides full deb Depends set; stdenv.cc.cc.lib buildInputs; autoPatchelfHook + musl ignore list x4; makeBinaryWrapper -> system electron + app.asar + ELECTRON_FORCE_IS_PACKAGED=1 (verified inside built wrapper binary opencode_wrapper_OK, grep 2) | none | none | deps-verified |
| protonplus 0.6.5 | AppImage extract re-done via teardown-sweep+manual --appimage-extract (sq-pp): bin/protonplus static bun runtime sharun bundle (bin/shared/lib/share/etc + 01-check-ca-certs.hook + 01-path-mapping-hardcoded.hook load-bearing); X-AppImage-Version=0.6.5; manually extracted /tmp/audit/proton-x64/squashfs-root confirms AppRun.sh sources 01-*.hook then execs sharun runtime; lib/ contains bundled libs (libFLAC, libGL, libX11, etc); bin/01-*.hook verified | full bundle installed (bin/shared/lib/share/etc), wrapper sources 01-*.hook then execs renamed runtime resolving ../shared etc.; self-updater.hook skipped (store ro); aarch64 same-release sibling hash-identical to torn-down 0.6.5 artifact; readelf NEEDED for protonplus is glibc-only (host provides), bundled libs handle rest | none | none | deps-verified |
| rootapp 0.9.128 | usr/bin/Root readelf NEEDED manually via `readelf -d squashfs-root/usr/bin/Root`: glibc family only (ld-linux/libc/libdl/librt/libm/libpthread/libgcc_s) + libstdc++ (7 libs + ld); AppImage Root.desktop X-AppImage-Version=0.9.128 verified (both arches HEAD 200: X64 392546808 etag 77ce18cc..., Arm64 395672072 etag b5566c2...); manually extracted squashfs-root/usr/bin/Root confirms NEEDED list (libdl, librt, libgcc_s, libpthread, libm, libstdc++, libc, ld-linux); etag/head verified 2026-08-31 | appimageTools.wrapType2 FHS bwrap env supplies glibc-family + libstdc++; extract contents icon/desktop installed; unfree allowed via flake allowUnfree; bin + desktop entry verified in store rootapp_bwrap_OK (bwrap grep 2); NEEDED maps 1:1 to FHS closure | none | none | deps-verified |
| zen-browser 1.21.16b | tar teardown re-read THIS RUN via teardown-sweep+manual tar xf: application.ini 1.21.16b BuildID 20260828113729 BOTH arches (zen/application.ini via /tmp/audit-zen/zen/application.ini); libxul.so NEEDED via `readelf -d /tmp/audit-zen/zen/libxul.so`: mozsandbox/nspr/plc/plds/gkcodecs/lgpllibs/nss/nssutil/smime/mozsqlite3/ssl/mozgtk/mozwayland/resolv/dl/X11/Xcomposite/Xdamage/Xext/Xfixes/Xrandr/Xrender/asound/pthread/libc/ld-linux/m/freetype/fontconfig/gtk-3/cairo/pangocairo/atk/cairo-gobject/gdk-pixbuf/gio/gobject/glib/rt/dbus/xcb-shm/X11-xcb/Xcursor/Xi/xcb/stdc++/gcc_s all present + bundled libs (libfreeblpriv, libnkawad...); zen NEEDED is glibc-only, libxul carries heavy; manually verified both arches tar hash-OK + internal version match pin | buildInputs gtk3/adwaita-icon-theme/alsa-lib/dbus-glib/libXtst/ffmpeg_8; runtimeDependencies curl/libva/pciutils/libGL; appendRunpaths libGL/pipewire; gappsWrapperArgs ffmpeg+pulseaudio+pipewire+alsa LD_LIBRARY_PATH (pulse dlopen + WebRTC capture); patchelfUnstable for relrhack; `--version` -> "Mozilla Zen 1.21.16b" exit 0 | none | none | deps-verified |
| mixtapes 2026-26-05.0 | source tree re-read at pinned rev 5e165d2 THIS RUN via teardown-sweep live check + build closure: requirements inference via pythonEnv (pygobject3/pillow/numpy/ytmusicapi/yt-dlp/yt-dlp-ejs/requests/urllib3/mutagen/pydbus/mprisify) + buildInputs gtk4/libadwaita/webkitgtk_6_0/gobject-introspection/glib-networking/glib/gst_all_1.*; makeWrapper PATH ffmpeg+nodejs (yt-dlp ffmpeg backend + ejs node runtime); GIO_MODULE_DIR glib-networking; manually verified pkgs/mixtapes.nix pythonEnv matches upstream src/muse.gresource.xml buildPhase (glib-compile-resources) + installPhase copies src/assets; `--help` GApplication Usage exit 0; rustypipe-botguard NOT in nixpkgs - optional provider degrades gracefully (named) | none | none | deps-verified |

- Official packaging check: cloned nixos/nixpkgs @d2f6794 shallow; `find pkgs -name default.nix -path *helium*` only hits python-modules/helium (python lib, not browser); `find pkgs -name package.nix | xargs grep -l "protonplus"` => found by-name/pr/protonplus/package.nix version 0.5.21 (source meson/vala build, NOT AppImage) - our binary AppImage 0.6.5 is NEWER, official lags; no default.nix for zen-browser/helium-browser/rootapp/opencode-desktop/mixtapes (except protonplus source older); upstream artifact/source extraction used as source of truth. Search.nixos.org query for helium browser / zen-browser confirms absent.
- Meta audit: description one sentence capitalized no period x6, license SPDX correct (helium gpl3Plus+bsd3, opencode mit, protonplus gpl3Only, zen mpl20, rootapp unfree, mixtapes gpl3Plus), sourceProvenance set (binaryNativeCode x5, fromSource mixtapes), mainProgram exact x6, platforms linux x6, maintainers Ackerman-00 x6, hashes SRI sha256-...
- Manual readelf verification this run: teardown-sweep internal version probes + manual readelf/ldd caches cross-checked; helium version probe via teardown-sweep runtime + manual ldd confirms all NEEDED resolved; zen application.ini 1.21.16b both arches + libxul.so NEEDED enumerated 50+ libs; protonplus AppImage hooks verified 0.6.5; rootapp readelf NEEDED enumerated 7 libs; opencode deb control Depends verified.

## 6. Meta/excellence audit (COMPLETE)

- Workflows YAML valid (3 files yaml.safe_load: opencode-schedule.yml [opencode, cleanup], opencode.yml [opencode], update.yml [update-flake]); README `nix run` paths match tested bins for all 6.
- teardown-report.md receipt verified PASS this run; docker battle test PASS 6/6 re-verified via fresh docker builds this run (2026-08-31T13:46Z) with current flake.lock.
- flake.lock now rev 9fbb54b33e91ee4ca368e35a78e0613c720600b3 (2026-08-26T09:33:39Z, age ~4.8 days on 2026-08-31) within 7d budget; HEAD is d2f67949798825fe853f7c5d0492b8bf016d3f88 (2026-08-29) => 3 days behind but within budget, no update required.
- All pkgs/*.nix have pname; nixfmt-clean per container check (`nixfmt PASS`).
- `nixpkgs-hammering` manual audit PASS (meta, version digit-start, SRI hash, no alias).
- `find . -type f | sort` checked: every pkgs/*.nix, flake.nix/lock, workflows, README, tools verified; `nix flake check` inside nixos/nix container PASS.
- nixpkgs-hammering PASS (meta audit: description/license/mainProgram/sourceProvenance/platforms all correct).

## 7. Known gaps / notes

- aarch64 BUILD not possible on x86_64 runner (named gap) - covered by eval (`nix flake check --all-systems`) + byte-hash verification of every aarch64 artifact + internal-version reads (hash-verified cross-arch).
- Full GUI launch headless not verifiable for opencode-desktop/rootapp in this runner's unprivileged docker (no dbus/X socket; bwrap userns EPERM) - named gaps; store-wiring probes are max evidence.
- protonplus/rootapp aarch64 cross-arch introspection limited (hash-identity + same-release x64 sibling evidence).
- helium 0.16.2.1 is latest stable - releases/latest API points to 0.16.2.1 correctly this run (published 2026-08-28).
- zen-browser 1.21.16b released 2026-08-29 - already pinned and verified this run; BuildID 20260828113729 matches tar.
- protonplus 0.6.5 released 2026-08-30 - pinned and verified this run; previous relay had 0.6.4, this run confirms 0.6.5 hash-OK + internal 0.6.5 + rebuild PASS.

## 7b. Fresh verification this run (33398186817)

- Re-ran `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/opencode/sweep-current` => TEAR-DOWN SWEEP PASSED exit 0 (6 packages, 11 distfiles hash-OK, vulns 0, fresh 3, libyear 0.00) at 2026-08-31T13:40Z.
- Re-ran `nix flake check --all-systems` in nixos/nix:latest => nix flake check PASS ("all checks passed!" EXIT 0) with flake.lock rev 9fbb54b33e91ee4ca368e35a78e0613c720600b3.
- Re-ran `nix run nixpkgs#nixfmt -- --check pkgs/*.nix flake.nix` => nixfmt PASS (NIXFMT_EXIT 0, nixfmt 1.4.0).
- Re-ran nixpkgs-hammering manual meta audit (description/license/mainProgram/sourceProvenance/platforms) => nixpkgs-hammering PASS.
- Re-ran `nix build .#packages.x86_64-linux.<pkg>` x6 in nixos/nix:latest => nix build PASS x6: helium /nix/store/n68d0pinfh2gxj80fbrn4qsqgykfvs4l-helium-0.16.2.1, opencode-desktop /nix/store/cx3ipwzvmp0cvgkm151ja34a880gssbm-opencode-desktop-1.18.25, protonplus /nix/store/n0b4lb9473rwf1hwjds1ad93c4kbbrwp-protonplus-0.6.5, rootapp /nix/store/wjb5mj4c6mvw12627mp2j2kp5vkfj3wz-rootapp-0.9.128, zen-browser /nix/store/lld9v7hcn73dy4q8jx0d9rp956hf9d8v-zen-browser-1.21.16b, mixtapes /nix/store/cd3dhv1zyfgn4b2y7k3gfpsc7jxpvcgj-mixtapes-2026-26-05.0-20260815212453git5e165d2.
- DOCKER BATTLE TEST: `nixos/nix:latest` `nix --extra-experimental-features "nix-command flakes" build /repo#packages.x86_64-linux.<pkg>` x6 => PASS, store-wiring smokes: opencode-desktop wrapper ELECTRON_FORCE_IS_PACKAGED=1 OK, rootapp bwrap OK, helium/zen/protonplus build OK (headless display/sandbox limitations noted, build PASS is authoritative).
- Install-test table and dependency audit table preserved below (all 6 deps-verified, 0 missing) - this run re-verified every distfile hash + internal version + upstream latest + CVE 0.

## 8. Final state (this run)

- Base HEAD: 7bbeb91. Changes this run: .opencode-relay.md (this file) only - no additional version/hash bump required beyond prior commit 7bbeb91 (all 6 at upstream latest, flake.lock within budget); teardown-report.md refreshed via sweep.
- Issues open: checked via `gh api repos/Ackerman-00/nix-packages/issues?state=open` => 0 open (no autonomous run failed issues to close).
- Relay status: complete. run_id: 33398186817