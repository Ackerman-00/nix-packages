# nix-packages autonomous-run relay state

status: complete
run_id: 31827238785

## 0. Pickup

- Previous relay (run 31798401496) was `status: complete`; nothing unfinished to continue.
- Started fresh on main @ 339eeca. Runner nix was 2.16.1; upgraded to 2.34.8 (locked nixpkgs d407951 requires >= 2.18) via `sudo env "PATH=$PATH" nix upgrade-nix`. Repo unshallowed for NO-CHURN git-log checks.

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): last 5 runs ALL green; last run 31760732709 (2026-08-14 01:28) committed nothing (all versions match upstream).
- No open issues. No open PRs.

## 2. Fixes this run

- **helium.nix + protonplus.nix: enabled `doInstallCheck = true`.** Both defined an `installCheckPhase` (version smoke test) but `doInstallCheck` defaults to `false` (via `config.doCheckByDefault` in stdenv), so the phase was dead code and `nix build` never executed the version smoke test. Verified BEFORE: `nix eval .#helium.doInstallCheck` -> false, build log showed no installCheckPhase. AFTER: `nix build` runs installCheckPhase, log prints "Helium 0.15.4.1 (Chromium 151.0.7922.137)". Committed as 6b20d34.
- No other fixes required - full inventory sweep found every package current, building, and runtime-verified.

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| helium | 0.15.4.1 | 0.15.4.1 (gh releases latest, pub 2026-08-12) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (strip v, pub 2026-08-13) | up-to-date |
| zen-browser | 1.21.14b | 1.21.14b (gh releases latest, pub 2026-08-13) | up-to-date |
| opencode-desktop | v1.18.18 | v1.18.18 (gh releases latest, pub 2026-08-13) | up-to-date |
| rootapp | latest (fixed URL, true ver 0.9.126 from X-AppImage-Version in Root.desktop) | hash unchanged after `--refresh` re-prefetch | up-to-date |

- All 8 pinned SRI hashes re-prefetched with `--refresh` and match byte-for-byte (helium x86_64+aarch64, protonplus x86_64+aarch64, zen x86_64+aarch64, opencode-deb, rootapp fixed URL).
- All 8 source URLs return 200 via `curl -sIL`.
- README package list (rootapp, opencode-desktop, helium, protonplus, zen-browser) matches flake.nix exactly.

## 4. Build/install verification ledger

| package | build (x86_64) | aarch64 eval | binary smoke | status |
|---|---|---|---|---|
| helium | PASS (fresh) | PASS (eval .name) | installCheckPhase: `--version` -> "Helium 0.15.4.1 (Chromium 151.0.7922.137)" | pass |
| protonplus | PASS (fresh) | PASS (eval .name) | installCheckPhase: `version` -> "ProtonPlus 0.6.4" | pass |
| zen-browser | PASS (fresh) | PASS (eval .name) | `--version` -> "Mozilla Zen 1.21.14b" | pass |
| opencode-desktop | PASS (fresh) | n/a (x86_64 only) | launches under xvfb, renderer starts ("[desktop-onboarding] starting first launch"); electron 42.5.1 | pass (GUI needs display = gap) |
| rootapp | PASS (fresh) | n/a (x86_64 only) | starts ("Starting application", "Using profile default") under `--privileged` | pass |

- `nix flake check --all-systems` green on main (both systems evaluate; aarch64 BUILD impossible on x86_64 runner - named gap). Re-run from a FRESH clone at /tmp/fresh-check on committed main @ 6b20d34 - all checks passed.
- Store paths deterministic: fresh-clone `--refresh` rebuild produced identical paths (helium xmsjwcf..., protonplus bjmcfaj...).
- sourceProvenance = [binaryNativeCode] on all 5 packages (nix eval verified).

## 5. Dependency audit (100% of inventory)

| package | upstream deps found | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium | main ELF NEEDED = 33 sonames (X11 family, asound, atk/atspi, cairo, cups, dbus, expat, gbm, nspr/nss, pango, udev, xcb, xkbcommon; libcap is transitive) | all covered by runtimeDeps | none | extra-but-harmless (dlopen coverage) | deps-verified (72 libs resolved via nix loader + wrapper env, ZERO not-found) |
| protonplus | static bun + bundled bin/shared/lib/etc; hooks 01-check-ca-certs.hook + 01-path-mapping-hardcoded.hook | binary bundles everything; wrapper sources 01-* hooks | none | none | deps-verified |
| zen-browser | NEEDED union of all ELFs (X11/xcb family, asound, atk, dbus, fontconfig, freetype, gtk3/gdk/pango/cairo/glib, nspr/nss); libxul + libmozsqlite3 ZERO not-found | gtk3, alsa-lib, dbus-glib, libXtst, ffmpeg_8, runtimeDependencies [curl libva pciutils libGL], appendRunpaths [libGL pipewire] | none added (all resolved) | none | deps-verified (ZERO not-found across all .so) |
| opencode-desktop | electron_42 (42.5.1 from locked nixpkgs, confirmed in container `electron --no-sandbox --version` = v42.5.1) + .node sidecars (glibc/musl ABI115 variants) | electron_42 via callPackage | none | none | deps-verified |
| rootapp | .NET self-contained + bwrap FHS sandbox; AppImage version 0.9.126 read from extracted Root.desktop | wrapType2 FHS | none | none | deps-verified (medium confidence - FHS self-contained, ldd n/a for bwrap wrapper) |

## 6. Container runtime test ledger (fresh `docker run --rm` per test, base ubuntu:24.04)

| package | nix build | container ldd | container smoke | status |
|---|---|---|---|---|
| helium | PASS | ZERO not found (via nix glibc-2.42 loader + wrapper LD_LIBRARY_PATH) | `--version` -> "Helium 0.15.4.1 (Chromium 151.0.7922.137)", exit 0 | verified |
| protonplus | PASS | ZERO not found (static bun) | `version` -> "ProtonPlus 0.6.4", exit 0; hook artifacts verified: /tmp/pKE -> lib, /tmp/3J=1Z -> share; with host certs mounted /tmp/.___host-certs/ca-certificates.crt -> /etc/ssl/certs/ca-certificates.crt | verified |
| zen-browser | PASS | ZERO not found (main + libxul + libmozsqlite3 + all .so) | `--version` -> "Mozilla Zen 1.21.14b", exit 0 | verified |
| opencode-desktop | PASS | wrapper + electron: ZERO not found | electron --no-sandbox --version -> "v42.5.1"; app renderer starts under xvfb | verified (GUI launch gap) |
| rootapp | PASS | ZERO not found (wrapper) | starts ("Starting application", "Using profile default") under `--privileged` (bwrap needs userns) | verified (needs userns) |

- Named gaps: aarch64 BUILD impossible on x86_64 runner (eval only, all 3 aarch64 packages evaluate green); GUI apps full window rendering needs a display (xvfb used for smoke only); rootapp needs user-namespace support in the container runtime (--privileged on the runner simulates a real machine).

## 7. What remains

- Nothing. Repo is current, all 5 packages build and pass clean-container runtime tests, all hashes match upstream, no open issues/PRs.

## 8. Final state

- main @ 6b20d34 (one fix commit this run: doInstallCheck on helium + protonplus). Working tree clean.
- Runner nix upgraded to 2.34.8 (runner-only, not a repo change).
- README.md package list matches flake.nix (rootapp, opencode-desktop, helium, protonplus, zen-browser).
