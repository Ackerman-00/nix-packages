# nix-packages autonomous-run relay state

run_id: 32051821528
status: complete

## 0. Pickup

- Previous relay (run 32026239263) was `status: complete`; nothing unfinished to continue.
- `git fetch origin && git pull --rebase origin main` => Already up to date. HEAD = d416abc.
- Docker 28.0.4 verified; `docker run --rm hello-world` confirmed daemon works.
- Nix 2.34.8 verified, flakes + nix-command enabled.
- Inventory discovered from flake.nix (NOT hardcoded): x86_64-linux = helium, protonplus, zen-browser, rootapp, opencode-desktop; aarch64-linux = helium, protonplus, zen-browser. README.md package list matches flake.nix (no discrepancy).

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): last run id 31983608307 (2026-08-17 00:59 UTC) SUCCESS, committed nothing (no package changed). Last 11 runs all green. No failed recent runs.
- opencode-schedule: all 19 runs green (most recent run 32051821528 is THIS run, in progress).
- No open issues (`gh api .../issues` => empty).
- No open PRs (`gh pr list --state open` => empty).
- update.yml is in-sync with pkgs/*.nix: 5 update blocks (rootapp fixed URL, opencode-desktop GitHub release, helium GitHub release x2 hashes, protonplus GitHub release x2 hashes, zen-browser GitHub release x2 hashes). No workflow changes needed.

## 2. Changes this run

- ZERO package changes. Every package is at its current upstream version with byte-matching hashes (re-verified with `--refresh`). No churn commits generated.

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package        | packaged version | upstream latest (verified via gh api)                 | status     |
|---|---|---|---|
| helium         | 0.15.5.1              | 0.15.5.1 (gh releases/latest)                         | up-to-date |
| protonplus     | 0.6.4                 | v0.6.4 -> 0.6.4 (strip v)                               | up-to-date |
| zen-browser    | 1.21.14b              | 1.21.14b (gh releases/latest)                           | up-to-date |
| opencode-desktop | v1.18.18           | v1.18.18 (gh releases/latest)                          | up-to-date |
| rootapp        | latest (fixed URL)    | hash unchanged after `--refresh` re-prefetch             | up-to-date |

- All 8 pinned SRI hashes re-prefetched with `--refresh` and byte-match (helium x64+arm, protonplus x64+arm, zen x64+arm, opencode deb, rootapp fixed URL).
- All 8 source URLs curl -sI = 200 (rootapp) or 302 (GitHub release redirect -> download). All reachable.
- git log --oneline checked per package: no commit carries a newer version/hash than current, so no churn.

## 4. Build/install verification ledger

| package          | build (x86_64) | aarch64 eval | binary smoke                                                                 | status |
|---|---|---|---|---|
| helium 0.15.5.1    | PASS           | PASS (eval)  | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0  | pass   |
| protonplus 0.6.4   | PASS           | PASS (eval)  | `version` -> "ProtonPlus 0.6.4", exit 0                                      | pass   |
| zen-browser 1.21.14b | PASS         | PASS (eval)  | `--version` -> "Mozilla Zen 1.21.14b", exit 0                                | pass   |
| opencode-desktop v1.18.18 | PASS    | n/a (x86_64 only) | electron `--version` -> "v42.5.1", exit 0; app prints "app starting { version: '1.18.18' }" | pass   |
| rootapp latest     | PASS           | n/a (x86_64 only) | bwrap wrapper -> "Starting application" / "Using profile default", exit 0  | pass   |

- `nix build --no-link --refresh .#helium .#protonplus .#zen-browser .#rootapp .#opencode-desktop` => EXIT 0 for all 5.
- `nix flake check --all-systems` => all checks passed! (all x86_64 derivations built + all aarch64 derivations evaluated green).
- All 5 packages have `meta.sourceProvenance = with lib.sourceTypes; [ binaryNativeCode ];` (verified via `nix eval`).

## 5. Dependency audit (100% of inventory)

| package          | upstream deps found (NEEDED sonames)                                                                 | in pkgs/*.nix                                                                                       | missing/added | extra/dropped | status          |
|---|---|---|---|---|---|
| helium 0.15.5.1    | 32 NEEDED sonames across 3 ELFs (helium, crashpad_handler, chromedriver): glib, gobject, gio, nspr, nss, nssutil, smime, atk, at-spi, dbus, cups, expat, xcb, xkbcommon, asound, gbm, X11, Xext, cairo, pango, udev, Xcomposite, Xdamage, Xfixes, Xrandr, gcc_s, dl, pthread, m, libc, ld-linux; interpreter /lib64/ld-linux-x86-64.so.2 (upstream Debian) | all in runtimeDeps; patchelf --set-interpreter -> nix glibc 2.42; LD_LIBRARY_PATH via wrapper | none            | none          | deps-verified   |
| protonplus 0.6.4   | static-pie bun binary (readelf NEEDED = 0); bundled bin/shared/lib/share/etc; hooks 01-check-ca-certs + 01-path-mapping-hardcoded | binary bundles everything; wrapper sources 01-* hooks | none            | none          | deps-verified   |
| zen-browser 1.21.14b | libxul NEEDED: libdl, libstdc++, libm, libgcc_s, libpthread, libc, ld-linux (7 sonames); bundles own ffmpeg codecs; interpreter patched by autoPatchelfHook | gtk3, alsa-lib, dbus-glib, libXtst, ffmpeg_8, autoPatchelfHook; runtimeDeps curl, libva, pciutils, libGL; appendRunpaths libGL+pipewire; patchelfFlags --no-clobber-old-sections | none            | none          | deps-verified   |
| opencode-desktop v1.18.18 | electron NEEDED: libc.so.6 + electron_42 closure; 7 .node sidecars (4 glibc, 3 musl) | electron_42 via callPackage; stdenv.cc.cc.lib; autoPatchelfIgnoreMissingDeps libc.musl-x86_64.so.1; autoPatchelfHook patchelfs glibc .node sidecars | none            | none          | deps-verified   |
| rootapp latest     | .NET self-contained: Root ELF NEEDED libdl, librt, libgcc_s, libm, libpthread, libstdc++, libc, ld-linux; interpreter /lib64/ld-linux-x86-64.so.2 (provided by bundled glibc via bwrap FHS) | wrapType2 FHS+bwrap; container-init provides AppRun; all deps bundled in fhsenv-rootfs | none            | none          | deps-verified   |

## 6. Container runtime test ledger (fresh `docker run --rm` per test, base ubuntu:24.04, /nix/store mounted ro)

| package          | nix build | container ldd                          | container smoke                                                      | status     |
|---|---|---|---|---|
| helium 0.15.5.1    | PASS      | ZERO_NOT_FOUND (3 ELFs, all sonames resolved via wrapper LD_LIBRARY_PATH) | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0 | verified   |
| protonplus 0.6.4   | PASS      | ZERO_NOT_FOUND (static-pie, 0 NEEDED)  | `version` -> "ProtonPlus 0.6.4", exit 0 (CA cert warning expected - hooks handle it on host) | verified   |
| zen-browser 1.21.14b | PASS    | ZERO_NOT_FOUND (7 NEEDED sonames resolved) | `--version` -> "Mozilla Zen 1.21.14b", exit 0 (Sandbox EPERM warning normal) | verified   |
| opencode-desktop v1.18.18 | PASS | ZERO_NOT_FOUND (electron binary)       | electron `--version --no-sandbox` -> "v42.5.1", exit 0; app prints "version: '1.18.18'" | verified   |
| rootapp latest     | PASS      | ZERO_NOT_FOUND (.NET Root binary)       | bwrap wrapper -> "Starting application" / "Using profile default", exit 0 (needs --privileged for bwrap userns) | verified   |

- Interpreters verified: helium = /nix/store/.../glibc-2.42-67/lib/ld-linux-x86-64.so.2 (nix dynamic linker, via patchelf); zen = same (nix glibc 2.42, via autoPatchelfHook). rootapp = /lib64/ld-linux-x86-64.so.2 (correct - provided by bundled glibc in bwrap FHS env). opencode-desktop electron = nix glibc 2.42.
- Fresh-checker re-run: rebuilt helium with `nix build --refresh`; store path unchanged (4lph1w68qgfcs1j9yyvyqrnpby345fdi); re-ran container smoke -> version prints, exit 0. Confirmed no churn.

## 7. Final state

- HEAD = d416abc + this relay commit. Working tree clean (`git status --porcelain` empty).
- README.md package list matches flake.nix (rootapp, opencode-desktop, helium, protonplus, zen-browser).
- flake lock not touched (no nixpkgs bump needed; builds green).

## Named gaps (not verified, and why)

- aarch64 BUILD: cannot build aarch64 binaries on this x86_64 runner. Verified aarch64 via `nix flake check --all-systems` (derivation evaluation green) only. aarch64 hashes prefetched and match.
- GUI launch in container: no display/X server in ubuntu:24.04 base container. Verified via headless flags only (--version, --version --no-sandbox). All binaries start far enough to print version. Full GUI launch is a named gap.
- opencode-desktop .node sidecar ldd: the 3 musl .node sidecars legitimately show `libc.musl-x86_64.so.1 not found` because they are intentionally ignored via `autoPatchelfIgnoreMissingDeps` (Electron loads the glibc variants instead). The 4 glibc .node sidecars are patchelf'd by autoPatchelfHook and resolve via the nix closure. This is correct by design.
- rootapp bwrap in non-privileged container: `bwrap: Creating new namespace failed` in default `docker run` (no userns). Verified with `docker run --privileged` which grants CAP_SYS_ADMIN; bwrap then works and the .NET app prints its startup banner. On real NixOS desktops user namespaces are enabled by default so this works without --privileged.
