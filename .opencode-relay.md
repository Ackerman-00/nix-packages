# nix-packages autonomous-run relay state

run_id: 32081375106
status: complete

## 0. Pickup

- Previous relay (run 32051821528) was `status: complete`; nothing unfinished to continue.
- `git fetch origin && git pull --rebase origin main` => Already up to date. HEAD = 2c44e3e.
- Runner Nix was 2.16.1 (TOO OLD for current nixpkgs which aborts below 2.18). Fixed via `sudo -i nix upgrade-nix` -> Nix 2.34.8, then all builds green. This was a real environment issue on this runner, not a repo bug.
- Docker 28.0.4 verified; `docker run --rm hello-world` confirmed daemon works.
- Git identity verified: `opencode-agent[bot] <41898282+opencode-agent[bot]@users.noreply.github.com>`.
- Inventory discovered from flake.nix (NOT hardcoded): x86_64-linux = helium, protonplus, zen-browser, rootapp, opencode-desktop; aarch64-linux = helium, protonplus, zen-browser. README.md package list matches flake.nix (no discrepancy).

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): last run id 31983608307 (2026-08-17 00:59 UTC) SUCCESS, committed nothing (all 4 versioned packages + rootapp hash reported "no change"). No failed recent runs.
- opencode-schedule: all 20 runs green (run 32081375106 is THIS run, in progress at check time).
- No open issues (`gh api .../issues?state=open` => empty).
- No open PRs (`gh pr list --state open` => empty).
- update.yml is in-sync with pkgs/*.nix: 5 update blocks (rootapp fixed URL, opencode-desktop GitHub release, helium GitHub release x2 hashes, protonplus GitHub release x2 hashes, zen-browser GitHub release x2 hashes). No workflow changes needed.

## 2. Changes this run

- ZERO package changes. Every package is at its current upstream version with byte-matching hashes (re-verified with `--refresh`). No churn commits generated.
- One runner-environment fix (not a repo change): upgraded Nix 2.16.1 -> 2.34.8 because nixpkgs in flake.lock aborts on Nix < 2.18 (`evaluation aborted ... requires an implementation of Nix with ... features`). After upgrade, `nix flake check --all-systems` and all builds pass.

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package        | packaged version | upstream latest (verified via gh api)                 | status     |
|---|---|---|---|
| helium         | 0.15.5.1              | 0.15.5.1 (gh releases/latest)                         | up-to-date |
| protonplus     | 0.6.4                 | v0.6.4 -> 0.6.4 (strip v)                               | up-to-date |
| zen-browser    | 1.21.14b              | 1.21.14b (gh releases/latest)                           | up-to-date |
| opencode-desktop | v1.18.18           | v1.18.18 (gh releases/latest)                          | up-to-date |
| rootapp        | latest (fixed URL)    | hash unchanged after `--refresh` re-prefetch; real version from artifact = 0.9.126 (X-AppImage-Version in Root.desktop) | up-to-date |

- All 8 pinned SRI hashes re-prefetched with `--refresh` and byte-match (helium x64+arm, protonplus x64+arm, zen x64+arm, opencode deb, rootapp fixed URL).
- All 8 source URLs curl -sI = 200 (rootapp) or 302 (GitHub release redirect -> download). All reachable.
- git log checked per package: only the prior relay commit touches the pkgs files in recent history; no commit carries a newer version/hash than current, so no churn.

## 4. Build/install verification ledger

| package          | build (x86_64) | aarch64 eval | binary smoke                                                                 | status |
|---|---|---|---|---|
| helium 0.15.5.1    | PASS           | PASS (eval)  | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0  | pass   |
| protonplus 0.6.4   | PASS           | PASS (eval)  | `version` -> "ProtonPlus 0.6.4", exit 0                                      | pass   |
| zen-browser 1.21.14b | PASS         | PASS (eval)  | `--version` -> "Mozilla Zen 1.21.14b", exit 0                                | pass   |
| opencode-desktop v1.18.18 | PASS    | n/a (x86_64 only) | electron `--version` -> "v42.5.1", exit 0; app main process starts (needs display for full GUI) | pass   |
| rootapp latest     | PASS           | n/a (x86_64 only) | bwrap wrapper -> "Starting application" / "Using profile default" (needs --privileged for userns) | pass   |

- `nix build .#helium .#protonplus .#zen-browser .#rootapp .#opencode-desktop --no-link` => EXIT 0 for all 5.
- Fresh-checker re-run: rebuilt ALL 5 from /tmp with `nix build /path#<pkg> --no-link --refresh` => EXIT 0; store paths byte-identical to before (helium=4lph1w68qgfcs1j9yyvyqrnpby345fdi, protonplus=bjmcfaj5zv7z0d0z6l4xs794qxl54r21, zen=v78acsdymsjr8k667aiiwswmp6s2xm1x, rootapp=l45vk28x07x9zr1x9400dl0ya17x2bhk, opencode=vi5mdchvnv1h26vi1hmrkd27iwibhpfs). Confirms no churn.
- `nix flake check --all-systems` => all checks passed! (all x86_64 derivations built + all aarch64 derivations evaluated green).
- All 5 packages have `meta.sourceProvenance = with lib.sourceTypes; [ binaryNativeCode ];` (verified via `nix eval .#<pkg>.meta.sourceProvenance`).

## 5. Dependency audit (100% of inventory; re-derived from fresh artifacts this run)

| package          | upstream deps found (NEEDED sonames)                                                                 | in pkgs/*.nix                                                                                       | missing/added | extra/dropped | status          |
|---|---|---|---|---|---|
| helium 0.15.5.1    | tore tar.xz fresh: 32 NEEDED sonames across helium ELF (glib, gobject, gio, nspr, nss, nssutil, smime, atk, atk-bridge, atspi, dbus, cups, expat, xcb, xkbcommon, asound, gbm, X11, Xext, cairo, pango, udev, Xcomposite, Xdamage, Xfixes, Xrandr, gcc_s, dl, pthread, m, libc, ld-linux); interpreter /lib64/ld-linux-x86-64.so.2 upstream | all 32 mapped to runtimeDeps (glib, nspr/nss, atk/at-spi2, dbus, cups, expat, libxcb, libxkbcommon, alsa-lib, libgbm, libx11/ext/composite/damage/fixes/randr, cairo, pango, udev, stdenv.cc.cc.lib); patchelf --set-interpreter -> nix glibc 2.42 | none            | none          | deps-verified   |
| protonplus 0.6.4   | static-pie bun binary (readelf NEEDED = 0); desktop X-AppImage-Version=0.6.4 (matches); bundled bin/shared/lib/share/etc | binary bundles everything; wrapper sources 01-* hooks (verified hooks create /tmp/pKE -> lib, /tmp/3J=1Z -> share in container) | none            | none          | deps-verified   |
| zen-browser 1.21.14b | libxul NEEDED = 46 sonames, of which 27 bundled inside lib/zen-bin (libmozsandbox, libnspr4, libplc4, libgkcodecs, liblgpllibs, libnss3, libmozsqlite3, libmozgtk, libmozwayland, etc.); external ones: X11/Xcomposite/Xdamage/Xext/Xfixes/Xrandr/Xrender, asound, freetype, fontconfig, gtk-3, gdk-3, pango, atk, cairo, gdk_pixbuf, glib/gobject/gio, dbus-1, xcb-shm, Xcursor, Xi, stdc++ | gtk3, alsa-lib, dbus-glib, libXtst, ffmpeg_8, autoPatchelfHook; runtimeDeps curl, libva, pciutils, libGL; appendRunpaths libGL+pipewire; patchelfUnstable --no-clobber-old-sections | none            | none          | deps-verified   |
| opencode-desktop v1.18.18 | electron NEEDED resolves via nix loader (ZERO_NOT_FOUND); app version 1.18.18 confirmed in app.asar; 7 .node sidecars: 3 musl (intentionally ignored via autoPatchelfIgnoreMissingDeps), 4 glibc (patchelf'd; pty.node NEEDED libutil,libstdc++,libgcc_s,libpthread,libc all resolve under nix loader; dlopen probe under nix loader = DLOPEN_FAIL only due to missing Node-API symbols from a plain C probe, Electron provides them - NOT a packaging bug) | electron_42 via callPackage; stdenv.cc.cc.lib; autoPatchelfIgnoreMissingDeps libc.musl-x86_64.so.1; autoPatchelfHook | none            | none          | deps-verified   |
| rootapp 0.9.126    | Root ELF NEEDED libdl, librt, libgcc_s, libpthread, libm, libstdc++, libc, ld-linux; interpreter /lib64/ld-linux-x86-64.so.2 (correct - provided by bundled glibc via bwrap FHS env) | wrapType2 FHS+bwrap; container-init provides AppRun; all deps bundled in fhsenv-rootfs | none            | none          | deps-verified   |

## 6. Container runtime test ledger (fresh `docker run --rm` per test, base ubuntu:24.04, /nix/store mounted ro)

| package          | nix build | container ldd                          | container smoke                                                      | status     |
|---|---|---|---|---|
| helium 0.15.5.1    | PASS      | ZERO_NOT_FOUND when resolved with wrapper LD_LIBRARY_PATH + nix loader (3 ELFs) | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0; raw binary with nix LD_LIBRARY_PATH also runs | verified   |
| protonplus 0.6.4   | PASS      | ZERO_NOT_FOUND (static-pie, 0 NEEDED)  | `version` -> "ProtonPlus 0.6.4", exit 0; hooks created /tmp/pKE + /tmp/3J=1Z symlinks in container (CA certs absent in bare container = expected warning) | verified   |
| zen-browser 1.21.14b | PASS    | ZERO_NOT_FOUND (zen ELF + libxul.so, 46 sonames) | `--version` -> "Mozilla Zen 1.21.14b", exit 0 (Sandbox EPERM warning normal) | verified   |
| opencode-desktop v1.18.18 | PASS | ZERO_NOT_FOUND (electron via nix loader; 4 glibc .node sidecars resolve) | electron `--version --no-sandbox` -> "v42.5.1", exit 0; app main process starts in container (stops at "Missing X server" = GUI gap) | verified   |
| rootapp 0.9.126    | PASS      | ZERO_NOT_FOUND (.NET Root binary)       | bwrap wrapper (--privileged) -> "Starting application" / "Using profile default" | verified   |

- Interpreters verified: helium = /nix/store/.../glibc-2.42-67/lib/ld-linux-x86-64.so.2 (nix dynamic linker, via patchelf); zen = same (nix glibc 2.42, via autoPatchelfHook). rootapp = /lib64/ld-linux-x86-64.so.2 (correct - provided by bundled glibc in bwrap FHS env). opencode-desktop electron = nix glibc 2.42.
- Note on container ldd GLIBC_ABI warnings: bare-container `ldd` uses ubuntu:24.04's OLD loader (glibc 2.39) against nix glibc 2.42 libs and can print `GLIBC_ABI_DT_X86_64_PLT not found` on mixed graphs. Verified this is an ldd artifact, not a real break: (a) wrapper smoke passes in the container, (b) resolving with the actual nix loader (`<nix-glibc>/ld-linux-x86-64.so.2 --list`) yields ZERO_NOT_FOUND for the same files, (c) binaries run under the nix interpreter on NixOS.

## 7. Final state

- HEAD = 2c44e3e + this relay commit. Working tree clean (`git status --porcelain` empty).
- README.md package list matches flake.nix (rootapp, opencode-desktop, helium, protonplus, zen-browser).
- flake lock not touched (no nixpkgs bump needed; builds green).

## Named gaps (not verified, and why)

- aarch64 BUILD: cannot build aarch64 binaries on this x86_64 runner. Verified aarch64 via `nix flake check --all-systems` (derivation evaluation green) only. aarch64 hashes prefetched and match.
- GUI launch in container: no display/X server in ubuntu:24.04 base container. Verified via headless flags only (--version, --version --no-sandbox). All binaries start far enough to print version. Full GUI launch is a named gap.
- opencode-desktop pty.node full dlopen: my dlopen probe fails on `undefined symbol: napi_call_threadsafe_function` because a plain C probe provides no Node-API symbols; Electron supplies them at runtime. The .node file's NEEDED libs all resolve under the nix loader. Full in-app load requires a display + Electron runtime, so it is a named gap - the library deps are verified resolved.
- opencode-desktop musl .node sidecars legitimately show libc.musl-x86_64.so.1 unresolved; intentionally ignored via `autoPatchelfIgnoreMissingDeps` (Electron loads glibc variants instead). Correct by design.
- rootapp bwrap in non-privileged container: `bwrap: setting up uid map: Permission denied` in default `docker run` (no userns). Verified with `docker run --privileged` which grants CAP_SYS_ADMIN; bwrap then works and the .NET app prints its startup banner. On real NixOS desktops user namespaces are enabled by default so this works without --privileged.