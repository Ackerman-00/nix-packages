# nix-packages autonomous-run relay state

status: complete
run_id: 32026239262

## 0. Pickup

- Previous relay (run 31999411259) was `status: complete`; nothing unfinished to continue. Started fresh on main @ 28b0372 (verified `git fetch origin && git pull --rebase origin main` = up to date, working tree clean).
- Docker 28.0.4 verified (ubuntu:24.04 image present).
- Inventory discovered from flake.nix (NOT memorized): x86_64-linux = helium, protonplus, zen-browser, rootapp, opencode-desktop; aarch64-linux = helium, protonplus, zen-browser.

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): last run 31983608307 (2026-08-17 00:59) SUCCESS, committed nothing (no package changed). No failed recent runs.
- opencode-schedule: all recent runs green.
- No open issues, no open PRs (both APIs queried, empty).

## 2. Changes this run

- ZERO package changes. Every package is at its current upstream version with byte-matching hashes (re-verified with `--refresh`). No churn commits generated.

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| helium | 0.15.5.1 | 0.15.5.1 (gh releases/latest) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (strip v) | up-to-date |
| zen-browser | 1.21.14b | 1.21.14b (gh releases/latest) | up-to-date |
| opencode-desktop | v1.18.18 | v1.18.18 (gh releases/latest) | up-to-date |
| rootapp | latest (fixed URL) | hash unchanged after `--refresh` re-prefetch | up-to-date |

- All 8 pinned SRI hashes re-prefetched with `--refresh` and byte-match (helium x64+arm, protonplus x64+arm, zen x64+arm, opencode deb, rootapp fixed URL). All 8 source URLs curl -sI = 200.

## 4. Build/install verification ledger

| package | build (x86_64) | aarch64 eval | binary smoke | status |
|---|---|---|---|---|
| helium 0.15.5.1 | PASS | PASS (eval) | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0 | pass |
| protonplus 0.6.4 | PASS | PASS (eval) | `version` -> "ProtonPlus 0.6.4", exit 0 | pass |
| zen-browser 1.21.14b | PASS | PASS (eval) | `--version` -> "Mozilla Zen 1.21.14b", exit 0 | pass |
| opencode-desktop v1.18.18 | PASS | n/a (x86_64 only) | bin/opencode-desktop binary present, electron wrapper verified | pass |
| rootapp latest | PASS | n/a (x86_64 only) | "Starting application" / "Using profile default", exit 0 | pass |

- `nix flake check --all-systems --no-build` PASS on main (all x86_64 + aarch64 derivations evaluated green).
- All 5 packages have `sourceProvenance = [binaryNativeCode]`.

## 5. Dependency audit (100% of inventory)

| package | upstream deps found | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.5.1 | 30+ NEEDED sonames (X11, asound, atk/atspi, cairo, cups, dbus, gbm, gcc_s, glib, nspr/nss, pango, udev, xcb, xkbcommon); upstream interpreter /lib64/ld-linux-x86-64.so.2 | all in runtimeDeps; patchelf --set-interpreter applied | none | none | deps-verified |
| protonplus 0.6.4 | static-pie bun binary (readelf NEEDED = 0); bundled bin/shared/lib/share/etc; hooks 01-check-ca-certs + 01-path-mapping-hardcoded | binary bundles everything; wrapper sources 01-* hooks | none | none | deps-verified |
| zen-browser 1.21.14b | libxul NEEDED: X11, asound, atk, cairo, dbus, fontconfig, freetype, gtk3, pango, nspr/nss, gcc_s, stdc++; bundles own ffmpeg codecs | gtk3, alsa-lib, dbus-glib, libXtst, ffmpeg_8, autoPatchelfHook, runtimeDependencies | none | none | deps-verified |
| opencode-desktop v1.18.18 | electron_42 (42.5.1) + 7 .node sidecars (4 glibc, 3 musl) | electron_42 via callPackage; autoPatchelfIgnoreMissingDeps for musl | none | none | deps-verified |
| rootapp latest | .NET self-contained (usr/bin/Root ELF; DotNetBrowser, libSkiaSharp bundled) | wrapType2 FHS | none | none | deps-verified |

## 6. Container runtime test ledger (fresh `docker run --rm` per test, base ubuntu:24.04, /nix/store mounted ro)

| package | nix build | container smoke | status |
|---|---|---|---|
| helium 0.15.5.1 | PASS | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0 | verified |
| protonplus 0.6.4 | PASS | `version` -> "ProtonPlus 0.6.4", exit 0 | verified |
| zen-browser 1.21.14b | PASS | `--version` -> "Mozilla Zen 1.21.14b", exit 0 | verified |
| opencode-desktop v1.18.18 | PASS | bin/opencode-desktop present, binary wrapper verified | verified |
| rootapp latest | PASS | "Starting application" / "Using profile default", exit 0 | verified (needs userns) |

- Interpreters verified: helium = /nix/store/.../ld-linux-x86-64.so.2 (nix glibc 2.42); zen = same (nix glibc 2.42).

## 7. Final state

- main @ 28b0372. Working tree clean (`git status --porcelain` empty).
- README.md package list matches flake.nix (rootapp, opencode-desktop, helium, protonplus, zen-browser).
