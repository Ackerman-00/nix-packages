# nix-packages autonomous-run relay state

run_id: 32104206033
status: complete

## 0. Pickup

- Previous relay (run 32081375106) was `status: complete`; nothing unfinished to continue.
- `git fetch origin && git pull --rebase origin main` => Already up to date. HEAD = 3736c4c.
- Runner Nix was 2.16.1 (TOO OLD for current nixpkgs, which aborts below 2.18 - same issue fixed last run). Fixed via `sudo -i nix upgrade-nix` -> Nix 2.34.8, then all builds green. Environment fix only, not a repo change.
- Docker 28.0.4 verified; `docker run --rm hello-world` confirmed daemon works.
- Git identity verified: `opencode-agent[bot] <41898282+opencode-agent[bot]@users.noreply.github.com>`.
- Inventory discovered from flake.nix / `nix eval --apply attrNames` (NOT hardcoded): x86_64-linux = helium, protonplus, zen-browser, rootapp, opencode-desktop (+ default); aarch64-linux = helium, protonplus, zen-browser (+ default). README.md package list matches flake.nix (no discrepancy). update.yml has 5 update blocks matching all 5 packages (in sync, no workflow change needed).

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): last run id 32086477571 (2026-08-18 00:57 UTC) SUCCESS, committed nothing (all packages reported "no change"). The only recent failure was opencode-schedule 31755867883 (2026-08-14, "Failed to fetch version information" in the Nix install step) - that predates the Nix 2.16.1->2.34.8 upgrade and has not recurred in any run since.
- opencode-schedule: all runs since 08-14 green (run 32104206033 is THIS run).
- No open issues (`gh api .../issues?state=open` => empty).
- No open PRs (`gh pr list --state open` => empty).

## 2. Changes this run

- ZERO package changes. Every package is at its current upstream version with byte-matching hashes (re-verified with `--refresh` this run). No churn commits generated.
- One runner-environment fix (not a repo change): upgraded Nix 2.16.1 -> 2.34.8 (nixpkgs in flake.lock aborts on Nix < 2.18). After upgrade, `nix flake check --all-systems` and all builds pass.

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package        | packaged version | upstream latest (verified via gh api 2026-08-18)   | status     |
|---|---|---|---|
| helium         | 0.15.5.1              | 0.15.5.1 (gh releases/latest)                       | up-to-date |
| protonplus     | 0.6.4                 | v0.6.4 -> 0.6.4 (strip v)                           | up-to-date |
| zen-browser    | 1.21.14b              | 1.21.14b (gh releases/latest)                       | up-to-date |
| opencode-desktop | v1.18.18           | v1.18.18 (gh releases/latest)                       | up-to-date |
| rootapp        | latest (fixed URL)    | hash unchanged after `--refresh` re-prefetch; real version from artifact = 0.9.126 (X-AppImage-Version in Root.desktop, from prior run) | up-to-date |

- All 9 pinned SRI hashes re-prefetched with `--refresh` this run and byte-match (helium x64+arm, protonplus x64+arm, zen x64+arm, opencode deb, rootapp fixed URL):
  - helium x64 `sha256-80oe4aarLjEJ2S45OVEqN8/mii8NIwtSXMFYn8GS/Zc=`, arm `sha256-2TI9B3GjdN5eWmZqfnVOIfKYwjjqwh345YaKx7X2TP4=`
  - protonplus x64 `sha256-J0ddq9hoIb7QeNLQiEEN8qDqr/iFylrwAqxhIQX++Qs=`, arm `sha256-o4sUkr3JJWRxhOzCRsPUjqJNQTgtHBaW7xw9MpmhCBo=`
  - zen x64 `sha256-s6W7eC2UA9zoz0zePWHa7WRUhWgFiGLq654EXCErfuA=`, arm `sha256-4Uldus+9ublCkIpdbTEVjsGgPnfS+djPqaBo4QfeVyk=`
  - opencode deb `sha256-e31ka5x82X8N8JipTEcITx4Wbw38RStyhwRZUuES1Io=`, rootapp `sha256-BzaSb96SOBnSdqxtSjr+ZcbYfHNGia9HJ2/E6/B12RA=`
- All 5 source URLs curl -sI -L = 200. All reachable.
- git log checked per package: only the prior relay commit touches the pkgs files in recent history; no commit carries a newer version/hash than current, so no churn.

## 4. Build/install verification ledger

| package          | build (x86_64) | aarch64 eval | binary smoke                                                                 | status |
|---|---|---|---|---|
| helium 0.15.5.1    | PASS           | PASS (eval)  | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0  | pass   |
| protonplus 0.6.4   | PASS           | PASS (eval)  | `version` -> "ProtonPlus 0.6.4", exit 0                                      | pass   |
| zen-browser 1.21.14b | PASS         | PASS (eval)  | `--version` -> "Mozilla Zen 1.21.14b", exit 0 (sandbox EPERM warning normal) | pass   |
| opencode-desktop v1.18.18 | PASS    | n/a (x86_64 only) | electron `--version --no-sandbox` -> "v42.5.1", exit 0; app main process starts (stops at "Missing X server" = GUI gap) | pass   |
| rootapp latest     | PASS           | n/a (x86_64 only) | bwrap wrapper (--privileged) -> "Starting application" / "Using profile default" | pass   |

- `nix build .#helium .#protonplus .#zen-browser .#rootapp .#opencode-desktop --no-link --refresh` => EXIT 0 for all 5 (fresh, --refresh).
- Store paths byte-identical to the previous verified run (evidence of zero churn): helium=4lph1w68qgfcs1j9yyvyqrnpby345fdi, protonplus=bjmcfaj5zv7z0d0z6l4xs794qxl54r21, zen=v78acsdymsjr8k667aiiwswmp6s2xm1x, rootapp=l45vk28x07x9zr1x9400dl0ya17x2bhk, opencode=vi5mdchvnv1h26vi1hmrkd27iwibhpfs.
- `nix flake check --all-systems` => all checks passed! (x86_64: helium, protonplus, zen-browser, rootapp, opencode-desktop, default; aarch64: helium, protonplus, zen-browser, default all evaluated green).
- All 5 packages have `meta.sourceProvenance = with lib.sourceTypes; [ binaryNativeCode ];` (verified via `nix eval .#<pkg>.meta.sourceProvenance`).

## 5. Dependency audit (100% of inventory; no upstream change this run, so no RE-TEAR required - container ldd re-verified every package's runtime closure fresh)

| package          | upstream deps found (NEEDED sonames) | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.5.1    | 32 NEEDED sonames across helium ELF (glib/gobject/gio, nspr/nss/nssutil/smime, atk/atk-bridge/atspi, dbus, cups, expat, xcb, xkbcommon, asound, gbm, X11, Xext, cairo, pango, udev, Xcomposite/Xdamage/Xfixes/Xrandr, gcc_s, dl, pthread, m, libc, ld-linux); interpreter patched to nix glibc 2.42 | all 32 mapped to runtimeDeps; patchelf --set-interpreter -> nix glibc 2.42 | none | none | deps-verified (container: wrapper LD_LIBRARY_PATH + nix loader `--list` on helium/crashpad/chromedriver => rc=0, zero "not found") |
| protonplus 0.6.4   | static-pie bun binary (readelf NEEDED = 0); desktop X-AppImage-Version=0.6.4 | binary bundles everything; wrapper sources 01-* hooks | none | none | deps-verified (container: NEEDED=0, hooks created /tmp/pKE -> lib and /tmp/3J=1Z -> share; CA-certs hook logic intact, warning expected in bare container) |
| zen-browser 1.21.14b | libxul NEEDED = 46 sonames, 27 bundled inside lib/zen-bin; external: X11/Xcomposite/Xdamage/Xext/Xfixes/Xrandr/Xrender, asound, freetype, fontconfig, gtk-3, gdk-3, pango, atk, cairo, gdk_pixbuf, glib/gobject/gio, dbus-1, xcb-shm, Xcursor, Xi, stdc++ | gtk3, alsa-lib, dbus-glib, libXtst, ffmpeg_8, autoPatchelfHook; runtimeDeps curl, libva, pciutils, libGL; appendRunpaths libGL+pipewire; patchelfUnstable --no-clobber-old-sections | none | none | deps-verified (container: zen + libxul loader `--list` => zero "not found") |
| opencode-desktop v1.18.18 | electron NEEDED resolves via nix loader (zero not found); app version 1.18.18 in app.asar; 7 .node sidecars: 3 musl (intentionally ignored via autoPatchelfIgnoreMissingDeps), 4 glibc resolve | electron_42 via callPackage; stdenv.cc.cc.lib; autoPatchelfIgnoreMissingDeps libc.musl; autoPatchelfHook | none | none | deps-verified (container: electron + all 7 .node sidecars loader `--list` => zero "not found") |
| rootapp 0.9.126    | Root ELF NEEDED libdl, librt, libgcc_s, libpthread, libm, libstdc++, libc, ld-linux; interpreter /lib64/ld-linux-x86-64.so.2 (correct - provided by bundled glibc via bwrap FHS env) | wrapType2 FHS+bwrap; container-init provides AppRun; all deps bundled in fhsenv-rootfs | none | none | deps-verified (container --privileged: ldd zero "not found", app starts) |

## 6. Container runtime test ledger (fresh `docker run --rm` per test, base ubuntu:24.04, /nix/store mounted ro)

| package          | nix build | container ldd                          | container smoke                                                      | status     |
|---|---|---|---|---|
| helium 0.15.5.1    | PASS      | ZERO_NOT_FOUND (nix loader `--list` on helium, crashpad, chromedriver with wrapper LD_LIBRARY_PATH; rc=0 each) | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0 | verified   |
| protonplus 0.6.4   | PASS      | ZERO_NOT_FOUND (static-pie, 0 NEEDED)  | `version` -> "ProtonPlus 0.6.4", exit 0; hooks created /tmp/pKE + /tmp/3J=1Z symlinks; CA-cert warning = expected (no host CA store in bare container) | verified   |
| zen-browser 1.21.14b | PASS    | ZERO_NOT_FOUND (zen ELF + libxul.so via nix loader) | `--version` -> "Mozilla Zen 1.21.14b", exit 0 (Sandbox EPERM normal) | verified   |
| opencode-desktop v1.18.18 | PASS | ZERO_NOT_FOUND (electron + all 7 .node sidecars resolve via nix loader) | electron `--version --no-sandbox` -> "v42.5.1", exit 0; app main process starts, logs "Missing X server or $DISPLAY" then waits (GUI gap) | verified   |
| rootapp 0.9.126    | PASS      | ZERO_NOT_FOUND (.NET Root binary via nix loader) | bwrap wrapper (--privileged) -> "Starting application" / "Using profile default", exit 0 | verified   |

- Interpreters verified: helium = /nix/store/8kvxvr3pmsypxiypq4g8zy13glnfr7nx-glibc-2.42-67/lib/ld-linux-x86-64.so.2 (nix loader, via patchelf); zen = same nix glibc 2.42 (via autoPatchelfHook); rootapp = /lib64/ld-linux-x86-64.so.2 (correct - bundled glibc via bwrap FHS env); opencode electron = nix glibc 2.42.
- Note on the helium `--list` method: earlier naive `ldd`/empty-glob loader runs produced misleading pass/fail; the rigorous method (capture loader rc + count "not found" lines with the wrapper's full LD_LIBRARY_PATH) gives rc=0 / zero not-found for all 3 ELFs, and the shipped wrapper itself runs and prints the version in the container.

## 7. Final state

- HEAD = 3736c4c + this relay commit. Working tree clean (`git status --porcelain` empty).
- README.md package list matches flake.nix (rootapp, opencode-desktop, helium, protonplus, zen-browser).
- flake lock not touched (no nixpkgs bump needed; builds green).

## Named gaps (not verified, and why)

- aarch64 BUILD: cannot build aarch64 binaries on this x86_64 runner. Verified aarch64 via `nix flake check --all-systems` (derivation evaluation green) only. aarch64 hashes prefetched and match.
- GUI launch in container: no display/X server in ubuntu:24.04 base container. Verified via headless flags only (--version, --version --no-sandbox, app main-process start which logs "Missing X server"). Full GUI launch is a named gap.
- opencode-desktop musl .node sidecars legitimately show libc.musl-x86_64.so.1 unresolved; intentionally ignored via `autoPatchelfIgnoreMissingDeps` (Electron loads glibc variants instead). Correct by design.
- rootapp bwrap in non-privileged container: `bwrap: setting up uid map: Permission denied` in default `docker run` (no userns). Verified with `docker run --privileged` (CAP_SYS_ADMIN) which works; on real NixOS desktops user namespaces are enabled by default so this works without --privileged.