run_id: 35818990066
status: unfinished

# nix-packages autonomous-run relay (run 35818990066) — IN PROGRESS, 2026-09-23

needs-owner: issue #11 — `.github/workflows/*` pushes are rejected (GITHUB_TOKEN has no `workflows` permission, verified on main + side branch + contents API); patches for the nixcache queue derivation and the manifest `--clobber` upload are attached to that issue (queue fix also on local branch `queue-fix-local` f433373). No other owner items.

## 0. Pickup / context

- Prior relay: run 35785205928 status complete; RELAY env not set → fresh run.
- RUN_ID=35818990066, base HEAD 0fe35c8.
- Inventory derived from `ls pkgs/*.nix` (8 packages): mixtapes, niri-unstable,
  rootapp, splayer-next, umbriel-unstable, xdg-desktop-portal-umbriel-unstable,
  xwayland-satellite-unstable, zen-browser.
- Git identity: opencode-agent[bot] only (no git config, no --author, no trailers).
- All nix evidence in fresh `nixos/nix:latest` containers
  (img:sha256:7a007c766426c1877758ddc5cb87a965ac131fc78c582ce0083d922d51ae945c,
  `docker pull` re-verified up-to-date this run). GitHub API only via `gh api`.

## 1. Work state so far (2026-09-23)

- Upstream live checks: ALL 5 rev-pins == upstream HEAD; zen 1.22.2b ==
  latest release; splayer v1.1.0 == latest release (lines in section 6).
- Artifact re-teardown (fresh downloads to /tmp, SRI re-hash): all 6 binary
  archives match pinned hashes EXACTLY (rootapp last-modified 2026-09-22 20:17
  GMT touch did NOT change bytes; internal versions: rootapp 0.9.134,
  zen application.ini 1.22.2b, splayer asar 1.1.0).
- niri-stack dep-diff (gh api contents, this run): niri flake.nix/Cargo.toml/
  Cargo.lock(486 crates == last receipt)/build.rs, sat Cargo.toml/Cargo.lock
  (142 crates == last receipt)/build.rs, umbriel nix/package.nix+meson.build,
  xdp meson.build — no deltas requiring expression changes.
- Official nixpkgs packaging checked (by-name): niri, xwayland-satellite,
  umbriel, xdg-desktop-portal-umbriel, splayer-next FOUND (splayer official =
  SOURCE build, same version 1.1.0); zen-browser/mixtapes/rootapp NOT in
  nixpkgs (404 + code search).
- FIX 1: pkgs/mixtapes.nix pythonEnv was missing `strenum` (requirements.txt
  StrEnum==0.4.15; upstream flake maps StrEnum→strenum). Added (nixpkgs
  python314Packages.strenum = 0.4.15, eval-verified).
- FIX 2: flake.nix gained `formatter` output (nixfmt-rfc-style) — `nix fmt`
  now works.
- FIX 3: flake.lock nixpkgs updated b1b8759 (2026-09-16, 6.9d old) →
  6774f7b (2026-09-22) per >7d rule; full rebuild green against new nixpkgs.
- NOT-IN-NIXPKGS report: requirements.txt also wants yt-dlp-get-pot{,-rustypipe}
  (PO-Token provider) — undefined variable in nixpkgs (nix eval, contents 404,
  code search empty). NOT invented; upstream's own flake drops it via `or null`
  too (same behavior). yt-dlp degrades to ungated formats.
- Toolchain so far: `nix flake check --all-systems` PASS exit 0 (fresh
  container, includes new formatter + both-systems eval); `nixfmt --check
  pkgs/*.nix flake.nix` PASS exit 0 (nixfmt 1.5.0). All 4 workflows
  yaml.safe_load OK; README `nix run` + systemPackages paths match all 8 pkgs.
- CONTAINER BATTLE TEST (fresh nixos/nix,3 parallel): ALL 8 packages build
  PASS. Smokes: zen `--version` exit0; mixtapes exit0; niri `--help` exit0;
  sat `-version` → 0.8.2 exit0; umbriel `--version` → 0.1.0 exit0;
  rootapp/splayer-next/xdp run = documented container caveats (see install-test
  table). A uniform docker-sweep rerun is producing per-pkg tokens now.
- teardown-sweep rerun PASSED exit 0 → teardown-report.md regenerated for
  8 pkgs/current pins and pushed (commit 31bacca).
- FIX 3b: tools/docker-sweep.py nix mode rewritten — it ran
  `nix-build '<nixpkgs>' -A <pkg>` (attrs that don't exist → guaranteed false
  failures) and never smoke-ran; now mounts the repo, builds
  `/repo#packages.x86_64-linux.<pkg>` in a fresh nixos/nix container, smoke-runs
  via `nix run` (--version/-version/--help/-h), records image digest; plus
  `.gitignore` now covers `result-*`. A shell-quoting bug in the first rewrite
  (`nix $EX` word-split making `flakes` a subcommand) was caught by the full
  sweep's 8 identical failures and fixed BEFORE any receipt counted (sweep
  rerunning; zen canary PASS: build + SMOKE_OK --version).
- NIXCACHE ROOT CAUSE (2 workflow bugs, found + patch-written this run):
  1) default queue greps only `rev = "<40hex>"` source builds → zen-browser/
     rootapp/splayer-next never queued (their narinfo were absent);
  2) manifest upload uses the no-clobber batch helper for `manifest.txt`,
     which always already exists → every publishing run dies after its NARs
     upload (run 35824969652: zen's 56 files OK, then red at
     "asset under the same name already exists: [manifest.txt]", relay-403
     class of "red but binaries landed" now explained).
  BOTH live in .github/workflows/* → **GITHUB_TOKEN cannot push workflow files
  (no `workflows` permission; verified on main, a side branch, and the contents
  API → 403)** → OWNER-ACL: issue #11 created with both patches verbatim
  (queue-fix also preserved on local branch queue-fix-local f433373).
- PUBLISH ORCHESTRATION (no workflow edit possible): legacy `manifest.txt`
  deleted from the nixcache release (byte-identical to the union of the 5
  per-pkg manifests, 920 nars — local copy kept) so the current workflow's
  manifest upload can succeed again. zen narinfo fngy0p5f4… now PRESENT
  (published by run 35824969652); run 35825312031 (06:07Z, update.yml relay)
  in flight. Still to publish at current outPaths: rootapp, splayer-next,
  mixtapes, umbriel-unstable, xwayland-satellite-unstable (lock-bump changed
  their store paths), + manifest-zen-browser via force_rebuild.
- OSV version-specific: rootapp@0.9.134 CLEAN, splayer-next@1.1.0 CLEAN,
  zen-browser@1.22.2b CLEAN (teardown-sweep); commit-based OSV for all 5
  git-pinned pkgs (mixtapes/niri/umbriel/xdp/sat pinned revs): 0 vulns each.
- Repology API unreachable from this run environment (connection refused on
  api.repology.org) — freshness proven via gh api tags/HEAD instead (section 6).

## 2. Dependency audit (mandatory table) — FINAL when builds finish

| package | upstream deps | in nix | missing | status |
|---|---|---|---|---|
| mixtapes | (row pending container token) | strenum fix applied | none after fix | pending |
| niri-unstable | (row pending container token) | matches upstream flake | none | pending |
| rootapp | (row pending container token) | (row pending) | none | pending |
| splayer-next | (row pending container token) | matches nixpkgs official version | none | pending |
| umbriel-unstable | (row pending container token) | matches upstream nix/package.nix | none | pending |
| xdg-desktop-portal-umbriel-unstable | (row pending container token) | matches nixpkgs official verbatim | none | pending |
| xwayland-satellite-unstable | (row pending container token) | matches nixpkgs official shape | none | pending |
| zen-browser | (row pending container token) | (row pending) | none | pending |

## 6. Always-live upstream checks (fresh this run)

upstream: mixtapes 00f4707 == HEAD | gh api repos/m-obeid/Mixtapes/commits/main | 2026-09-23
upstream: niri-unstable 5f4469b == HEAD | gh api repos/niri-wm/niri/commits/main | 2026-09-23
upstream: rootapp 0.9.134 == nix 0.9.134 | curl -sIL installer.rootapp.com 200 + fresh SRI re-hash match + Root.desktop X-AppImage-Version | 2026-09-23
upstream: splayer-next v1.1.0 == nix 1.1.0 | gh api repos/SPlayer-Dev/SPlayer-Next/releases/latest | 2026-09-23
upstream: umbriel-unstable bbca89f == HEAD | gh api repos/noctalia-dev/umbriel/commits/main | 2026-09-23
upstream: xdg-desktop-portal-umbriel-unstable d7a1bc3 == HEAD | gh api repos/noctalia-dev/xdg-desktop-portal-umbriel/commits/main | 2026-09-23
upstream: xwayland-satellite-unstable add2795 == HEAD | gh api repos/Supreeeme/xwayland-satellite/commits/main | 2026-09-23
upstream: zen-browser 1.22.2b == nix 1.22.2b | gh api repos/zen-browser/desktop/releases/latest | 2026-09-23

gh-rate: 5000/5000 2026-09-23

## 8. Handoff / next steps for this run

1. docker-sweep full run finishing (mixtapes+niri+zen done PASS) → commit
   docker-report.md + write final section-5 teardown tokens + audit +
   install-test tables from its results.
2. Publish chain: wait out run 35825312031 → for each pkg still missing at
   current outPath (rootapp, splayer-next, mixtapes, umbriel-unstable,
   xwayland-satellite-unstable + zen force for manifest): delete local
   `manifest.txt` asset if the prior run recreated it, dispatch
   `-f packages="<pkg>"`, verify green + manifest-<pkg> content covers the
   current outPath; end state = 8 manifests + manifest.txt == union, then
   leak diff (release assets ⊖ union must be empty or GC'd).
3. nixpkgs-hammering sample (≥3 pkgs) + final `nix flake check`/`nixfmt`
   PASS strings in a fresh container.
4. Final relay rewrite (all tables/tokens/sections) →
   `bash tools/verify-nix.sh` must exit 0 → paste report →
   `status: complete` → push → close any `autonomous run failed:*` issues
   (0 open). Keep needs-owner: issue #11 (workflow-push patches) listed.
