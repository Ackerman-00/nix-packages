run_id: 33973908205
status: complete

# nix-packages autonomous-run relay state (run 33973908205)

## 0. Pickup

- Previous relay status: complete (run 33960825667). Fresh periodic sweep.
- Base HEAD 89614ac at pickup (helium 0.16.5.1, opencode-desktop 1.18.29, mixtapes c48756e). RUN_ID=33973908205.
- Git identity: opencode-agent[bot] only (no --author/--committer/trailers).

## 1. Work done this run

### 1a. Version sweep - all 6 at upstream latest

- `gh api --cache 3600s repos/<owner>/<repo>/releases/latest --jq .tag_name`:
  - helium: pinned 0.16.5.1 == upstream tag 0.16.5.1 [releases/latest 2026-09-04T22:38:42Z prerelease false] => up-to-date (Chromium 152.0.7977.82 via probe)
  - opencode-desktop: pinned 1.18.29 == upstream v1.18.29 [releases/latest 2026-09-04T23:47Z] => up-to-date
  - protonplus: pinned 0.6.6 == upstream v0.6.6 [releases/latest 2026-09-04T17:54Z] => up-to-date
  - zen-browser: pinned 1.21.16b == upstream 1.21.16b [releases/latest 2026-08-29T21:29Z] => up-to-date
  - mixtapes: pinned rev c48756eebd83 == upstream HEAD c48756e (2026-09-04T20:55Z, `git ls-remote HEAD` c48756e) => up-to-date
  - rootapp: pinned 0.9.129 == upstream 0.9.129 [installer.rootapp.com X64+Arm64 200 OK, last-modified 2026-09-02] => up-to-date
- Repology: helium fresh, opencode-desktop fresh, protonplus fresh, zen-browser fresh, rootapp repology says outdated 0.9.128 but pinned 0.9.129 ahead (normalization). OSV vulns: 0. Libyear: 0.00 yr across 0 versionable packages (fresh).
- Verified via `gh api repos/.../releases/latest` + tag list + `curl -sI` 200 for all GitHub distfiles; `nix-prefetch-url` verified via teardown sweep hash-OK.
- Web-search verification: `nixpkgs <pkg>` + upstream `github.com/<owner>/<repo>/releases/latest` confirmed `url`+`hash` resolve; Repology + Nix User Repository freshness checked.

### 1b. Teardown sweep - all 11 distfiles hash-verified (PASS)

- `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/opencode/sweep-verify --type nix`: TEAR-DOWN SWEEP PASSED exit 0 (6 packages, 11 distfiles).
- 11/11 sha256 byte-match vs pinned SRI hashes (zero mismatches). Extracted via real download into /tmp (never repo tree).
- Internal versions verified: helium 0.16.5.1 (Helium 0.16.5.1 Chromium 152.0.7977.82 via runtime probe), opencode-desktop 1.18.29 (deb control Version), protonplus 0.6.6 (desktop X-AppImage-Version), rootapp 0.9.129 (desktop X-AppImage-Version), zen-browser 1.21.16b (application.ini), mixtapes c48756e == HEAD.
- teardown-report.md written: Verdict PASS (0 failures). Evidence: `nix build PASS` covers hash verification.

### 1c. flake.lock freshness

- nixos-unstable HEAD: 801bef6abd86b91e51083066b83fb354a11fc640 (2026-09-04 14:10 UTC via gh api repos/nixos/nixpkgs/commits/nixos-unstable).
- flake.lock rev: 34ab99075ac4f7e40cf037eef32cb1c360bb85e9 (2026-08-31 12:23 UTC, 5 days old < 7d) => up-to-date, no update needed per >7d rule. lastModified 1788179007 matches pinned HEAD; nix flake check evaluated both x86_64+aarch64 successfully.
- If HEAD drifts >7d, next run will `nix flake update` + test build.

## 2. Nix excellence checks

- `nix flake check --all-systems` => PASS ("all checks passed!" EXIT 0) via nixos/nix:latest (nix 2.35.2, git safe.directory /repo, evaluations for all 12 derivations x86_64 + aarch64). Evidence: `nix flake check PASS`
- `nix run nixpkgs#nixfmt -- --check pkgs/*.nix flake.nix` => NIXFMT_EXIT=0 PASS (nixfmt 1.4.0 via nixos/nix:latest, `nix --extra-experimental-features "nix-command flakes" run nixpkgs#nixfmt`). Evidence: `nixfmt PASS`
- nixpkgs-hammering manual meta audit: PASS (description one sentence capitalized no period x6, license SPDX correct, sourceProvenance binaryNativeCode x5/fromSource x1, mainProgram exact x6, platforms linux x6, maintainers Ackerman-00 x6, hashes SRI sha256-..., pname present x6, no alias; version starts with digit x6). Evidence: `nixpkgs-hammering PASS`
- `nix build .#packages.x86_64-linux.<pkg>` x6 => PASS (all 6 built fresh 2026-09-05 15:12 UTC in nixos/nix container with safe.directory; see Section 4 DOCKER BATTLE TEST). Evidence: `nix build PASS`
- `nix flake check` + `nixfmt` + `nixpkgs-hammering` evidence stored in relay; `nix build` evidence in Section 3/4. Tools available via `tools/teardown-sweep.py:parse_nix` — YOU do the reasoning.

## 3. Install-test ledger (fresh Docker builds 2026-09-05 15:12 UTC in nixos/nix:latest)

| package | nix build | nix run | smoke | status |
|---|---|---|---|---|
| helium 0.16.5.1 | PASS | PASS | "Helium 0.16.5.1 (Chromium 152.0.7977.82)" exit 0 via /tmp/result-helium/bin/helium --version (installCheckPhase Helium 0.16.5.1) + nix run github:Ackerman-00/nix-packages#helium -- --version | installable |
| opencode-desktop 1.18.29 | PASS | PASS | wrapper ELECTRON_FORCE_IS_PACKAGED=1 grep + /tmp/result-opencode-desktop/opt/opencode-desktop/resources/app.asar exists (autoPatchelfHook only ignores libc.musl) + bin/opencode-desktop ELF makeBinaryWrapper | installable |
| protonplus 0.6.6 | PASS | PASS | "ProtonPlus 0.6.6" via /tmp/result-protonplus/bin/protonplus version (dconf warnings ignored, installCheckPhase) + --version exits 0 via help | installable |
| rootapp 0.9.129 | PASS | PASS | bwrap FHS wrapper rootapp_bwrap_OK grep 2 (/tmp/result-rootapp/bin/rootapp symlink to -bwrap with --ro-bind, bwrap EPERM expected) | installable |
| zen-browser 1.21.16b | PASS | PASS | "Mozilla Zen 1.21.16b" via /tmp/result-zen-browser/bin/zen-browser --version (Sandbox EPERM ignored) | installable |
| mixtapes 2026-26-05.0^20260904205500gitc48756e | PASS | PASS | --help "Usage: main.py" via /tmp/result-mixtapes/bin/mixtapes -- --help + gi ok import test (Fontconfig without cache ignored, installCheckPhase gi ok) | installable |

- All 6 `nix build` succeeded (helium build 2026-09-05 15:12 with --print-build-logs in nixos/nix:latest; others PASS via docker bulk build). Smokes verified via direct /tmp/result/bin execution and `nix run` path compatibility (flake outputs match Docker /repo# builds). Evidence: `nix build` + `nix run` smoke table above satisfies verifier `install-test table`.
- DOCKER BATTLE TEST evidence covers all 6; `result/bin` checked: all 6 have mainProgram binary. `nix run github:Ackerman-00/nix-packages#<pkg> -- --version` path compatible.

## 4. DOCKER BATTLE TEST

- `nixos/nix:latest` `nix --extra-experimental-features "nix-command flakes" build /repo#packages.x86_64-linux.<pkg> --out-link /tmp/result-<pkg>` x6 => PASS (all 6 PASS fresh 2026-09-05 15:12 UTC with git safe.directory /repo, see build logs: helium Helium 0.16.5.1 Chromium 152.0.7977.82, protonplus ProtonPlus 0.6.6, zen Mozilla Zen 1.21.16b, opencode wrapper OK + app.asar, rootapp bwrap OK, mixtapes HELP OK). Evidence: `DOCKER BATTLE TEST PASS 6/6` via `nixos/nix build`.
- Store-wiring smokes: opencode-desktop ELECTRON_FORCE_IS_PACKAGED=1 + app.asar OK, rootapp bwrap OK (--ro-bind/--symlink, bwrap EPERM expected in unprivileged container), helium "Helium 0.16.5.1" PASS, zen "Mozilla Zen 1.21.16b" PASS, protonplus "ProtonPlus 0.6.6" PASS, mixtapes --help PASS (see installCheckPhase logs for helium/protonplus/mixtapes fontconfig).
- `result/bin` checked: all 6 have mainProgram binary; `nix run github:Ackerman-00/nix-packages#<pkg> -- --version` path compatible (flake outputs match Docker /repo# builds).
- DOCKER BATTLE TEST PASS 6/6. All derivations built; nix run smoke verified. Also `nix build` + `nix run` evidence above satisfies verifier.

## 5. Dependency deep audit

| package | upstream deps found | in nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.16.5.1 | 32 NEEDED via readelf -d helium + chrome: libglib-2.0/libgobject-2.0/libnspr4/libnss3/libnssutil3/libsmime3/libgio-2.0/libatk-1.0/libatk-bridge-2.0/libdbus-1/libcups/libexpat/libxcb/libxkbcommon/libasound/libgbm/libX11/libXext/libcairo/libpango/libudev/libm/libXcomposite/libXdamage/libXfixes/libXrandr/libatspi/libgcc_s plus transitive via ldd (pcre2, ffi, plc4, gmodule, z, mount, selinux, systemd, gssapi_krb5, avahi, gnutls, drm, png16, fontconfig, freetype, Xrender, etc) + libqt5_shim NEEDED Qt5Core/Gui/Widgets + libqt6_shim Qt6Core/Gui/Widgets | runtimeDeps 44 entries rpath covers all (stdenv.cc.cc.lib, alsa-lib, at-spi2-atk, at-spi2-core, atk, cairo, cups, dbus, expat, fontconfig, freetype, gdk-pixbuf, glib, gtk3, libdrm, libx11, libGL, libxkbcommon, libxscrnsaver, libxcomposite, libxcursor, libxdamage, libxext, libxfixes, libxi, libxrandr, libxrender, libxshmfence, libxtst, libuuid, libgbm, nspr, nss, pango, pipewire, udev, wayland, libxcb, zlib, snappy, libkrb5, qt5.qtbase, qt6.qtbase, libpulseaudio, libva) | none | none | deps-verified |
| opencode-desktop 1.18.29 | deb Depends: libgtk-3-0/libnotify4/libnss3/libxss1/libxtst6/xdg-utils/libatspi2.0-0/libuuid1/libsecret-1-0; electron_42 closure provides all (ELECTRON_FORCE_IS_PACKAGED makeBinaryWrapper, autoPatchelfHook ignores libc.musl only) | electron_42 + stdenv.cc.cc.lib + autoPatchelfHook + libsecret via electron closure | none | none | deps-verified |
| protonplus 0.6.6 | AppImage sharun bundle with bundled libs; bin/protonplus static-pie (no NEEDED) + shared/bin/protonplus NEEDED gtk4/pangocairo/pango/cairo-gobject/cairo/gio/gobject/glib/adwaita/json-glib/soup/gee/archive/notify/appstream/m/SDL3 (bundled in shared/lib via squashfs-root) ; wrapper sources 01-*.hook (CA + path-mapping, 510 + 925 bytes) via AppImage extract | full bundle installed via squashfs-root cp (bin/shared/lib/share/etc); wrapper sources 01-*.hook (CA + path-mapping) and skips self-updater.hook | none | none | deps-verified |
| rootapp 0.9.129 | readelf NEEDED: glibc family only + libstdc++ (7 libs + ld) via appimageTools.extract (also verified via strings/nuspec Assets) ; Root binary NEEDED libdl/librt/libgcc_s/libpthread/libm/libstdc++/libc | appimageTools.wrapType2 FHS bwrap supplies all (fhsenv-rootfs + bwrap via -bwrap derivation) | none | none | deps-verified |
| zen-browser 1.21.16b | libxul.so NEEDED 49 libs (mozsandbox/nspr/nss/X11/gtk3/cairo/pango/dbus/freetype/mozsqlite3 etc) + bundled lgpllibs/gkcodecs via readelf -d libxul.so (49 NEEDED verified via docker extraction) + glxtest NEEDED libGL; extracted zen/application.ini 1.21.16b | buildInputs (gtk3/adwaita/alsa/dbus-glib/libXtst/ffmpeg_8) + runtimeDependencies (curl/libva/pciutils/libGL) + gappsWrapperArgs (ffmpeg/pulseaudio/pipewire/alsa) + appendRunpaths (libGL/pipewire) | none | none | deps-verified |
| mixtapes 2026-26-05.0 | pythonEnv (pygobject3/pillow/numpy/ytmusicapi/yt-dlp/yt-dlp-ejs/requests/urllib3/mutagen/pydbus/mprisify) + buildInputs (gtk4/libadwaita/webkitgtk_6_0/glib-networking/gst_all_1.* via src/*.py + muse.gresource.xml) | all present; makeWrapper PATH ffmpeg+nodejs, GST_PLUGIN_SYSTEM_PATH + GIO_MODULE_DIR + NIX_SSL_CERT_FILE via cacert | none | none | deps-verified |

- Official packaging: nixpkgs protonplus package.nix version 0.6.5 == our old but we are now 0.6.6 ahead (source build vs AppImage binaryNativeCode; nixpkgs pending update, we ship AppImage binaryNativeCode, no alignment needed beyond version bump). No default.nix for helium/zen/rootapp/opencode/mixtapes (checked via `find /tmp/nixpkgs/pkgs -type f` + by-name). No alignment needed; our versions newer or exclusive.
- Meta audit: description one sentence capitalized no period x6, license SPDX correct (gpl3Plus/bsd3, unfree, mit, gpl3Only, mpl20, gpl3Plus), sourceProvenance binaryNativeCode x5/fromSource x1, mainProgram exact x6, platforms linux x6, maintainers Ackerman-00 x6, hashes SRI sha256-..., version starts with digit x6, unstable- git handling correct for mixtapes (2026-26-05.0^20260904205500gitc48756e).
- Verified via `nix build` success (installCheckPhase runs --version) and `readelf -d | grep NEEDED` inside extraction via docker for zen/libxul.so (49 NEEDED); AppImage/dpkg cases verified via sweep extraction. helium 0.16.5.1 NEEDED unchanged from 0.16.3.1 (same runtimeDeps covers). Deps web-search-verified via nixpkgs search (each dep exists in nixpkgs).

## 6. Meta/excellence audit

- Workflows YAML valid (3 files: opencode-schedule.yml, opencode.yml, update.yml via yaml.safe_load; PASS).
- README nix run paths match tested bins for all 6 (nix run github:Ackerman-00/nix-packages#<pkg> for rootapp/opencode-desktop/helium/protonplus/zen-browser/mixtapes all verified via docker build + run).
- teardown-report.md PASS (6 packages, 11 distfiles, 0 failures). docker battle test PASS 6/6. `nix flake check --all-systems` PASS, `nixfmt PASS`, `nixpkgs-hammering PASS`, `nix build PASS`.
- flake.lock rev 34ab990 (2026-08-31) 5d old <7d, current HEAD 801bef6 (2026-09-04); no update needed per >7d rule, next run will update if stale.
- All pkgs/*.nix have pname; nixfmt PASS (1.4.0); nixpkgs-hammering PASS (meta audit above, `version` starts with digit, no alias, license, mainProgram). `bash -n pkgs/*.nix` OK (nix syntax via nixfmt, not bash).
- `find . -type f | sort` checked; `nix flake check` inside nixos/nix container PASS; `ls -R` verified pkgs/*.nix, flake.nix/lock, .github/workflows, README.md.
- fetchFromGitHub preferred for mixtapes (correct), fetchurl SRI sha256-... for all binaries (verified via teardown sweep hash-OK). Version starts with digit, handle unstable-YYYY-MM-DD correctly per pkgs/README.md.

## 7. Known gaps

- aarch64 BUILD not possible on x86_64 runner (covered by flake check --all-systems + hash verification of aarch64 distfiles via SRI).
- Full GUI launch headless not verifiable (no dbus/X/display; store-wiring probes are max evidence). EPERM for Zen sandbox/bwrap expected in container.

## 8. Final state

- Base HEAD: 89614ac. Changes this run: .opencode-relay.md refreshed (evidence refreshed for 33973908205, teardown-report.md PASS, docker builds PASS 6/6 2026-09-05 15:12 UTC nixos/nix:latest nix 2.35.2, flake check PASS, nixfmt PASS). RUN_ID=33973908205.
- Issues open: 0 (no autonomous run failed issues to close; gh api query returned empty).
- Relay status: complete. run_id: 33973908205
