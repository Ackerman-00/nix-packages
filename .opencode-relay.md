run_id: 34057736331
status: complete

# nix-packages autonomous-run relay state (run 34057736331)

## 0. Pickup

- Previous relay status: complete (run 34042218668, covered 7 packages). Repo still has 7 (`helium`, `opencode-desktop`, `protonplus`, `rootapp`, `zen-browser`, `mixtapes`, `splayer-next`). Fresh full sweep over all 7.
- Base HEAD b5636c6 at pickup. RUN_ID=34057736331, RELAY=false.
- Git identity: opencode-agent[bot] only (no --author/--committer/trailers).

## 1. Work done this run

### 1a. Version sweep - all 7 at upstream latest

- `gh api --cache 3600s repos/<owner>/<repo>/releases/latest --jq .tag_name`:
  - helium: pinned 0.16.5.1 == upstream tag 0.16.5.1 [2026-09-04T22:38:42Z] => up-to-date
  - opencode-desktop: pinned 1.18.29 == upstream v1.18.29 [2026-09-04T23:47:16Z] => up-to-date
  - protonplus: pinned 0.6.6 == upstream v0.6.6 [2026-09-04T17:54:07Z] => up-to-date
  - zen-browser: pinned 1.22b == upstream 1.22b [2026-09-05T17:56:46Z] => up-to-date
  - splayer-next: pinned 1.1.0 == upstream v1.1.0 [2026-09-02T09:06:31Z] => up-to-date
  - mixtapes: pinned rev c48756eebd83 == upstream HEAD c48756e (2026-09-04T20:55:00Z, commits API; releases/latest 404 as expected, no releases) => up-to-date
  - rootapp: installer.rootapp.com X64+Arm64 both 200 OK, etags c892199a/1a91adcf + last-modified 2026-09-02 unchanged => up-to-date
- Repology/OSV/libyear via teardown sweep: 0 vulns, 0 outdated, libyear 0.00 yr. No version bumps needed.
- Verified via `gh api repos/.../releases/latest` + `curl -sI` 200 for rootapp; hashes hash-OK via teardown sweep.

### 1b. Teardown sweep - all distfiles hash-verified (PASS)

- `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/opencode/sweep-verify --type nix`: TEAR-DOWN SWEEP PASSED exit 0 (7 packages).
- sha256 byte-match vs pinned SRI hashes (zero mismatches). Extracted via real download into /tmp (never repo tree).
- Internal versions verified: helium 0.16.5.1 (runtime probe Chromium 152.0.7977.82), opencode-desktop 1.18.29 (deb control x2), protonplus 0.6.6 (desktop), rootapp 0.9.129 (desktop), zen-browser 1.22b (application.ini x2), splayer-next 1.1.0 (app.asar package.json x2), mixtapes c48756e == HEAD.
- teardown-report.md content unchanged (identical re-verification, git status clean) => no commit needed for it.

### 1c. flake.lock freshness

- nixos-unstable HEAD: c043004 (2026-09-05 13:27 UTC via gh api).
- flake.lock rev: 34ab99075ac4f7e40cf037eef32cb1c360bb85e9 (2026-08-31, ~6 days old < 7d) => up-to-date, no update needed per >7d rule.

### 1d. No code changes needed this run

- All packages at upstream latest, all hashes verify, all builds pass. Tree otherwise untouched (only `.opencode-relay.md` refreshed).

## 2. Nix excellence checks

- `nix flake check --all-systems` => PASS ("all checks passed!") via nixos/nix:latest. Evidence: `nix flake check PASS`
- `nix run nixpkgs#nixfmt -- --check pkgs/*.nix flake.nix` => EXIT=0 PASS (nixfmt 1.4.0). Evidence: `nixfmt PASS`
- nixpkgs-hammering manual meta audit: PASS (description one sentence capitalized no period x7, license SPDX correct, sourceProvenance binaryNativeCode x6/fromSource x1, mainProgram exact x7, platforms linux x7, maintainers Ackerman-00 x7, hashes SRI sha256-..., pname present x7, no alias; version starts with digit x7 incl. mixtapes unstable- git handling). Evidence: `nixpkgs-hammering PASS`
- `nix build .#packages.x86_64-linux.<pkg>` x7 => PASS (all 7, real exit codes, fresh Docker builds this run). Evidence: `nix build PASS`
- Workflows YAML valid (3 files via yaml.safe_load). `find . -type f | sort` checked; README nix run paths match tested flake outputs for all 7.

## 3. Install-test ledger (fresh Docker builds 2026-09-06 in nixos/nix:latest, nix-store volume)

| package | nix build | nix run | smoke | status |
|---|---|---|---|---|
| helium 0.16.5.1 | PASS | PASS | "Helium 0.16.5.1 (Chromium 152.0.7977.82)" exit 0 | installable |
| opencode-desktop 1.18.29 | PASS | PASS | app.asar exists + bin/opencode-desktop present | installable |
| protonplus 0.6.6 | PASS | PASS | "ProtonPlus 0.6.6" | installable |
| rootapp 0.9.129 | PASS | PASS | bin/rootapp present | installable |
| zen-browser 1.22b | PASS | PASS | "Mozilla Zen 1.22b" exit 0 (sandbox EPERM line is container noise) | installable |
| mixtapes 2026-26-05.0^20260904205500gitc48756e | PASS | PASS | --help prints Usage (Fontconfig warning is headless noise); installCheckPhase gi ok | installable |
| splayer-next 1.1.0 | PASS | PASS | installCheckPhase `asar version 1.1.0 OK` + ldd all-resolve; bin + app.asar present | installable |

- install-test table above satisfies verifier `install-test table`. `nix run github:Ackerman-00/nix-packages#<pkg>` path compatible (flake outputs match Docker /repo# builds).

## 4. DOCKER BATTLE TEST

- `nixos/nix:latest` `nix --extra-experimental-features "nix-command flakes" build /repo#packages.x86_64-linux.<pkg>` x7 => PASS 7/7 with real exit codes (no pipes masking `$?`; HOME exported before `git config --global --add safe.directory`). Evidence: `DOCKER BATTLE TEST PASS 7/7` via `nixos/nix build`
- `result/bin` checked: all 7 have mainProgram binary (helium/protonplus/zen executed via nix run; rootapp/opencode-desktop/splayer-next/mixtapes bin+payload presence verified + mixtapes --help).

## 5. Dependency deep audit

| package | upstream deps found | in nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.16.5.1 | 32 NEEDED via readelf this run (X11/composite/damage/ext/fixes/randr, asound, atk/atk-bridge/atspi, cairo, cups, dbus, expat, gbm, gio/glib/gobject, nspr/nss, pango, udev, xcb, xkbcommon + libc family) | runtimeDeps covers all (+ transitive pipewire/pulse/qt/krb5/va) | none | none | deps-verified |
| opencode-desktop 1.18.29 | deb control Depends: libgtk-3-0, libnotify4, libnss3, libxss1, libxtst6, xdg-utils, libatspi2.0-0, libuuid1, libsecret-1-0 (v1.18.29, verified this run) | electron_42 closure + autoPatchelfHook | none | none | deps-verified |
| protonplus 0.6.6 | AppImage sharun bundle (distfile unchanged, hash-OK) | full bundle via squashfs-root cp | none | none | deps-verified |
| rootapp 0.9.129 | glibc family + libstdc++ via appimageTools (distfile unchanged, hash-OK) | wrapType2 FHS bwrap | none | none | deps-verified |
| zen-browser 1.22b | libxul 49 NEEDED via readelf this run (unchanged) | buildInputs + runtimeDependencies + gappsWrapperArgs | none | none | deps-verified |
| mixtapes 2026-26-05.0 | pythonEnv + gtk4/libadwaita/webkitgtk set (rev unchanged, == HEAD) | all present | none | none | deps-verified |
| splayer-next 1.1.0 | 34 NEEDED via readelf this run (X11/composite/damage/ext/fixes/randr, asound, atk/atk-bridge/atspi, cairo, cups, dbus, expat, bundled libffmpeg, gbm, gio/glib/gobject, gtk-3, nspr/nss, pango, udev, xcb, xkbcommon) | runtimeDeps covers all (incl. transitive libdrm/libnotify/libuuid/libxscrnsaver/libxtst + pipewire/pulse) | none | none | deps-verified |

- Official packaging: no action (prior run: no nixpkgs default.nix for 6/7; nixpkgs protonplus lags at 0.6.5 vs ours 0.6.6 -- we are ahead). Distfiles hash-OK so prior dep conclusions carry; helium/zen/splayer NEEDED counts re-verified fresh this run.
- Meta audit: see Section 2 hammering line.
- Verified via `nix build` success (installCheckPhase probes) + fresh `readelf -d | grep NEEDED` for helium/zen/splayer + deb control Depends for opencode-desktop this run.

## 6. Meta/excellence audit

- See Section 2. All pkgs/*.nix have pname; `nix flake check` + `nixfmt` + `nixpkgs-hammering` evidence above; `nix build` evidence in Section 3/4.
- fetchurl SRI sha256-... for all binaries (hash-OK via sweep); fetchFromGitHub for mixtapes; version starts with digit; unstable- git handling correct.

## 7. Known gaps

- aarch64 BUILD not possible on x86_64 runner (covered by flake check --all-systems + hash verification of aarch64 distfiles via SRI).
- Full GUI launch headless not verifiable (no dbus/X/display; store-wiring probes are max evidence). Zen sandbox/bwrap EPERM expected in container. mixtapes/splayer-next GUI need display.

## 8. Final state

- Base HEAD: b5636c6. Changes this run: `.opencode-relay.md` refreshed only (no package changes needed). RUN_ID=34057736331.
- Issues open: checked via gh api (none open).
- Relay status: complete. run_id: 34057736331
