# nix-packages autonomous-run relay state

status: complete
run_id: 31999411259

## 0. Pickup

- Previous relay (run 31979546571) was `status: complete`; nothing unfinished to continue. Started fresh on main @ 6e46044 (verified `git fetch origin && git pull --rebase origin main` = up to date, working tree clean).
- Runner nix was 2.16.1 (fresh runner, old again). First build attempt hit "cached failure of attribute" on helium; root cause was nix 2.16.1 too old for the locked nixpkgs (rev d407951, requires >= 2.18). Upgraded to 2.34.8 via `sudo env "PATH=$PATH" nix upgrade-nix`. Runner-only change, not committed. After upgrade the flake evaluates and builds cleanly.
- Docker 28.0.4 verified (hello-world exit 0); ubuntu:24.04 pulled fresh for all container tests (new `docker run --rm` container per test).
- Inventory discovered from flake.nix (NOT memorized): x86_64-linux = helium, protonplus, zen-browser, rootapp, opencode-desktop; aarch64-linux = helium, protonplus, zen-browser. `nix eval --json .#packages.x86_64-linux --apply builtins.attrNames` = ["default","helium","opencode-desktop","protonplus","rootapp","zen-browser"].

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): last run 31983608307 (2026-08-17 00:59) SUCCESS, committed nothing (no package changed). No failed recent runs.
- opencode-schedule: all recent runs green (previous maintenance 31979546571 success). One historical failure (31755867883, 2026-08-14) predates the relay system; not current.
- No open issues, no open PRs (both APIs queried, empty).

## 2. Changes this run

- ZERO package changes. Every package is at its current upstream version with byte-matching hashes (re-verified with `--refresh`). No churn commits generated. All packages rebuilt from scratch on an EMPTY store (this runner's store was empty at start) with `--refresh`; all installCheckPhase / smoke tests ran on the final store paths.

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| helium | 0.15.5.1 | 0.15.5.1 (gh releases/latest + 5 recent tags) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (strip v) | up-to-date |
| zen-browser | 1.21.14b | 1.21.14b (gh releases/latest + web: neowin/newreleases confirm 08-13) | up-to-date |
| opencode-desktop | v1.18.18 | v1.18.18 (gh releases/latest) | up-to-date |
| rootapp | latest (fixed URL) | hash unchanged after `--refresh` re-prefetch; real version torn from extracted AppImage = 0.9.126 (Root.desktop X-AppImage-Version + Comment "Root 0.9.126") | up-to-date |

- All 8 pinned SRI hashes re-prefetched with `--refresh` and byte-match (helium x64+arm, protonplus x64+arm, zen x64+arm, opencode deb, rootapp fixed URL). All 8 source URLs curl -sI = 200.
- Real versions independently re-torn this run: rootapp Root.desktop X-AppImage-Version=0.9.126; opencode deb control Version: 1.18.18; zen application.ini Version=1.21.14b BuildID=20260811103047; helium `--version` = "Helium 0.15.5.1 (Chromium 151.0.7922.137)"; protonplus `version` = "ProtonPlus 0.6.4"; opencode electron `--no-sandbox --version` = "v42.5.1".
- update.yml source URLs in sync with pkgs/*.nix (5 blocks, no drift). README package list matches flake.nix exactly.

## 4. Build/install verification ledger

| package | build (x86_64) | aarch64 eval | binary smoke | status |
|---|---|---|---|---|
| helium 0.15.5.1 | PASS (fresh --refresh, store /nix/store/4lph1w68...) | PASS (eval .name = "helium-0.15.5.1") | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0 | pass |
| protonplus | PASS (fresh, /nix/store/bjmcfaj5...) | PASS | `version` -> "ProtonPlus 0.6.4", exit 0 | pass |
| zen-browser | PASS (fresh, /nix/store/v78acsd...) | PASS | `--version` -> "Mozilla Zen 1.21.14b", exit 0 | pass |
| opencode-desktop | PASS (fresh, /nix/store/vi5mdch...) | n/a (x86_64 only) | electron `--no-sandbox --version` -> "v42.5.1", exit 0; app wrapper launches electron main headless, reaches X11 init then fails only at no $DISPLAY (GUI gap) | pass (GUI launch gap) |
| rootapp | PASS (fresh, /nix/store/l45vk28...) | n/a (x86_64 only) | "Starting application" / "Using profile default" under --privileged, exit 0 | pass (needs userns) |

- `nix flake check --all-systems --no-build` PASS on main (all x86_64 derivations + aarch64 derivations evaluated green). aarch64 BUILD impossible on x86_64 runner = accepted named gap.
- Store paths byte-identical to previous run's record (4lph1w68 / bjmcfaj5 / v78acsd / l45vk28 / vi5mdch) - deterministic, rebuilt from scratch on an empty store, not stale cache.
- sourceProvenance = [binaryNativeCode] on all 5 packages (`nix eval --no-eval-cache`).

## 5. Dependency audit (100% of inventory; artifacts re-torn this run)

| package | upstream deps found (this run) | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.5.1 | 30 NEEDED sonames re-read from fresh-extracted tarball (X11 family, asound, atk/atk-bridge/atspi, cairo, cups, dbus, expat, gbm, gcc_s, glib/gio/gobject, nspr/nss/nssutil/smime, pango, udev, xcb, xkbcommon); upstream interpreter = /lib64/ld-linux-x86-64.so.2 | all in runtimeDeps (stdenv.cc.cc.lib covers gcc_s/libstdc++, alsa-lib, at-spi2-atk, at-spi2-core, atk, cairo, cups, dbus, expat, libgbm, glib, nspr, nss, pango, udev, libx11/libxcb/... family, libxkbcommon) | none | none (extra-but-harmless dlopen coverage) | deps-verified (nix loader --list in clean container: ZERO "not found" on helium + crashpad handler; patchelf --set-interpreter confirmed pointing at nix glibc loader) |
| protonplus | static-pie bun binary (readelf NEEDED count = 0) + bundled bin/shared/lib/share/etc; hooks 01-check-ca-certs + 01-path-mapping-hardcoded | binary bundles everything; wrapper sources 01-* hooks | none | none | deps-verified (in container with host CA store: /tmp/.___host-certs/ca-certificates.crt symlink created; /tmp/pKE -> lib, /tmp/3J=1Z -> share symlinks created; without store, app prints "WARNING: Cannot find CA Certificates in host!" proving hook is load-bearing) |
| zen-browser | libxul NEEDED re-read from fresh-extracted tarball: X11 family (incl. libX11-xcb, libxcb-shm), asound, atk, cairo, dbus, fontconfig, freetype, gtk3/gdk-pixbuf, pango, nspr/nss/nssutil/smime/ssl, gcc_s, stdc++; bundles own libmozavcodec/libmozavutil/libgkcodecs (Firefox codecs - system ffmpeg not needed) | gtk3, alsa-lib, dbus-glib, libXtst, ffmpeg_8, autoPatchelfHook, runtimeDependencies, appendRunpaths | none | none | deps-verified (nix loader --list on zen + libxul in container: ZERO "not found"; interpreter = nix glibc loader; "Added support for FFmpeg 63" in release notes refers to bundled codecs, not system ffmpeg - no packaging change needed) |
| opencode-desktop | locked electron_42 (42.5.1) + 7 .node sidecars (4 glibc: watcher, pty, msgpackr x2; 3 musl twins) | electron_42 via callPackage; autoPatchelfIgnoreMissingDeps for musl variants | none (musl twins intentionally unloadable on glibc - glibc twins resolve) | none | deps-verified (nix loader on 4 glibc sidecars in container: ZERO "not found"; electron --version v42.5.1) |
| rootapp | .NET self-contained (usr/bin/Root ELF, interpreter /lib64 inside FHS bwrap sandbox; DotNetBrowser, libSkiaSharp, libHarfBuzzSharp, libonigwrap, libuiohook bundled) | wrapType2 FHS | none | none | deps-verified (medium confidence - FHS self-contained; runs in container under --privileged) |

- No upstream change for any package since the last audit (all hashes byte-identical with --refresh re-prefetch), so prior derivations stand; this run independently re-tore helium + zen NEEDED lists, the opencode deb control, and the rootapp AppImage desktop from the CURRENT artifacts.

## 6. Container runtime test ledger (fresh `docker run --rm` per test, base ubuntu:24.04, /nix/store mounted ro)

| package | nix build | container ldd | container smoke | status |
|---|---|---|---|---|
| helium 0.15.5.1 | PASS | ZERO "not found" (nix glibc-2.42 loader --list with wrapper LD_LIBRARY_PATH, 43 entries) on helium + helium_crashpad_handler | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0; interpreter = nix glibc loader | verified |
| protonplus | PASS | static pie, readelf NEEDED = 0 | `version` -> "ProtonPlus 0.6.4", exit 0; CA-cert hook + path-mapping hooks fire (all 3 symlinks created) | verified |
| zen-browser | PASS | ZERO "not found" (nix loader on zen + libxul) | `--version` -> "Mozilla Zen 1.21.14b", exit 0; interpreter = nix glibc loader | verified |
| opencode-desktop | PASS | ZERO "not found" (nix loader on 4 glibc .node sidecars) | electron `--no-sandbox --version` -> "v42.5.1", exit 0; app wrapper launches headless, X11 init fails only at no $DISPLAY (runs 20s until timeout) | verified (GUI launch gap) |
| rootapp | PASS | ZERO (bwrap FHS rootfs provides /lib64) | starts ("Starting application", "Using profile default") under --privileged, exit 0 | verified (needs userns) |

- Every container test ran in a brand-new container (fresh `docker run --rm` per package), store paths mounted ro from THIS run's fresh builds.
- Named gaps: aarch64 BUILD impossible on x86_64 runner (eval only - all 3 aarch64 packages evaluate green); GUI apps full window rendering needs a display (headless smoke only); rootapp needs user-namespace support (--privileged simulates a real machine); protonplus CA-cert hook only fires when a host cert store exists (verified both with and without one).
- NOTE: ubuntu ldd would report GLIBC_ABI_DT_X86_64_PLT "not found" on nix glibc-2.42 libs (ubuntu glibc 2.39 loader too old) - loader-mismatch false positive; authoritative check uses the nix interpreter (`ld-linux --list`), which reports ZERO "not found" everywhere.

## 7. Adversarial audit

- Attack checked (version staleness): could I be trusting the file's versions? No - gh api releases/latest + 5-recent-tags queried independently for all 4 GitHub-release packages this run; rootapp re-prefetched with --refresh and version torn from the extracted AppImage (0.9.126).
- Attack checked (hash staleness): could pinned hashes be stale? No - all 8 re-prefetched with --refresh and byte-match.
- Attack checked (container masking missing deps): ubuntu:24.04 has no nix closure or buildInputs; only libs available are from each package's own closure via the /nix/store mount; authoritative nix-loader check reports ZERO missing everywhere.
- Attack checked (stale cache): this runner's store was EMPTY at start (all packages "NOT-BUILT"); every package was rebuilt from scratch with --refresh and produced store paths byte-identical to the previous run's record - deterministic, not cached.
- Attack checked (interpreter not fixed): fresh-extracted upstream helium + zen tarballs confirmed upstream interpreter = /lib64/ld-linux-x86-64.so.2; the packages' patchelf fixes it to the nix glibc loader (verified via patchelf --print-interpreter on the BUILT store paths).
- update.yml/pkgs URL sync + README/flake inventory sync confirmed.

## 8. What remains

- Nothing. Repo is current, all 5 packages build and pass clean-container runtime tests, all hashes match upstream byte-for-byte, no open issues/PRs, no failing Actions runs.

## 9. Final state

- main @ 6e46044 + this relay update. Working tree clean (`git status --porcelain` empty).
- README.md package list matches flake.nix (rootapp, opencode-desktop, helium, protonplus, zen-browser).
