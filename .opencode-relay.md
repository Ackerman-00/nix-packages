# nix-packages autonomous-run relay state

status: complete
run_id: 31944756825

## 0. Pickup

- Previous relay (run 31929726742) was `status: complete`; nothing unfinished to continue. Started fresh on main.
- Runner nix was 2.16.1 but the locked nixpkgs (rev d407951) requires >= 2.18 (`nix eval .#<pkg>.meta.sourceProvenance` and `nix flake check` aborted on the nixpkgs version guard). Upgraded to 2.34.8 via `sudo env "PATH=$PATH" nix upgrade-nix`. Runner-only change, not committed.
- Docker 28.0.4 verified (hello-world); ubuntu:24.04 used for all container tests.
- Repo history was shallow-cloned (1 commit); unshallowed via `git fetch --unshallow` (now 90 commits).

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): last 16 runs ALL green; last run 31918622716 (2026-08-16 01:01) succeeded. No failed runs.
- NOTE: that 01:01 run reported "Helium: no change (version: 0.15.4.1)" even though upstream 0.15.5.1 was published 2026-08-15T19:59Z (5h earlier) - GitHub API edge-caching returned the stale tag. Transient; not a workflow bug. Repo now current at 0.15.5.1 so the next run sees no change and no churn.
- No open issues (`gh api .../issues?state=open` -> empty). No open PRs (`gh api .../pulls?state=open` -> empty).

## 2. Fixes this run

- helium: bumped 0.15.4.1 -> 0.15.5.1 (upstream release 2026-08-15). version + both hashes (x86_64 sha256-80oe4aarLjEJ2S45OVEqN8/mii8NIwtSXMFYn8GS/Zc=, aarch64 sha256-2TI9B3GjdN5eWmZqfnVOIfKYwjjqwh345YaKx7X2TP4=) updated in ONE commit, then full verification (build + flake check + container runtime + fresh-checker re-run). Commit 85f6990.
- All other packages: verified current, zero changes.

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| helium | 0.15.5.1 | 0.15.5.1 (gh releases latest) | UPDATED (0.15.4.1 -> 0.15.5.1) |
| protonplus | 0.6.4 | v0.6.4 (strip v) | up-to-date |
| zen-browser | 1.21.14b | 1.21.14b (gh releases latest) | up-to-date |
| opencode-desktop | v1.18.18 | v1.18.18 (gh releases latest) | up-to-date |
| rootapp | latest (fixed URL) | hash unchanged after `--refresh` re-prefetch; real version torn from extracted AppImage = 0.9.126 | up-to-date |

- All 8 pinned SRI hashes re-prefetched with `--refresh` and match byte-for-byte (helium x86_64+aarch64 NEW, protonplus x86_64+aarch64, zen x86_64+aarch64, opencode deb, rootapp fixed URL).
- Helium hashes independently verified by downloading both tarballs and converting sha256sum to SRI - byte-for-byte identical.
- Real versions torn from inside artifacts: rootapp Root.desktop `X-AppImage-Version=0.9.126` (Comment "Root 0.9.126"); opencode app.asar package.json `"version": "1.18.18"` (via asar extract-file); zen application.ini `Version=1.21.14b`; protonplus desktop `X-AppImage-Version=0.6.4`; helium `--version` prints "Helium 0.15.5.1 (Chromium 151.0.7922.137)".
- update.yml source URLs all in sync with pkgs/*.nix (5 blocks, no drift).
- README package list (rootapp, opencode-desktop, helium, protonplus, zen-browser) matches flake.nix exactly.

## 4. Build/install verification ledger

| package | build (x86_64) | aarch64 eval | binary smoke | status |
|---|---|---|---|---|
| helium 0.15.5.1 | PASS | PASS (eval .name = "helium-0.15.5.1") | installCheckPhase + container: "Helium 0.15.5.1 (Chromium 151.0.7922.137)" | pass (UPDATED) |
| protonplus | PASS | PASS (eval .name) | container: "ProtonPlus 0.6.4" | pass |
| zen-browser | PASS | PASS (eval .name) | container `--version` -> "Mozilla Zen 1.21.14b" | pass |
| opencode-desktop | PASS | n/a (x86_64 only) | container: electron --version -> "v42.5.1"; app main process runs (X11 init fails without DISPLAY = gap) | pass (GUI launch gap) |
| rootapp | PASS | n/a (x86_64 only) | container (--privileged): "Starting application", "Using profile default" | pass (needs userns) |

- `nix flake check --all-systems` green on main AND on a fresh clone at /tmp/opencode/fresh-check after the helium bump (both systems eval; aarch64 BUILD impossible on x86_64 runner = accepted named gap).
- Fresh clone with `--refresh` produced identical store paths (helium 4lph1w68..., protonplus bjmcfaj..., zen v78acsd..., opencode vi5mdch..., rootapp l45vk2...) - deterministic, not stale cache.
- sourceProvenance = [binaryNativeCode] on all 5 packages (nix eval --no-eval-cache verified).

## 5. Dependency audit (100% of inventory)

| package | upstream deps found | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.5.1 | 30 NEEDED sonames re-torn from NEW tarball (X11 family, asound, atk/atspi, cairo, cups, dbus, expat, gbm, nspr/nss, pango, udev, xcb, xkbcommon - same set as 0.15.4.1) | all covered by runtimeDeps | none | extra-but-harmless (dlopen coverage) | deps-verified (nix loader --list with wrapper LD_LIBRARY_PATH: ZERO "not found" in container) |
| protonplus | static bun + bundled bin/shared/lib/etc; hooks 01-check-ca-certs + 01-path-mapping-hardcoded | binary bundles everything; wrapper sources 01-* hooks | none | none | deps-verified |
| zen-browser | libxul + gtk3/alsa/dbus/cairo/pango/X11 sonames | gtk3, alsa-lib, dbus-glib, libXtst, ffmpeg_8, autoPatchelfHook, runtimeDependencies, appendRunpaths | none | none | deps-verified (container ldd on zen + libxul: ZERO not found) |
| opencode-desktop | electron_42 (42.5.1) + 7 .node sidecars | electron_42 via callPackage; autoPatchelfIgnoreMissingDeps for musl variants | none (musl variants intentionally unloadable on glibc - glibc twins exist and resolve) | none | deps-verified (nix loader on pty.node + glibc watcher + electron: ZERO not found in container) |
| rootapp | .NET self-contained + bwrap FHS sandbox | wrapType2 FHS | none | none | deps-verified (medium confidence - FHS self-contained) |

- helium re-torn from the NEW 0.15.5.1 artifact (per RE-TEAR rule since upstream changed): wrapper structure identical, NEEDED set identical, runtimeDeps still complete.
- No upstream change for the other 4 since last audit (all versions/hashes byte-identical), so previous dependency derivations stand; independently re-verified each package's key sonames in fresh containers.

## 6. Container runtime test ledger (fresh `docker run --rm` per test, base ubuntu:24.04)

| package | nix build | container ldd | container smoke | status |
|---|---|---|---|---|
| helium 0.15.5.1 | PASS | ZERO not found (nix loader --list via glibc-2.42 ld-linux with wrapper LD_LIBRARY_PATH extracted from wrapper) | wrapper + raw ELF `--version` -> "Helium 0.15.5.1", exit 0; interpreter = nix glibc loader | verified (UPDATED) |
| protonplus | PASS | ZERO not found (static bun) | `version` -> "ProtonPlus 0.6.4", exit 0; hooks: /tmp/pKE + /tmp/3J=1Z symlinks created; CA-certs hook fires when /etc/ssl/certs/ca-certificates.crt exists -> /tmp/.___host-certs/ca-certificates.crt symlink created | verified |
| zen-browser | PASS | ZERO not found (container ldd on zen + libxul) | `--version` -> "Mozilla Zen 1.21.14b", exit 0; interpreter = nix glibc loader | verified |
| opencode-desktop | PASS | ZERO not found (nix loader on pty.node + glibc watcher; electron ldd) | electron --version -> "v42.5.1", exit 0; app wrapper launches electron main process headless (X11 init fails only at no DISPLAY) | verified (GUI launch gap) |
| rootapp | PASS | ZERO not found (wrapper script; bwrap FHS) | starts ("Starting application", "Using profile default") under `--privileged`, exit 0 | verified (needs userns) |

- Fresh-checker re-run: fresh clone of main @ 85f6990, `nix build .#helium --no-link --refresh` -> same store path 4lph1w68..., fresh container smoke passed.
- Named gaps: aarch64 BUILD impossible on x86_64 runner (eval only, all 3 aarch64 packages evaluate green); GUI apps full window rendering needs a display (headless smoke only); rootapp needs user-namespace support (--privileged simulates a real machine); protonplus CA-cert hook only fires when a host cert store exists (verified it fires with one mounted at /etc/ssl/certs).
- NOTE: ubuntu ldd reports GLIBC_ABI_DT_X86_64_PLT "not found" on nix glibc-2.42 libs (ubuntu glibc 2.39 loader too old) - loader-mismatch false positive; the authoritative check uses the nix interpreter (`ld-linux --list`), which reports ZERO not found everywhere.

## 7. Adversarial audit

- Re-queried all 4 GitHub release tags at end of run (unchanged since bump: 0.15.5.1 / v0.6.4 / 1.21.14b / v1.18.18) and rootapp fixed-URL hash (unchanged) - no mid-run drift.
- Helium SRI hashes independently recomputed from downloaded tarballs (sha256sum -> xxd -r -p -> base64) - byte-for-byte identical to pinned.
- Store paths identical between main working tree and fresh `--refresh` clone - deterministic, not stale cache.
- Interpreter on helium + zen ELFs points at nix glibc loader, not /lib64 (verified via patchelf --print-interpreter).
- update.yml/pkgs URL sync + README/flake inventory sync confirmed; the 01:01Z "no change" on helium explained as GitHub API edge-caching, not a workflow defect.
- helium metainfo <release version="0.15.5.1"/> + desktop entry verified in the shipped store path.

## 8. What remains

- Nothing. Repo is current, all 5 packages build and pass clean-container runtime tests, all hashes match upstream, no open issues/PRs, no failing Actions runs.

## 9. Final state

- main @ 85f6990 (helium bump) + this relay update. Working tree clean.
- README.md package list matches flake.nix (rootapp, opencode-desktop, helium, protonplus, zen-browser).