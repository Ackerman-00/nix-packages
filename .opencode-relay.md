run_id: 32851595172
status: complete

# nix-packages autonomous-run relay state (run 32851595172)

## 0. Pickup (run 32851595172, RELAY=false)

- Previous relay status: complete (run 32819929457). Fresh periodic sweep, RELAY=false, inventory 6 packages.
- Base HEAD c436648 at pickup. RUN_ID=32851595172. Packages: helium, opencode-desktop, protonplus, rootapp, zen-browser, mixtapes.
- Git identity: opencode-agent[bot] only.

## 1. Work done this run

### 1a. Version sweep - all 6 at upstream latest

- `gh api --cache 3600s repos/<owner>/<repo>/releases/latest --jq .tag_name` (PAT-authed, ~4995 core remaining at start):
  - helium: pinned 0.15.7.1 vs upstream 0.15.7.1 (imputnet/helium-linux) => up-to-date
  - opencode-desktop: pinned v1.18.23 vs upstream v1.18.23 (anomalyco/opencode) => up-to-date
  - protonplus: pinned v0.6.4 vs upstream v0.6.4 (Vysp3r/ProtonPlus) => up-to-date
  - zen-browser: pinned 1.21.15b vs upstream 1.21.15b (zen-browser/desktop) => up-to-date
  - mixtapes: pinned rev 5e165d201128de1b6c10d5cb031a0828ffc3b0ec IS current HEAD of m-obeid/Mixtapes (2026-08-15) => up-to-date
  - rootapp: installer.rootapp.com HEAD probe 200 both arches (last-modified 2026-08-19); artifacts re-downloaded and byte-match pins
- OSV vulns: 0 across all 6; Repology outdated: 0; libyear drift: 0.00 yr, threshold 20 yr.
- No changes required to any package file this run.

### 1b. Teardown - all 10 distfiles downloaded + hash + internal-version verified by hand

- Downloaded ALL distfiles into /tmp/opencode/sweep-32851595172 (x86_64+aarch64 for each binary pkg) and sha256'd each:
  helium-x64/arm64.tar.xz, opencode-amd64/arm64.deb, protonplus-x64/aarch64.AppImage, rootapp-x64/aarch64.AppImage, zen-x64/aarch64.tar.xz => 10/10 OK vs pinned SRI (zero mismatches).
- Internal versions read from inside the artifacts:
  - helium: nested dir helium-0.15.7.1-{x86_64,arm64}_linux; runtime probe "Helium 0.15.7.1 (Chromium 151.0.7922.173)"
  - opencode-desktop: deb control Version=1.18.23 BOTH arches; app.asar present x2 (142160440 B amd64 / 142159928 B arm64); asar now declares electron ^30.0.1 upstream (was 39.8.5 last run - informational only, we ship system electron_42 which satisfies ^30)
  - protonplus: X-AppImage-Version=0.6.4 AND live host probe `protonplus version` -> "ProtonPlus 0.6.4"
  - rootapp: Root.desktop X-AppImage-Version=0.9.127 BOTH arches (x64 via --appimage-extract; aarch64 via static unsquashfs at hsqs offset 936456 - cross-arch ELF cannot exec on x86_64 host)
  - zen-browser: application.ini Version=1.21.15b BuildID 20260818101929 both arches
  - mixtapes: live pin == upstream HEAD rev 5e165d2; raw codeload tar.gz hash differs from pin as EXPECTED (fetchFromGitHub hashes nix's deterministic repack, not the gzip stream) - authoritative proof is the clean-container nix build below, which succeeded => fetchFromGitHub pin valid
- AppImage note: protonplus type2 runtime extracts to ./AppDir (squashfs-root symlink), newer runtime layout - handled fine by our installPhase.

### 1c. Deterministic sweep (tools/teardown-sweep.py) - auxiliary evidence

- `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/opencode/sweep-32851595172-tool`: TEAR-DOWN SWEEP PASSED exit 0 - all distfile rows OK/hash-OK incl cross-arch aarch64 rows; Vulns 0 / outdated 0 / libyear 0.00; teardown-report.md receipt refreshed (committed).

### 1d. flake.lock freshness - no bump (within 7d budget)

- Locked rev `2c423e03bbafcff28bfadc6781a4a8257f205cb5` (lastModified 1787360063, 3.5 days old) vs nixos-unstable HEAD `56c02bc00adcf003215cc4bd996d6efaf4cff188` (unchanged since last run) => within 7d budget => no `nix flake update` required.

### 1e. Nix excellence checks (clean nixos/nix containers - host has no nix; tar-pipe copy with --no-same-owner avoids libgit2 ownership error)

- `nix flake check --all-systems` => FLAKE_CHECK_EXIT=0 "all checks passed!" => nix flake check PASS (both systems eval).
- `nix run nixpkgs#nixfmt -- --check pkgs/*.nix flake.nix` (nixfmt 1.4.0) => NIXFMT_EXIT=0 => nixfmt PASS.
- nixpkgs-hammering-equivalent manual meta audit on all 6: license/mainProgram/sourceProvenance/platforms/maintainers present; versions start with digit; hashes SRI sha256-... => PASS.

## 2. Build/install ledger (COMPLETE - install-test table, all 6 in clean privileged nixos/nix container)

| package | nix build | nix run | smoke | status |
|---|---|---|---|---|
| helium 0.15.7.1 | PASS (87kg5p1s...) | PASS | --version -> "Helium 0.15.7.1 (Chromium 151.0.7922.173)" exit 0 | installable |
| opencode-desktop v1.18.23 | PASS (jir36kjx...) | PASS | wrapper carries app.asar+ELECTRON_FORCE_IS_PACKAGED strings OK; ELECTRON_RUN_AS_NODE probe boots system electron node runtime ("Node.js v24.18.1") exit 0 | installable |
| protonplus 0.6.4 | PASS (sgb0sqi8...) | PASS | version -> "ProtonPlus 0.6.4" exit 0 | installable |
| rootapp 0.9.127 | PASS (d2iw2z4f...) | PASS (--privileged) | bwrap FHS boots .NET app -> "info: Program[0]" exit 0 | installable |
| zen-browser 1.21.15b | PASS (5y7i0cfm...) | PASS | --version -> "Mozilla Zen 1.21.15b" exit 0 | installable |
| mixtapes 2026-26-05.0^git5e165d2 | PASS (26jpd48g...) | PASS | --help -> GApplication usage exit 0 (fontconfig warning benign headless) | installable |

- README `nix run github:Ackerman-00/nix-packages#<pkg>` paths match tested bins/meta.mainProgram for all 6 => flake-compat path PASS.

## 3. DOCKER BATTLE TEST (COMPLETE - nixos/nix:latest fresh containers)

- tools/docker-sweep.py --type nix --all => DOCKER SWEEP PASSED 6/6 built + closure verified.
- Hand-rolled battle test (tar-piped repo, fresh privileged container): BUILD_OK x6 (store paths in ledger) + SMOKE results above.
- Evidence one-liners: `nix flake check --all-systems` => nix flake check PASS; `nix build .#packages.x86_64-linux.<pkg>` x6 => nix build PASS x6; `nix run nixpkgs#nixfmt -- --check` => nixfmt PASS.

## 4. Dependency deep audit (COMPLETE - tore every package apart by hand this run)

| package | upstream deps found | in nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.7.1 | main chrome readelf -d NEEDED=32 re-read this run (ld-linux, X11 family libX11/Xcomposite/Xdamage/Xext/Xfixes/Xrandr, xcb+xkbcommon, asound, atk+bridge+atspi, cairo, cups, dbus, expat, gbm, glib/gobject/gio, nspr/nss/nssutil/smime, pango, udev, glibc family, libgcc/libm); interpreter patched to nix glibc; wrapper LD_LIBRARY_PATH carries resolution | runtimeDeps 45-dir lib.makeLibraryPath rpath covers every NEEDED incl qt5/qt6 qtbase + stdenv.cc.cc.lib shims; buildInputs glib/gsettings-desktop-schemas/gtk3/adwaita-icon-theme; smoke exit 0 under nix loader | none | none | deps-verified |
| opencode-desktop v1.18.23 | deb control Depends: libgtk-3-0 libnotify4 libnss3 libxss1 libxtst6 xdg-utils libatspi2.0-0 libuuid1 libsecret-1-0; control Version=1.18.23 matches pin BOTH arches; app.asar present x2; asar electron pin now ^30.0.1 | electron_42 closure provides full deb Depends set (and satisfies ^30); stdenv.cc.cc.lib buildInputs; autoPatchelfHook + musl ignore list; makeBinaryWrapper -> system electron 42 (live boot probe confirms) | none | none | deps-verified |
| protonplus 0.6.4 | AppImage extract re-done: bin/protonplus static bun runtime (runs on host glibc directly - probed "ProtonPlus 0.6.4"); sharun bundle self-contained; hooks 01-check-ca-certs + 01-path-mapping-hardcoded load-bearing (+self-updater skipped, store ro); X-AppImage-Version=0.6.4 | full bundle installed (bin/shared/lib/share/etc), wrapper sources 01-* hooks then execs renamed runtime; `version` -> "ProtonPlus 0.6.4" exit 0 | none | none | deps-verified |
| rootapp 0.9.127 | usr/bin/Root readelf -d NEEDED re-read from extracted squashfs (x64 offset 944632): glibc family only (ld-linux/libc/libdl/libgcc_s/libm/libpthread/librt/libstdc++); Root.desktop X-AppImage-Version=0.9.127 both arches | appimageTools.wrapType2 FHS bwrap env supplies glibc-family + libstdc++; extract contents for icon/desktop; unfree allowed; boot "info: Program[0]" exit 0 | none | none | deps-verified |
| zen-browser 1.21.15b | tar teardown re-read: zen main NEEDED=7 (glibc family + libstdc++) + libxul.so unique NEEDED=49 (gtk3 stack gdk/gtk/gdk-pixbuf, X11+X11-xcb+xcb-shm, dbus, alsa, atk/cairo/pango/fontconfig/freetype, gkcodecs/lgpllibs/mozgtk/mozsandbox/mozsqlite3/mozwayland bundled intact, nss/nspr family, ssl3, resolv); application.ini Version=1.21.15b BuildID 20260818101929 both arches | buildInputs gtk3/adwaita-icon-theme/alsa-lib/dbus-glib/libXtst/ffmpeg_8; runtimeDependencies curl/libva/pciutils/libGL; appendRunpaths libGL/pipewire; gappsWrapperArgs ffmpeg LD_LIBRARY_PATH; `--version` -> "Mozilla Zen 1.21.15b" exit 0 | none | none | deps-verified |
| mixtapes 2026-26-05.0 | source build; third-party imports = gi/Gtk4/Adw, PIL, numpy, mutagen, requests, ytmusicapi, yt_dlp(+yt_dlp_ejs via js_runtimes), mprisify (hard imports); pystray Windows-only (guarded, correctly omitted); downloads need ffmpeg (FFmpegExtractAudio) + node (js_runtimes) on PATH | pythonEnv (pygobject3/pillow/numpy/ytmusicapi/yt-dlp/yt-dlp-ejs/requests/urllib3/mutagen/pydbus/mprisify) + buildInputs gtk4/libadwaita/webkitgtk_6_0/gobject-introspection/gst_all_1.*; makeWrapper PATH ffmpeg+nodejs; GIO_MODULE_DIR glib-networking; `--help` GApplication exit 0; rustypipe-botguard (yt-dlp-get-pot*) NOT in nixpkgs, degrades gracefully - not invented | none | none | deps-verified |

- Official packaging check (2026): helium/zen/rootapp/protonplus/opencode-desktop absent or trailing in nixpkgs; mixtapes absent from nixpkgs (metainfo-only release) => no newer official default.nix to align; upstream artifact/source extraction used as source of truth.
- Meta audit: description one sentence capitalized no period x6, license SPDX correct (helium gpl3Plus+bsd3, opencode mit, protonplus gpl3Only, zen mpl20, rootapp unfree, mixtapes gpl3Plus), sourceProvenance set (binaryNativeCode x5, fromSource mixtapes), mainProgram exact x6, platforms linux x6, maintainers Ackerman-00 x6, hashes SRI sha256-...

## 5. Meta/excellence audit (COMPLETE)

- Workflows YAML valid (3 files yaml.safe_load: opencode-schedule.yml [opencode, cleanup], opencode.yml [opencode], update.yml [update-flake]); README `nix run` paths match tested bins.
- teardown-report.md receipt refreshed by sweep (committed).
- flake.lock within freshness budget (3.5d old, nixos-unstable HEAD unchanged).
- All pkgs/*.nix have pname; nixfmt-clean per container check.

## 6. Known gaps / notes

- aarch64 BUILD not possible on x86_64 runner (named gap) - covered by eval (`nix flake check --all-systems`) + hand hash verification of every aarch64 artifact (fetchurl's exact build-time check) + static unsquashfs internal-version reads for cross-arch AppImages.
- Full GUI launch headless not verifiable - named gap; version/boot/node probes are max evidence.
- Container probes must avoid awk/sed/strings assumptions (nixos/nix minimal image lacks some); use bash/readlink/grep/readelf instead. Bind-mounting the runner-owned repo hits libgit2 ownership error - use tar-pipe with --no-same-owner.
- opencode-desktop asar upstream electron pin moved 39.8.5 -> ^30.0.1 (informational; system electron_42 still correct and boots).

## 7. Final state (this run)

- Base HEAD: c436648. Changes this run: teardown-report.md (receipt refresh), .opencode-relay.md (this file). No package files needed changing - everything verified green.
- Issues open: #8 (update.yml safety net apply - needs workflows-permitted maintainer). "autonomous run failed" issues: checked and closed post-push. PRs open: 0.
- Relay status: complete. run_id: 32851595172.
