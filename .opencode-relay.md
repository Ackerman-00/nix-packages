# nix-packages autonomous-run relay state

run_id: 32399466058
status: complete

## 0. Pickup

- Previous relay (run 32375902538) status: complete. This run (32399466058): fresh start, RELAY=false.
- Inventory discovered from flake.nix: x86_64-linux = helium, opencode-desktop, protonplus, rootapp, zen-browser (+ default=rootapp); aarch64-linux = helium, opencode-desktop, protonplus, rootapp, zen-browser (+ default=helium). 5 packages, both systems now identical (rootapp added to aarch64 this run).

## 1. Work done this run

### 1a. rootapp: ADDED aarch64 support (PUSHED `2b4d820`)
- Missing-arch audit found upstream ships a real ARM AppImage at `https://installer.rootapp.com/installer/Linux/Arm64/Root.AppImage` (HTTP 200; `file` => ELF ARM aarch64 static-pie; unsquashfs-extracted => Root.desktop X-AppImage-Version=0.9.127, same structure as x86_64). The package was x86_64-only - a genuine missing-update gap.
- New arm64 hash prefetched --refresh: `sha256-zGjAfI04zb1VE8zAtPMZ2gzUcvCwWlz9X8LV/dpUUk4=`. x86_64 hash unchanged `sha256-LFTZZC9qhHdRgQXzNa6j74hNn8oxjIR/J+bTM0DB3Js=`.
- Restructured pkgs/rootapp.nix to per-arch `archives`, changed meta.platforms to lib.platforms.linux; flake.nix now exposes rootapp on BOTH systems; README rootapp line updated to "(AppImage, x86_64 + aarch64)".
- Verified: `nix build .#rootapp --refresh` EXIT 0 (x86_64), container smoke "Starting application" exit 0, aarch64 eval `.#packages.aarch64-linux.rootapp.name` => rootapp-latest + binaryNativeCode.
- NOTE: `appimageTools.extractType2` uses unsquashfs (arch-independent) so the aarch64 build path is sound; aarch64 BUILD is impossible on this x86_64 runner (named gap, eval-only verified).

### 1b. Issue #6 OPENED: update.yml RootApp block must become dual-arch
- The current update.yml rootapp block uses `grep -oP 'hash = "\K[^"]+'` which now returns BOTH per-arch hashes. Simulation proved it reports "RootApp: hash updated" every run BUT the sed rewrites the same x64 hash so `git commit` fails ("nothing to commit", swallowed by `|| true`) - NO churn commits are created. Real gaps: misleading log + arm64 hash never auto-updated.
- Cannot push .github/workflows/ (REPO_FULL_ACCESS_TOKEN not set - `gh secret list` returns 403). Opened issue #6 with the full dual-arch block diff and urgency note. Owner must apply it.
- verified no churn: fresh git repo simulation shows zero diff after sed + failed commit (see relay run log).

## 2. Build/install ledger (COMPLETE - all 5)

| package | build (x86_64) | aarch64 eval | binary smoke | status |
|---|---|---|---|---|
| helium 0.15.6.1 | PASS | PASS (eval) | --version -> "Helium 0.15.6.1 (Chromium 151.0.7922.169)" exit 0 | pass |
| opencode-desktop v1.18.19 | PASS | PASS (eval) | starts, stops at "Missing X server or $DISPLAY" (GUI gap) | pass |
| protonplus 0.6.4 | PASS | PASS (eval) | version -> "ProtonPlus 0.6.4" exit 0; hooks /tmp/pKE + /tmp/3J=1Z | pass |
| rootapp 0.9.127 (latest) | PASS | PASS (eval, NEW) | bwrap wrapper -> "Starting application" exit 0 (--privileged) | pass |
| zen-browser 1.21.15b | PASS | PASS (eval) | --version -> "Mozilla Zen 1.21.15b" exit 0 | pass |

- `nix build .#helium .#opencode-desktop .#protonplus .#rootapp .#zen-browser --no-link --refresh` => EXIT 0 on main (2b4d820).
- `nix flake check --all-systems` => all checks passed (both systems eval; aarch64 BUILD impossible - named gap).
- Store paths (deterministic, identical in fresh clone): helium aavmgij8h..., opencode jska7nxg..., protonplus bjmcfaj5..., rootapp qia4835h..., zen gp084iqxw....

## 3. Version sweep (COMPLETE - 100% of inventory)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| helium | 0.15.6.1 | 0.15.6.1 (release 2026-08-19) | up-to-date |
| opencode-desktop | v1.18.19 | v1.18.19 (release 2026-08-20) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (release 2026-08-13) | up-to-date |
| rootapp | latest (real 0.9.127) | fixed URL; 0.9.127 verified inside artifact | up-to-date |
| zen-browser | 1.21.15b | 1.21.15b (release 2026-08-19) | up-to-date |

- All 9 asset URLs: rootapp x64/arm 200, GitHub assets 302 (normal redirect). All 10 hashes (incl. new arm64 rootapp) byte-match re-prefetch --refresh.
- Latest-tag confirmation: `git ls-remote --tags` sorts helium 0.15.6.1 highest, protonplus v0.6.4 highest, zen 1.21.15b highest, opencode v1.18.19 highest.
- Real versions from inside artifacts: helium --version "0.15.6.1"; zen application.ini Version=1.21.15b; protonplus X-AppImage-Version=0.6.4; opencode app.asar package.json 1.18.19; rootapp Root.desktop X-AppImage-Version=0.9.127 (both arches).
- flake.lock nixpkgs rev d407951; all packages build with it - no update (no-churn).

## 4. Updater simulation (update.yml replayed by hand, ALL blocks)

- RootApp: current block now prints "hash updated" misleadingly on dual-arch file but creates NO commit (verified: zero diff + "nothing to commit"). arm64 hash coverage needs owner fix (issue #6).
- Opencode: version + both hashes match => no change. Dual-arch block still correct.
- Helium: version + both hashes match => no change.
- ProtonPlus: version + both hashes match => no change.
- Zen: version + both hashes match => no change.
- GitHub Actions health: last 3 "Packages Auto-Update" runs (32319293398/32203223778/32086477571) ALL SUCCESS. Failed run 32375450926 is an owner-commit artifact (event=push), not the updater.

## 5. Dependency deep audit (COMPLETE)

| package | upstream deps found | in pkgs/*.nix | missing/added | status |
|---|---|---|---|---|
| helium 0.15.6.1 | 32 direct NEEDED on main ELF | runtimeDeps covers all (container LDD_ZERO_NOT_FOUND) | none | deps-verified |
| opencode-desktop v1.18.19 | wrapper -> system electron 42.5.1; app.asar.unpacked native modules (parcel-watcher glibc+musl, node-pty, msgpackr-extract) present | electron_42 + stdenv | none | deps-verified |
| protonplus 0.6.4 | static-pie bun runtime, NEEDED=0 | n/a | none | deps-verified |
| rootapp 0.9.127 | FHS wrapType2 + bwrap | wrapType2 | none | deps-verified |
| zen-browser 1.21.15b | libxul 48 NEEDED (firefox family) | buildInputs+runtimeDeps+appendRunpaths (container LDD_ZERO_NOT_FOUND) | none | deps-verified |

- Interpreters: helium + zen main ELFs point at nix loader (glibc-2.42-67). protonplus static-pie (none). rootapp FHS. opencode wrapper -> system electron.
- Confidence: high (all 5 container-tested in clean containers on final store paths; both helium and zen show zero not-found in bare ubuntu:24.04).
- Observation (NOT a repo bug): opencode-desktop uses makeBinaryWrapper whose --add-flags passes `${NIXOS_OZONE_WL:+...}` LITERALLY (verified via strace: electron argv contains the literal string). This exactly matches nixpkgs' own pkgs/by-name/op/opencode-desktop/package.nix (same makeBinaryWrapper + same --add-flags pattern). App launches fine (container smoke reaches display error). Minor UX: ozone hint not conditionally applied on Wayland. Left unchanged to stay byte-identical to nixpkgs reference.

## 6. Container runtime ledger (COMPLETE - all 5)

| package | nix build | container ldd | container smoke | status |
|---|---|---|---|---|
| helium 0.15.6.1 | PASS | ZERO_NOT_FOUND | --version -> Helium 0.15.6.1 exit 0 | verified |
| opencode-desktop v1.18.19 | PASS | ZERO_NOT_FOUND | starts, "Missing X server or $DISPLAY" (GUI gap) | verified |
| protonplus 0.6.4 | PASS | ZERO_NOT_FOUND | version -> ProtonPlus 0.6.4 exit 0; hooks /tmp/pKE + /tmp/3J=1Z created; CA hook verified (with host /etc/ssl/certs/ca-certificates.crt present, /tmp/.___host-certs/ca-certificates.crt symlink created) | verified |
| rootapp 0.9.127 | PASS | ZERO_NOT_FOUND | bwrap wrapper -> "Starting application" exit 0 (--privileged) | verified |
| zen-browser 1.21.15b | PASS | ZERO_NOT_FOUND | --version -> Mozilla Zen 1.21.15b exit 0 | verified |

- All tests in FRESH containers (`docker run --rm`), /nix/store mounted ro, ubuntu:24.04 base (nothing preinstalled).

## 7. Fresh-checker pass (COMPLETE)

- Fresh clone /tmp/freshcheck of main (2b4d820): `nix build .#helium .#opencode-desktop .#protonplus .#rootapp .#zen-browser --no-link --refresh` EXIT 0; `nix flake check --all-systems` all checks passed. Store paths identical to main-repo build.
- Fresh containers on the same store paths: all 5 pass (helium/zen version smoke exit 0, opencode reaches display error, protonplus hooks, rootapp "Starting application").

## 8. Missing-package audit (COMPLETE)

- Inventory sync: flake.nix (5 pkgs both systems), README.md (5 entries), update.yml (5 blocks), pkgs/*.nix (5 files) ALL agree. rootapp now on aarch64 too.
- Upstream artifact types unchanged: helium/zen tar.xz, protonplus AppImage (sharun), opencode deb, rootapp AppImage (fixed URL, now dual-arch). No arch dropped; rootapp gained arm64 (fixed this run).
- New-app scan (web-verified): no new official companion apps with prebuilt Linux artifacts for helium/opencode/protonplus/zen/rootapp. nixpkgs trails: opencode-desktop 1.18.18 vs 1.18.19 (1 patch), protonplus 0.5.21 vs 0.6.4 (5 releases); helium/zen/rootapp absent from nixpkgs.

## 9. Known gaps / notes

- aarch64 BUILD not possible on x86_64 runner (named gap) - aarch64 verified by eval + hash prefetch only, incl. new rootapp arm64.
- GUI apps (opencode-desktop) stop at "Missing X server or $DISPLAY" headless - expected, not a bug.
- REPO_FULL_ACCESS_TOKEN not set: workflow-file changes cannot be pushed (TOKENS rule). Issue #6 opened with exact update.yml diff for owner (rootapp dual-arch block; also fixes misleading "hash updated" log). Until applied, the updater creates NO churn commits (verified) but arm64 rootapp hash is not auto-tracked.
- opencode-desktop ozone flag literal-passing matches nixpkgs exactly (see section 5) - left unchanged.

## 10. Final state

- main HEAD: `2b4d820` (rootapp aarch64 support), pushed. Working tree clean.
- Commits this run: `2b4d820`.
- Issues: #6 OPEN (update.yml rootapp dual-arch, owner action needed). No other open issues, no open PRs.
- Relay status: complete. run_id: 32399466058.