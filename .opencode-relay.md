# nix-packages autonomous-run relay state

status: complete
run_id: 31777329288

## 0. Pickup

- Previous relay (run 31731650097) was `status: complete`; nothing unfinished to continue.
- Started fresh on main @ 6b15111 (protonplus sharun wrapper fix). Nix on this fresh runner was 2.16.1; upgraded to 2.34.8 (locked nixpkgs d407951 requires >= 2.18) via `sudo env "PATH=$PATH" nix upgrade-nix`.

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): last 10 runs ALL green; last run 31760732709 (2026-08-14 01:28) committed nothing (no upstream change).
- Issue #1 "autonomous run failed: 2026-08-14" was auto-filed after the scheduled opencode-schedule run 31755867883 died in 22s with `Failed to fetch version information` (opencode CLI could not reach its version API) - transient infrastructure failure, NOT a repo bug, no repo state left broken. **Closed** with full diagnosis as not-a-bug.
- No open PRs. No other open issues.

## 2. Fixes this run

- None needed. All four packages were already current and green on main; no churn commits made (NO-CHURN rule). Nix version on runner upgraded to 2.34.8 (runner-only change, not a repo change).

## 3. Version accuracy sweep (100% of inventory)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| helium | 0.15.4.1 | 0.15.4.1 (gh releases latest, pub 2026-08-12) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (strip v, pub 2026-08-13) | up-to-date |
| opencode-desktop | v1.18.18 | v1.18.18 (gh releases latest, pub 2026-08-13) | up-to-date |
| rootapp | latest (fixed URL) | hash unchanged after `--refresh` re-prefetch; real version from artifact (X-AppImage-Version + sq.version) = 0.9.126 | up-to-date |

- All pinned SRI hashes re-prefetched with `--refresh` and match byte-for-byte (x86_64 + aarch64 for helium/protonplus).
- Adversarial check: listed recent releases per repo (not just /latest) to rule out stale API caching - pinned versions are the genuinely newest tags.
- README package list matches flake.nix exactly.

## 4. Build/install verification ledger

| package | build (x86_64) | aarch64 eval | binary smoke | status |
|---|---|---|---|---|
| helium | PASS (fresh --refresh) | PASS (eval .name) | `--version` -> "Helium 0.15.4.1 (Chromium 151.0.7922.137)" | pass |
| protonplus | PASS (fresh --refresh) | PASS (eval .name) | `version` -> "ProtonPlus 0.6.4" | pass |
| opencode-desktop | PASS (fresh --refresh) | n/a (x86_64 only) | launches under xvfb, stays alive (GUI needs display = gap) | pass |
| rootapp | PASS (fresh --refresh) | n/a (x86_64 only) | starts ("Starting application"; bwrap needs userns) | pass |

- `nix flake check --all-systems` green on final main (both systems evaluate; aarch64 BUILD impossible on x86_64 runner - named gap).
- sourceProvenance = [binaryNativeCode] on all 4 packages (nix eval verified).

## 5. Dependency audit (100% of inventory)

| package | upstream deps found | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium | union NEEDED of ALL 9 ELFs = 39 sonames (glib/Qt5/Qt6/X11/Xcb/Xkb/common/atk/atspi/cairo/cups/dbus/expat/gbm/nspr/nss/pango/udev/asound + glibc) | all 39 covered by runtimeDeps (Qt shims bundled libqt5_shim/libqt6_shim resolve to qtbase) | none | extra-but-harmless entries (libxscrnsaver, pipewire, etc.) cover dlopen'd libs | deps-verified (high confidence - readelf union fully mapped) |
| protonplus | statically-linked bun runtime + bundled bin/shared/lib/etc | binary bundles everything | none (share/ dir from prev fix present incl. gschema.xml, 25 locales) | none | deps-verified (high confidence) |
| opencode-desktop | electron_42 (42.5.1 from locked nixpkgs; deb bundled 42.3.3, same major) + 7 .node sidecars (glibc variants need only libc/libstdc++/libutil - all in closure) | electron_42 via callPackage | none | none (musl variants autoPatchelf-ignored, never loaded on glibc) | deps-verified (high confidence) |
| rootapp | .NET self-contained (Root ELF + bundled libSkiaSharp/libHarfBuzzSharp/libonigwrap/libuiohook/DotNetBrowser) + bwrap FHS sandbox | wrapType2 FHS | none | none | deps-verified (medium confidence - FHS AppImage self-contained, ldd not applicable to bwrap wrapper) |

## 6. Container runtime test ledger (fresh `docker run --rm` per test)

| package | nix build | container ldd | container smoke | status |
|---|---|---|---|---|
| helium | PASS | interpreter = nix ld-linux (/nix/store/...glibc-2.42-67/lib/ld-linux-x86-64.so.2); all 39 NEEDED in runtimeDeps | `--version` -> "Helium 0.15.4.1 (Chromium 151.0.7922.137)", exit 0 | verified |
| protonplus | PASS | 0 not found (static bun) | `version` -> "ProtonPlus 0.6.4", exit 0; hook artifacts verified: /tmp/.___host-certs/ca-certificates.crt -> host cert, /tmp/pKE -> lib, /tmp/3J=1Z -> share (with host CA cert mounted, as on any real machine) | verified |
| opencode-desktop | PASS | wrapper 0 not found; electron binary 0 not found | launches under xvfb (stays alive until killed; dbus errors are headless-env noise) | verified (GUI launch gap) |
| rootapp | PASS | FHS sandbox resolves (bwrap wrapper, not a dynamic exe) | starts ("Starting application", "Using profile default") under `--privileged` (docker blocks userns -> privileged simulates real machine) | verified (needs userns) |

- Named gaps: aarch64 BUILD impossible on x86_64 runner (eval only, both aarch64 packages evaluate green); GUI apps full window rendering needs a display (helped by xvfb for smoke only).

## 7. What remains

- Nothing. Repo is current, all packages build and pass clean-container runtime tests, all hashes match upstream, no open issues/PRs.

## 8. Final state

- main @ 6b15111 (no repo changes this run; only .opencode-relay.md updated). Working tree clean.
- Runner nix upgraded to 2.34.8 (runner-only, not a repo change).
- README.md package list matches flake.nix (rootapp, opencode-desktop, helium, protonplus).