# nix-packages autonomous-run relay state

run_id: 32158523109
status: complete

## 0. Pickup

- Previous relay (run 32133109452) was `status: complete`; nothing unfinished to continue.
- NOTE: repo history was reset by the owner since the last run - main now has a SINGLE commit (f4cdd64, owner-authored "prompt:" commit). Working tree on top of it is clean and contains the full repo (flake.nix, pkgs/*, .github/workflows). This run treats the current tree as truth.
- `git fetch origin && git pull --rebase origin main` => Already up to date.
- Runner Nix = 2.35.2 (no upgrade needed this run; prior runs upgraded 2.16 -> 2.34).
- Docker 28.0.4 verified; hello-world passed.
- Git identity verified before commits: `opencode-agent[bot] <41898282+opencode-agent[bot]@users.noreply.github.com>` (git config + GIT_AUTHOR_*/GIT_COMMITTER_* env).
- Inventory discovered from flake.nix (`nix eval --apply attrNames`, NOT hardcoded): x86_64-linux = helium, protonplus, zen-browser, rootapp, opencode-desktop (+ default); aarch64-linux = helium, protonplus, zen-browser (+ default). README list matches flake.nix. update.yml has 5 update blocks matching all 5 packages (in sync).

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): last run 32086477571 (2026-08-18 00:57 UTC) SUCCESS, committed nothing. All 5 blocks ran; visible results: RootApp/Helium/ProtonPlus/Zen all "no change". The Opencode Desktop result echo is ABSENT from the run log (minor observation - the run was green, no commit was made, and this run's independent simulation confirms opencode was up-to-date, so no impact). Earlier runs all SUCCESS.
- opencode-schedule: this run 32158523109 is the current one; prior relay runs green.
- No open issues (`gh api .../issues?state=open` => 0).
- No open PRs (`gh pr list --state open` => empty).

## 2. Changes this run

- ZERO package changes. Every package verified at its current upstream version with byte-matching hashes (all 9 re-prefetched with `--refresh` this run). No churn commits generated.
- No runner-environment fixes needed (Nix 2.35.2 already recent).

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package          | packaged version | upstream latest (verified 2026-08-18)                        | status     |
|---|---|---|---|
| helium           | 0.15.5.1         | 0.15.5.1 (gh releases/latest 2026-08-15 + git tags newest)   | up-to-date |
| protonplus       | 0.6.4            | v0.6.4 -> 0.6.4 (gh releases/latest 2026-08-13 + tags)       | up-to-date |
| zen-browser      | 1.21.14b         | 1.21.14b (gh releases/latest 2026-08-13 + tags)              | up-to-date |
| opencode-desktop | v1.18.18         | v1.18.18 (gh releases/latest 2026-08-13 + tags)              | up-to-date |
| rootapp          | latest (fixed URL) | hash unchanged after `--refresh`; in-artifact X-AppImage-Version = 0.9.126 (Root.desktop) | up-to-date |

- Independent "latest is actually latest" check via `git ls-remote --tags` on all 4 GitHub repos: pinned versions are the newest tags (helium 0.15.5.1, protonplus v0.6.4, zen 1.21.14b, opencode v1.18.18).
- All 9 pinned SRI hashes re-prefetched with `--refresh` and byte-match:
  - helium x64 `sha256-80oe4aarLjEJ2S45OVEqN8/mii8NIwtSXMFYn8GS/Zc=`, arm `sha256-2TI9B3GjdN5eWmZqfnVOIfKYwjjqwh345YaKx7X2TP4=`
  - protonplus x64 `sha256-J0ddq9hoIb7QeNLQiEEN8qDqr/iFylrwAqxhIQX++Qs=`, arm `sha256-o4sUkr3JJWRxhOzCRsPUjqJNQTgtHBaW7xw9MpmhCBo=`
  - zen x64 `sha256-s6W7eC2UA9zoz0zePWHa7WRUhWgFiGLq654EXCErfuA=`, arm `sha256-4Uldus+9ublCkIpdbTEVjsGgPnfS+djPqaBo4QfeVyk=`
  - opencode deb `sha256-e31ka5x82X8N8JipTEcITx4Wbw38RStyhwRZUuES1Io=`, rootapp `sha256-BzaSb96SOBnSdqxtSjr+ZcbYfHNGia9HJ2/E6/B12RA=`
- All 8 GitHub asset URLs + rootapp fixed URL return HTTP 200 (`curl -sIL`). All reachable.
- updater-URL sync check: every URL in update.yml matches the URL pattern in the corresponding pkgs/*.nix (template vars verified). No workflow change needed.

## 4. Build/install verification ledger

| package          | build (x86_64) | aarch64 eval | binary smoke                                                                   | status |
|---|---|---|---|---|
| helium 0.15.5.1    | PASS           | PASS (eval)  | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0      | pass   |
| protonplus 0.6.4   | PASS           | PASS (eval)  | `version` -> "ProtonPlus 0.6.4", exit 0                                        | pass   |
| zen-browser 1.21.14b | PASS         | PASS (eval)  | `--version` -> "Mozilla Zen 1.21.14b", exit 0 (sandbox EPERM warning normal)   | pass   |
| opencode-desktop v1.18.18 | PASS    | n/a (x86_64 only) | `--version --no-sandbox` exit 0; main process starts, stops at "Missing X server or $DISPLAY" (GUI gap) | pass |
| rootapp latest     | PASS           | n/a (x86_64 only) | bwrap wrapper (--privileged) -> "Starting application" / "Using profile default", exit 0 | pass |

- `nix build .#helium .#protonplus .#zen-browser .#rootapp .#opencode-desktop --no-link --refresh` => EXIT 0.
- Store paths byte-identical to the previous verified run (zero churn evidence): helium=4lph1w68qgfcs1j9yyvyqrnpby345fdi, protonplus=bjmcfaj5zv7z0d0z6l4xs794qxl54r21, zen=v78acsdymsjr8k667aiiwswmp6s2xm1x, rootapp=l45vk28x07x9zr1x9400dl0ya17x2bhk, opencode=vi5mdchvnv1h26vi1hmrkd27iwibhpfs.
- `nix flake check --all-systems` => all checks passed (x86_64 + aarch64 eval green).
- All 5 packages have `meta.sourceProvenance = binaryNativeCode` (verified via `nix eval --json .#<pkg>.meta.sourceProvenance`).
- Fresh-checker pass: cloned main to /tmp/freshcheck, `nix build --refresh` all 5 => EXIT 0, store paths byte-identical. Reproducible.

## 5. Dependency audit (100% of inventory; no upstream change this run => no RE-TEAR required, container ldd re-verified every runtime closure fresh)

| package          | upstream deps found (NEEDED sonames) | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.5.1    | 32 NEEDED sonames across helium ELF; interpreter patched to nix glibc 2.42 | all 32 in runtimeDeps; patchelf --set-interpreter | none | none | deps-verified (container: FULL wrapper LD_LIBRARY_PATH extracted (2756 chars) + nix loader `--list` on helium/crashpad/chromedriver => rc=0 each, zero "not found") |
| protonplus 0.6.4   | static-pie bun binary (NEEDED=0); desktop X-AppImage-Version=0.6.4 | n/a (bundles everything) | none | none | deps-verified (container: NEEDED=0, hooks create /tmp/pKE -> lib + /tmp/3J=1Z -> share; CA-certs warning expected in bare container) |
| zen-browser 1.21.14b | libxul 46 NEEDED, 27 bundled; external gtk3/alsa/dbus-glib/libXtst/ffmpeg + runtimeDeps curl/libva/pciutils/libGL | buildInputs + runtimeDependencies + appendRunpaths + autoPatchelfHook + patchelfUnstable --no-clobber-old-sections | none | none | deps-verified (container: zen ELF + libxul.so loader --list => rc=0, zero "not found") |
| opencode-desktop v1.18.18 | electron real ELF resolves via nix loader; 4 glibc .node sidecars resolve; 3 musl sidecars link libc.musl (intentionally ignored) | electron_42 + autoPatchelfIgnoreMissingDeps libc.musl | none | none | deps-verified (container: electron ELF rc=0 zero not-found; 4 glibc sidecars notfound=0; musl sidecars expected-missing, Electron loads glibc variants) |
| rootapp 0.9.126    | .NET Root binary; interpreter /lib64/ld-linux (correct - bundled glibc via bwrap FHS env) | wrapType2 FHS+bwrap, container-init | none | none | deps-verified (container --privileged: app starts, exit 0) |

- Artifact tears this run (in /tmp, never repo tree):
  - helium tar.xz: store tree = artifact tree verbatim minus helium.desktop (moved to share/applications); only patched ELFs + substituted wrapper differ.
  - protonplus AppImage: X-AppImage-Version=0.6.4; store ships bin/shared/lib/share/etc, drops AppImage-only scaffolding (AppRun*, .DirIcon, .env, .preload, .anylinux.c, .gtk-class-fix.c, sharun).
  - zen tar.xz: application.ini Version=1.21.14b BuildID=20260811103047; store = artifact verbatim except 28 patched ELFs (interpreter only).
  - opencode deb: DEBIAN/control Version: 1.18.18; app.asar byte-identical (140970336); usr/share complete; .desktop regenerated via makeDesktopItem (intentional), icons renamed opencode-desktop.png (intentional).
  - rootapp AppImage: Root.desktop X-AppImage-Version=0.9.126; extracted via --appimage-extract.

## 6. Container runtime test ledger (fresh `docker run --rm` per test, base ubuntu:24.04, /nix/store mounted ro)

| package          | nix build | container ldd                          | container smoke                                                           | status   |
|---|---|---|---|---|
| helium 0.15.5.1    | PASS      | ZERO_NOT_FOUND (nix loader --list on 3 ELFs with real wrapper LD_LIBRARY_PATH, rc=0 each) | wrapper `--version` -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)", exit 0 | verified |
| protonplus 0.6.4   | PASS      | ZERO_NOT_FOUND (NEEDED=0)             | `version` -> "ProtonPlus 0.6.4", exit 0; hooks created /tmp/pKE + /tmp/3J=1Z | verified |
| zen-browser 1.21.14b | PASS    | ZERO_NOT_FOUND (zen + libxul via nix loader rc=0) | `--version` -> "Mozilla Zen 1.21.14b", exit 0 (EPERM normal)              | verified |
| opencode-desktop v1.18.18 | PASS | ZERO_NOT_FOUND (electron real ELF + 4 glibc sidecars) | `--version --no-sandbox` exit 0; main process starts, "Missing X server or $DISPLAY" (GUI gap) | verified |
| rootapp 0.9.126    | PASS      | ZERO_NOT_FOUND (.NET Root via bwrap FHS env) | bwrap wrapper (--privileged) -> "Starting application" / "Using profile default", exit 0 | verified |

- Interpreters verified: helium = nix glibc 2.42 loader (patchelf); zen = nix glibc 2.42 (autoPatchelfHook); opencode electron real ELF = nix glibc 2.42; rootapp = /lib64/ld-linux-x86-64.so.2 (correct - bundled glibc via bwrap FHS env).

## 7. Missing-package / new-app audit

- Inventory in sync: README package list == flake.nix packages == update.yml blocks == pkgs/*.nix files (5/5/5/5). No package in one but not the others.
- Upstream artifact types unchanged: helium ships tar.xz (we ship tar.xz - still the least-surgery artifact, AppImage also exists upstream but tar.xz is correct choice), protonplus sharun AppImage, zen tar.xz, opencode amd64 deb, rootapp fixed URL AppImage. No format/arch changes detected.
- New-app scan (web search on all 5 upstream projects): no new official companion Linux desktop apps shipping prebuilt binaries that this repo should add. Helium's other releases are mac/windows; protonplus ships no companion; zen ships mac/windows/android/ios; opencode's CLI is in nixpkgs (desktop client already packaged); rootapp is closed-source with no companion.

## 8. Final state

- HEAD = f4cdd64 + this relay commit. Working tree clean (`git status --porcelain` empty).
- No package commits this run (nothing changed - zero churn policy).
- flake.lock not touched (no nixpkgs bump; builds green with locked rev from 2026-07).

## Named gaps (not verified, and why)

- aarch64 BUILD: cannot build aarch64 binaries on this x86_64 runner. Verified aarch64 via `nix flake check --all-systems` (derivation eval green) + prefetched hashes matching. Named gap.
- GUI launch in container: no display/X server in ubuntu:24.04. Verified via headless flags + main-process start only. Named gap.
- opencode-desktop musl .node sidecars legitimately link `libc.musl-x86_64.so.1` (musl-only); intentionally ignored via autoPatchelfIgnoreMissingDeps - Electron loads the glibc variants on glibc systems. Correct by design.
- rootapp bwrap in non-privileged container: `bwrap: setting up uid map: Permission denied` without userns. Verified with `docker run --privileged`. On real NixOS desktops user namespaces are enabled by default, so this works without --privileged.
- Minor observation (no action): the last "Packages Auto-Update" run's Opencode Desktop result echo is missing from the GitHub log (run green, no commit, independent simulation confirms no change was needed). Log-capture artifact only; the updater logic is correct.