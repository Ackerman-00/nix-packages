# nix-packages autonomous-run relay state

status: complete
run_id: 31851092629

## 0. Pickup

- Previous relay (run 31827238785) was `status: complete`; nothing unfinished to continue. Started fresh on main @ 7423937.
- Runner nix was 2.16.1; upgraded to 2.34.8 (locked nixpkgs d407951 requires >= 2.18) via `sudo env "PATH=$PATH" nix upgrade-nix`; before the upgrade `nix flake show` errored "expected a derivation" on rootapp - gone after upgrade. Runner-only change, not committed.
- Docker 28.0.4 verified working (`docker run --rm hello-world`); ubuntu:24.04 pulled.

## 1. Actions health / issues / PRs

- "Packages Auto-Update" (update.yml): last ~40 runs ALL green; last run 31760732709 (2026-08-14 01:28) succeeded. No failing runs.
- No open issues (`gh api repos/Ackerman-00/nix-packages/issues` -> []). No open PRs (`gh api .../pulls` -> []).

## 2. Fixes this run

- NONE REQUIRED. Full inventory sweep: every package current, builds green, runtime-verified in clean containers.

## 3. Version accuracy sweep (100% of inventory = 5 packages)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| helium | 0.15.4.1 | 0.15.4.1 (gh releases latest) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (strip v) | up-to-date |
| zen-browser | 1.21.14b | 1.21.14b (gh releases latest) | up-to-date |
| opencode-desktop | v1.18.18 | v1.18.18 (gh releases latest) | up-to-date |
| rootapp | latest (fixed URL) | hash unchanged after `--refresh` re-prefetch | up-to-date |

- All 8 pinned SRI hashes re-prefetched with `--refresh` and match byte-for-byte (helium x86_64+aarch64, protonplus x86_64+aarch64, zen x86_64+aarch64, opencode-deb, rootapp fixed URL). Helium tarball additionally downloaded + sha256sum = pinned SRI.
- All 8 source URLs return the pinned hashes via `nix store prefetch-file --refresh`.
- update.yml source URLs all in sync with pkgs/*.nix (5 blocks, no drift).
- README package list (rootapp, opencode-desktop, helium, protonplus, zen-browser) matches flake.nix exactly.

## 4. Build/install verification ledger

| package | build (x86_64) | aarch64 eval | binary smoke | status |
|---|---|---|---|---|
| helium | PASS | PASS (eval .name) | `--version` -> "Helium 0.15.4.1 (Chromium 151.0.7922.137)" | pass |
| protonplus | PASS | PASS (eval .name) | installCheckPhase: `version` -> "ProtonPlus 0.6.4" | pass |
| zen-browser | PASS | PASS (eval .name) | `--version` -> "Mozilla Zen 1.21.14b" | pass |
| opencode-desktop | PASS | n/a (x86_64 only) | wrapper launches; electron v42.5.1; renderer starts | pass (GUI needs display = gap) |
| rootapp | PASS | n/a (x86_64 only) | starts ("Starting application", "Using profile default") under `--privileged` | pass |

- `nix flake check --all-systems` green on main. FRESH-CLONE re-check at /tmp/fresh-check: all checks passed; `--refresh` rebuild produced IDENTICAL store paths (helium xmsjwcf..., protonplus bjmcfaj..., zen v78acsd..., rootapp l45vk2..., opencode vi5mdch...).
- sourceProvenance = [binaryNativeCode] on all 5 packages (nix eval verified).

## 5. Dependency audit (100% of inventory)

| package | upstream deps found | in pkgs/*.nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium | real binary NEEDED = 32 sonames (X11 family, asound, atk/atspi, cairo, cups, dbus, expat, gbm, nspr/nss, pango, udev, xcb, xkbcommon) torn from artifact this run | all covered by runtimeDeps | none | extra-but-harmless (dlopen coverage) | deps-verified (nix loader --list: ZERO not found) |
| protonplus | static bun + bundled bin/shared/lib/etc; hooks 01-check-ca-certs + 01-path-mapping-hardcoded | binary bundles everything; wrapper sources 01-* hooks | none | none | deps-verified |
| zen-browser | libxul + libmozsqlite3 + all libs | gtk3, alsa-lib, dbus-glib, libXtst, ffmpeg_8, runtimeDependencies, appendRunpaths | none | none | deps-verified (ZERO not found) |
| opencode-desktop | electron_42 (42.5.1) + 7 .node sidecars (parcel watcher glibc/musl, node-pty, msgpackr) | electron_42 via callPackage | none | none | deps-verified |
| rootapp | .NET self-contained + bwrap FHS sandbox | wrapType2 FHS | none | none | deps-verified (medium confidence - FHS self-contained, ldd n/a for bwrap) |

## 6. Container runtime test ledger (fresh `docker run --rm` per test, base ubuntu:24.04)

| package | nix build | container ldd | container smoke | status |
|---|---|---|---|---|
| helium | PASS | ZERO not found via nix glibc-2.42 loader (all 3 ELFs: helium, crashpad, chromedriver) | `--version` -> "Helium 0.15.4.1 (Chromium 151.0.7922.137)", exit 0 | verified |
| protonplus | PASS | ZERO not found (static bun) | `version` -> "ProtonPlus 0.6.4", exit 0; hooks: /tmp/pKE -> lib, /tmp/3J=1Z -> share, /tmp/.___host-certs/ca-certificates.crt -> host cert (with host cert mounted, simulates real machine) | verified |
| zen-browser | PASS | ZERO not found (zen + libxul) | `--version` -> "Mozilla Zen 1.21.14b", exit 0 | verified |
| opencode-desktop | PASS | wrapper + electron: ZERO not found | electron --no-sandbox --version -> "v42.5.1"; app renderer starts headless | verified (GUI launch gap) |
| rootapp | PASS | bwrap wrapper: ZERO not found | starts ("Starting application", "Using profile default") under `--privileged` (bwrap needs userns) | verified (needs userns) |

- Named gaps: aarch64 BUILD impossible on x86_64 runner (eval only, all 3 aarch64 packages evaluate green); GUI apps full window rendering needs a display (headless/xvfb smoke only); rootapp needs user-namespace support (--privileged on the runner simulates a real machine); protonplus CA-cert hook only fires when a host cert store exists (base ubuntu image ships none - real machines have one).
- Note: `ldd` from the base image mixes host glibc 2.39 with nix glibc 2.42 and reports a spurious `GLIBC_ABI_DT_X86_64_PLT not found`; the authoritative check uses the nix loader (`<nix-glibc>/lib/ld-linux-x86-64.so.2 --list`) which shows ZERO not found and the binary runs.

## 7. What remains

- Nothing. Repo is current, all 5 packages build and pass clean-container runtime tests, all hashes match upstream, no open issues/PRs.

## 8. Final state

- main @ 7423937 (no commits needed this run - everything already current; runner-only nix upgrade to 2.34.8). Working tree clean.
- README.md package list matches flake.nix (rootapp, opencode-desktop, helium, protonplus, zen-browser).