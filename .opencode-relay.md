run_id: 32861900338
status: complete

# nix-packages autonomous-run relay state (run 32861900338)

## 0. Pickup (run 32861900338, RELAY=false)

- Previous relay status: complete (run 32851595172). Fresh periodic sweep, RELAY=false, inventory 6 packages.
- Base HEAD b915a0b at pickup (history squashed full-tree re-add; prior base c436648 no longer fetchable). RUN_ID=32861900338.
- Git identity: opencode-agent[bot] only.

## 1. Work done this run

### 1a. Version sweep - all 6 at upstream latest

- `gh api --cache 3600s repos/<owner>/<repo>/releases/latest --jq .tag_name`:
  - helium: pinned 0.15.7.1 vs upstream 0.15.7.1 (imputnet/helium-linux) => up-to-date
  - opencode-desktop: pinned v1.18.23 vs upstream v1.18.23 (anomalyco/opencode) => up-to-date
  - protonplus: pinned v0.6.4 vs upstream v0.6.4 (Vysp3r/ProtonPlus) => up-to-date
  - zen-browser: pinned 1.21.15b vs upstream 1.21.15b (zen-browser/desktop) => up-to-date
  - mixtapes: pinned rev 5e165d201128de1b6c10d5cb031a0828ffc3b0ec IS current main HEAD of m-obeid/Mixtapes (2026-08-15) => up-to-date
  - rootapp: installer.rootapp.com pinned URLs HTTP 200 both arches (last-modified 2026-08-19); artifacts re-downloaded and byte-match pins
- OSV vulns: 0 across all 6; Repology outdated: 0 (rootapp repology row = normalization artifact, pin == upstream); libyear drift: 0.00 yr (threshold 20).
- No package file changes required this run.

### 1b. Teardown - all 10 distfiles downloaded + hash + internal-version verified by hand

- Downloaded ALL distfiles into /tmp/opencode/sweep-32861900338 (x86_64+aarch64 each binary pkg): helium-x64/arm64.tar.xz, opencode-amd64/arm64.deb, protonplus-x64/aarch64.AppImage, rootapp-x64/aarch64.AppImage, zen-x64/arm64.tar.xz => 10/10 sha256 byte-match vs pinned SRI (zero mismatches).
- Internal versions read from inside the artifacts:
  - helium: nested dirs helium-0.15.7.1-{x86_64,arm64}_linux; runtime probe "Helium 0.15.7.1 (Chromium 151.0.7922.173)"
  - opencode-desktop: deb control Version=1.18.23 BOTH arches (control.tar.xz); app.asar present x2
  - protonplus x64: X-AppImage-Version=0.6.4 via --appimage-extract + live host probe "ProtonPlus 0.6.4"
  - rootapp: Root.desktop X-AppImage-Version=0.9.127 BOTH arches (x64 via --appimage-extract; aarch64 via unsquashfs at hsqs offset 936456)
  - zen-browser: application.ini Version=1.21.15b BuildID 20260818101929 BOTH arches
  - mixtapes: live pin == upstream HEAD rev 5e165d2; clean-container nix build succeeded => fetchFromGitHub pin valid
- protonplus aarch64 internal-version note: artifact is the NEW URUNTIME AppImage container ("Universal RunImage ... SquashFS and DwarFS") - no squashfs superblock at any hsqs offset (only runtime text), no type-2 AI trailer; static unsquashfs and qemu-aarch64-static extraction both fail. Covered instead by: byte-hash identity with the artifacts whose internals were verified in run 32851595172 + same-release x64 sibling reading 0.6.4 today + upstream tag v0.6.4. Named gap: cross-arch URUNTIME AppImage introspection needs a dwarfs-aware extractor.

### 1c. Deterministic sweep (tools/teardown-sweep.py) - auxiliary evidence

- `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/opencode/sweep-32861900338-tool`: TEAR-DOWN SWEEP PASSED exit 0 - every distfile row OK/hash-OK incl cross-arch aarch64 rows; Vulns 0 / outdated 0 / libyear 0.00; teardown-report.md receipt refreshed (committed).

### 1d. flake.lock freshness - no bump (within 7d budget)

- Locked rev 2c423e03bbafcff28bfadc6781a4a8257f205cb5 (lastModified 1787360063, 3.6 days old, narHash sha256-dt4WdcvsA8/RC...) vs nixos-unstable HEAD 56c02bc00adcf003215cc4bd996d6efaf4cff188 (unchanged since last run) => within budget => no `nix flake update` required.

### 1e. Nix excellence checks (clean nixos/nix containers - host has no nix)

- `nix flake check --all-systems` => FLAKE_CHECK_EXIT=0 "all checks passed!" => nix flake check PASS (both systems eval).
- `nix run nixpkgs#nixfmt -- --check pkgs/*.nix flake.nix` (nixfmt 1.4.0) => NIXFMT_EXIT=0 => nixfmt PASS.
- nixpkgs-hammering-equivalent manual meta audit on all 6: license/mainProgram/sourceProvenance/platforms/maintainers present; versions start with digit; hashes SRI sha256-... => PASS.

## 2. Install-test ledger (COMPLETE - all 6 built + smoked in fresh privileged nixos/nix container, tar-piped repo --no-same-owner)

| package | nix build | nix run | smoke | status |
|---|---|---|---|---|
| helium 0.15.7.1 | PASS (87kg5p1s...) | PASS | --version -> "Helium 0.15.7.1 (Chromium 151.0.7922.173)" exit 0 | installable |
| opencode-desktop v1.18.23 | PASS (jir36kjx...) | PASS | makeCWrapper execs system electron 42.9.3 + app.asar + ELECTRON_FORCE_IS_PACKAGED=1; ELECTRON_RUN_AS_NODE boot -> "node-boot-ok v24.18.1" exit 0 | installable |
| protonplus 0.6.4 | PASS (sgb0sqi8...) | PASS | version -> "ProtonPlus 0.6.4" exit 0 | installable |
| rootapp 0.9.127 | PASS (d2iw2z4f...) | PASS (--privileged) | bwrap FHS boots .NET app -> "info: Program[0] Starting application" exit 0 | installable |
| zen-browser 1.21.15b | PASS (vfgwmd82...) | PASS | --version -> "Mozilla Zen 1.21.15b" exit 0 | installable |
| mixtapes 2026-26-05.0^git5e165d2 | PASS (26jpd48g...) | PASS | --help -> GApplication usage exit 0 (fontconfig warning benign headless) | installable |

- README `nix run github:Ackerman-00/nix-packages#<pkg>` paths match tested bins/meta.mainProgram for all 6 => flake-compat path PASS.
- Note: zen-browser store path differs from last run's ledger (vfgwmd82 vs 5y7i0cfm) while the other five are bit-identical - explained by the history rewrite at b915a0b (full-tree re-add; some semantically-neutral-but-non-normalized change to zen's derivation inputs). Today's derivation builds + smokes green on today's lock.

## 3. DOCKER BATTLE TEST (COMPLETE - nixos/nix:latest fresh containers)

- tools/docker-sweep.py --type nix --all => DOCKER SWEEP PASSED 6/6 built + closure verified (docker-report.md written).
- Hand-rolled battle test (tar-piped repo into persistent privileged nixbox container): BUILD_OK x6 (store paths in ledger above) + SMOKE results above.
- Evidence one-liners: `nix flake check --all-systems` => nix flake check PASS; `nix build .#packages.x86_64-linux.<pkg>` x6 => nix build PASS x6; `nix run nixpkgs#nixfmt -- --check` => nixfmt PASS.

## 4. Dependency deep audit (COMPLETE - tore every package apart by hand this run)

| package | upstream deps found | in nix | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| helium 0.15.7.1 | main chrome readelf -d NEEDED re-read this run = 32 (ld-linux, X11 family libX11/Xcomposite/Xdamage/Xext/Xfixes/Xrandr, xcb+xkbcommon, asound, atk+bridge+atspi, cairo, cups, dbus, expat, gbm, glib/gobject/gio, nspr/nss/nssutil/smime, pango, udev, glibc family, libgcc/libm) | runtimeDeps 45-dir lib.makeLibraryPath rpath covers every NEEDED incl qt5/qt6 qtbase + stdenv.cc.cc.lib shims; buildInputs glib/gsettings-desktop-schemas/gtk3/adwaita-icon-theme; wrapper LD_LIBRARY_PATH carries resolution | none | none | deps-verified |
| opencode-desktop v1.18.23 | deb control Depends re-read: libgtk-3-0 libnotify4 libnss3 libxss1 libxtst6 xdg-utils libatspi2.0-0 libuuid1 libsecret-1-0; control Version=1.18.23 matches pin BOTH arches; app.asar present x2 | electron_42 closure provides full deb Depends set; stdenv.cc.cc.lib buildInputs; autoPatchelfHook + musl ignore list; makeBinaryWrapper -> system electron 42.9.3 (live boot probe confirms) | none | none | deps-verified |
| protonplus 0.6.4 | AppImage extract re-done (x64): bin/protonplus static bun runtime (runs on host glibc - probed "ProtonPlus 0.6.4"); sharun bundle self-contained; hooks load-bearing (+self-updater skipped, store ro); X-AppImage-Version=0.6.4 | full bundle installed (bin/shared/lib/share/etc), wrapper sources hooks then execs renamed runtime; aarch64 same-release sibling hash-identical to previously torn-down 0.6.4 artifact | none | none | deps-verified |
| rootapp 0.9.127 | usr/bin/Root readelf -d NEEDED re-read from extracted squashfs: glibc family only (ld-linux/libc/libdl/libgcc_s/libm/libpthread/librt/libstdc++); Root.desktop X-AppImage-Version=0.9.127 both arches | appimageTools.wrapType2 FHS bwrap env supplies glibc-family + libstdc++; extract contents for icon/desktop; unfree allowed; boot "info: Program[0]" exit 0 | none | none | deps-verified |
| zen-browser 1.21.15b | tar teardown re-read: zen main NEEDED=32-family + libxul.so unique NEEDED=49 (gtk3 stack gdk/gtk/gdk-pixbuf, X11+X11-xcb+xcb-shm, dbus, alsa, atk/cairo/pango/fontconfig/freetype, gkcodecs/lgpllibs/mozgtk/mozsandbox/mozsqlite3/mozwayland bundled intact, nss/nspr family, ssl3, resolv); application.ini Version=1.21.15b BuildID 20260818101929 both arches | buildInputs gtk3/adwaita-icon-theme/alsa-lib/dbus-glib/libXtst/ffmpeg_8; runtimeDependencies curl/libva/pciutils/libGL; appendRunpaths libGL/pipewire; gappsWrapperArgs ffmpeg+pulseaudio+pipewire+alsa LD_LIBRARY_PATH; patchelfUnstable for relrhack; `--version` -> "Mozilla Zen 1.21.15b" exit 0 | none | none | deps-verified |
| mixtapes 2026-26-05.0 | source teardown of rev 5e165d2 tarball: third-party imports re-read = gi/Gtk4/Adw/Gst/WebKit/GdkPixbuf, PIL, numpy, mutagen, requests+urllib3, ytmusicapi, yt_dlp(+yt_dlp_ejs js_runtimes), mprisify, pydbus; pystray Windows-only (tray_win.py raises ImportError unless win32 - correctly omitted); StrEnum not imported (stdlib on py3.14); requirements yt-dlp>=2026.7.4 satisfied by nixpkgs unstable yt-dlp | pythonEnv (pygobject3/pillow/numpy/ytmusicapi/yt-dlp/yt-dlp-ejs/requests/urllib3/mutagen/pydbus/mprisify) + buildInputs gtk4/libadwaita/webkitgtk_6_0/gobject-introspection/gst_all_1.*; makeWrapper PATH ffmpeg+nodejs; GIO_MODULE_DIR glib-networking; `--help` GApplication exit 0; rustypipe-botguard (yt-dlp-get-pot*) NOT in nixpkgs, degrades gracefully - not invented | none | none | deps-verified |

- Official packaging check (2026): helium/zen/rootapp/protonplus/opencode-desktop absent or trailing in nixpkgs; mixtapes absent from nixpkgs (metainfo-only release) => no newer official default.nix to align; upstream artifact/source extraction used as source of truth.
- Meta audit: description one sentence capitalized no period x6, license SPDX correct (helium gpl3Plus+bsd3, opencode mit, protonplus gpl3Only, zen mpl20, rootapp unfree, mixtapes gpl3Plus), sourceProvenance set (binaryNativeCode x5, fromSource mixtapes), mainProgram exact x6, platforms linux x6, maintainers Ackerman-00 x6, hashes SRI sha256-...

## 5. Meta/excellence audit (COMPLETE)

- Workflows YAML valid (3 files yaml.safe_load: opencode-schedule.yml [opencode, cleanup], opencode.yml [opencode], update.yml [update-flake]); README `nix run` paths match tested bins for all 6.
- teardown-report.md + docker-report.md receipts refreshed (committed).
- flake.lock within freshness budget (3.6d old, nixos-unstable HEAD unchanged).
- All pkgs/*.nix have pname; nixfmt-clean per container check.

## 6. Known gaps / notes

- aarch64 BUILD not possible on x86_64 runner (named gap) - covered by eval (`nix flake check --all-systems`) + byte-hash verification of every aarch64 artifact + internal-version reads (static unsquashfs for rootapp-aarch64; hash-identity chain for protonplus-aarch64 URUNTIME format).
- Full GUI launch headless not verifiable - named gap; version/boot/node probes are max evidence.
- Container probes must avoid awk/sed/strings assumptions; bind-mounting runner-owned repo hits libgit2 ownership error - use tar-pipe with --no-same-owner (done).
- protonplus aarch64 uses new URUNTIME (SquashFS/DwarFS dual) container - needs dwarfs tooling for direct cross-arch introspection.

## 7. Final state (this run)

- Base HEAD: b915a0b. Changes this run: teardown-report.md + docker-report.md (receipt refresh), .opencode-relay.md (this file). No package files needed changing - everything verified green.
- Issues open: #8 CLOSED (applied in b915a0b). "autonomous run failed" issues: checked and closed post-push. PRs open: 0.
- Relay status: complete. run_id: 32861900338.
