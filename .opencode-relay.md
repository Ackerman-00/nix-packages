# nix-packages autonomous-run relay state

run_id: 32365316766
status: complete

## 0. Pickup

- Previous relay (run 32314148677, commit a4b8430) status: complete. Nothing unfinished to continue.
- This run (32365316766): `git fetch origin && git pull --rebase origin main` => up to date at a4b8430. Repo unshallowed for full git history.
- Inventory discovered from flake.nix: x86_64-linux = helium, protonplus, zen-browser, opencode-desktop, rootapp (+ default=rootapp); aarch64-linux = helium, protonplus, zen-browser, opencode-desktop (+ default=helium). 5 packages.

## 1. Work done this run

### 1a. opencode-desktop bumped v1.18.18 -> v1.18.19 (PUSHED `0da7975`)
- Upstream released v1.18.19 on 2026-08-20T06:22:06Z (stable, not prerelease). `gh api repos/anomalyco/opencode/releases/latest` => v1.18.19; git ls-remote tags confirm v1.18.19 is newest.
- New hashes prefetched with --refresh: amd64 `sha256-WpSBGv97QJ2UOfrJ6r6ctIgOGTLLPA6Hujh/5yV5h08=`, arm64 `sha256-8J7rRv4UFghfTTZZIbbN3/tc4ncA48CtVvNKTFWkmmQ=`.
- Tore the deb: dpkg-deb -x; app.asar extracted via @electron/asar; package.json version = **1.18.19** (matches pinned). app.asar.unpacked native modules (parcel-watcher, node-pty, msgpackr-extract) present.
- `nix build .#opencode-desktop --no-link --refresh` PASS. Container runtime (ubuntu:24.04, fresh): ldd ZERO_NOT_FOUND, app starts and stops at "Missing X server or $DISPLAY" (GUI gap, expected for electron).
- Committed `0da7975 chore(autonomous): opencode-desktop: bump v1.18.18 -> v1.18.19` and pushed.

### 1b. Version sweep (COMPLETE - 100% of inventory)
- helium 0.15.5.1 == upstream latest stable (0.15.6.1 is a PRERELEASE; releases/latest = 0.15.5.1). x86_64 80oe4aar... and arm64 2TI9B3Gj... byte-match re-prefetch.
- protonplus 0.6.4 == upstream latest v0.6.4. x86_64 J0ddq9ho... and arm64 o4sUkr3J... byte-match.
- zen-browser 1.21.15b == upstream latest. x86_64 Lq6mZLhA... and arm64 c6YJuVTn... byte-match.
- opencode-desktop v1.18.19 == upstream latest (UPDATED, 1a).
- rootapp: fixed URL re-prefetched, hash sha256-LFTZZC9q... byte-matches pinned.
- All 9 GitHub/fixed asset URLs return HTTP 200 (curl -sI -L).
- Real versions from inside artifacts: helium "Helium 0.15.5.1 (Chromium 151.0.7922.137)", zen "Mozilla Zen 1.21.15b" (application.ini Version=1.21.15b), protonplus X-AppImage-Version=0.6.4, opencode package.json 1.18.19, rootapp Root.desktop X-AppImage-Version=0.9.127.
- flake.lock nixpkgs rev d407951 (2026-07-05); all packages build with it - no update (no-churn rule).

### 1c. Updater simulation (update.yml logic replayed by hand, ALL blocks)
- RootApp block: prefetch hash == pinned => no change (updater would NOT commit).
- Helium/ProtonPlus/Zen blocks: prefetch == pinned, version == upstream => no change (updater would NOT commit).
- Opencode block: **BUG CONFIRMED LIVE** (issue #3). OPEN_OLD_HASH grep returns BOTH hash lines (2), comparison always fires, and the range-sed `/url = "https://github.com/anomalyco/opencode/releases/download/,/hash =/` re-enters on the arm64 URL and writes the amd64 hash into the arm64 slot. Simulation output: line 15 (arm64 hash) would change from `sha256-8J7rRv4U...` to `sha256-WpSBGv97...` (amd64 hash). The next daily run (~Aug 21 00:00 UTC) WILL clobber the arm64 hash again.
- **Owner action REQUIRED (blocking)**: apply the dual-arch diff from issue #3 body to `.github/workflows/update.yml`. Bot cannot push workflow changes (TOKENS rule). Bot restored the correct arm64 hash in 0da7975 (correct as of now) but each daily run will re-clobber until the workflow is fixed.

### 1d. Issues/PRs
- Issue #5 "autonomous run failed: 2026-08-20" — CLOSED as not-a-bug (transient infra). The previous run failed in the "Install opencode (latest)" step ("Failed to fetch version information" at 05:48 UTC); `curl -sI https://opencode.ai/install` now returns 307 (endpoint live). No repo content affected. Comment + close with evidence.
- Issue #3 "update.yml dual-arch" — still OPEN (owner action required). Added status-update comment documenting v1.18.19 bump and re-confirmed live clobber.
- PRs: none open.

## 2. Build/install ledger (COMPLETE)

| package | build (x86_64) | aarch64 eval | binary smoke | status |
|---|---|---|---|---|
| helium 0.15.5.1 | PASS | PASS (eval) | --version -> "Helium 0.15.5.1 (Chromium 151.0.7922.137)" exit 0 | pass |
| protonplus 0.6.4 | PASS | PASS (eval) | version -> "ProtonPlus 0.6.4" exit 0; hooks /tmp/pKE + /tmp/3J=1Z | pass |
| zen-browser 1.21.15b | PASS | PASS (eval) | --version -> "Mozilla Zen 1.21.15b" exit 0 | pass |
| opencode-desktop v1.18.19 | PASS | PASS (eval) | starts, stops at display (GUI gap) | pass |
| rootapp 0.9.127 | PASS | n/a (x86_64 only) | bwrap wrapper -> "Starting application" exit 0 | pass |

- `nix build .#helium .#protonplus .#zen-browser .#opencode-desktop .#rootapp --no-link --refresh` => EXIT 0.
- `nix flake check --all-systems` => all checks passed (both systems eval; aarch64 BUILD impossible on x86_64 runner - named gap).
- Store paths: helium 4lph1w68..., protonplus bjmcfaj5..., zen gp084iqxw..., opencode jska7nxg...-v1.18.19, rootapp qia4835h....

## 3. Dependency audit (COMPLETE)

| package | NEEDED | in pkgs/*.nix | missing/added | status |
|---|---|---|---|---|
| helium 0.15.5.1 | 32 direct NEEDED on main ELF; all resolve via nix loader + wrapper LD_LIBRARY_PATH (0 not-found) | runtimeDeps covers | none | deps-verified |
| protonplus 0.6.4 | static-pie bun runtime, NEEDED=0 | n/a | none | deps-verified |
| zen-browser 1.21.15b | libxul 49 NEEDED (83 transitives) resolve (0 not-found) | buildInputs+runtimeDeps | none | deps-verified |
| opencode-desktop v1.18.19 | deb Version=1.18.19; electron_42 42.7.1 from flake-locked nixpkgs | electron_42 + stdenv | none | deps-verified |
| rootapp 0.9.127 | FHS wrapType2 + bwrap | wrapType2 | none | deps-verified |

- Interpreters: helium and zen main ELFs point at nix loader `/nix/store/8kvxvr3pmsypxiypq4g8zy13glnfr7nx-glibc-2.42-67/lib/ld-linux-x86-64.so.2`. opencode wrapper is makeBinaryWrapper -> system electron. protonplus static-pie (no interpreter). rootapp FHS-wrapped.
- Note: container ldd shows a glibc-version-mix artifact (ubuntu 24.04 loader/libc 2.39 vs nix glibc 2.42) when using ubuntu's loader; the nix-loader check (`$NIX_LD --list`) is authoritative and shows 0 not-found for all.
- Confidence: high (all 5 container-tested with nix loader in clean containers).

## 4. Container runtime ledger (COMPLETE)

| package | nix build | container ldd | container smoke | status |
|---|---|---|---|---|
| helium 0.15.5.1 | PASS | ZERO_NOT_FOUND (nix loader) | --version -> Helium 0.15.5.1 exit 0 | verified |
| protonplus 0.6.4 | PASS | ZERO_NOT_FOUND (NEEDED=0) | version -> ProtonPlus 0.6.4 exit 0; hooks created; CA-certs symlink created when host store present | verified |
| zen-browser 1.21.15b | PASS | ZERO_NOT_FOUND (libxul 0/0) | --version -> Mozilla Zen 1.21.15b exit 0 | verified |
| opencode-desktop v1.18.19 | PASS | ZERO_NOT_FOUND | starts, "Missing X server or $DISPLAY" exit (GUI gap) | verified |
| rootapp 0.9.127 | PASS | ZERO_NOT_FOUND | bwrap wrapper -> "Starting application" exit 0 (--privileged) | verified |

## 5. Fresh-checker pass (COMPLETE)

- Fresh clone `/tmp/freshcheck` of final main (0da7975): `nix build .#all --no-link --refresh` EXIT 0; `nix flake check --all-systems` passed; aarch64 eval all 4 names OK (helium-0.15.5.1, protonplus-0.6.4, zen-browser-1.21.15b, opencode-desktop-v1.18.19).
- Store paths deterministic/identical to main run.
- Fresh containers (new `docker run --rm` per test) on fresh store paths: helium/zen/protonplus smoke exit 0, rootapp "Starting application", opencode reaches display error. All pass.

## 6. Missing-package audit (COMPLETE)

- Inventory sync: flake.nix (x86_64 5 pkgs, aarch64 4 pkgs), README.md (5 entries), update.yml (5 blocks), pkgs/*.nix (5 files) ALL agree. No discrepancies.
- Upstream artifact types unchanged: helium/zen tar.xz, protonplus AppImage (sharun), opencode deb, rootapp AppImage (fixed URL). No arch added/dropped. opencode still ships amd64+arm64 deb (verified in v1.18.19 assets).
- New-app scan: no new official companion apps with prebuilt Linux artifacts found for helium/opencode/protonplus/zen/rootapp. opencode CLI is already in nixpkgs (opencode 1.18.18); the desktop app (this flake's focus) is at 1.18.19, ahead of nixpkgs.

## 7. Known gaps / notes

- aarch64 BUILD not possible on x86_64 runner (named gap) - aarch64 verified by eval + hash prefetch only.
- GUI apps (opencode-desktop) stop at "Missing X server or $DISPLAY" headless - expected, not a bug.
- Container ldd glibc-mix artifact - nix-loader check authoritative (0 not-found).
- update.yml issue #3 (opencode dual-arch sed) still live; owner must apply workflow diff. Bot cannot (TOKENS rule).

## 8. Final state

- main HEAD: `0da7975` (opencode v1.18.19 bump), pushed. Working tree clean.
- Commits this run: `0da7975` (opencode bump).
- Issues: #5 closed (not-a-bug, transient infra). #3 open (owner action required).
- Relay status: complete. run_id: 32365316766.